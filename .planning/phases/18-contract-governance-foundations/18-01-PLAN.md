---
phase: 18-contract-governance-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/capabilities/errors.py
  - src/construct/capabilities/registry.py
  - src/construct/mcp/server.py
  - src/construct/cli.py
  - tests/integration/test_surface_parity.py
  - tests/contract/test_mcp_contracts.py
  - AGENTS.md
autonomous: true
requirements: [GOV-01]

must_haves:
  truths:
    - "Invoking `knowledge.card.list` with one payload through the real `construct` CLI process and through the real MCP dispatch path returns the same success verdict, the same message, and the same record key set."
    - "A payload carrying an undeclared field is rejected on both surfaces with the same reason string, and the capability handler never runs."
    - "A payload whose field set exactly equals the model's declared field set passes through the seam unchanged; adding exactly one undeclared field flips it to rejected on both surfaces (GOV-01 adjacency edge)."
    - "An empty payload for a model with required fields raises the seam's typed input error naming the missing fields identically on both surfaces; a model whose fields are all optional validates and runs with its declared defaults (GOV-01 empty edge)."
    - "No capability-specific identifier appears in `src/construct/mcp/server.py` — the MCP surface stays registry-generated after the seam insertion."
    - "AGENTS.md no longer instructs a reader not to modify `src/` or `tests/`, and no longer states that GSD planning is inactive."
  artifacts:
    - path: "src/construct/capabilities/errors.py"
      provides: "Typed seam errors so both surfaces render one reason string"
      exports: ["CapabilityInputError", "CapabilityNotFoundError"]
    - path: "src/construct/capabilities/registry.py"
      provides: "The single validating invocation seam"
      contains: "def invoke"
    - path: "tests/integration/test_surface_parity.py"
      provides: "Differential CLI-process vs MCP-dispatch parity harness (D-08)"
  key_links:
    - from: "src/construct/mcp/server.py"
      to: "src/construct/capabilities/registry.py"
      via: "make_handler calls registry.invoke instead of capability.handler"
      pattern: "invoke\\("
    - from: "src/construct/cli.py"
      to: "src/construct/capabilities/registry.py"
      via: "knowledge card list dispatches through the seam"
      pattern: "invoke\\("
---

<objective>
Build the one validating invocation seam GOV-01 requires and prove it end-to-end on a
single capability across both real surfaces before any other capability, call site, or
model is touched.

Purpose: `registry.invoke()` does not exist. `input_model` is computed and discarded
(`registry.py:63` builds the JSON Schema, `mcp/server.py:40-44` never passes it to
`add_tool`), which is why `CardListInput`'s `extra="forbid"` is inert over MCP (WR-02).
This seam is the cross-phase contract Phase 19's generated HTTP adapter routes through, so
its signature and its error shape must be proven correct on the agent's best context before
25 call sites are converted onto it.

Output: `capabilities/errors.py`, `CapabilityRegistry.invoke`, an MCP surface that dispatches
through the seam and advertises the model's own schema, one CLI call site converted, and a
differential parity harness driving a real `construct` subprocess against real MCP dispatch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-contract-governance-foundations/18-CONTEXT.md
@.planning/phases/18-contract-governance-foundations/18-RESEARCH.md
@.planning/phases/18-contract-governance-foundations/18-PATTERNS.md
@.planning/phases/18-contract-governance-foundations/18-VALIDATION.md
@AGENTS.md

**Read AGENTS.md with this correction in hand:** two of its guardrails are stale and, read
literally, forbid this entire phase — "Do not modify `archive/v01-python/`, `src/`, or
`tests/`…" and "**No active GSD.** `.planning/` was archived…". `.planning/` is the live GSD
tree, STATE.md records seven shipped v0.3 phases plus v0.4.1, and the whole v0.5 milestone is
Python work in `src/`. Task 3 of this plan corrects both. Every other AGENTS.md directive —
`.venv/bin/python` for all runtime and tests, `from __future__ import annotations`, full type
annotations, `raise … from exc` at boundaries, `ConfigDict(extra="forbid")` on models — is
live and binding.

**Measured corrections that supersede CONTEXT.md prose** (research executed against the live
repo, 2026-07-26): D-07's "three positional call sites in `cli.py`" is wrong — there are 25
`.handler(` call sites plus non-CLI callers; that work is Plan 03, not this plan.
CONTEXT.md's "515-test suite" is wrong — the suite collects 532.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one capability, one seam, two real surfaces" — knowledge.card.list only</name>
  <precondition>The full pytest suite is green on `dev-v05` before any edit — run `.venv/bin/python -m pytest -q` and record the collected/passed counts in the SUMMARY. D-05 declines a `_KNOWN_BROKEN`-style allowlist, which only works if failures this phase causes can be told apart from failures it inherits (research assumption A3).</precondition>
  <reversibility rating="costly">D-05 makes the seam strict on both surfaces from day one; a later relaxation would have to be applied per capability across both surfaces and encoded into the differential test as expected behaviour — the exact parity fork GOV-01 exists to close.</reversibility>
  <files>src/construct/capabilities/errors.py, src/construct/capabilities/registry.py, src/construct/mcp/server.py, src/construct/cli.py, tests/integration/test_surface_parity.py</files>
  <read_first>
    - `src/construct/capabilities/registry.py` (all 68 lines — `get` at `:36-40` is the error convention to preserve, `list_mcp_tools` at `:55-65` is where `input_schema` is computed)
    - `src/construct/mcp/server.py` (all 52 lines — `:30-38` already performs the seam's step 3 correctly; `:40-44` is where the computed schema is dropped)
    - `src/construct/capabilities/catalog.py:145-147` (`CardListInput` with `extra="forbid"`) and `:314-323` (the `knowledge.card.list` record)
    - `src/construct/cli.py:1455-1475` (the `knowledge card list` command and its `get_registry().get("knowledge.card.list")` call at `:1462`)
    - `tests/contract/test_card_list_cli_mcp.py` (the exact analog — module docstring convention, `_card_workspace` fixture builder at `:30-60`, `test_cli_mcp_schema_parity` at `:146-166`, and `test_mcp_no_hardcoded_card_list` at `:112-120`)
    - `AGENTS.md` § Error Handling and § Code Style
  </read_first>
  <action>
Create `src/construct/capabilities/errors.py` with `from __future__ import annotations`, defining
two domain exceptions the seam raises so both surfaces render one reason string:
`CapabilityNotFoundError` (carries `cap_id` and the sorted available ids) and
`CapabilityInputError` (carries `cap_id` and a `reason` string built from the pydantic
`ValidationError`). Both subclass `Exception`, not `ValueError` — a `ValueError` subclass can be
captured by a pydantic field validator further up the stack. Chain with `raise … from exc` per
AGENTS.md § Error Handling.

Add `invoke(self, cap_id: str, payload: dict) -> Any` to `CapabilityRegistry` in
`src/construct/capabilities/registry.py`, implementing exactly the three steps in RESEARCH
Pattern 1: resolve the record via the existing `get`, validate with
`cap.input_model.model_validate(payload)`, then call `cap.handler(**model.model_dump())` —
the same call convention `mcp/server.py:33` already uses (D-07). Convert the `KeyError` from
`get` into `CapabilityNotFoundError` and the `pydantic.ValidationError` into
`CapabilityInputError`, both with `from exc`. Full type annotations. Do NOT add a leniency
flag, a strict/lenient parameter, or an allowlist argument — D-05 makes the seam strict for
every capability on every surface with no per-surface exception.

In `src/construct/mcp/server.py`, change the single line inside `make_handler` from calling
`capability.handler(**kwargs)` to calling the seam with the capability id and `kwargs`. This is
a pure insertion: do NOT restructure the file, do NOT add capability-specific branches, and do
NOT change the `_serialize_result` / `json.dumps` shape or the existing broad `except` that
returns an `error` key. The file is 52 lines of registry-driven generation and that structure is
the proof Phase 19's adapter depends on.

In `src/construct/cli.py`, convert the `knowledge card list` command at `:1455-1475` to build a
plain payload dict from its Typer options and dispatch through the seam instead of reaching for
`get_registry().get("knowledge.card.list")` and calling `.handler(...)`. Catch
`CapabilityInputError` and `CapabilityNotFoundError` and render them per AGENTS.md § Error
Handling — `typer.echo(f"ERROR {exc}")` then `raise typer.Exit(code=1)` — never letting a raw
exception reach the user. Leave the other 24 `.handler(` call sites untouched; Plan 03 owns them.

Create `tests/integration/test_surface_parity.py` following `test_card_list_cli_mcp.py`'s shape:
`from __future__ import annotations`, a module docstring naming GOV-01/D-08 and the wave status,
a module-level `(capability_id, mcp_tool_name, payload)` case table seeded with the single row
for `knowledge.card.list` / `construct_list_cards`, and a `_card_workspace(tmp_path)` helper that
builds its fixture through the real `initialize_workspace` + `create_card` services (never by
hand-writing files, never into `test-ws/`). Two deltas from the analog are mandatory (RESEARCH
Pitfall 5): the CLI arm runs the real process via `subprocess.run` on
`.venv/bin/python -m construct.cli …`, not `typer.testing.CliRunner`, because the registry
singleton is cached in a module global at `catalog.py:988-993` and an in-process runner shares
it; and the MCP arm drives the closure that `create_server()` registered with FastMCP, not
`cap.handler` directly. Assert that both arms agree on the success verdict, the message, and the
key set of the returned records. Carry the analog's anti-restructure guard across: a test that
reads `mcp/server.py` as text and asserts no capability-specific tool name appears in it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/test_surface_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from construct.capabilities.catalog import get_registry; r=get_registry(); print(r.invoke('knowledge.card.list', {'workspace': '.'}).success)"` runs without `AttributeError` (the seam exists and is callable).
    - `.venv/bin/python -c "import inspect; from construct.capabilities.registry import CapabilityRegistry as R; print(inspect.signature(R.invoke))"` prints a signature taking `cap_id` and `payload` and returning an annotated type.
    - `grep -c 'errors import\|from construct.capabilities.errors' src/construct/capabilities/registry.py` is at least 1.
    - `.venv/bin/python -m pytest tests/integration/test_surface_parity.py -x -q` passes with at least 3 collected tests.
    - `.venv/bin/python -m pytest tests/contract/test_card_list_cli_mcp.py tests/contract/test_mcp_contracts.py -q` still passes — the insertion did not fork the existing MCP contract.
    - `.venv/bin/python -m pytest -q` collects the same number of tests as the recorded baseline plus the new file's tests, with no new failures.
  </acceptance_criteria>
  <done>One capability is dispatched by the real CLI process and by real MCP dispatch through a single `registry.invoke`, and a test proves both arms agree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The seam's rejection contract — one reason string on both surfaces, and the advertised schema stops lying</name>
  <files>src/construct/mcp/server.py, tests/integration/test_surface_parity.py, tests/contract/test_mcp_contracts.py</files>
  <read_first>
    - `src/construct/capabilities/errors.py` and `src/construct/capabilities/registry.py` (as written by Task 1)
    - `src/construct/mcp/server.py:36-44` (the broad `except` that renders `{"error": str(exc)}`, and the `add_tool` call)
    - `tests/integration/test_surface_parity.py` (as written by Task 1)
    - `tests/contract/test_card_list_cli_mcp.py:85-94` (the existing `extra="forbid"` rejection pattern)
    - `src/construct/capabilities/registry.py:55-65` (`list_mcp_tools` — the `input_schema` key already carried in every entry)
  </read_first>
  <behavior>
    - Rejection parity: `{"workspace": <ws>, "bogus": 1}` for `knowledge.card.list` is rejected by the real CLI process (non-zero exit, reason on stderr or stdout) and by real MCP dispatch (an `error` value), and the two reason strings are equal after stripping each surface's own framing.
    - Missing-required parity: `{}` for a model with a required field produces one reason naming the missing field, identical on both arms.
    - All-optional acceptance: a capability whose model fields are all optional validates an empty payload and runs with its declared defaults rather than erroring.
    - Exact-fit acceptance: a payload whose key set equals the model's declared field set is accepted; the same payload plus exactly one undeclared key is rejected on both arms.
    - Structural guard: no capability-specific tool name is present in `mcp/server.py` source text.
  </behavior>
  <action>
Resolve research assumption A1 first and record the answer in the SUMMARY: run
`.venv/bin/python -c "import inspect; from mcp.server.fastmcp import FastMCP; print(inspect.signature(FastMCP.add_tool))"`
and read whether the pinned FastMCP accepts an explicit input-schema override. If it does, pass
the already-computed `entry["input_schema"]` from `list_mcp_tools()` into `add_tool` so the
advertised MCP schema is the capability's own model rather than one FastMCP infers from the
generic `**kwargs` signature — this is the specific fix for WR-02 / research Finding G1. If it
does not, make no change to `add_tool` and state plainly in the SUMMARY that enforcement is
closed by the seam's own `model_validate` while the *advertised* schema remains a discoverability
gap, so the phase records the limit instead of implying it away.

Extend `tests/integration/test_surface_parity.py` with the rejection rows described in
`<behavior>`. Compare the two surfaces' reason strings after normalising away each surface's own
framing — the CLI prefixes with its error label and the MCP arm nests the string under its error
key — so the assertion is about the seam's reason, not about the renderers. Drive the CLI arm as
a real subprocess for every rejection row: this is precisely where a shared-process test lies,
because the exit code and stderr are the signals the seam's error conversion is supposed to
produce.

Pick the all-optional and exact-fit cases from capabilities that already exist and already carry
`extra="forbid"` (`knowledge.card.list` is the seeded one; `curation.inspect` and
`workspace.validate` are the other pre-forbidden read capabilities per research Finding G2) so
this task does not depend on Plan 02's model work.

If passing the schema to `add_tool` changes what `list_mcp_tools()`-derived contract tests
observe, update `tests/contract/test_mcp_contracts.py` in the same commit rather than leaving a
red assertion behind.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/test_surface_parity.py tests/contract/test_mcp_contracts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/integration/test_surface_parity.py -x -q` passes and collects at least 6 tests.
    - A test in `tests/integration/test_surface_parity.py` asserts equality between the CLI reason and the MCP reason for an undeclared-field payload — verified by `grep -c 'bogus' tests/integration/test_surface_parity.py` being at least 1.
    - Running `.venv/bin/python -m construct.cli knowledge card list --workspace <tmp-ws> --bogus 1` exits non-zero and prints a message naming the rejected field rather than a Python traceback.
    - The SUMMARY records the `inspect.signature(FastMCP.add_tool)` output verbatim and states whether `input_schema` is now advertised (A1 resolved either way).
    - `.venv/bin/python -m pytest tests/contract tests/unit -q` passes.
  </acceptance_criteria>
  <done>A rejected payload produces one reason on both surfaces, and the phase has recorded — not assumed — whether the advertised MCP schema is the model's.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the two AGENTS.md directives that forbid this phase</name>
  <files>AGENTS.md</files>
  <read_first>
    - `AGENTS.md` (whole file — both stale directives and the surrounding live conventions)
    - `.planning/STATE.md` § Current Position (the evidence that `.planning/` is live and v0.5 is Python work in `src/`)
  </read_first>
  <action>
AGENTS.md carries two directives that are contradicted by the current state of the repository
and that, read literally, prohibit every task in Phase 18:

1. The instruction not to modify `archive/v01-python/`, `src/`, or `tests/` unless the Python
   approach is explicitly being resumed. The Python approach *was* resumed: v0.4, v0.4.1 and
   v0.5 are all `src/`-side milestones. Rewrite the directive so it protects only
   `archive/v01-python/`, which is genuinely frozen, and says so.
2. The claim that there is no active GSD and that `.planning/` was archived. `.planning/` is the
   live GSD tree with a current STATE.md, an active v0.5 ROADMAP, and this phase's own artifacts
   in it. Rewrite it to state that `.planning/` is the live planning tree and that GSD commands
   are the working method.

Correct both in place. Change nothing else in AGENTS.md — the `.venv/bin/python` rule, the
pytest invocation rule, the Pydantic conventions, the `from __future__ import annotations` rule,
the type-annotation rule and the error-handling rules are all live and every task in this phase
depends on them. In particular, do NOT weaken the `ConfigDict(extra="forbid")` convention: this
phase generalizes it across all 28 capability models (D-06), and D-03's `extra="ignore"` relaxation
is a deliberate, user-locked exception scoped to the derived views projection only — record that
exception as a one-line note beside the convention so a future reviewer does not "restore" it.
  </action>
  <verify>
    <automated>grep -c 'archive/v01-python' AGENTS.md && grep -ci 'planning' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Do not modify \`archive/v01-python/\`, \`src/\`, or \`tests/\`" AGENTS.md` returns 0.
    - `grep -ci "No active GSD" AGENTS.md` returns 0.
    - `grep -c 'archive/v01-python' AGENTS.md` is at least 1 — the genuinely frozen tree is still protected.
    - `grep -c '.venv/bin/python' AGENTS.md` is unchanged from before the edit (the live workflow rules survive).
    - `grep -c 'extra="forbid"' AGENTS.md` is at least 1, and the file carries a one-line note naming the views-projection exception.
  </acceptance_criteria>
  <done>AGENTS.md no longer forbids the phase it is meant to govern, and its live conventions are intact.</done>
</task>

</tasks>

## Artifacts this phase produces

Every symbol below is created by Phase 18 and does not exist in the repository today. The
source-grounding pass should treat all of them as new, not as drift.

| Symbol / path | Kind | Created by |
|---|---|---|
| `src/construct/capabilities/errors.py` | new module | 18-01 |
| `CapabilityInputError`, `CapabilityNotFoundError` | exception classes | 18-01 |
| `CapabilityRegistry.invoke(cap_id, payload)` | method (the GOV-01 seam) | 18-01 |
| `tests/integration/test_surface_parity.py` | new test file | 18-01 |
| `tests/contract/test_capability_seam.py` | new test file | 18-02 |
| `WorkflowStatusInput` | pydantic model (replaces the wrong model on `workflow.status`) | 18-02 |
| `ViewsValidateInput`, the `views.validate_data` capability record | model + registry record (D-02) | 18-03 |
| `WorkspaceStatsFile`, `CurationHistoryFile` | pydantic models (D-18) | 18-04 |
| `src/construct/views/contracts.py`, `GLOBAL_FILE_CONTRACTS`, `PER_WORKSPACE_FILE_CONTRACTS` | new module + path-to-model tables | 18-05 |
| `CurationProposal.proposal_id` | model field (D-09) | 18-06 |
| `_validate_proposal_id` | field validator | 18-06 |
| `IncompleteDecisionMap`, `StaleQueue` | exception classes (D-10 / D-11) | 18-06 |
| `checkpoint_id` on `CurationRunResult` / `RunResult`, and on `CurationReviewInput` / `ReviewInput` | model fields (D-11 ETag) | 18-06 |
| `tests/contract/test_canonical_write_boundary.py` | new test file | 18-07 |
| `CurationRunResult.escalated` | model field (D-16) | 18-08 |
| the new escalate event action string | audit-trail event action (D-16) | 18-08 |
| `OperationResult.outcome` | dataclass field decoupling reported outcome from the success flag | 18-08 |
| `tests/integration/test_surface_honesty.py` | new test file | 18-08 |

## Flagged assumptions carried by this plan

- **A1 (`FastMCP.add_tool` schema override)** — unverified at research time. Task 2 resolves it by
  execution and records the answer either way. If the parameter does not exist, GOV-01's
  "one seam, same schema on both surfaces" is true for *behaviour* and false for *discovery*, and
  the phase says so rather than implying otherwise.
- **A3 (green 532-test baseline)** — asserted as this plan's leading precondition. D-05's
  no-allowlist stance is only workable against a green baseline.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → capability handler | An agent-supplied JSON payload crosses into Python kwargs here. Today nothing validates it. |
| CLI argv → capability handler | Typer-parsed user input crosses into the same handlers by a different, unvalidated path. |
| Seam → handler | The one place a validated model becomes a handler call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | `mcp/server.py::make_handler` → any capability handler | high | mitigate | Task 1 routes MCP dispatch through `registry.invoke`, which calls `input_model.model_validate` before the handler runs; Task 2 proves an undeclared field is rejected. |
| T-18-02 | Tampering | `registry.list_mcp_tools` → `FastMCP.add_tool` | high | mitigate | Task 2 passes the model's own JSON Schema to `add_tool` where the pinned FastMCP supports it, closing WR-02's discoverability half; the enforcement half is closed unconditionally by the seam. |
| T-18-10 | Information disclosure | `CapabilityInputError` reason string rendered to an MCP client | medium | mitigate | The reason is built from pydantic's field-level error summary (field names and constraint names), never from raw payload values or filesystem paths. |
| T-18-11 | Denial of service | Broad `except Exception` in `mcp/server.py:36-37` swallowing seam errors | low | accept | Pre-existing and deliberately preserved — CONTEXT.md forbids restructuring `mcp/server.py`. The seam raising a typed error is what makes the swallowed string comparable across surfaces. |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs nothing (research § Package Legitimacy Audit: no new packages). Any plan proposing a new dependency is a scope change and must stop for a decision. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/integration/test_surface_parity.py -x -q` passes.
- `.venv/bin/python -m pytest -q` shows no regression against the baseline captured in Task 1's precondition.
- `mcp/server.py` remains registry-generated: no capability-specific identifier in its source text.
</verification>

<success_criteria>
- One capability round-trips identically through the real CLI process and real MCP dispatch, and an undeclared field is rejected identically on both — GOV-01's success criterion proven on a slice before it is proven at scale.
- `registry.invoke` exists with the signature Phase 19's HTTP adapter will route through, and no leniency knob exists on it.
- AGENTS.md no longer prohibits the work of this phase.
</success_criteria>

<output>
Create `.planning/phases/18-contract-governance-foundations/18-01-SUMMARY.md` when done.
Record in it: the baseline test counts, the `FastMCP.add_tool` signature verbatim, and whether
`input_schema` is now advertised over MCP.
</output>
