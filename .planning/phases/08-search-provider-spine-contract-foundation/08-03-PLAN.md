---
phase: 08-search-provider-spine-contract-foundation
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - src/construct/search/providers/tavily.py
  - src/construct/search/registry.py
  - tests/search/test_search_contract.py
  - tests/search/test_search_provider_mock.py
  - tests/contract/test_mcp_contracts.py
autonomous: true
requirements: [SRCH-01, SRCH-03, SRCH-04]
user_setup:
  - service: tavily
    why: "Optional live search verification (manual-only per 08-VALIDATION.md)"
    env_vars:
      - name: TAVILY_API_KEY
        source: "Tavily dashboard → API keys"

must_haves:
  truths:
    - "Full search contract test suite passes offline with mock provider only"
    - "MCP contract tests include construct_research_search in expected tool set and handler invocation"
    - "CLI research search contract test passes on fixture workspace"
    - "TavilySearchProvider normalizes SDK responses to SearchResult contract"
    - "Tavily SDK import isolated to providers/tavily.py with lazy import and ProviderUnavailableError fallback"
    - "Full pytest regression suite remains green after Phase 8 additions"
  artifacts:
    - path: "tests/search/test_search_contract.py"
      provides: "SRCH-01/03 contract coverage — registry, CLI, SOT immutability"
      contains: "test_cli_research_search"
    - path: "tests/contract/test_mcp_contracts.py"
      provides: "construct_research_search in expected MCP tool set"
      contains: "construct_research_search"
    - path: "src/construct/search/providers/tavily.py"
      provides: "TavilySearchProvider adapter"
      contains: "class TavilySearchProvider"
  key_links:
    - from: "src/construct/search/registry.py"
      to: "src/construct/search/providers/tavily.py"
      via: "lazy import when default_provider is tavily"
      pattern: "TavilySearchProvider"
    - from: "tests/contract/test_mcp_contracts.py"
      to: "research.search handler"
      via: "_payload_for construct_research_search"
      pattern: "construct_research_search"
---

<objective>
Complete Phase 8 verification: full offline contract suite, MCP contract parity update, Tavily adapter behind optional extra, and v0.3 regression gate.

Purpose: Close SRCH-03 (offline contract tests), finalize SRCH-01/04 coverage, and prove no regression to existing capabilities.
Output: Green `tests/search/` and updated MCP contracts; Tavily adapter ready for manual live verification.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-search-provider-spine-contract-foundation/08-01-SUMMARY.md
@.planning/phases/08-search-provider-spine-contract-foundation/08-02-SUMMARY.md
@.planning/phases/08-search-provider-spine-contract-foundation/08-VALIDATION.md

<interfaces>
From tests/contract/test_mcp_contracts.py (expected tool set to extend):
```python
expected = {
    "construct_validate",
    ...
    "construct_bridge_detect",
    # ADD: "construct_research_search"
}
```

From tests/contract/test_mcp_contracts.py _payload_for:
```python
"construct_bridge_detect": {"workspace_path": ws},
# ADD:
"construct_research_search": {"workspace_path": ws, "query": "contract test query"},
```

Tavily SDK (Context7 verified):
```python
from tavily import TavilyClient, InvalidAPIKeyError, UsageLimitExceededError, TimeoutError as TavilyTimeoutError
client.search(query, max_results=5, timeout=30)
# results: title, url, content, score
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete contract tests and MCP contract update</name>
  <files>tests/search/test_search_contract.py, tests/contract/test_mcp_contracts.py, tests/search/conftest.py</files>
  <read_first>
    - tests/search/test_search_contract.py (from Plan 02 — extend)
    - tests/contract/test_mcp_contracts.py:53-119 (expected set and _payload_for)
    - tests/contract/test_cli_contracts.py (CLI contract pattern)
    - tests/search/conftest.py (search_workspace fixture)
    - src/construct/pipelines/research_search.py (ResearchSearchInput fields)
  </read_first>
  <action>
    Complete `tests/search/test_search_contract.py` (SRCH-01, SRCH-03):

    1. **`test_research_search_normalized`** (may exist from Plan 02 — strengthen):
       - Use `search_workspace` fixture with mock provider
       - Invoke `get_registry().get("research.search").handler(workspace_path=str(ws), query="quantum gravity")`
       - Assert `result.success is True`
       - Assert `result.data["batches"][0]["results"][0]` contains keys: `title`, `url`, `snippet`, `source_tier`, `score`
       - Snapshot hashes of `search-seeds.json`, `connections.json`, `log/events.jsonl`, card count before/after — must be unchanged

    2. **`test_research_search_seed_cluster`**:
       - Invoke with `cluster_id` matching a seed in workspace search-seeds.json
       - Assert success and non-empty results

    3. **`test_cli_research_search`**:
       - Use `typer.testing.CliRunner` + `construct.cli:app`
       - `runner.invoke(app, ["research", "search", "-w", str(ws), "-q", "quantum gravity", "--json"])`
       - Assert `exit_code == 0` and `"success": true` in output

    4. **`test_research_search_degraded_error`** (SRCH-04):
       - Configure mock fixture with rate-limit error query
       - Assert handler returns `success=False` and errors list non-empty with recognizable error type

    Update `tests/contract/test_mcp_contracts.py`:
    - Add `"construct_research_search"` to `expected` set in `test_mcp_tool_count` (count becomes 12 tools)
    - Add payload to `_payload_for`: `{"workspace_path": ws, "query": "contract test query"}`
    - Add `"construct_research_search"` to `@pytest.mark.parametrize` list in handler invocation tests if parametrized separately

    Ensure search_workspace fixture writes search.yaml if test-ws copies lack it — helper to overlay mock search config on copied fixture workspaces for MCP tests.

    Run: `.venv/bin/python -m pytest tests/search/test_search_contract.py tests/contract/test_mcp_contracts.py -k "research_search or mcp_tool" -x`
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/search/test_search_contract.py tests/contract/test_mcp_contracts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/search/test_search_contract.py` has at least 4 passing tests covering normalized results, seed cluster, CLI, and degraded error
    - `test_mcp_tool_count` expected set equals actual set including `construct_research_search`
    - `test_every_mcp_handler_invokes_without_type_error` passes for `construct_research_search`
    - Full `tests/search/` directory passes: `.venv/bin/python -m pytest tests/search/ -x -q`
  </acceptance_criteria>
  <done>Offline contract suite and MCP contract tests fully cover research.search without network.</done>
</task>

<task type="auto">
  <name>Task 2: Tavily adapter, factory wiring, and full regression</name>
  <files>src/construct/search/providers/tavily.py, src/construct/search/registry.py, tests/search/test_search_provider_mock.py</files>
  <read_first>
    - src/construct/search/providers/tavily.py (to create)
    - src/construct/search/registry.py (factory — add Tavily branch)
    - src/construct/search/providers/mock.py (normalization reference)
    - tests/fixtures/search/tavily_basic.json (sample response for unit normalization test)
    - pyproject.toml optional search extra
    - .planning/phases/08-search-provider-spine-contract-foundation/08-RESEARCH.md (Tavily exception mapping)
  </read_first>
  <action>
    Create `src/construct/search/providers/tavily.py` — **ONLY file that imports tavily**:

    - `TavilySearchProvider(SearchProvider)` implementing all four ABC methods
    - Lazy import inside `__init__` or factory method:
      ```python
      try:
          from tavily import TavilyClient, InvalidAPIKeyError, ...
      except ImportError as exc:
          raise ProviderUnavailableError(provider_name="tavily", message="Install with: pip install -e '.[search]'") from exc
      ```
    - Read API key from `os.environ[config.api_key_env]`; missing env → `AuthError`
    - Map Tavily response: `content` → `snippet`, `score` → `score`, default `source_tier=3` (search-time heuristic per RESEARCH A3), extras → `provider_specific`
    - Exception mapping per RESEARCH: `InvalidAPIKeyError` → `AuthError`, `UsageLimitExceededError` → `RateLimitError`, `TavilyTimeoutError` → `NetworkError`
    - `search_batch` loops queries calling `search()`; `search_by_seed_cluster` delegates to cluster terms like mock provider

    Update `src/construct/search/registry.py`:
    - When `default_provider == tavily`, instantiate `TavilySearchProvider`
    - Keep mock as default for missing/unknown

    Add unit test `tests/search/test_search_provider_mock.py::test_tavily_normalization` (no network):
    - Test normalization helper using `tavily_basic.json` fixture parsed as if from SDK
    - Skip if tavily not installed OR mock the client response dict only (preferred: pure normalization function test without SDK import)

    Optional: `@pytest.mark.network` test file `tests/search/test_tavily_integration.py` skipped by default — only if TAVILY_API_KEY set; NOT required for phase gate.

    Final regression: `.venv/bin/python -m pytest -x -q` — all 228+ baseline tests plus new search tests must pass.

    Verify isolation: `grep -r "from tavily" src/construct/` returns exactly one file (`providers/tavily.py`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "from tavily" src/construct/` matches only `src/construct/search/providers/tavily.py`
    - Factory with tavily config raises `ProviderUnavailableError` when tavily-python not installed (test with monkeypatch ImportError)
    - Tavily normalization unit test passes without network
    - Full pytest suite green (exit code 0)
    - `tests/search/` all green offline without TAVILY_API_KEY
  </acceptance_criteria>
  <done>Tavily adapter implemented behind optional extra; factory wired; full regression suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tavily API → TavilySearchProvider | External network response crosses adapter boundary |
| Tavily SDK → Python process | Third-party library loaded lazily |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-08 | Information disclosure | Tavily API key | mitigate | Read from env via api_key_env name only; AuthError on missing key without echoing value |
| T-8-09 | Tampering | Tavily JSON response | mitigate | Map through Pydantic SearchResult; unknown fields go to provider_specific only |
| T-8-10 | Elevation | Tavily SDK import leak | mitigate | Lazy import in tavily.py only; grep gate in test; ProviderUnavailableError when extra missing |
| T-8-11 | Repudiation | MCP contract drift | mitigate | Update test_mcp_contracts expected set and _payload_for in same plan |
</threat_model>

<verification>
Phase gate commands:
1. `.venv/bin/python -m pytest tests/search/ -x -q`
2. `.venv/bin/python -m pytest tests/contract/ -x -q`
3. `.venv/bin/python -m pytest -x -q`
</verification>

<success_criteria>
All SRCH-01 through SRCH-04 requirements satisfied with automated tests; MCP parity verified; Tavily adapter isolated; zero regression in full suite.
</success_criteria>

<output>
After completion, create `.planning/phases/08-search-provider-spine-contract-foundation/08-03-SUMMARY.md`
</output>
