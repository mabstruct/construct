---
phase: 08-search-provider-spine-contract-foundation
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/construct/pipelines/research_search.py
  - src/construct/capabilities/catalog.py
  - src/construct/cli.py
  - src/construct/storage/workspace.py
  - src/construct/services/validation.py
  - src/construct/services/init.py
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-04]
user_setup: []

must_haves:
  truths:
    - "WorkspaceLoader.load_search_config() reads and validates `.construct/search.yaml`"
    - "Workspace init copies search.yaml template into new workspaces"
    - "Workspace validation reports errors for invalid search.yaml"
    - "research.search handler returns normalized OperationResult without any SOT writes"
    - "CLI `construct research search` invokes registry handler and displays results"
    - "MCP tool construct_research_search auto-exposes via registry without server.py edits"
    - "Structured degraded errors surface in OperationResult.errors when provider fails"
  artifacts:
    - path: "src/construct/pipelines/research_search.py"
      provides: "research_search() read-only PIPE handler"
      contains: "def research_search"
    - path: "src/construct/capabilities/catalog.py"
      provides: "research.search CapabilityRecord with RT-03 shim"
      contains: "research.search"
    - path: "src/construct/cli.py"
      provides: "research Typer subgroup with search command"
      contains: "research_app"
    - path: "src/construct/storage/workspace.py"
      provides: "load_search_config()"
      contains: "load_search_config"
  key_links:
    - from: "src/construct/cli.py research search command"
      to: "src/construct/capabilities/catalog.py research.search handler"
      via: "get_registry().get('research.search').handler(...)"
      pattern: "research\\.search"
    - from: "src/construct/pipelines/research_search.py"
      to: "src/construct/search/registry.py SearchProviderFactory"
      via: "factory.create(loader.load_search_config(), workspace)"
      pattern: "SearchProviderFactory"
    - from: "src/construct/pipelines/research_search.py"
      to: "ingest/knowledge write helpers"
      via: "MUST NOT import or call"
      pattern: "ingest_source|create_card|append_event"
---

<objective>
Wire the search spine into CONSTRUCT invoke surfaces: workspace config loading/validation/init, read-only research.search PIPE handler, capability registry registration with RT-03 shim, and CLI research search command.

Purpose: Satisfy SRCH-01 (CLI/MCP research.search without SOT writes), complete SRCH-02 (config load at runtime), and SRCH-04 (degraded errors in OperationResult) per D-05 MCP/CLI auto-exposure.
Output: End-to-end search invocation from CLI and MCP through shared registry handler.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-search-provider-spine-contract-foundation/08-01-SUMMARY.md
@.planning/phases/08-search-provider-spine-contract-foundation/08-CONTEXT.md
@.planning/phases/08-search-provider-spine-contract-foundation/08-RESEARCH.md

<interfaces>
From src/construct/pipelines/graph_status.py (handler pattern to mirror):
```python
def graph_status(workspace: str | Path) -> OperationResult:
    loader = WorkspaceLoader(root)
    ...
    return OperationResult(success=True, message=..., data=data)
```

From src/construct/capabilities/catalog.py (RT-03 shim pattern):
```python
def _validate_shim(*args, **kwargs):
    if args:
        return validate_workspace(args[0])
    return validate_workspace(kwargs["path"])
```

From src/construct/cli.py (ask_app pattern):
```python
ask_app = typer.Typer(no_args_is_help=True, name="ask", ...)
@ask_app.command()
def domain(...):
    cap = get_registry().get("ask.domain")
    result = cap.handler(...)
    _display_result(result, json_output)
```

From Plan 01 (search module exports):
```python
class SearchProvider(ABC): ...
class SearchProviderFactory: ...
class SearchError: ...
class RateLimitError(SearchError): ...
class ResearchSearchInput(BaseModel): ...  # define in research_search.py or catalog
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace config load, validation, and init template copy</name>
  <files>src/construct/storage/workspace.py, src/construct/services/validation.py, src/construct/services/init.py, tests/search/test_search_config.py</files>
  <read_first>
    - src/construct/storage/workspace.py:94-110 (load_model_routing, load_search_seeds)
    - src/construct/services/validation.py:127-152 (model-routing and search-seeds validation blocks)
    - src/construct/services/init.py:58-66 (model-routing template copy)
    - src/construct/schemas/config.py (SearchConfig from Plan 01)
    - CONSTRUCT-CLAUDE-impl/construct/templates/search.yaml
  </read_first>
  <action>
    **`storage/workspace.py`:** Add `load_search_config(self) -> SearchConfig` immediately after `load_model_routing()`, identical try/except pattern reading `.construct/search.yaml` → `SearchConfig.model_validate(...)`, raising `WorkspaceLoadError` on failure. Import `SearchConfig` from `construct.schemas.config`.

    **`services/validation.py` (SRCH-02):** Add parallel block after model-routing check:
    ```python
    if loader.resolve(".construct/search.yaml").exists():
        try:
            loader.load_search_config()
        except WorkspaceLoadError as exc:
            report.add_error(".construct/search.yaml", str(exc))
    ```
    When `search-seeds.json` was loaded successfully, cross-validate that any `default_provider` references valid provider keys only (schema already enforces). Optionally warn if search.yaml missing when invoked — validation does not require file existence (per RESEARCH A4).

    **`services/init.py`:** After model-routing copy line, add:
    ```python
    shutil.copy(TEMPLATE_DIR / "search.yaml", workspace_root / ".construct" / "search.yaml")
    ```
    Update `_write_workspace_doc()` support-paths bullet to mention `.construct/search.yaml`.

    Extend `tests/search/test_search_config.py`:
    - Test `WorkspaceLoader.load_search_config()` on search_workspace fixture
    - Test `validate_workspace` reports error when search.yaml has invalid provider block

    Do NOT register capability or add CLI in this task.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/search/test_search_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - New workspace from `initialize_workspace()` contains `.construct/search.yaml` copied from template
    - `WorkspaceLoader(tmp_ws).load_search_config().default_provider.value == "mock"`
    - `validate_workspace` on workspace with malformed search.yaml reports error on `.construct/search.yaml`
    - Existing validate tests for model-routing unchanged and still pass
  </acceptance_criteria>
  <done>Search config loads via WorkspaceLoader, validates during workspace validate, and is scaffolded on init.</done>
</task>

<task type="auto">
  <name>Task 2: research.search handler, registry registration, and CLI command</name>
  <files>src/construct/pipelines/research_search.py, src/construct/capabilities/catalog.py, src/construct/cli.py, tests/search/test_search_contract.py</files>
  <read_first>
    - src/construct/pipelines/graph_status.py (OperationResult read-only handler)
    - src/construct/capabilities/catalog.py:320-350 (ask.domain / bridge.detect registration + shim area)
    - src/construct/cli.py:327-393 (ask_app / bridge_app pattern)
    - src/construct/mcp/server.py:26-44 (auto-exposure — no edits needed)
    - src/construct/search/registry.py (SearchProviderFactory from Plan 01)
    - .planning/phases/08-search-provider-spine-contract-foundation/08-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Create `src/construct/pipelines/research_search.py`:

    **`ResearchSearchInput` (Pydantic, extra=forbid):**
    - `workspace_path: str`
    - `query: str | None = None` (direct search per D-06)
    - `queries: list[str] | None = None` (batch)
    - `cluster_id: str | None = None` (seed-cluster per D-06)
    - `max_results: int | None = Field(default=None, ge=1, le=50)`
    - `provider_override: str | None = None`
    - `@model_validator(mode="after")`: exactly one of query/queries/cluster_id must be set; enforce `max_queries_per_request` and `max_batch_queries` from loaded config caps

    **`ResearchSearchOutput`:** serializable dict shape: `{ "batches": [...SearchBatchOutput.model_dump...], "provider": str, "degraded": bool }`

    **`research_search(input: ResearchSearchInput) -> OperationResult`:**
    1. Load workspace via `WorkspaceLoader(Path(input.workspace_path))`
    2. Load `SearchConfig` via `loader.load_search_config()` — on missing file return `OperationResult(success=False, message="missing .construct/search.yaml", errors=[...])`
    3. Build provider via `SearchProviderFactory.create(config, workspace=loader.root)`; honor `provider_override` if set and valid
    4. Dispatch: `query` → `provider.search()`, `queries` → `provider.search_batch()`, `cluster_id` → `provider.search_by_seed_cluster()`
    4b. When `cluster_id` is `manual-ingest` or `web-ingest` (reserved ingest clusters), allow search but include `warnings: ["cluster is reserved for ingest, not a research target"]` in output data metadata (RESEARCH open question #2 resolution).
    5. Enforce caps: truncate results, set `truncated=True`, respect `max_results_per_query`
    6. On `SearchError` subclasses (SRCH-04): if `config.caps.degraded_on_provider_error`, return `OperationResult(success=False, message=exc.message, errors=[OperationError(reason=exc.__class__.__name__, field=exc.provider_name, ...)])` with structured metadata in data; include `retry_after_seconds` for RateLimitError
    7. On success: `OperationResult(success=True, message=f"Search complete ({len(results)} results)", data=ResearchSearchOutput(...).model_dump(mode="json"))`
    8. **CRITICAL:** Do NOT import or call `ingest_source`, `create_card`, `edit_card`, `append_event`, or any seed timestamp update helpers. Phase 8 is read-only.

    **`catalog.py` (D-05, SRCH-01):** Register after bridge.detect block:
    ```python
    registry.register(CapabilityRecord(
        id="research.search",
        name="Research Search",
        description="Provider-agnostic web search returning normalized results (read-only)",
        input_model=ResearchSearchInput,
        output_model=OperationResult,
        handler=_research_search_shim,
        cli_name="research.search",
        mcp_tool_name="construct_research_search",
    ))
    ```
    Implement `_research_search_shim(*args, **kwargs)` RT-03 adapter: if args positional, map to ResearchSearchInput fields; if kwargs, pass through `ResearchSearchInput(**kwargs)`.

    **`cli.py`:** Add `research_app = typer.Typer(name="research", ...)` with `@research_app.command(name="search")`:
    - Options: `--workspace/-w`, `--query/-q`, `--queries` (repeatable or comma-separated), `--cluster-id`, `--max-results`, `--json/-j`
    - Validate mutually exclusive query modes at CLI level (typer echo ERROR if none or multiple)
    - `cap = get_registry().get("research.search")`; call handler; `_display_result(result, json_output)`

    Add minimal integration test in `tests/search/test_search_contract.py` (create file if absent):
    - `test_research_search_normalized`: invoke handler directly with mock workspace, assert success, assert result data contains normalized fields, assert no change to refs/cards/search-seeds/events file hashes

    MCP auto-exposes via existing server loop — verify tool appears: `get_registry().list_mcp_tools()` includes `construct_research_search`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/search/test_search_contract.py::test_research_search_normalized -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `get_registry().get("research.search").handler(workspace_path=..., query="test")` returns OperationResult with success=True and data containing SearchResult-shaped dicts
    - Handler with mock error fixture query returns success=False with error reason containing `RateLimitError` class name
    - `construct research search -w {fixture} -q "quantum gravity" --json` exits 0 and prints JSON with `"success": true`
    - `construct_research_search` appears in `registry.list_mcp_tools()` names
    - Before/after handler call: `search-seeds.json`, `log/events.jsonl`, `refs/`, `cards/` unchanged (hash or mtime assertion in test)
    - Handler with `cluster_id="manual-ingest"` returns success with `warnings` containing ingest-reserved message
    - `grep -E "ingest_source|create_card|append_event" src/construct/pipelines/research_search.py` returns no matches
  </acceptance_criteria>
  <done>research.search is registered, callable from CLI and MCP, returns normalized read-only results, and surfaces structured degraded errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP kwargs → ResearchSearchInput | Untrusted user/agent input crosses Pydantic boundary |
| External provider → OperationResult.data | Third-party search content returned to caller |
| Handler → workspace filesystem | Must remain read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | Tampering | research_search handler | mitigate | Explicit ban on ingest/knowledge/event imports; test asserts no SOT mutation |
| T-8-05 | Information disclosure | OperationResult error messages | mitigate | Redact env var values in AuthError messages; never echo API key values |
| T-8-06 | Spoofing | Search result URLs | accept | URLs passed through for Phase 9 scoring; normalize via Pydantic url str field |
| T-8-07 | Denial of service | Batch query caps | mitigate | Enforce max_queries_per_request and max_batch_queries from SearchCapsConfig at handler entry |
</threat_model>

<verification>
`.venv/bin/python -m pytest tests/search/test_search_contract.py tests/search/test_search_config.py -x -q`
CLI smoke: `.venv/bin/python -m construct research search -w test-ws/my-construct -q "test" --json` (after copying search.yaml into fixture or using search_workspace)
</verification>

<success_criteria>
research.search callable via CLI and MCP registry; config loads from workspace; degraded errors structured; zero SOT writes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-search-provider-spine-contract-foundation/08-02-SUMMARY.md`
</output>
