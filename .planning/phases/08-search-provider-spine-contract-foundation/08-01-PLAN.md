---
phase: 08-search-provider-spine-contract-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/search/__init__.py
  - src/construct/search/errors.py
  - src/construct/search/models.py
  - src/construct/search/provider.py
  - src/construct/search/providers/__init__.py
  - src/construct/search/providers/mock.py
  - src/construct/search/registry.py
  - src/construct/schemas/config.py
  - CONSTRUCT-CLAUDE-impl/construct/templates/search.yaml
  - pyproject.toml
  - tests/search/conftest.py
  - tests/search/test_search_config.py
  - tests/search/test_search_provider_mock.py
  - tests/fixtures/search/mock_happy_path.json
  - tests/fixtures/search/mock_rate_limit.json
  - tests/fixtures/search/mock_auth_error.json
  - tests/fixtures/search/mock_network_error.json
  - tests/fixtures/search/tavily_basic.json
autonomous: true
requirements: [SRCH-02, SRCH-03, SRCH-04]
user_setup: []

must_haves:
  truths:
    - "SearchConfig schema validates workspace `.construct/search.yaml` with provider blocks and caps"
    - "SearchProvider ABC exposes search(), search_batch(), search_by_seed_cluster(), get_capabilities()"
    - "MockSearchProvider returns fixture-driven results with configurable latency and error injection"
    - "SearchProviderFactory resolves default_provider from config to mock or tavily adapter slot"
    - "Six granular SearchError subclasses are independently raiseable with structured metadata"
  artifacts:
    - path: "src/construct/schemas/config.py"
      provides: "SearchProviderName, SearchConfig, SearchCapsConfig, provider block models"
      contains: "class SearchConfig"
    - path: "src/construct/search/provider.py"
      provides: "SearchProvider ABC with 4 abstract methods"
      contains: "class SearchProvider"
    - path: "src/construct/search/providers/mock.py"
      provides: "MockSearchProvider with JSON fixture map and error injection"
      contains: "class MockSearchProvider"
    - path: "src/construct/search/registry.py"
      provides: "SearchProviderFactory"
      contains: "SearchProviderFactory"
    - path: "CONSTRUCT-CLAUDE-impl/construct/templates/search.yaml"
      provides: "Workspace search config template defaulting to mock provider"
      contains: "default_provider: mock"
  key_links:
    - from: "src/construct/search/registry.py"
      to: "src/construct/search/providers/mock.py"
      via: "factory instantiates MockSearchProvider when config.default_provider is mock"
      pattern: "MockSearchProvider"
    - from: "src/construct/search/providers/mock.py"
      to: "tests/fixtures/search/*.json"
      via: "fixture_dir config loads per-query response or error blocks"
      pattern: "fixture_dir"
    - from: "src/construct/schemas/config.py"
      to: "CONSTRUCT-CLAUDE-impl/construct/templates/search.yaml"
      via: "template round-trips through SearchConfig.model_validate"
      pattern: "SearchConfig"
---

<objective>
Establish the search provider spine core: workspace SearchConfig schema, granular error taxonomy, normalized result models, four-method SearchProvider ABC, configurable mock provider, and provider factory.

Purpose: Deliver the provider-agnostic foundation (D-01 config location, D-02 four-method interface, D-03 hybrid normalization, D-04 mock fidelity, D-07 error taxonomy) before any invoke-surface wiring.
Output: `src/construct/search/` package, SearchConfig in schemas, search.yaml template, test scaffolds, and offline mock provider tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-search-provider-spine-contract-foundation/08-CONTEXT.md
@.planning/phases/08-search-provider-spine-contract-foundation/08-RESEARCH.md
@.planning/phases/08-search-provider-spine-contract-foundation/08-PATTERNS.md

<interfaces>
From src/construct/schemas/config.py (existing patterns to mirror):
```python
ENV_VAR_PATTERN = re.compile(r"^[A-Z][A-Z0-9_]*$")
KEBAB_CASE_PATTERN = re.compile(r"^[a-z0-9]+(?:-[a-z0-9]+)*$")

class ProviderConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")
    provider: ProviderName
    model: str
    api_key_env: str | None = None
```

From src/construct/storage/workspace.py:
```python
def load_model_routing(self) -> ModelRoutingConfig:
    try:
        return ModelRoutingConfig.model_validate(self.read_yaml(".construct/model-routing.yaml"))
    except ValidationError as exc:
        raise WorkspaceLoadError(f"invalid .construct/model-routing.yaml: {exc}") from exc

def load_search_seeds(self) -> SearchSeedsFile: ...
```

From src/construct/services/knowledge.py:
```python
@dataclass
class OperationResult:
    success: bool = True
    message: str = ""
    errors: list[OperationError] = field(default_factory=list)
    data: Any = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SearchConfig schema, template, test scaffolds, and optional Tavily extra</name>
  <files>src/construct/schemas/config.py, CONSTRUCT-CLAUDE-impl/construct/templates/search.yaml, pyproject.toml, tests/search/conftest.py, tests/search/test_search_config.py, tests/fixtures/search/mock_happy_path.json, tests/fixtures/search/mock_rate_limit.json, tests/fixtures/search/mock_auth_error.json, tests/fixtures/search/mock_network_error.json, tests/fixtures/search/tavily_basic.json</files>
  <read_first>
    - src/construct/schemas/config.py (ProviderConfig, ModelRoutingConfig, SearchCluster patterns)
    - CONSTRUCT-CLAUDE-impl/construct/templates/model-routing.yaml (template structure)
    - .planning/phases/08-search-provider-spine-contract-foundation/08-CONTEXT.md (D-01 config location)
    - .planning/phases/08-search-provider-spine-contract-foundation/08-RESEARCH.md (recommended search.yaml template)
    - pyproject.toml (optional-dependencies block)
    - tests/llm/conftest.py (create_test_workspace fixture to reuse)
  </read_first>
  <action>
    Add to `src/construct/schemas/config.py` per D-01 and SRCH-02:

    - `SearchProviderName(str, Enum)`: values `mock`, `tavily`
    - `SearchCapsConfig`: `max_queries_per_request`, `max_results_per_query`, `max_batch_queries`, `max_raw_content_chars`, `degraded_on_provider_error` (all with sensible defaults matching RESEARCH template)
    - `MockProviderConfig`: `type: Literal["mock"]`, `fixture_dir: str`
    - `TavilyProviderConfig`: `type: Literal["tavily"]`, `api_key_env: str` (required, validated with `ENV_VAR_PATTERN`), `search_depth`, `topic`, `max_results`, `include_raw_content`, `timeout_seconds`, `include_answer`
    - `SearchConfig`: `version: int = 1`, `default_provider: SearchProviderName`, `providers: dict[str, MockProviderConfig | TavilyProviderConfig]` (discriminated by `type`), `caps: SearchCapsConfig`
    - Add `@model_validator(mode="after")` ensuring `default_provider` key exists in `providers` dict

    Create `CONSTRUCT-CLAUDE-impl/construct/templates/search.yaml` with `default_provider: mock`, mock block pointing `fixture_dir: tests/fixtures/search` (relative path resolved from repo root at test time; document in template comment that workspace init copies as-is), tavily block with `api_key_env: TAVILY_API_KEY` only (never literal secret).

    Add to `pyproject.toml`:
    ```toml
    search = ["tavily-python>=0.7,<1"]
    ```

    Create Wave 0 test scaffolds:
    - `tests/search/conftest.py`: `search_workspace` fixture calling `create_test_workspace` then writing `.construct/search.yaml` with mock provider + fixture_dir; `mock_fixtures_dir` fixture
    - `tests/fixtures/search/mock_happy_path.json`, `mock_rate_limit.json`, `mock_auth_error.json`, `mock_network_error.json`, `tavily_basic.json` per RESEARCH fixture format (`query`, optional `latency_ms`, `response.results[]` OR `error.type` + metadata)
    - `tests/search/test_search_config.py`: template round-trip via `SearchConfig.model_validate(yaml.load(...))`; reject invalid `api_key_env`; reject missing default_provider in providers; reject literal secret patterns if `api_key:` key present (validator or test assertion)
    - `tests/search/test_search_provider_mock.py`: placeholder `test_error_injection` and `test_result_cap` marked xfail or skip until Task 2 implements mock provider — remove skip in Task 2

    Do NOT add `load_search_config()` to WorkspaceLoader yet (Plan 02). Do NOT register capability yet.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/search/test_search_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `SearchConfig.model_validate` accepts the new template YAML without error
    - Invalid `api_key_env: "lowercase"` raises ValidationError
    - `tests/search/conftest.py` exists and `search_workspace` fixture creates workspace with `.construct/search.yaml`
    - `pyproject.toml` contains `search = ["tavily-python>=0.7,<1"]` under optional-dependencies
    - Five fixture JSON files exist under `tests/fixtures/search/`
  </acceptance_criteria>
  <done>SearchConfig schema, search.yaml template, pytest scaffolds, and optional Tavily extra are in place; config unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Search module core — errors, models, ABC, mock provider, factory</name>
  <files>src/construct/search/__init__.py, src/construct/search/errors.py, src/construct/search/models.py, src/construct/search/provider.py, src/construct/search/providers/__init__.py, src/construct/search/providers/mock.py, src/construct/search/registry.py, tests/search/test_search_provider_mock.py</files>
  <read_first>
    - src/construct/search/errors.py (to create — mirror WorkspaceLoadError pattern from storage/workspace.py)
    - src/construct/search/models.py (to create)
    - src/construct/search/provider.py (to create — first ABC in codebase)
    - src/construct/search/providers/mock.py (to create — extend MockChatAnthropic pattern from tests/llm/conftest.py)
    - src/construct/search/registry.py (to create)
    - src/construct/schemas/config.py (SearchConfig from Task 1)
    - .planning/phases/08-search-provider-spine-contract-foundation/08-CONTEXT.md (D-02, D-03, D-04, D-06, D-07)
  </read_first>
  <action>
    Create `src/construct/search/` package per locked decisions:

    **`errors.py` (D-07, SRCH-04):** Base `SearchError(Exception)` with `provider_name: str`, `message: str`. Subclasses: `NetworkError`, `RateLimitError` (add `retry_after_seconds: float | None`), `AuthError`, `QuotaExceededError`, `ParseError`, `ProviderUnavailableError`. Chain with `raise ... from exc` at adapter boundaries.

    **`models.py` (D-03):**
    - `SearchResult`: `title`, `url`, `snippet`, `source_tier: int` (Field ge=1 le=5), `score: float` (ge=0.0 le=1.0), `provider_specific: dict[str, Any] = Field(default_factory=dict)`, `source_domain: str | None = None`; `extra="forbid"` on core fields
    - `SearchBatchOutput`: `results: list[SearchResult]`, `truncated: bool = False`, `query: str | None`, `cluster_id: str | None`, `provider_name: str`, `errors: list[dict[str, Any]] = Field(default_factory=list)` for degraded partial batches
    - `ProviderCapabilities`: documents batch support, seed-cluster support, max_results ceiling

    **`provider.py` (D-02):** `SearchProvider(ABC)` with abstract methods:
    - `search(query: str, *, max_results: int, cluster_id: str | None = None) -> SearchBatchOutput`
    - `search_batch(queries: list[str], *, max_results: int) -> list[SearchBatchOutput]`
    - `search_by_seed_cluster(cluster_id: str, workspace: Path, *, max_results: int) -> SearchBatchOutput` — reads `search-seeds.json` via `WorkspaceLoader.load_search_seeds()`, finds cluster by id, builds query from `terms` joined; raises `ParseError` if cluster missing; does NOT write `last_queried` (D-06 read-only)
    - `get_capabilities() -> ProviderCapabilities`

    **`providers/mock.py` (D-04):** `MockSearchProvider(SearchProvider)` loading JSON fixtures from configured `fixture_dir`. Match fixtures by exact `query` string key. Support `latency_ms` sleep, `error.type` raising mapped subclass (`RateLimitError`, `NetworkError`, `AuthError`, etc.). Normalize fixture results to `SearchResult`. Enforce `max_results` cap returning `truncated=True` when exceeded.

    **`registry.py`:** `SearchProviderFactory.create(config: SearchConfig, workspace: Path | None = None) -> SearchProvider`:
    - Resolve `config.default_provider` → instantiate `MockSearchProvider` or defer Tavily to Plan 03 (raise `ProviderUnavailableError` with install hint if tavily selected but adapter not yet present — Plan 03 adds adapter)
    - Apply caps from `config.caps` wrapping provider calls
    - Unknown provider type → `ProviderUnavailableError`

    Implement `tests/search/test_search_provider_mock.py`:
    - `test_happy_path_fixture`: query matching mock_happy_path returns normalized SearchResult fields
    - `test_error_injection`: `__error_rate_limit__` or fixture query raises `RateLimitError`
    - `test_result_cap`: max_results below fixture count sets `truncated=True`
    - `test_search_by_seed_cluster`: with search-seeds.json in workspace returns results for cluster terms

    Guard: `grep -r "from tavily" src/construct/search/` must return zero matches in Plan 01.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/search/test_search_provider_mock.py tests/search/test_search_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `MockSearchProvider.search("quantum gravity")` returns `SearchBatchOutput` with at least one `SearchResult` containing `title`, `url`, `snippet`, `source_tier`, `score`
    - Error fixture raises `RateLimitError` with `retry_after_seconds` when configured
    - Result cap enforcement sets `truncated=True`
    - `search_by_seed_cluster` reads seeds read-only and does not mutate search-seeds.json mtime/content
    - `SearchProviderFactory.create(mock_config)` returns `MockSearchProvider` instance
    - No `from tavily import` anywhere under `src/construct/search/` yet
  </acceptance_criteria>
  <done>Four-method SearchProvider ABC, mock provider with fixture oracle, error taxonomy, normalized models, and factory are implemented with passing unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture JSON → MockSearchProvider | Untrusted file content loaded from fixture_dir |
| SearchConfig YAML → Pydantic | User-editable workspace config crosses validation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Information disclosure | SearchConfig YAML | mitigate | `api_key_env` name only via ENV_VAR_PATTERN validator; test rejects literal `api_key:` in committed template |
| T-8-02 | Tampering | Mock fixture JSON | mitigate | Parse through Pydantic SearchResult models with extra=forbid on core fields |
| T-8-03 | Denial of service | Mock latency_ms | accept | Test-only fixture; production caps enforced in Plan 02 handler |
</threat_model>

<verification>
`.venv/bin/python -m pytest tests/search/ -x -q` passes. `grep -r "from tavily" src/construct/search/` returns no matches.
</verification>

<success_criteria>
SearchConfig validates template YAML; MockSearchProvider serves fixture-driven normalized results with error injection and cap truncation; SearchProviderFactory resolves mock provider; all SRCH-02/03/04 unit foundations pass offline.
</success_criteria>

<output>
After completion, create `.planning/phases/08-search-provider-spine-contract-foundation/08-01-SUMMARY.md`
</output>
