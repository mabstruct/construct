---
phase: 12-curation-l3-gates-review-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/llm/conftest.py
  - tests/llm/test_curation_promote.py
  - tests/llm/test_curation_run.py
  - tests/contract/test_curation_run_cli_mcp.py
  - tests/contract/test_skill_migration.py
autonomous: true
requirements: [CUR-02, CUR-03, CUR-04, CUR-05, API-04]

must_haves:
  truths:
    - "A failing test asserts no canonical write happens before Command(resume=approve)"
    - "A failing test asserts card.evaluate returns PromotionDecision over non-mature cards and escalates on retry failure"
    - "A failing test asserts the curation-cycle placeholder path is unreachable from CLI/MCP"
    - "A failing test asserts migrated skills carry no WebSearch/WebFetch/Write in allowed-tools"
    - "All new tests are RED at end of this plan (implementation lands in later waves)"
  artifacts:
    - path: "tests/llm/test_curation_promote.py"
      provides: "CUR-02 gate unit tests (decision mapping, retry-escalate, outage, key-leak)"
      min_lines: 80
    - path: "tests/llm/test_curation_run.py"
      provides: "CUR-03/04/05 HITL + reviewed-write + no-unreviewed-write integration tests"
      contains: "test_no_writes_before_approval"
    - path: "tests/contract/test_curation_run_cli_mcp.py"
      provides: "curation.review + card.evaluate parity + no-placeholder contract tests"
      contains: "test_no_placeholder_curation_path"
    - path: "tests/contract/test_skill_migration.py"
      provides: "API-04 forbidden-tool guard for the three migrated skills"
      min_lines: 20
    - path: "tests/llm/conftest.py"
      provides: "PromotionDecision + ConnectionTypeDecision mock-seam fixtures"
      contains: "PromotionDecision"
  key_links:
    - from: "tests/llm/test_curation_promote.py"
      to: "construct.llm.factory.build_chat_model"
      via: "monkeypatch seam (ConfigurableStructuredMock)"
      pattern: "build_chat_model"
    - from: "tests/contract/test_skill_migration.py"
      to: "CONSTRUCT-CLAUDE-impl/claude/skills/*/SKILL.md"
      via: "read_text() frontmatter assertion"
      pattern: "allowed-tools"
---

<objective>
Create the Wave 0 failing-test scaffold for Phase 12 so every later implementation
task has a Nyquist verify target that is RED now and goes GREEN as its wave lands.
This plan writes ONLY tests and mock seams — no production code.

Purpose: The anti-placeholder and no-unreviewed-write tests (CUR-05) are the safety
net for the spine invariant "no canonical write without a real judgment AND a human
approval." They MUST exist and fail before the D-10 placeholder removal and the
reviewed-write graft, and pass after.
Output: 1 new gate unit-test file, 1 new skill-migration guard, extensions to the
curation-run integration suite, the curation CLI/MCP contract suite, and the LLM
conftest mock seams.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-curation-l3-gates-review-application/12-CONTEXT.md
@.planning/phases/12-curation-l3-gates-review-application/12-RESEARCH.md
@.planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md
@.planning/phases/12-curation-l3-gates-review-application/12-VALIDATION.md

<interfaces>
<!-- Contracts the new tests assert against. These shapes are CREATED in Plans 02-05;
     the tests here are written against them and stay RED until then. -->

Gate output (Plan 02 creates — tests assert this shape):
  PromotionDecision(extra="forbid"): card_id:str; decision:Literal["promote","hold","escalate"];
    target_lifecycle:Literal["growing","mature"]|None=None; reasoning:str;
    method:Literal["rule-based","llm-judgment"]="llm-judgment"
  ConnectionTypeDecision(extra="forbid"): from_card_id:str; to_card_id:str;
    connection_type:ConnectionType; reasoning:str

Existing runners the integration tests drive (curation_run.py):
  run_curation_run(CurationRunInput) -> CurationRunResult   # line 514
  inspect_curation_run(CurationInspectInput) -> CurationRunResult   # line 552
  review_curation_run(CurationReviewInput) -> CurationRunResult   # Plan 04 creates
  CurationRunResult.status currently Literal["completed","degraded","failed"]
    (Plan 03 adds "awaiting_review")

Mock seam (conftest.py:40-43, 65-101): monkeypatch target
  "construct.llm.factory.build_chat_model"; ConfigurableStructuredMock returns any
  caller-supplied structured object; InvalidOutputMock (conftest.py:104) drives retries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mock seams + card.evaluate / connection-typing gate unit tests (RED)</name>
  <files>tests/llm/conftest.py, tests/llm/test_curation_promote.py</files>
  <read_first>
    - tests/llm/conftest.py (ConfigurableStructuredMock 65-101, InvalidOutputMock 104, monkeypatch seam 40-43)
    - tests/llm/test_research_score.py (analog — mirror its 24 test functions, esp. test_invalid_output_retries_then_succeeds:263, test_invalid_output_skip_with_reason_when_both_fail:278, test_total_outage_when_all_provider_failures:318, test_run_gate_raises_on_total_outage:331, test_sanitized_error_never_leaks_key_token:348)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (curation_promote.py section + conftest section)
    - .planning/phases/12-curation-l3-gates-review-application/12-VALIDATION.md (CUR-02 rows)
  </read_first>
  <action>
    In conftest.py add two fixture builders that hand ConfigurableStructuredMock a
    PromotionDecision-shaped and a ConnectionTypeDecision-shaped canned return; reuse
    InvalidOutputMock for the retry path. Keep the monkeypatch target
    "construct.llm.factory.build_chat_model" unchanged. Do NOT add a new mock class —
    only fixture builders.
    Create tests/llm/test_curation_promote.py mirroring test_research_score.py:
    decision-mapping tests (promote/hold/escalate with reasoning over non-mature cards),
    test_failure_escalates (retry-then-escalate sets decision="escalate" AND
    method="rule-based"; genuine borderline keeps method="llm-judgment"),
    test_total_outage_when_all_provider_failures, test_run_gate_raises_on_total_outage,
    a candidate pre-filter test (lifecycle==mature and archived excluded before fan-out),
    and copy test_sanitized_error_never_leaks_key_token verbatim against the new outage
    class (CardEvaluateOutageError). Add connection-typing gate tests in the same file
    asserting a ConnectionType enum is assigned to a bridge_detect candidate pair with
    reasoning. Import targets from construct.llm.curation_promote / curation_connect (do
    not exist yet — tests are RED).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/llm/test_curation_promote.py -x -q 2>&1 | grep -Eq "ModuleNotFoundError|ImportError|collected" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - test_curation_promote.py collects (or fails on the expected missing import of curation_promote/curation_connect) — it does NOT error on its own syntax.
    - File contains test_failure_escalates and a verbatim key-leak test against CardEvaluateOutageError.
    - conftest.py exposes a PromotionDecision-shaped and a ConnectionTypeDecision-shaped mock fixture; existing research/curation tests still import cleanly.
  </acceptance_criteria>
  <done>Gate unit tests exist and are RED pending Plan 02; conftest mock seams added without breaking existing collection.</done>
</task>

<task type="auto">
  <name>Task 2: Curation-run HITL + reviewed-write integration tests (RED)</name>
  <files>tests/llm/test_curation_run.py</files>
  <read_first>
    - tests/llm/test_curation_run.py (existing; keep test_steps_return_concrete_findings:188 green-eligible)
    - tests/llm/test_research_run.py (analog HITL tests: test_no_writes_before_approval:130, gate-interrupt-only:758, pauses-at-gate:789, cross_process_resume:270, reject_all/approve_all:215, inspect-no-resume:317, idempotent_rerun:390)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (curation_run.py section + test_curation_run.py section)
    - .planning/phases/12-curation-l3-gates-review-application/12-VALIDATION.md (CUR-03/04/05 rows)
  </read_first>
  <action>
    Extend test_curation_run.py mirroring test_research_run.py by name:
    test_no_writes_before_approval (load cards/connections snapshot, run to the
    interrupt, assert no card lifecycle change, no new connection in connections.json,
    no archive — CUR-03 spine), test_reviewed_promotion_applied (approve → edit_card
    lifecycle write; reject → no write), test_reviewed_connection_idempotent (approve →
    add_connection write; rerun → "Connection already exists", no duplicate),
    test_reviewed_archive_applied (archive only when approved AND
    auto_archive_on_decay=true), test_single_consolidated_gate (one gate_id, one resume
    covers promotion+connection+archive), test_empty_queue_no_pause (clean run →
    completed without awaiting_review), test_no_unreviewed_writes (assert every write
    call originates downstream of the interrupt node), test_cross_process_resume (reopen
    a fresh checkpointer and resume), test_inspect_pending_review (inspect reports
    awaiting_review + gate_queue, never resumes), test_curation_events_emitted (per-step
    + gate-review events via append_event per spec §6.6). Use the conftest mock seams
    from Task 1 for the gate LLM calls. Drive runners run_curation_run /
    review_curation_run / inspect_curation_run.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/llm/test_curation_run.py -q 2>&1 | grep -Eq "failed|error|passed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - The new test functions named above all exist (grep -c finds each of test_no_writes_before_approval, test_single_consolidated_gate, test_no_unreviewed_writes, test_inspect_pending_review).
    - The new tests are RED (review_curation_run import / awaiting_review status absent until Plans 03-04); test_steps_return_concrete_findings:188 is unaffected.
  </acceptance_criteria>
  <done>HITL + reviewed-write + no-unreviewed-write integration tests exist and are RED pending Plans 03-04.</done>
</task>

<task type="auto">
  <name>Task 3: CLI/MCP parity + no-placeholder contract test + skill-migration guard (RED)</name>
  <files>tests/contract/test_curation_run_cli_mcp.py, tests/contract/test_skill_migration.py</files>
  <read_first>
    - tests/contract/test_curation_run_cli_mcp.py (existing _CAPS:30-33, test_registered:46, test_mcp_no_hardcoded_curation:76-82, CLI presence:88-91)
    - src/construct/capabilities/catalog.py (_get_workflow_steps usage at 308-312; definition ~725-741)
    - src/construct/cli.py (_get_workflow_steps_from_registry:208-216, workflow resume:262)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md (allowed-tools line 3 carries WebSearch, WebFetch)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (catalog + skill sections + Discrepancy 3)
  </read_first>
  <action>
    Extend _CAPS with "curation.review":"construct_curation_review" and
    "card.evaluate":"construct_card_evaluate" and reuse every existing assertion
    (registration, positional-arg rejection, MCP auto-discovery, CLI presence). Add
    test_no_placeholder_curation_path asserting the symbol _get_workflow_steps is gone
    from catalog.py AND that no "curation-cycle" placeholder handler is reachable from
    either the registry or the cli workflow group (grep the read_text of both files for
    the deleted symbol). Keep test_mcp_no_hardcoded_curation green (do NOT edit
    mcp/server.py). Create tests/contract/test_skill_migration.py: for each of
    construct-research-cycle, construct-curation-cycle, construct-card-evaluate read the
    SKILL.md frontmatter and assert no "WebSearch", "WebFetch", or workspace-write tool
    (Write/Edit) remains in allowed-tools (mirror the read_text + assert-not-in pattern).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/contract/test_curation_run_cli_mcp.py tests/contract/test_skill_migration.py -q 2>&1 | grep -Eq "failed|passed|error" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - _CAPS contains curation.review and card.evaluate; test_no_placeholder_curation_path exists.
    - test_skill_migration.py asserts forbidden tools absent for all three named skills (currently RED — construct-research-cycle still carries WebSearch/WebFetch).
    - test_mcp_no_hardcoded_curation remains present and is not weakened.
  </acceptance_criteria>
  <done>Contract parity, no-placeholder, and skill-migration guards exist and are RED pending Plans 05-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → factory seam | tests inject mock LLM output via monkeypatch; no real provider/key crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | key-leak regression test | mitigate | Copy test_sanitized_error_never_leaks_key_token verbatim against CardEvaluateOutageError so the Plan 02 sanitizer is guarded from day one |
| T-12-02 | Tampering | unreviewed-write safety net | mitigate | test_no_writes_before_approval + test_no_unreviewed_writes authored here are the executable contract enforcing CUR-03 in Plans 03-04 |
| T-12-SC | Tampering | npm/pip installs | accept | No package installs in this phase (RESEARCH: zero new deps); no slopcheck surface |
</threat_model>

<verification>
- `.venv/bin/pytest tests/llm/test_curation_promote.py tests/llm/test_curation_run.py tests/contract/test_curation_run_cli_mcp.py tests/contract/test_skill_migration.py -q` collects all new tests (RED is expected — implementation is in later waves).
- `.venv/bin/pytest tests/ -q` shows no NEW collection errors from unrelated suites (existing green tests stay green; the new RED tests are the only additions).
</verification>

<success_criteria>
- All Wave 0 gaps from 12-VALIDATION.md exist as failing tests.
- No production code touched; only tests/ and conftest seams.
- Each later-wave task has a named, currently-RED test to turn GREEN.
</success_criteria>

<output>
Create `.planning/phases/12-curation-l3-gates-review-application/12-01-SUMMARY.md` when done.
</output>
