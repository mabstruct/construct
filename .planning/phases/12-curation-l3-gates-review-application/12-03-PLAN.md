---
phase: 12-curation-l3-gates-review-application
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - src/construct/llm/curation_run.py
autonomous: true
requirements: [CUR-03]
decisions: [D-02, D-05, D-06, D-07]

must_haves:
  truths:
    - "The graph computes all promotion, connection, and archive proposals into ONE gate_queue before any pause"
    - "process_inbox contains only interrupt() — no writes, no event emission"
    - "A run with an empty gate_queue routes straight to compile_report without pausing"
    - "The promotion producer evaluates only non-mature, non-archived cards"
    - "CurationRunResult.status can be awaiting_review"
  artifacts:
    - path: "src/construct/llm/curation_run.py"
      provides: "gate_queue state channel, CurationProposal envelope, producer nodes, interrupt-only process_inbox, conditional short-circuit edge"
      contains: "def process_inbox"
  key_links:
    - from: "src/construct/llm/curation_run.py promotion_review"
      to: "construct.llm.curation_promote.evaluate_all"
      via: "producer node calls the L3 gate"
      pattern: "evaluate_all"
    - from: "src/construct/llm/curation_run.py connection_maintenance"
      to: "construct.llm.curation_connect"
      via: "bridge_detect candidates → connection-typing gate → gate_queue"
      pattern: "bridge_detect"
    - from: "src/construct/llm/curation_run.py process_inbox"
      to: "langgraph.interrupt"
      via: "single consolidated pause"
      pattern: "interrupt\\("
---

<objective>
Graft the read-side of the research_run HITL machine onto the durable Phase-11
curation graph: extend the state channels, turn the three deferred skip-nodes into
PROPOSAL PRODUCERS that compute promotion / connection / archive proposals into ONE
heterogeneous gate_queue, replace process_inbox with an interrupt-only consolidated
review gate, and add the conditional short-circuit so an empty queue never pauses.

Purpose: This establishes the CUR-03 spine "by construction" — all proposals are
computed before the single pause; no write node exists yet upstream of the interrupt.
Output: curation_run.py read-side (producers + interrupt + routing). NO writes yet
(Plan 04 adds the post-gate apply nodes). No graph restructuring of the deterministic
integrity/decay/orphan prefix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-curation-l3-gates-review-application/12-CONTEXT.md
@.planning/phases/12-curation-l3-gates-review-application/12-RESEARCH.md
@.planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md

<interfaces>
<!-- Analog spine + the gates this plan consumes. -->

Analog (research_run.py): state channels 71-102 (gate_queue/decisions/status) ·
  GateQueueEntry:142-153 · gate_review (interrupt-only):437-452 ·
  _route_after_score + add_conditional_edges:831-867 · run_research_run pause
  detection:933-942

Existing curation_run.py: CurationRunState · CurationRunResult.status
  Literal["completed","degraded","failed"]:137 · _deferred_step:261 ·
  promotion_review/process_inbox/views_refresh_hook skip-nodes:451-460 ·
  build_curation_run_graph:466-497 · connection_maintenance (already calls bridge_detect):395-422 ·
  decay_scan (auto_archive_on_decay read; archived filter):194/317 · _aggregate_status:503-511

Gates (Plan 02): curation_promote.evaluate_all(cards,...)->list[PromotionDecision];
  curation_connect.type_all(candidates,...)->list[ConnectionTypeDecision]

Recommended envelope (Claude's discretion — RESEARCH Code Examples):
  CurationProposal(extra="forbid"): kind: Literal["promotion","connection","archive","escalate"];
    decision: str (default = gate recommendation, D-07); payload: dict
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: State channels + CurationProposal envelope + awaiting_review status</name>
  <files>src/construct/llm/curation_run.py</files>
  <read_first>
    - src/construct/llm/curation_run.py (CurationRunState, CurationRunResult:130-137, status comment:86)
    - src/construct/llm/research_run.py (state channels 71-102, GateQueueEntry 142-153)
    - tests/llm/test_curation_run.py (test_single_consolidated_gate, test_inspect_pending_review — RED)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (curation_run.py "Extend the state channels" + Pitfall 5)
  </read_first>
  <action>
    Add to CurationRunState: gate_queue: list[dict] (CurationProposal dumps), decisions:
    Any (resume payload), and the per-write output channels Plan 04 will fill. Define the
    CurationProposal envelope model (extra="forbid", kind:
    Literal["promotion","connection","archive","escalate"], decision: str default = the
    gate recommendation per D-07, payload: dict) in curation_run.py. Add "awaiting_review"
    to the CurationRunResult.status Literal (now completed|degraded|failed|awaiting_review).
    Keep state holding only plain serializable data — never a WorkspaceLoader / sqlite conn
    / LLM client (Pitfall 3).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from construct.llm.curation_run import CurationProposal, CurationRunResult; import typing; assert 'awaiting_review' in str(CurationRunResult.model_fields['status'].annotation); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - CurationProposal importable with kind/decision/payload and extra="forbid".
    - CurationRunResult.status Literal includes awaiting_review.
    - No loader/conn/client stored in state (grep -v '^#' curation_run.py | grep -c "state\[.*WorkspaceLoader" == 0).
  </acceptance_criteria>
  <done>State carries a heterogeneous gate_queue + decisions; awaiting_review is a first-class status.</done>
</task>

<task type="auto">
  <name>Task 2: Producer nodes + interrupt-only process_inbox + empty-queue short-circuit</name>
  <files>src/construct/llm/curation_run.py</files>
  <read_first>
    - src/construct/llm/curation_run.py (promotion_review/process_inbox skip-nodes 451-460, connection_maintenance 395-422, decay_scan 194/317, build_curation_run_graph 466-497)
    - src/construct/llm/research_run.py (gate_review interrupt-only 437-452, _route_after_score + add_conditional_edges 831-867)
    - src/construct/llm/curation_promote.py, src/construct/llm/curation_connect.py (Plan 02 gates)
    - src/construct/pipelines/bridge_detect.py (candidate output 233-282)
    - tests/llm/test_curation_run.py (test_no_writes_before_approval, test_empty_queue_no_pause, test_single_consolidated_gate — RED)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (producer nodes + Pattern 1/2; Pitfalls 1,2,5)
  </read_first>
  <action>
    Replace the three _deferred_step bodies with real producers (NO pauses, NO writes —
    they write ONLY into gate_queue):
    (a) promotion_review: load cards, pre-filter lifecycle!=mature AND !=archived (D-02),
        call curation_promote.evaluate_all, enqueue only promote + escalate items as
        CurationProposal(kind="promotion"|"escalate"); plain hold is events-only (D-07,
        logged not enqueued).
    (b) connection_maintenance: keep the existing bridge_detect call, feed candidate pairs
        to curation_connect.type_all, enqueue each typed result as
        CurationProposal(kind="connection").
    (c) a decay-archive producer: from decay_scan candidates, when
        governance auto_archive_on_decay is true, enqueue CurationProposal(kind="archive")
        per decayed card (skip already-archived).
    Replace process_inbox with an interrupt-ONLY node: decisions =
    interrupt({"gate_id": ..., "gate_queue": state["gate_queue"]}); return
    {"decisions": decisions}. NOTHING else in that node (no write, no append_event) —
    re-runs top-to-bottom on resume (Pitfall 1). Add a _route_before_inbox conditional
    edge from the last producer: "process_inbox" if gate_queue else "compile_report"
    (Pitfall 2). Teach _aggregate_status the awaiting_review status so a paused/reviewed
    gate is NOT degraded (Pitfall 5). Sanitize any node exception via the existing
    _sanitize_error (222-231) — never echo raw provider text.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/llm/test_curation_run.py -k "no_writes_before_approval or empty_queue or single_consolidated or interrupt or pauses" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_no_writes_before_approval, test_empty_queue_no_pause, test_single_consolidated_gate pass.
    - process_inbox body contains exactly one interrupt() and no add_connection/edit_card/archive_card/append_event call (grep -v '^#' over the node).
    - Promotion producer excludes mature and archived cards before calling evaluate_all.
    - An empty gate_queue completes the run without status awaiting_review.
  </acceptance_criteria>
  <done>The graph computes all proposals into one queue, pauses once at an interrupt-only gate, and short-circuits cleanly on an empty queue. Read-side CUR-03 spine holds by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producers → gate_queue | proposals computed from LLM gates + card content; checkpointed but not yet written to SOT |
| run-start pass ↔ resume pass | LangGraph re-executes the interrupted node top-to-bottom on resume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | process_inbox interrupt node | mitigate | Node holds ONLY interrupt() — no write, no event (Pitfall 1); test_no_writes_before_approval enforces it. Pre-approval write is impossible because no write node exists upstream of the interrupt |
| T-12-07 | Elevation of Privilege | producer proposals from card content | mitigate | Producers only enqueue typed CurationProposal envelopes; injected card text can at most propose — the Plan 04 write boundary sits strictly downstream of the human resume |
| T-12-08 | Tampering (transport) | producer/gate node logging | mitigate | logging only, never print() (Pitfall 6); exceptions routed through _sanitize_error |
| T-12-SC | Tampering | npm/pip installs | accept | No new packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/llm/test_curation_run.py -k "no_writes or empty_queue or single_consolidated" -x -q` GREEN.
- `.venv/bin/pytest tests/llm/test_curation_run.py::test_steps_return_concrete_findings -x -q` still GREEN (no placeholder regressions).
- Grep proves process_inbox has no write/event side effect.
</verification>

<success_criteria>
- Three producers enqueue promotion/connection/archive/escalate proposals into one gate_queue.
- process_inbox is interrupt-only; empty queues short-circuit to compile_report.
- _aggregate_status knows awaiting_review.
- No canonical write exists upstream of the interrupt (write nodes are Plan 04).
</success_criteria>

<output>
Create `.planning/phases/12-curation-l3-gates-review-application/12-03-SUMMARY.md` when done.
</output>
