---
phase: 12-curation-l3-gates-review-application
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/construct/llm/curation_promote.py
  - src/construct/llm/curation_connect.py
  - src/construct/llm/config.yaml
autonomous: true
requirements: [CUR-02, CUR-03]

must_haves:
  truths:
    - "card.evaluate produces a real promote/hold/escalate PromotionDecision with reasoning for every non-mature card"
    - "A card that fails the LLM gate after one retry is escalated with method='rule-based'"
    - "The connection-typing gate assigns a ConnectionType to a bridge_detect candidate pair with reasoning"
    - "Total provider outage raises a gate error; partial failures degrade per-item"
    - "Provider error text is sanitized to class-name + safe line — no key token leaks"
  artifacts:
    - path: "src/construct/llm/curation_promote.py"
      provides: "card.evaluate L3 gate: PromotionDecision, evaluate_one, evaluate_all, run_gate, CardEvaluateOutageError"
      contains: "class PromotionDecision"
      min_lines: 200
    - path: "src/construct/llm/curation_connect.py"
      provides: "connection-typing L3 gate: ConnectionTypeDecision, type_one, type_all"
      contains: "class ConnectionTypeDecision"
      min_lines: 120
    - path: "src/construct/llm/config.yaml"
      provides: "card.evaluate + curation.connection_type gate config entries"
      contains: "card.evaluate"
  key_links:
    - from: "src/construct/llm/curation_promote.py"
      to: "construct.llm.factory.build_chat_model"
      via: "build_scoring_llm seam"
      pattern: "factory\\.build_chat_model"
    - from: "src/construct/llm/curation_connect.py"
      to: "construct.schemas.workspace.ConnectionType"
      via: "structured-output target enum"
      pattern: "ConnectionType"
---

<objective>
Create the two L3 LLM judgment gates this phase needs, by copying research_score.py
symbol-for-symbol: card.evaluate (promotion judgment, CUR-02) and the connection-typing
gate (D-05, feeds CUR-03). Add their config.yaml entries so they do not fall back to
the research.score provider/cap.

Purpose: These are pure judgment producers — they never write to the SOT. They turn
cards and bridge candidates into reviewable proposals; the human gate (Plans 03-04) is
the write constraint.
Output: curation_promote.py, curation_connect.py, and two new gates blocks in config.yaml.
Turns the Plan 01 tests/llm/test_curation_promote.py suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-curation-l3-gates-review-application/12-CONTEXT.md
@.planning/phases/12-curation-l3-gates-review-application/12-RESEARCH.md
@.planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md

<interfaces>
<!-- Source contracts to copy from / write against. -->

Analog (copy structure end-to-end): src/construct/llm/research_score.py
  ScoredFinding:44-61 · build_scoring_llm:190-197 · score_one:250-297 ·
  _score_one_with_retry:406-434 · score_all:458-532 · run_gate ·
  _is_provider_outage_cause/_safe_scoring_cause/_PROVIDER_OUTAGE_*/_CODE_BOUNDARY_PATTERN:326-389 ·
  ResearchScoreOutageError · gate-config fallback to research.score:543

Spec §6.4 PromotionDecision (gate output contract):
  card_id; decision: promote|hold|escalate; target_lifecycle: growing|mature|None;
  reasoning; method: rule-based|llm-judgment

Enums (use real values — NOT "seedling"):
  Lifecycle (card.py:35-39): seed | growing | mature | archived
  ConnectionType (workspace.py:29): supports|contradicts|extends|parallels|requires|enables|challenges|inspires|gap-for

Connection-gate input (bridge_detect.py:233-244 candidate dict):
  pair_id, from_card_id, to_card_id, from_domain, to_domain, from_title, to_title,
  l1_structural, l2_shared_categories, pre_score
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: card.evaluate promotion gate (curation_promote.py)</name>
  <files>src/construct/llm/curation_promote.py</files>
  <read_first>
    - src/construct/llm/research_score.py (full module — the symbol-for-symbol analog)
    - src/construct/schemas/card.py (Lifecycle enum 35-39, CardAuthor 62)
    - src/construct/llm/curation_run.py (archived-exclusion guard at 317; _validate_run_id 53-66)
    - tests/llm/test_curation_promote.py (the RED suite this task turns GREEN)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (curation_promote.py section; Discrepancy 1)
  </read_first>
  <behavior>
    - evaluate_one(card,...) returns PromotionDecision(decision in promote|hold|escalate, reasoning non-empty)
    - candidate pre-filter excludes lifecycle==mature AND lifecycle==archived before fan-out (D-02)
    - failed card after one retry → PromotionDecision(decision="escalate", method="rule-based", reasoning=safe_cause); borderline → method="llm-judgment"
    - evaluate_all: scored_ok==0 and provider_failures==len(items) → total_outage True; run_gate raises CardEvaluateOutageError
    - sanitizer never echoes a raw provider key token
  </behavior>
  <action>
    Mirror research_score.py: score_one→evaluate_one, _score_one_with_retry→
    _evaluate_one_with_retry (two-attempt loop verbatim, swap _skip_finding_for_failure
    for _escalate_decision_for_failure setting decision="escalate", method="rule-based"),
    score_all→evaluate_all (ThreadPoolExecutor(max_workers=max(1,min(cap,len(items)))),
    as_completed, ordered results, total-outage discrimination), plus run_gate. Define
    PromotionDecision IN THIS MODULE (extra="forbid", target_lifecycle:
    Literal["growing","mature"]|None) — never in catalog.py (circular-import hazard).
    build_scoring_llm routes through factory.build_chat_model. Use
    with_structured_output(PromotionDecision, method="json_schema"). DROP the
    research.score relevance clamp_action — promotion has no relevance band; the human
    gate is the constraint. Copy _is_provider_outage_cause / _safe_scoring_cause /
    _PROVIDER_OUTAGE_* / _CODE_BOUNDARY_PATTERN verbatim; rename ResearchScoreOutageError
    → CardEvaluateOutageError. Apply the D-02 pre-filter (skip mature and archived) before
    the fan-out, mirroring curation_run.py:317. Resolve gate config under id
    "card.evaluate".
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/llm/test_curation_promote.py -k "promote or evaluate or escalate or outage or leak or filter" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/llm/test_curation_promote.py promotion/escalate/outage/key-leak/pre-filter tests pass.
    - PromotionDecision.target_lifecycle rejects "seed"/"seedling"/"archived" (Pydantic Literal); only growing|mature|None accepted.
    - No fenced gate model defined in catalog.py; models live in curation_promote.py.
  </acceptance_criteria>
  <done>card.evaluate gate produces real promote/hold/escalate judgments with retry-then-escalate and outage discrimination; CUR-02 unit tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: connection-typing gate (curation_connect.py)</name>
  <files>src/construct/llm/curation_connect.py</files>
  <read_first>
    - src/construct/llm/research_score.py (fan-out/retry/outage skeleton — same as Task 1)
    - src/construct/pipelines/bridge_detect.py (candidate shape 233-282)
    - src/construct/schemas/workspace.py (ConnectionType enum 29-40, ConnectionRecord.type required 48-54)
    - tests/llm/test_curation_promote.py (connection-typing gate tests, RED)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (curation_connect.py section; Discrepancy 2)
  </read_first>
  <behavior>
    - type_one(candidate) returns ConnectionTypeDecision(connection_type is a valid ConnectionType, reasoning non-empty)
    - input is a bridge_detect candidate pair dict, NOT an existing ConnectionRecord
    - type_all reuses the same retry + total-outage discrimination as card.evaluate
    - a still-failing candidate after retry is dropped/skipped (no invalid connection proposed)
  </behavior>
  <action>
    Create curation_connect.py structurally identical to curation_promote.py's fan-out:
    define ConnectionTypeDecision IN THIS MODULE (extra="forbid", from_card_id,
    to_card_id, connection_type: ConnectionType [required enum, no None — Discrepancy 2],
    reasoning). type_one builds messages from the bridge_detect candidate fields
    (from_title/to_title/from_domain/to_domain/l2_shared_categories) and calls
    with_structured_output(ConnectionTypeDecision, method="json_schema"). Reuse the same
    ThreadPoolExecutor fan-out, _evaluate_one_with_retry-style two-attempt loop, and the
    CardEvaluateOutageError-style outage class + verbatim sanitizer (import or re-derive
    from curation_promote to avoid duplication). The gate only PROPOSES a type; the
    actual add_connection write is a Plan 04 post-gate node. Resolve gate config under id
    "curation.connection_type".
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/llm/test_curation_promote.py -k "connection or type" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Connection-typing tests pass; ConnectionTypeDecision.connection_type rejects a non-ConnectionType string.
    - type_all input accepts a bridge_detect candidate dict (no ConnectionRecord required).
    - No SOT write performed by this module (grep -v '^#' src/construct/llm/curation_connect.py | grep -c "add_connection" returns 0).
  </acceptance_criteria>
  <done>Connection-typing gate assigns a ConnectionType to bridge candidates with reasoning; D-05 gate tests GREEN; no write performed.</done>
</task>

<task type="auto">
  <name>Task 3: gate config entries in config.yaml</name>
  <files>src/construct/llm/config.yaml</files>
  <read_first>
    - src/construct/llm/config.yaml (gates.research.score block 24-31)
    - src/construct/llm/research_score.py (config fallback to research.score:543 — what we are preventing)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (config.yaml section; RESEARCH A2)
  </read_first>
  <action>
    Under gates: add a card.evaluate block and a curation.connection_type block mirroring
    the research.score block exactly (provider: anthropic, temperature: 0.2,
    review_required: true, concurrency_cap: 5). This prevents the run_gate resolver from
    silently falling back to the research.score provider/cap for the two new gate ids.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import yaml,pathlib; g=yaml.safe_load(pathlib.Path('src/construct/llm/config.yaml').read_text())['gates']; assert 'card.evaluate' in g and 'curation.connection_type' in g, g.keys(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - config.yaml gates map contains card.evaluate and curation.connection_type with provider/temperature/review_required/concurrency_cap.
    - Loading config does not raise; existing research.score block unchanged.
  </acceptance_criteria>
  <done>Both new gate ids resolve their own config; no research.score fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider → gate | untrusted model output (card content may carry prompt injection) crosses into structured PromotionDecision/ConnectionTypeDecision |
| provider exception → logs/result | provider error text may carry an API key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Elevation of Privilege | card.evaluate / connection gate output | mitigate | with_structured_output(extra="forbid") confines output to the typed schema; the gate only PROPOSES — no write surface in either module (verified by grep gate in Task 2). The human approval gate (Plans 03-04) is the write constraint |
| T-12-04 | Information Disclosure | provider exception handling | mitigate | Copy _is_provider_outage_cause/_safe_scoring_cause + _CODE_BOUNDARY_PATTERN verbatim; outage class carries a pre-sanitized safe_message; key-leak test (Plan 01 T-12-01) guards it |
| T-12-05 | Tampering (transport) | gate node logging | mitigate | logging only, never print() — stdout is the MCP JSON-RPC transport (Pitfall 6) |
| T-12-SC | Tampering | npm/pip installs | accept | Zero new packages; reuses installed langchain-core/pydantic |
</threat_model>

<verification>
- `.venv/bin/pytest tests/llm/test_curation_promote.py -x -q` GREEN.
- `.venv/bin/pytest tests/llm/ -q` shows no regression in research_score tests.
- `grep -v '^#' src/construct/llm/curation_promote.py src/construct/llm/curation_connect.py | grep -c "print("` returns 0 (logging-only).
</verification>

<success_criteria>
- Both gates mirror research_score.py's retry + bounded fan-out + outage discrimination.
- Gate models defined in their own modules (no catalog.py circular-import).
- target_lifecycle constrained to growing|mature|None; ConnectionType is a required enum.
- config.yaml carries both gate ids.
</success_criteria>

<output>
Create `.planning/phases/12-curation-l3-gates-review-application/12-02-SUMMARY.md` when done.
</output>
