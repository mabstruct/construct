---
phase: 12-curation-l3-gates-review-application
plan: 06
type: execute
wave: 6
depends_on: ["12-05"]
files_modified:
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-card-evaluate/SKILL.md
autonomous: false
requirements: [API-04]

must_haves:
  truths:
    - "No migrated skill carries WebSearch, WebFetch, or a workspace-write tool in allowed-tools"
    - "construct-research-cycle delegates to construct research run / research review"
    - "construct-curation-cycle delegates to construct curation run / curation review and drives the consolidated review loop"
    - "construct-card-evaluate is a thin wrapper over construct card evaluate with no inline judgment rules"
  artifacts:
    - path: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md"
      provides: "thin research orchestrator (scope → CLI → review loop → narrate)"
      contains: "construct research"
    - path: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md"
      provides: "thin curation orchestrator delegating to curation run/review"
      contains: "construct curation"
    - path: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-card-evaluate/SKILL.md"
      provides: "thin wrapper over construct card evaluate"
      contains: "construct card evaluate"
  key_links:
    - from: "construct-curation-cycle/SKILL.md"
      to: "construct curation run / construct curation review"
      via: "Bash(construct ...) delegation + conversational review loop"
      pattern: "construct curation"
    - from: "construct-card-evaluate/SKILL.md"
      to: "construct card evaluate"
      via: "thin CLI wrapper"
      pattern: "construct card evaluate"
---

<objective>
Migrate the three Claude-native skills to thin orchestrators that delegate to the
Python CLI/MCP capabilities, removing every direct WebSearch / WebFetch / workspace
write (API-04). The judgment and write logic now lives in Python (Plans 02-05); the
skills only negotiate scope, invoke the capability, drive the review loop
conversationally, and narrate the digest/report.

Purpose: API-04 — one coherent delegation story, no duplicate judgment logic left in
skill text, no skill that can write the SOT directly.
Output: three migrated SKILL.md files. Turns tests/contract/test_skill_migration.py
GREEN, then a human verifies the conversational review loop end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-curation-l3-gates-review-application/12-CONTEXT.md
@.planning/phases/12-curation-l3-gates-review-application/12-RESEARCH.md
@.planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md

<interfaces>
<!-- Migration source + the thin-delegator analog + the CLI commands skills call. -->

Analog (thin delegator): CONSTRUCT-CLAUDE-impl/claude/skills/construct-card-create/SKILL.md
  (118 lines, "The skill drives the conversation; Python enforces contracts.")

Migration sources (D-08/D-09):
  construct-research-cycle/SKILL.md (215 lines; allowed-tools line 3 carries
    WebSearch, WebFetch; steps 3-5 inline search/score/ingest)
  construct-curation-cycle/SKILL.md (286 lines; already mostly delegates)
  construct-card-evaluate/SKILL.md (178 lines; inline promotion rules 57-120 → fold into Python gate)

CLI commands the skills delegate to (shipped + Plans 02-05):
  construct research run / construct research review (Phase 10)
  construct curation run / construct curation review / construct card evaluate (this phase)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate research-cycle + curation-cycle skills to thin orchestrators</name>
  <files>CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md, CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md (allowed-tools line 3; inline steps 3-5)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md (current delegation)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-card-create/SKILL.md (thin-delegator analog)
    - tests/contract/test_skill_migration.py (the RED guard)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (skill migration section; D-08)
  </read_first>
  <action>
    construct-research-cycle: drop WebSearch, WebFetch from the allowed-tools frontmatter
    (keep Read, Bash(construct), MCP(connect)); replace inline steps 3-5 with: negotiate
    scope (domains/seeds, candidate count) → invoke `construct research run` → present the
    pending gate queue → collect approve/reject → `construct research review` to resume →
    narrate the digest. Zero direct WebSearch/WebFetch/workspace writes.
    construct-curation-cycle: keep allowed-tools (Read, Bash(construct), MCP(connect));
    rewire its body to `construct curation run` → present the consolidated gate_queue
    (promotion/connection/archive/escalate items, surfacing the method field so
    failure-escalations are visibly distinct from borderline ones) → collect
    approve/reject (approve-all/reject-all) → `construct curation review` to resume →
    narrate the report. No inline curation logic survives.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/contract/test_skill_migration.py -k "research_cycle or curation_cycle" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_skill_migration assertions for research-cycle and curation-cycle pass (no WebSearch/WebFetch/Write in allowed-tools).
    - `grep -c "WebSearch\|WebFetch" construct-research-cycle/SKILL.md` returns 0.
    - Both skills reference `construct research run`/`research review` and `construct curation run`/`curation review` respectively.
  </acceptance_criteria>
  <done>research-cycle and curation-cycle are thin orchestrators delegating to the CLI with no direct web/search/write tools.</done>
</task>

<task type="auto">
  <name>Task 2: Fold card-evaluate skill into a thin wrapper over construct card evaluate</name>
  <files>CONSTRUCT-CLAUDE-impl/claude/skills/construct-card-evaluate/SKILL.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-card-evaluate/SKILL.md (inline promotion rules 57-120)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-card-create/SKILL.md (thin-delegator analog)
    - tests/contract/test_skill_migration.py (card-evaluate guard)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (skill migration; D-09)
  </read_first>
  <action>
    Replace the inline promotion-judgment rules (lines ~57-120) with a thin wrapper that
    invokes `construct card evaluate` and presents its PromotionDecision output
    (promote/hold/escalate + reasoning + method). No duplicate LLM-judgment logic in skill
    text — the Python card.evaluate gate (Plan 02) is the single source of judgment.
    allowed-tools carries no WebSearch/WebFetch/workspace-write tool. Surface the method
    field so failure-driven escalations (method="rule-based") read differently from
    borderline ones (method="llm-judgment").
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/contract/test_skill_migration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Full test_skill_migration.py passes for all three skills.
    - card-evaluate SKILL.md contains `construct card evaluate` and no inline promotion-threshold ruleset.
  </acceptance_criteria>
  <done>card-evaluate is a thin wrapper over the Python gate; no duplicate judgment logic remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify the conversational review loop end-to-end</name>
  <what-built>Three migrated skills that delegate to construct research/curation/card CLI capabilities with no direct WebSearch/WebFetch/workspace writes; the curation skill drives the consolidated approve/reject review loop.</what-built>
  <how-to-verify>
    1. Run the migrated `construct-curation-cycle` skill against `test-ws/my-construct`.
    2. Confirm it invokes `construct curation run` (not inline logic) and pauses presenting the consolidated gate_queue (promotion/connection/archive/escalate items, with the method field visible).
    3. Approve a subset, reject the rest; confirm it calls `construct curation review` and only approved items are written (re-run `construct curation inspect` to see outcomes).
    4. Confirm zero direct WebSearch/WebFetch/Write occurred during the session.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the skill review loop.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → tool surface | a skill's allowed-tools defines what it can do directly; web/write tools would bypass the Python contract layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | Elevation of Privilege | skill allowed-tools | mitigate | Drop WebSearch/WebFetch/workspace-write from all three skills; test_skill_migration.py asserts their absence so a regression fails CI. All writes flow through the Python reviewed-write path |
| T-12-18 | Tampering | duplicate judgment logic in skill text | mitigate | card-evaluate inline rules folded into the Python gate (D-09); no second, unguarded judgment path can drift from the tested gate |
| T-12-SC | Tampering | npm/pip installs | accept | No new packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/contract/test_skill_migration.py -x -q` GREEN.
- `.venv/bin/pytest tests/ -q` fully GREEN (phase gate).
- Human-verify of the curation skill review loop signed off.
</verification>

<success_criteria>
- All three skills carry no WebSearch/WebFetch/workspace-write tools.
- Skills delegate to construct research/curation/card capabilities; no inline judgment or web logic.
- The consolidated review loop works conversationally end-to-end (human-verified).
</success_criteria>

<output>
Create `.planning/phases/12-curation-l3-gates-review-application/12-06-SUMMARY.md` when done.
</output>
