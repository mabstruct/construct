---
phase: 12-curation-l3-gates-review-application
plan: 05
type: execute
wave: 5
depends_on: ["12-02", "12-04"]
files_modified:
  - src/construct/capabilities/catalog.py
  - src/construct/cli.py
autonomous: true
requirements: [CUR-02, CUR-03, CUR-05]

must_haves:
  truths:
    - "card.evaluate and curation.review are registered with cli_name + mcp_tool_name and invokable from CLI and MCP"
    - "curation.inspect surfaces pending-review state in its description and output"
    - "The legacy curation-cycle placeholder lambdas are removed from BOTH catalog.py and cli.py"
    - "No placeholder no-op handler is reachable from the CLI workflow group or the registry"
    - "MCP parity is achieved without editing mcp/server.py"
  artifacts:
    - path: "src/construct/capabilities/catalog.py"
      provides: "card.evaluate + curation.review registration + shims; _get_workflow_steps removed"
      contains: "card.evaluate"
    - path: "src/construct/cli.py"
      provides: "curation review command; workflow placeholder rewired/removed"
      contains: "curation"
  key_links:
    - from: "src/construct/capabilities/catalog.py"
      to: "construct.llm.curation_run.review_curation_run"
      via: "curation.review handler shim"
      pattern: "review_curation_run"
    - from: "src/construct/capabilities/catalog.py"
      to: "construct.llm.curation_promote"
      via: "card.evaluate handler shim"
      pattern: "card.evaluate"
---

<objective>
Expose the new capabilities and excise the legacy fake-success path. Register
card.evaluate and curation.review through the existing cli_name + mcp_tool_name shim
pattern (MCP parity is free via registry auto-discovery), extend the curation.inspect
description, add the `curation review` CLI command, and remove the D-10
`_get_workflow_steps("curation-cycle")` placeholder lambdas from BOTH catalog.py and
cli.py atomically (Pitfall 6 — the symbol is imported/called in two files).

Purpose: closes CUR-05's "no placeholder reachable" requirement and surfaces the new
gates/review on the canonical CLI/MCP surface so the skills (Plan 06) can delegate to
them.
Output: catalog.py + cli.py. Turns the Plan 01 contract tests
(test_no_placeholder_curation_path, curation.review/card.evaluate registration) GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-curation-l3-gates-review-application/12-CONTEXT.md
@.planning/phases/12-curation-l3-gates-review-application/12-RESEARCH.md
@.planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md

<interfaces>
<!-- Registration analogs + the deletion targets. -->

Analog registrations (catalog.py): research.score:389-398 · research.review:411-420 ·
  curation.run:434-441 · curation.inspect:444-451 · shims _research_review_shim:554-559 ·
  _curation_result_to_operation:572-593 · _curation_run_shim:596

Runners to wrap (Plans 02/04): curation_promote.run_gate("card.evaluate", CardEvaluateInput);
  curation_run.review_curation_run(CurationReviewInput)

Placeholder deletion (D-10 / Pitfall 6 — TWO files):
  catalog.py: _get_workflow_steps call site in workflow.run lambda:308-312; def ~725-741
  cli.py: import + _get_workflow_steps_from_registry:208-216; workflow resume caller:262;
    workflow run command:219-234

Open-Q 1 resolution (adopt): redirect `construct workflow run curation-cycle` → curation.run,
  or remove the workflow run/resume group entirely. curation.run is the sole canonical path.

CardEvaluateInput / CurationReviewInput: define in their gate modules (curation_promote.py /
  curation_run.py), NOT in catalog.py (circular-import hazard). If a thin input model is needed
  for card.evaluate it lives in curation_promote.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register card.evaluate + curation.review; extend curation.inspect; add CLI command</name>
  <files>src/construct/capabilities/catalog.py, src/construct/cli.py</files>
  <read_first>
    - src/construct/capabilities/catalog.py (research.score 389-398, research.review 411-420, curation block 432-451, shims 554-611)
    - src/construct/cli.py (curation sub-app run/inspect commands, _display_result, workflow sub-app 200-266)
    - src/construct/llm/curation_promote.py (run_gate + CardEvaluateInput) and src/construct/llm/curation_run.py (review_curation_run + CurationReviewInput)
    - tests/contract/test_curation_run_cli_mcp.py (extended _CAPS + test_registered — RED)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (catalog + cli sections)
  </read_first>
  <action>
    Register card.evaluate (id="card.evaluate", cli_name="card.evaluate",
    mcp_tool_name="construct_card_evaluate", handler=_card_evaluate_shim) mirroring
    research.score; register curation.review (id="curation.review",
    cli_name="curation.review", mcp_tool_name="construct_curation_review",
    handler=_curation_review_shim) mirroring research.review. Both shims reject positional
    args (raise TypeError) and wrap their runner via _curation_result_to_operation (for
    review) / the research.score sanitizing wrapper shape (for card.evaluate). Update the
    curation.inspect description to mention pending-review state (no registration shape
    change — the extended inspect_curation_run body already surfaces gate_queue). Add a
    `curation review` Typer command alongside `curation run`/`curation inspect` using the
    same get_registry().get("curation.review") → cap.handler(**kwargs) → _display_result
    shape. Do NOT edit mcp/server.py (auto-discovery; guarded by test_mcp_no_hardcoded_curation).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/contract/test_curation_run_cli_mcp.py::test_registered tests/contract/test_curation_run_cli_mcp.py::test_mcp_no_hardcoded_curation -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_registered passes for curation.review and card.evaluate (cli_name + mcp_tool_name present; positional args rejected).
    - test_mcp_no_hardcoded_curation stays GREEN (mcp/server.py untouched).
    - `construct curation review --help` resolves the command; both shims raise TypeError on positional args.
  </acceptance_criteria>
  <done>card.evaluate + curation.review are registry-backed and invokable from CLI and MCP; curation.inspect advertises pending-review.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the curation-cycle placeholder lambdas from catalog.py + cli.py (D-10)</name>
  <files>src/construct/capabilities/catalog.py, src/construct/cli.py</files>
  <read_first>
    - src/construct/capabilities/catalog.py (_get_workflow_steps def ~725-741 + call site in workflow.run lambda 308-312)
    - src/construct/cli.py (import of _get_workflow_steps, _get_workflow_steps_from_registry 208-216, workflow run 219-234, workflow resume 262)
    - tests/contract/test_curation_run_cli_mcp.py (test_no_placeholder_curation_path — RED)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (Placeholder removal shared pattern; Pitfall 6; Discrepancy 3)
    - .planning/phases/12-curation-l3-gates-review-application/12-RESEARCH.md (Open-Q 1 recommendation)
  </read_first>
  <action>
    Atomically (both files in one change so no ImportError) remove the placeholder:
    delete _get_workflow_steps from catalog.py and the workflow.run lambda's call to it;
    in cli.py remove the import and _get_workflow_steps_from_registry and rewire the
    `workflow run`/`workflow resume` callers. Per Open-Q 1 (adopt the recommendation):
    redirect `construct workflow run curation-cycle` to curation.run, OR remove the
    workflow run/resume command group entirely — whichever leaves NO placeholder no-op
    reachable and keeps the rest of the workflow sub-app importing cleanly. curation.run
    is the sole canonical curation entrypoint. Ensure `construct --help` and the full CLI
    still import without error after deletion.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/contract/test_curation_run_cli_mcp.py::test_no_placeholder_curation_path -x -q && .venv/bin/python -c "import construct.cli, construct.capabilities.catalog; print('IMPORT-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - test_no_placeholder_curation_path passes (symbol _get_workflow_steps gone; no curation-cycle placeholder reachable).
    - `grep -rn "_get_workflow_steps" src/construct/` returns no definition or call.
    - construct.cli and construct.capabilities.catalog import without ImportError; `construct --help` exits 0.
  </acceptance_criteria>
  <done>The legacy fake-success curation-cycle path is gone from both files; curation.run is the sole canonical path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP caller → capability handler | external invocation crosses into the registry shim |
| placeholder removal → import graph | a half-removed symbol breaks the CLI import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Spoofing/Tampering | capability shims | mitigate | Shims reject positional args (keyword-only) and wrap runners via the sanitizing _curation_result_to_operation; inputs validated by the gate modules' Pydantic models (extra="forbid", run_id validator) |
| T-12-15 | Tampering | residual placeholder path | mitigate | D-10 removal in BOTH files atomically; test_no_placeholder_curation_path + import smoke test guard against a half-removal ImportError and a surviving fake-success handler |
| T-12-16 | Tampering (transport) | MCP parity | mitigate | mcp/server.py untouched; registry auto-discovery only (test_mcp_no_hardcoded_curation) |
| T-12-SC | Tampering | npm/pip installs | accept | No new packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/contract/test_curation_run_cli_mcp.py -q` GREEN (registration + parity + no-placeholder).
- `.venv/bin/pytest tests/ -q` no API-05 regression (existing CLI/MCP/validation behavior intact).
- `construct --help` and `construct curation review --help` exit 0.
</verification>

<success_criteria>
- card.evaluate + curation.review registered with CLI/MCP parity; curation.inspect advertises pending-review.
- No `_get_workflow_steps` symbol or curation-cycle placeholder anywhere in src/.
- CLI imports cleanly; mcp/server.py unedited.
</success_criteria>

<output>
Create `.planning/phases/12-curation-l3-gates-review-application/12-05-SUMMARY.md` when done.
</output>
