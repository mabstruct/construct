---
phase: 12-curation-l3-gates-review-application
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - src/construct/llm/curation_run.py
autonomous: true
requirements: [CUR-03, CUR-04]
decisions: [D-04, D-07]

must_haves:
  truths:
    - "Only approved proposals are written; rejected and escalate items produce no SOT write this run"
    - "Approved promotions write lifecycle via edit_card; approved connections write via add_connection; approved archives via archive_card"
    - "Writes are idempotent: a rerun or crash-resume never double-writes"
    - "review_curation_run refuses to re-resume a completed run"
    - "inspect reports awaiting_review + gate_queue + outcomes and never resumes"
    - "Events are emitted for every step and the gate review per spec §6.6"
  artifacts:
    - path: "src/construct/llm/curation_run.py"
      provides: "apply_promotions/apply_connections/apply_archives nodes, decision resolution, review_curation_run, extended inspect_curation_run, event emission"
      contains: "def review_curation_run"
  key_links:
    - from: "src/construct/llm/curation_run.py apply_promotions"
      to: "construct.services.knowledge.edit_card"
      via: "post-gate lifecycle write"
      pattern: "edit_card"
    - from: "src/construct/llm/curation_run.py apply_connections"
      to: "construct.services.knowledge.add_connection"
      via: "post-gate idempotent connection write"
      pattern: "add_connection"
    - from: "src/construct/llm/curation_run.py apply_archives"
      to: "construct.services.knowledge.archive_card"
      via: "post-gate archive write"
      pattern: "archive_card"
    - from: "src/construct/llm/curation_run.py"
      to: "construct.services.event_log.append_event"
      via: "step + gate-review audit events"
      pattern: "append_event"
---

<objective>
Graft the write-side of the HITL machine onto curation_run.py: the three post-gate
apply nodes (promotions / connections / archives) that run ONLY after
Command(resume=...), the decision-resolution + approve-all/reject-all logic, the
review_curation_run resume runner with its paused-state guard, run-start pause
detection, the extended inspect_curation_run, and event emission. This is where the
canonical writes finally land — behind the single human approval.

Purpose: Complete CUR-03 (reviewed writes, idempotent, only-approved) and CUR-04
(inspect surfaces pending review + outcomes + events). escalate items are review-only
this phase (record outcome + event, no SOT write — RESEARCH Open-Q 3).
Output: curation_run.py write-side. Turns the remaining test_curation_run.py reviewed-
write / resume / inspect / events tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-curation-l3-gates-review-application/12-CONTEXT.md
@.planning/phases/12-curation-l3-gates-review-application/12-RESEARCH.md
@.planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md

<interfaces>
<!-- Write surfaces + analog resume/inspect machine. -->

Write surfaces (knowledge.py): edit_card:241 (lifecycle update, author=CardAuthor.curator);
  archive_card:314 (author=CardAuthor.curator); add_connection:372
  (created_by=ConnectionAuthor.construct; dup → success=True,"Connection already exists":416-423)
Authors: CardAuthor.curator (card.py:64); ConnectionAuthor (workspace.py:41)

Analog (research_run.py): WRITE BOUNDARY add_edge("gate_review","ingest_batch"):868 ·
  per-item try/except isolation:611 · _resolve_decisions/_normalize_decision:461-498 ·
  _build_resume_decisions:979-993 · review_research_run paused-state guard:996-1035 ·
  run_research_run pause detection:933-942 · inspect_research_run:1038-1082

Existing curation_run.py: run_curation_run:514 · inspect_curation_run:552
  (nonexistent-run→failed guard:569) · _open_checkpointer:202-216 · _validate_run_id:53-66 ·
  _sanitize_error:222-231 · _aggregate_status:503-511
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-gate apply nodes + decision resolution + WRITE BOUNDARY edges</name>
  <files>src/construct/llm/curation_run.py</files>
  <read_first>
    - src/construct/llm/curation_run.py (build_curation_run_graph 466-497, _aggregate_status 503-511, decay_scan archived filter 317)
    - src/construct/llm/research_run.py (WRITE BOUNDARY edge 868, per-item isolation 611, _resolve_decisions/_normalize_decision 461-498, _build_resume_decisions 979-993)
    - src/construct/services/knowledge.py (edit_card 241, archive_card 314, add_connection 372 + dup behavior 416-423)
    - tests/llm/test_curation_run.py (test_reviewed_promotion_applied, test_reviewed_connection_idempotent, test_reviewed_archive_applied, test_no_unreviewed_writes, test_idempotent_rerun — RED)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (post-gate write nodes + Pattern 5; Discrepancy 1)
  </read_first>
  <action>
    Add three apply-nodes downstream of the interrupt (WRITE BOUNDARY — mirror
    research_run.py:868 add_edge("gate_review","ingest_batch")):
    - apply_promotions: for each APPROVED promotion proposal, edit_card(workspace,
      card_id, {"lifecycle": target_lifecycle}, author=CardAuthor.curator); skip if the
      card is already at target_lifecycle (idempotent). target_lifecycle is growing|mature
      only (Discrepancy 1 — never seed/archived).
    - apply_connections: for each APPROVED connection proposal, add_connection(workspace,
      from_id, to_id, ConnectionType(...), created_by=ConnectionAuthor.construct);
      add_connection already dedups (dup → "Connection already exists"), so a rerun is a
      no-op.
    - apply_archives: for each APPROVED archive proposal, archive_card(workspace, card_id,
      author=CardAuthor.curator); skip if already archived.
    Wrap each per-item apply in try/except so one failing write never aborts the batch
    (mirror research_run.py:611); sanitize via _sanitize_error. Copy
    _resolve_decisions/_normalize_decision and _build_resume_decisions: default per-item
    decision = the gate recommendation (D-07); approve-all/reject-all supported; ONLY
    approved items write; escalate items are review-only (record outcome, NO write —
    Open-Q 3). Wire the post-gate edges process_inbox → apply_promotions →
    apply_connections → apply_archives → compile_report.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/llm/test_curation_run.py -k "reviewed_promotion or reviewed_connection or reviewed_archive or no_unreviewed or idempotent" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_reviewed_promotion_applied, test_reviewed_connection_idempotent, test_reviewed_archive_applied, test_no_unreviewed_writes pass.
    - A rejected proposal produces no SOT write; an escalate proposal produces no SOT write this run.
    - All edit_card/add_connection/archive_card call sites are in apply-nodes downstream of process_inbox (grep proves no write symbol upstream of the interrupt).
    - Rerun of an applied run double-writes nothing (idempotent skip / add_connection dedup).
  </acceptance_criteria>
  <done>Approved-only canonical writes land behind the interrupt, idempotently, with per-item isolation; escalate is review-only.</done>
</task>

<task type="auto">
  <name>Task 2: review/run-start/inspect runners + event emission</name>
  <files>src/construct/llm/curation_run.py</files>
  <read_first>
    - src/construct/llm/curation_run.py (run_curation_run 514-549, inspect_curation_run 552-583 + guard 569, _open_checkpointer 202-216, _validate_run_id 53-66)
    - src/construct/llm/research_run.py (review_research_run 996-1035, run pause detection 933-942, inspect_research_run 1038-1082)
    - src/construct/services/event_log.py (append_event)
    - CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md §6.6 (curation + gate-review event types)
    - tests/llm/test_curation_run.py (test_cross_process_resume, test_inspect_pending_review, test_curation_events_emitted — RED)
    - .planning/phases/12-curation-l3-gates-review-application/12-PATTERNS.md (review runner + extend inspect + Pattern 3; Pitfall 5)
  </read_first>
  <action>
    Add CurationReviewInput (extra="forbid", reuse _validate_run_id via
    field_validator("run_id") — path-traversal guard V5) and review_curation_run mirroring
    research_run.py:996-1035: open the persistent SqliteSaver via _open_checkpointer,
    snap = graph.get_state(cfg); if snap.next != ("process_inbox",) return a completed or
    failed result WITHOUT re-running write nodes (WR-05 idempotent completion); else
    decisions = _build_resume_decisions(inp, gate_queue) and graph.invoke(Command(resume=
    decisions), cfg). Extend run_curation_run to detect the pause (when "__interrupt__" in
    result and snap.next == ("process_inbox",) → return status="awaiting_review" with the
    gate_queue). Extend inspect_curation_run: add a snap.next == ("process_inbox",) →
    awaiting_review branch BEFORE the not-values check, surface gate_queue + review
    outcomes; keep the nonexistent/stale-run → failed guard (569) and verify it tolerates
    a shape mismatch (read-only, never resumes). Emit events via append_event for each
    step and the gate review (approved/rejected, curation cycle complete) per spec §6.6 —
    these live in the apply/report nodes and the review runner, NEVER in process_inbox.
    Keep the persistent sqlite conn form (Pitfall 2); close in finally.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/llm/test_curation_run.py -k "cross_process_resume or inspect_pending_review or events_emitted" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_cross_process_resume, test_inspect_pending_review, test_curation_events_emitted pass.
    - review_curation_run on a completed run returns completed/failed without a new write (re-invoke is a no-op).
    - inspect_curation_run on a paused run reports awaiting_review + gate_queue and performs no resume; on a stale/foreign run_id returns failed (no crash).
    - CurationReviewInput rejects a non-kebab run_id (path-traversal guard) and extra fields.
  </acceptance_criteria>
  <done>Resume/inspect runners surface pending review + outcomes + events without re-running writes; CUR-04 GREEN; cross-process resume works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human resume → write nodes | approved decisions authorize canonical SOT writes |
| review input → checkpoint path | run_id/gate_id flow into the .construct/workflow/*.sqlite path |
| node exception → result/log | provider/write error text may carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Elevation of Privilege | apply nodes | mitigate | Only items with an approved decision write (only-approved invariant); rejected/escalate produce no write; test_no_unreviewed_writes enforces it. Writes sit strictly downstream of the interrupt edge |
| T-12-10 | Tampering | double-write on rerun / crash-resume | mitigate | Skip-if-exists (lifecycle already at target / already archived) + add_connection dedup + paused-state resume guard (snap.next != process_inbox → no re-write); test_idempotent_rerun + WR-05 guard |
| T-12-11 | Tampering | run_id/gate_id path traversal | mitigate | CurationReviewInput reuses _validate_run_id KEBAB_CASE_PATTERN field_validator + extra="forbid" |
| T-12-12 | Repudiation | event-log integrity | mitigate | append_event (append-only, non-blocking) records every step + gate-review outcome per §6.6, emitted only in apply/report/review nodes (never the interrupt node, avoiding double-fire) |
| T-12-13 | Information Disclosure | write/provider exceptions | mitigate | _sanitize_error reduces to class-name + safe line; per-item try/except isolation prevents leak-on-abort |
| T-12-SC | Tampering | npm/pip installs | accept | No new packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/llm/test_curation_run.py -q` fully GREEN (all reviewed-write + resume + inspect + events tests).
- `.venv/bin/pytest tests/llm/ -q` no research_run regression.
- Grep confirms no edit_card/add_connection/archive_card/append_event call inside process_inbox.
</verification>

<success_criteria>
- Only-approved canonical writes, idempotent across reruns and crash-resume.
- escalate is review-only (no SOT write this phase).
- review/inspect never re-run a completed run; inspect surfaces awaiting_review + gate_queue + outcomes.
- Events emitted per spec §6.6 from post-gate nodes only.
</success_criteria>

<output>
Create `.planning/phases/12-curation-l3-gates-review-application/12-04-SUMMARY.md` when done.
</output>
