---
phase: 17-architecture-doc-set-daily-run-discoverability
plan: 04
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - CONSTRUCT-CLAUDE-spec/config-topology.md
  - CONSTRUCT-CLAUDE-spec/README_FIRST.md
  - CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md
autonomous: true
requirements: [DOC-02]
tags: [documentation, config-topology, model-routing, spec-fence]

must_haves:
  truths:
    - "CONSTRUCT-CLAUDE-spec/config-topology.md no longer exists on disk (D-06)."
    - "No reference to config-topology remains anywhere under CONSTRUCT-CLAUDE-spec/ (D-06)."
    - "README_FIRST.md's directory-layout row (:74) redirects to artifact-catalog.md + workspace-contract.md instead of config-topology.md (D-06)."
    - "spec-v04-agentworkflows.md:211 marks model-routing.yaml deprecated/inert and names llm/config.yaml the LLM config authority (D-07)."
    - "spec-v04-agentworkflows.md:557's dual-config-confusion risk row reflects the resolved state (model-routing deprecated per Phase 14) (D-07)."
  artifacts:
    - CONSTRUCT-CLAUDE-spec/README_FIRST.md
    - CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md
  key_links:
    - "config-topology.md deletion -> README_FIRST.md + artifact-catalog.md + workspace-contract.md redirect targets"
  prohibitions:
    - "Do not edit the sealed v0.4-MILESTONE-AUDIT.md or the planning docs (ROADMAP/REQUIREMENTS/PROJECT/STATE/CONCERNS) that merely record the delete decision — they are not deferrers."
    - "Do not delete model-routing.yaml itself or remove it from REQUIRED_PATHS — only mark it deprecated in the spec doc (workspace-format change deferred to v0.5+)."
---

<objective>
Finish the DOC-02 config-truth work: DELETE the stale `config-topology.md`, redirect its live spec-tree deferrers, and close the Phase 14/16 fence by marking the last two stale model-routing references in `spec-v04-agentworkflows.md` deprecated. Satisfies ROADMAP criterion 3 (and D-07's cross-phase hand-off).

Purpose: `config-topology.md` (dated 2026-04-23) is stale across all three sections and its three roles are already owned by milestone-corrected docs (workspace artifacts -> workspace-contract.md; capability/CLI/MCP inventory -> artifact-catalog.md; layer/deployment model -> architecture-overview.md). Correcting it would create a fourth copy of the workspace contract — duplicated truth is how drift starts. Nothing in code/tests references it (grep-verified).
Output: config-topology.md gone, no dangling references, and the last model-routing spec-tree references resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-CONTEXT.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- **Deleted:** `CONSTRUCT-CLAUDE-spec/config-topology.md` (via `git rm`).
- **Modified:** `CONSTRUCT-CLAUDE-spec/README_FIRST.md` — the directory-layout table row at :74 redirected.
- **Modified:** `CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md` — two line-scoped edits (:211 config-surfaces row, :557 risk row).

No new code symbols or tests. This plan depends on 17-03 (which removed the config-topology deferrers the catalog owned) so the repo-wide "no config-topology references" grep can pass here.

<tasks>

<task type="auto">
  <name>Task 1: Delete config-topology.md and redirect its deferrers (D-06)</name>
  <files>CONSTRUCT-CLAUDE-spec/config-topology.md, CONSTRUCT-CLAUDE-spec/README_FIRST.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-spec/config-topology.md (the file being deleted — confirm its three stale sections; the self-reference at :82 is moot after deletion)
    - CONSTRUCT-CLAUDE-spec/README_FIRST.md (the deferrer row at :74: "| [config-topology.md](config-topology.md) | Directory layout of `CONSTRUCT-CLAUDE-impl/` |")
    - CONSTRUCT-CLAUDE-spec/artifact-catalog.md (the redirect target for inventory — already expanded in 17-03; confirm its config-topology deferrers were removed by 17-03)
    - CONSTRUCT-CLAUDE-spec/workspace-contract.md (the redirect target for workspace artifacts — inherits config-topology.md's §3 role; already holds the model-routing deprecation truth at :78)
    - .planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md (Finding 6 — delete safety, the complete deferrer map, Runtime State Inventory confirming no code refs)
  </read_first>
  <action>
    Delete CONSTRUCT-CLAUDE-spec/config-topology.md with `git rm CONSTRUCT-CLAUDE-spec/config-topology.md` (grep-verified zero code/test refs; its three roles are owned by workspace-contract.md, artifact-catalog.md, architecture-overview.md; the model-routing deprecation truth already lives in workspace-contract.md:78 and nfrs.md:72 — nothing is lost). Redirect the README_FIRST.md:74 directory-layout row: replace the config-topology.md link with pointers to `artifact-catalog.md` (the config/inventory layers) and `workspace-contract.md` (the workspace directory layout) — or remove the row if the two targets are already listed nearby; the row must no longer name config-topology.md. Do NOT edit the sealed v0.4-MILESTONE-AUDIT.md or the planning docs (ROADMAP/REQUIREMENTS/PROJECT/STATE/CONCERNS) — they record the delete DECISION and are not deferrers. After the edit, the repo-wide grep `grep -rn config-topology CONSTRUCT-CLAUDE-spec/` must return nothing (17-03 already removed the artifact-catalog.md deferrers; this task removes the README_FIRST.md one and the file itself). If the grep still finds a hit, resolve it (it should only be planning docs outside CONSTRUCT-CLAUDE-spec/, which are out of scope).
  </action>
  <verify>
    <automated>test ! -e CONSTRUCT-CLAUDE-spec/config-topology.md</automated>
    <automated>! grep -rn "config-topology" CONSTRUCT-CLAUDE-spec/ && echo "no spec-tree refs remain"</automated>
    <automated>grep -q "config-topology" CONSTRUCT-CLAUDE-spec/README_FIRST.md && { echo "FAIL: README_FIRST still refs config-topology"; exit 1; } || echo "README_FIRST clean"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e CONSTRUCT-CLAUDE-spec/config-topology.md` passes (file deleted).
    - `grep -rn config-topology CONSTRUCT-CLAUDE-spec/` returns nothing (no dangling spec-tree references).
    - README_FIRST.md:74's row redirects to artifact-catalog.md + workspace-contract.md (or is removed) and no longer names config-topology.md (source assertion).
    - The sealed milestone audit and planning docs were NOT edited (they legitimately still mention config-topology as the delete decision — out of scope).
  </acceptance_criteria>
  <done>config-topology.md is deleted, README_FIRST.md redirected, and no CONSTRUCT-CLAUDE-spec/ reference to it remains.</done>
</task>

<task type="auto">
  <name>Task 2: Close the model-routing fence in spec-v04-agentworkflows.md (D-07)</name>
  <files>CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md (the config-surfaces table row at :211 presenting `.construct/model-routing.yaml (workspace) | Cognitive tier documentation; must not duplicate provider secrets`; the `src/construct/llm/config.yaml` row already at :209; the Risks table dual-config-confusion row at :557)
    - .planning/phases/14-durable-state-config-truth/14-CONTEXT.md (D-01: model-routing.yaml deprecated/inert, llm/config.yaml authoritative — the resolved state to propagate; D-02: the edit fence that handed :211/:557 to Phase 17)
    - .planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md (Finding 7 — the exact fence points)
  </read_first>
  <action>
    Two line-scoped edits, no structural rewrite. At :211, mark `.construct/model-routing.yaml` deprecated/inert and name `src/construct/llm/config.yaml` (already listed at :209) as the authoritative LLM config surface — the row must no longer present model-routing.yaml as a live config domain. At :557, update the "Dual config confusion (model-routing.yaml vs search.yaml vs llm/config.yaml)" risk row to reflect the RESOLVED state: model-routing.yaml is deprecated per Phase 14 D-01/D-02 and llm/config.yaml is authoritative, so the confusion is closed (not merely "validation warns on conflict"). Do NOT delete model-routing.yaml itself, remove it from REQUIRED_PATHS, or touch the loader/fixtures — that workspace-format change is deferred to v0.5+ (Phase 14 deferred it). These are the last two stale model-routing references in the live spec tree.
  </action>
  <verify>
    <automated>grep -nE "model-routing" CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md</automated>
    <automated>grep -qiE "deprecat" CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md && grep -q "llm/config.yaml" CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md</automated>
  </verify>
  <acceptance_criteria>
    - Every surviving `model-routing` mention in spec-v04-agentworkflows.md is framed as deprecated/inert (manual review of each grep hit — none present it as a live authority).
    - :211's config-surfaces row names `llm/config.yaml` as the LLM config authority and marks model-routing.yaml deprecated (source assertion).
    - :557's risk row reflects the resolved state (model-routing deprecated), not an open "validation warns on conflict" risk.
    - model-routing.yaml is NOT deleted and REQUIRED_PATHS is untouched (out of scope — deferred).
  </acceptance_criteria>
  <done>spec-v04:211 and :557 present model-routing.yaml as deprecated with llm/config.yaml authoritative — the fence is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> spec doc tree | Deleting a doc could orphan a live deferrer (a reader following a dead link) or a code reference. Grep-verified there are zero code/test references; the only live spec-tree deferrers are README_FIRST.md and the artifact-catalog.md rows (removed in 17-03). |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04-01 | Tampering | Deleting config-topology.md while a live reference still points at it | low | mitigate | RESEARCH Finding 6 grep-verified zero code/test refs; Task 1's terminal grep asserts no CONSTRUCT-CLAUDE-spec/ reference remains after the README_FIRST redirect + 17-03's catalog-deferrer removal. |
| T-17-04-02 | Information disclosure | A spec row still presenting model-routing.yaml as a live LLM authority (dual-config confusion for a developer) | low | mitigate | Task 2 marks both :211 and :557 deprecated and names llm/config.yaml authoritative; grep confirms deprecation framing. |
| T-17-04-SC | Tampering (supply chain) | package installs | low | accept | No npm/pip/cargo installs; no dependency change. No [ASSUMED]/[SUS] packages to gate. |

No high-severity threats. This plan deletes one stale doc and edits two spec lines; no runtime state, network, auth, or input-parsing surface is touched (Runtime State Inventory confirmed all five categories empty).
</threat_model>

<verification>
- `test ! -e CONSTRUCT-CLAUDE-spec/config-topology.md`
- `grep -rn config-topology CONSTRUCT-CLAUDE-spec/` returns nothing
- README_FIRST.md no longer references config-topology.md
- spec-v04-agentworkflows.md:211/:557 present model-routing.yaml as deprecated, llm/config.yaml authoritative
- `pytest tests/contract/test_doc_command_references.py -x` green (no doc-command regression from the edits)
- `pytest -q` full suite green
</verification>

<success_criteria>
config-topology.md is deleted with every document that deferred to it updated accordingly, and the last two stale model-routing references in the live spec tree are resolved to the Phase 14 deprecated state.
</success_criteria>

<output>
Create `.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-04-SUMMARY.md` when done.
</output>
