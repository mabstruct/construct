---
phase: 17-architecture-doc-set-daily-run-discoverability
plan: 03
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - tests/contract/test_artifact_catalog.py
  - CONSTRUCT-CLAUDE-spec/artifact-catalog.md
autonomous: true
requirements: [DOC-02]
tags: [documentation, catalog, contract-guard, introspection]

must_haves:
  truths:
    - "tests/contract/test_artifact_catalog.py exists and imports _command_paths/LEAF_COMMANDS/VALID_PATHS from test_doc_command_references and get_registry from construct.capabilities.catalog (D-04)."
    - "The guard asserts every get_registry().list() id (live = 28) has a catalog row (D-05)."
    - "The guard asserts every get_registry().list_mcp_tools() entry (live = 22) has a catalog row (D-05)."
    - "The guard asserts every Typer leaf (live = 34) is represented, treating registry and Typer as two distinct sources and NOT requiring every leaf to carry a registry id (views/spike/tag holdout) (D-05)."
    - "The guard asserts every construct-* skill dir (25 after 17-02) has a catalog row, including construct-spike-run and construct-daily-cycle (D-05)."
    - "The guard carries a vacuity meta-guard so a degenerate/empty introspection set fails loudly, not silently (D-05)."
    - "artifact-catalog.md gains a runtime-surface section with capability/CLI/MCP rows and the construct-spike-run + construct-daily-cycle skill rows; search-spine and LLM-gate rows are present but narrative (DOC-02)."
    - "pytest tests/contract/test_artifact_catalog.py exits 0 (DOC-02)."
    - "The guard asserts introspected surfaces are a SUBSET of catalog rows; extra narrative rows (search spine, LLM gates) are permitted, exact-equality is not required."
    - "The guard compares set membership, not row order — catalog row ordering is not asserted."
  artifacts:
    - tests/contract/test_artifact_catalog.py
    - CONSTRUCT-CLAUDE-spec/artifact-catalog.md
  key_links:
    - "test_artifact_catalog.py <-> get_registry() / _command_paths(app) / skills glob (four introspection sources)"
    - "catalog rows <-> live introspection (guard-checked, never hand-typed frozen integers)"
  prohibitions:
    - "Do not hand-type a frozen capability count the guard does not enforce; counts derive from live introspection (never write 27)."
    - "Do not imply views/spike/tag route through the registry — they are the recorded holdout (Phase 15 D-03)."
    - "_KNOWN_BROKEN in test_doc_command_references.py must stay empty; do not add entries."
---

<objective>
Make `artifact-catalog.md` an honest, staleness-proofed inventory: add a NEW mechanical guard (`tests/contract/test_artifact_catalog.py`) that asserts the catalog's rows match live introspection of the registry, the Typer app, the MCP tool list, and the skills glob — so the inventory physically cannot silently rot — and expand the catalog with the runtime surface it lacks today (capabilities/CLI/MCP rows) plus the missing `construct-spike-run` skill row. Satisfies ROADMAP criterion 2.

Purpose: The catalog today is entirely CONSTRUCT03-audit / Claude-config shaped (agents/skills/workflows) with hand-typed counts ("23 skills"). It has NO rows for the L2/L3 Python runtime surface, and the hand-typed counts reset the rot clock every edit. The guard is the durable deliverable; the row edits alone are not.
Output: A guard test + a structurally expanded catalog whose counts are live-introspection-backed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-CONTEXT.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- **New file:** `tests/contract/test_artifact_catalog.py` — the catalog-vs-introspection guard. New module-level helpers/constants it defines (e.g. a catalog-row extractor for the doc, and per-source assertion tests) are CREATED here — the source-grounding review must not treat them as drift against pre-phase code. It IMPORTS (does not redefine) `_command_paths`, `LEAF_COMMANDS`, `COMMAND_GROUPS`, `VALID_PATHS` from `tests/contract/test_doc_command_references.py` and `get_registry` from `construct.capabilities.catalog`.
- **Modified:** `CONSTRUCT-CLAUDE-spec/artifact-catalog.md` — a new "Runtime capabilities (L2/L3)" section (capability/CLI/MCP rows), a `construct-spike-run` skill row, a `construct-daily-cycle` skill row, updated skills-count narrative, and removal of the two config-topology deferrers at :37 and :262.

Note: `construct-daily-cycle` (the skill dir this guard counts) is produced by plan 17-02 (this plan depends on 17-02).

<!-- planner-discipline-allow: config-topology -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the catalog-vs-introspection guard, RED (D-04/D-05)</name>
  <files>tests/contract/test_artifact_catalog.py</files>
  <behavior>
    - Reusing the introspection helpers, every registered capability id (get_registry().list(), live 28) must appear as a row in artifact-catalog.md -> assert subset.
    - Every MCP tool name (get_registry().list_mcp_tools(), live 22) must appear as a row -> assert subset.
    - Every Typer leaf command (LEAF_COMMANDS, live 34) must be represented, without requiring a registry id for each (views/spike/tag holdout: only 26 of 28 caps carry cli_name).
    - Every construct-* skill dir on disk (25 after 17-02, incl. construct-spike-run and construct-daily-cycle) must have a catalog row.
    - Vacuity meta-guard: if any introspection source returns a degenerate/empty set, the suite fails loudly (assert len(reg.list()) > 25, len(reg.list_mcp_tools()) > 20, ("daily","run") in VALID_PATHS, a known skill dir present) — an empty introspection result must FAIL, never pass vacuously.
  </behavior>
  <read_first>
    - tests/contract/test_doc_command_references.py (the reusable introspection: `from construct.cli import app`, `_command_paths` :92-121, module-level `LEAF_COMMANDS`/`COMMAND_GROUPS`/`VALID_PATHS` :120-121, the `_IMPL`/`_SKILLS_DIR` path anchors :38/:303, the meta-guard `test_command_surface_is_discoverable` :214-222, the shrink-only `_KNOWN_BROKEN` discipline :173-206)
    - src/construct/capabilities/catalog.py (the registry the guard introspects; the holdout note ~:344-348 that `construct views generate` reaches the generator by an independent path — views/spike/tag are NOT registry-routed)
    - src/construct/capabilities/registry.py (CapabilityRecord fields id/cli_name/mcp_tool_name :16-24; accessors list :42, list_mcp_tools :55)
    - CONSTRUCT-CLAUDE-spec/artifact-catalog.md (the doc the guard parses for rows — its table structure)
    - .planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md (Finding 4 introspection contract + Pitfalls 1/2/4)
    - .planning/phases/17-architecture-doc-set-daily-run-discoverability/17-PATTERNS.md (the test_artifact_catalog.py section — helper imports, meta-guard pattern lines 78-86, holdout nuance)
  </read_first>
  <action>
    Create tests/contract/test_artifact_catalog.py as a NEW file that IMPORTS the introspection helpers rather than re-implementing a Typer walk: `from test_doc_command_references import LEAF_COMMANDS, COMMAND_GROUPS, VALID_PATHS, _command_paths` (they are module-level and importable) and `from construct.capabilities.catalog import get_registry`. Reuse the `_IMPL` / skills-dir path anchor form for the filesystem glob of `CONSTRUCT-CLAUDE-impl/claude/skills/construct-*/`. Read artifact-catalog.md once and extract the set of documented rows (capability ids, cli command paths, mcp tool names, and skill dir names present in its tables/code spans). Write these assertions: (1) every `get_registry().list()` id has a catalog row; (2) every `get_registry().list_mcp_tools()` entry has a catalog row; (3) every Typer leaf in `LEAF_COMMANDS` is represented in the catalog, treating the registry (28 caps / 22 MCP) and the Typer app (34 leaves) as TWO DISTINCT sources — do NOT assert every leaf maps to a registry id (views/spike/tag are the recorded holdout); (4) every `construct-*` skill dir on disk has a catalog row (this catches construct-spike-run, the criterion-named missing row, and construct-daily-cycle from 17-02). Frame each as a SUBSET assertion (introspected ⊆ documented) so the catalog may carry extra narrative rows (search spine, LLM gates) without failing, and compare by set membership (NOT row order). Add a mandatory vacuity meta-guard mirroring test_command_surface_is_discoverable: assert len(get_registry().list()) > 25, len(get_registry().list_mcp_tools()) > 20, ("daily","run") in VALID_PATHS, and a known skill dir exists — so an import error or renamed accessor that returns an empty set FAILS loudly instead of making every subset assertion vacuously true. If any allowlist for known-missing rows is needed, model it on the shrink-only `_KNOWN_BROKEN` discipline (an entry removable only by making the row exist) — but do NOT touch test_doc_command_references.py's own `_KNOWN_BROKEN`, which must stay empty. At this point the suite is RED (catalog lacks the runtime-surface and spike-run/daily-cycle rows); Task 2 turns it GREEN. Do NOT hardcode 27 anywhere — the live capability count is 28 and the guard derives it live.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('tests/contract/test_artifact_catalog.py').read()); print('parses ok')"</automated>
    <automated>grep -qE "from test_doc_command_references import|import test_doc_command_references" tests/contract/test_artifact_catalog.py && grep -q "get_registry" tests/contract/test_artifact_catalog.py</automated>
    <automated>grep -q "27" tests/contract/test_artifact_catalog.py && { echo "FAIL: hardcoded 27"; exit 1; } || echo "no hardcoded 27"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contract/test_artifact_catalog.py` exists and parses.
    - It imports `_command_paths`/`LEAF_COMMANDS`/`VALID_PATHS` from `test_doc_command_references` and `get_registry` from `construct.capabilities.catalog` (source assertion) — it does not re-implement a Typer walk.
    - It contains a vacuity meta-guard asserting `len(get_registry().list()) > 25`, `len(get_registry().list_mcp_tools()) > 20`, and `("daily","run") in VALID_PATHS` (source assertion).
    - It does NOT hardcode the integer 27 (behavior: `grep -c '\b27\b' test_artifact_catalog.py` == 0).
    - Running the guard now is RED (the catalog does not yet carry the runtime-surface / spike-run / daily-cycle rows) — `pytest tests/contract/test_artifact_catalog.py` exits non-zero at this task's end (expected; Task 2 makes it green).
  </acceptance_criteria>
  <done>The guard exists, reuses the FIX-04 helpers, has a vacuity meta-guard, encodes the registry/Typer holdout nuance, and fails RED against the un-expanded catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Expand artifact-catalog.md with the runtime surface + missing skill rows, GREEN (DOC-02)</name>
  <files>CONSTRUCT-CLAUDE-spec/artifact-catalog.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-spec/artifact-catalog.md (the full file: the "Related documents" table with the config-topology row at :37; the "Skills (23)" section :117-183; the "Reference layer" :186; the Maintenance list step 6 config-topology deferrer at :262; the CONSTRUCT03 audit sections to leave intact)
    - src/construct/capabilities/catalog.py (the 28 capability ids + cli_name/mcp_tool_name for each — the row source of truth; the views/spike/tag holdout note)
    - tests/contract/test_artifact_catalog.py (the guard from Task 1 — the exact row shapes/paths it parses; author the catalog rows so the guard's extractor finds them)
    - .planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md (Finding 5 catalog structure gap; Open Question 2 recommends a distinct new "Runtime capabilities (L2/L3)" section)
  </read_first>
  <action>
    Add a new top-level "Runtime capabilities (L2/L3)" section to artifact-catalog.md (distinct from the existing CONSTRUCT03 skill matrix, which stays intact — minimize churn) containing guard-backed rows for: every registered capability (by id), its CLI command path, and its MCP tool name; mark the views/spike/tag rows explicitly as reaching the CLI by an independent path (the registry holdout — do NOT imply they route through the registry). Keep the search spine and the LLM gates present as NARRATIVE rows (descriptive, not enumerable from a single registry — the guard does not assert them). In the existing "Skills" section, add the missing `construct-spike-run` row and the new `construct-daily-cycle` row, and change the hand-typed "Skills (23)" heading to reflect the live count as a guard-checked figure — phrase any narrative count as "live introspection" (the guard enforces it), never as a frozen hand-typed integer, and never write 27 for capabilities (the live count is 28). Remove the two config-topology deferrers this file owns: the "Related documents" row at :37 that points to config-topology.md, and Maintenance step 6 at :262 ("Update config-topology.md if directory layout changes") — the catalog now owns the directory/inventory role, and config-topology.md is deleted in plan 17-04. Author the new rows in the exact shape the Task 1 guard parses so the suite goes GREEN. Do NOT edit the sealed milestone audit or planning docs.
  </action>
  <verify>
    <automated>pytest tests/contract/test_artifact_catalog.py -x -q</automated>
    <automated>grep -q "construct-spike-run" CONSTRUCT-CLAUDE-spec/artifact-catalog.md && grep -q "construct-daily-cycle" CONSTRUCT-CLAUDE-spec/artifact-catalog.md</automated>
    <automated>test $(grep -c "config-topology" CONSTRUCT-CLAUDE-spec/artifact-catalog.md) -eq 0</automated>
    <automated>test $(grep -c '\b27 capabilities\b' CONSTRUCT-CLAUDE-spec/artifact-catalog.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/contract/test_artifact_catalog.py -x` exits 0 (all four subset assertions + the vacuity meta-guard pass).
    - The catalog contains a `construct-spike-run` row and a `construct-daily-cycle` row (source assertion).
    - The catalog contains capability/CLI/MCP rows in a runtime-surface section (source assertion — the guard's subset checks pass).
    - `grep -c "config-topology" artifact-catalog.md` == 0 (both deferrers removed).
    - No frozen "27 capabilities" figure appears (behavior: `grep -c '27 capabilities' artifact-catalog.md` == 0); counts are described as live-introspection-backed.
    - The views/spike/tag rows are marked as the registry holdout (not implied to route through the registry).
  </acceptance_criteria>
  <done>The catalog carries the runtime surface + spike-run + daily-cycle rows, the config-topology deferrers are gone, and the guard is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| introspection sources -> guard assertions | The guard reads live code (registry, Typer app) and the filesystem skills glob. A broken import or renamed accessor returning an empty set would make every subset assertion vacuously true — a false green that hides catalog rot. This is the one real integrity concern. |
| doc rows -> guard extractor | If the catalog's row shape drifts from what the extractor parses, the guard could silently find zero rows and pass. The vacuity meta-guard closes this. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03-01 | Tampering / Information disclosure | The catalog guard going vacuous (empty introspection set) | medium | mitigate | Mandatory vacuity meta-guard (Task 1): assert len(reg.list())>25, len(reg.list_mcp_tools())>20, ("daily","run") in VALID_PATHS, a known skill dir present — an empty set FAILS loudly. Mirrors test_command_surface_is_discoverable. |
| T-17-03-02 | Spoofing | Catalog implying views/spike/tag are registry-routed (misrepresents the write contract) | low | mitigate | Task 2 marks the holdout rows explicitly; Task 1 treats registry and Typer as two distinct sources and does not assert every leaf has a registry id. |
| T-17-03-03 | Tampering | A hand-typed frozen count (e.g. 27) re-introducing rot | low | mitigate | Guard derives counts live; verify greps assert no `27` in the test and no "27 capabilities" in the doc. |
| T-17-03-SC | Tampering (supply chain) | package installs | low | accept | No npm/pip/cargo installs; pytest already present. No [ASSUMED]/[SUS] packages to gate. |

No high-severity threats. The guard is read-only introspection over already-shipped code; the only integrity risk (vacuity) is mechanically mitigated by the meta-guard. No auth/session/crypto/new-network surface introduced.
</threat_model>

<verification>
- `pytest tests/contract/test_artifact_catalog.py -x` green
- `pytest tests/contract/test_doc_command_references.py -x` green, `_KNOWN_BROKEN` empty
- `pytest -q` full suite green (count grows vs the ~515 anchor — not a regression)
- `grep -c "config-topology" CONSTRUCT-CLAUDE-spec/artifact-catalog.md` == 0
- Catalog has construct-spike-run + construct-daily-cycle rows; runtime-surface section present
- No hardcoded 27 in the guard or a frozen "27 capabilities" in the doc
</verification>

<success_criteria>
artifact-catalog.md contains rows for every registered capability, CLI command, and MCP tool with counts backed by live introspection, plus the search spine, the LLM gates, and the construct-spike-run row — all protected by a mechanical guard that fails loudly rather than vacuously.
</success_criteria>

<output>
Create `.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-03-SUMMARY.md` when done.
</output>
