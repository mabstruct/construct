---
phase: 09-llm-provider-factory-research-score
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/llm/factory.py
  - src/construct/llm/ask_domain.py
  - pyproject.toml
  - tests/llm/test_factory.py
  - tests/llm/conftest.py
  - tests/llm/test_ask_domain.py
  - tests/contract/test_ask_domain_mocked.py
autonomous: false
requirements: [RSCH-01]
user_setup:
  - service: openai
    why: "OpenAI is wired (D-02) to prove cross-vendor swap. Optional extra only; offline tests use the mock, so no key is required to complete this phase."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys (ONLY needed for a live OpenAI smoke test, never for the offline test suite)"

must_haves:
  truths:
    - "Both ask.domain and research.score construct chat models through one factory.build_chat_model path (spec-line-546 anti-pattern paid off) (D-01)"
    - "factory.build_chat_model maps ProviderConfig.type -> concrete ChatModel for langchain_anthropic and langchain_openai (D-03)"
    - "Missing/unknown provider raises a clear GATE_PROVIDER_ERROR (langchain_openai absence does not crash a base install) (D-02)"
    - "ask.domain behaves identically after the retrofit (no regression) (D-01)"
    - "The full test suite (>=253 tests) stays green after the retrofit"
  artifacts:
    - path: "src/construct/llm/factory.py"
      provides: "build_chat_model(cfg: ProviderConfig, *, temperature) -> ChatModel"
      contains: "def build_chat_model"
      min_lines: 25
    - path: "tests/llm/test_factory.py"
      provides: "Factory type-map, lazy-import error, unknown-provider error tests"
      contains: "GATE_PROVIDER_ERROR"
    - path: "pyproject.toml"
      provides: "llm-openai optional extra"
      contains: "llm-openai"
  key_links:
    - from: "src/construct/llm/ask_domain.py"
      to: "src/construct/llm/factory.py"
      via: "factory.build_chat_model(provider_cfg, temperature=...)"
      pattern: "factory\\.build_chat_model"
    - from: "tests/llm/test_ask_domain.py (patched_llm fixture)"
      to: "construct.llm.factory.build_chat_model"
      via: "monkeypatch seam shared by both gates"
      pattern: "construct\\.llm\\.factory\\.build_chat_model"
---

<objective>
Build the model-agnostic LLM provider factory and retrofit `ask_domain.py` to use it, replacing the hardcoded `ChatAnthropic(...)` construction (closes the spec-v04 line 546 anti-pattern per D-01). The factory becomes the single provider-construction path that both `ask.domain` (this plan) and `research.score` (Plan 02/03) consume.

Purpose: One consistent, swappable provider path across all gates. This is the precondition for a "model-agnostic scoring gate" (RSCH-01 phase goal).
Output: `src/construct/llm/factory.py`, `pyproject.toml` `llm-openai` extra, retrofitted `ask_domain.py`, repointed test monkeypatch seam, and a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-llm-provider-factory-research-score/09-CONTEXT.md
@.planning/phases/09-llm-provider-factory-research-score/09-RESEARCH.md
@.planning/phases/09-llm-provider-factory-research-score/09-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do NOT re-explore. -->

From src/construct/llm/config.py (ProviderConfig — the factory input):
```python
class ProviderConfig(BaseModel):
    model_config = {"extra": "forbid"}
    type: str = "langchain_anthropic"   # also "langchain_openai", "langchain_ollama"
    model: str = "claude-sonnet-4-20250514"
    max_tokens: int = 4096
    timeout_seconds: int = 60
    base_url: str | None = None
```

GATE_PROVIDER_ERROR convention (config.py:73): provider-config failures use the
`"GATE_PROVIDER_ERROR: ..."` message prefix so tests can assert on it. Reuse it.

Analog factory to mirror (structural template): src/construct/search/registry.py
`SearchProviderFactory.create` (lines 77-116) — branch-on-type with a lazy optional
import (tavily) inside the branch, raise on unknown type.

ask_domain.py current construction site (lines 228-232, inside the `llm_synthesize`
node) builds `ChatAnthropic(model=state["model"], temperature=0.2, max_tokens=4096)`.
`run_gate` already resolves `provider_cfg` (line 374) but only threads `model` into
state, NOT the full ProviderConfig.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify langchain-openai package legitimacy before declaring the extra (T-09-SC)</name>
  <read_first>
    - .planning/phases/09-llm-provider-factory-research-score/09-RESEARCH.md (## Package Legitimacy Audit — langchain-openai marked [ASSUMED]; slopcheck not run offline)
  </read_first>
  <what-built>Nothing built yet. This is a pre-flight supply-chain gate required because `langchain-openai` is marked `[ASSUMED]` in the research audit (slopcheck was not run). It is a first-party LangChain package (same `langchain-ai/langchain` monorepo as the already-trusted `langchain-core`/`langchain-anthropic`) and is added here only as an OPTIONAL extra that the offline test suite never installs.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/langchain-openai/ and confirm: publisher is the LangChain org, latest version is in the 1.2.x–1.3.x line, source repo is github.com/langchain-ai/langchain.
    2. Confirm the import name used by the factory (`from langchain_openai import ChatOpenAI`) matches the documented package.
    3. Confirm you accept declaring `llm-openai = ["langchain-openai>=1.2,<2"]` as an optional extra that is NOT installed during this phase.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the package is the official LangChain integration package and approves declaring the optional extra.
    - This checkpoint is NOT auto-approvable (workflow.auto_advance is ignored for legitimacy gates).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed, or describe concerns to halt the extra declaration.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the provider factory + declare the llm-openai optional extra</name>
  <files>src/construct/llm/factory.py, pyproject.toml, tests/llm/test_factory.py</files>
  <read_first>
    - src/construct/search/registry.py (lines 77-116 — SearchProviderFactory.create: the branch-on-type + lazy-optional-import structural analog)
    - src/construct/llm/config.py (ProviderConfig fields + GATE_PROVIDER_ERROR convention at line 73)
    - src/construct/llm/ask_domain.py (lines 228-232 — the ChatAnthropic ctor kwargs the factory must reproduce: model, temperature, max_tokens)
    - pyproject.toml (lines 22-24 — existing `search = ["tavily-python>=0.7,<1"]` optional-extra precedent)
  </read_first>
  <behavior>
    - Test: build_chat_model(ProviderConfig(type="langchain_anthropic", model="claude-sonnet-4-20250514", max_tokens=4096)) returns a ChatAnthropic instance whose model/max_tokens match the config and temperature matches the passed kwarg.
    - Test: build_chat_model with type="langchain_openai" while langchain_openai is NOT importable raises RuntimeError whose message starts with "GATE_PROVIDER_ERROR" and names the `llm-openai` extra. (Simulate the missing import, e.g. monkeypatch builtins import or assert the message contract if the package is genuinely absent — langchain-openai is not installed in the venv.)
    - Test: build_chat_model with an unknown type (e.g. "langchain_ollama" or "bogus") raises RuntimeError whose message starts with "GATE_PROVIDER_ERROR" and names the unknown type.
  </behavior>
  <action>Create `src/construct/llm/factory.py` with `build_chat_model(cfg: ProviderConfig, *, temperature: float = 0.2)`. Branch on `cfg.type`: `"langchain_anthropic"` lazy-imports `ChatAnthropic` and returns `ChatAnthropic(model=cfg.model, temperature=temperature, max_tokens=cfg.max_tokens)`; `"langchain_openai"` lazy-imports `ChatOpenAI` inside a try/except ImportError that re-raises `RuntimeError("GATE_PROVIDER_ERROR: provider 'langchain_openai' requires the optional 'llm-openai' extra. Install: pip install 'construct[llm-openai]'")`; any other type (including the out-of-scope `langchain_ollama`, per D-02) raises `RuntimeError(f"GATE_PROVIDER_ERROR: unknown provider type '{cfg.type}'")`. Mirror the lazy-import-inside-branch shape from SearchProviderFactory. Do NOT use `init_chat_model` (the `langchain` umbrella package is not installed — see RESEARCH anti-patterns). Add `llm-openai = ["langchain-openai>=1.2,<2"]` to `[project.optional-dependencies]` in pyproject.toml. Never log or echo `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` (T-09-06): the factory only constructs the client, which reads keys from env internally.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/llm/test_factory.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def build_chat_model" src/construct/llm/factory.py` returns a hit.
    - `grep -c "GATE_PROVIDER_ERROR" src/construct/llm/factory.py` >= 2 (lazy-import error + unknown-provider error).
    - `grep -n "llm-openai" pyproject.toml` returns a hit under `[project.optional-dependencies]`.
    - `grep -n "init_chat_model" src/construct/llm/factory.py` returns NOTHING.
    - `./.venv/bin/python -m pytest tests/llm/test_factory.py -x -q` passes.
  </acceptance_criteria>
  <done>factory.build_chat_model returns the correct ChatModel per type, raises GATE_PROVIDER_ERROR for missing-extra and unknown types, the optional extra is declared, and test_factory.py is green.</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit ask_domain.py to the factory and repoint the test monkeypatch seam (D-01)</name>
  <files>src/construct/llm/ask_domain.py, tests/llm/conftest.py, tests/llm/test_ask_domain.py, tests/contract/test_ask_domain_mocked.py</files>
  <read_first>
    - src/construct/llm/ask_domain.py (lines 8 import, 206-262 llm_synthesize node, 343-423 run_gate — see how provider_cfg is resolved at 371-376 and how `model` is threaded into state)
    - src/construct/llm/factory.py (the build_chat_model created in Task 2)
    - tests/llm/conftest.py (lines 25-49 — the `MockChatAnthropic` class lives here; its docstring at line 30 carries a `construct.llm.ask_domain.ChatAnthropic` usage example. NOTE: the `patched_llm` fixture is NOT in conftest.py.)
    - tests/llm/test_ask_domain.py (lines 55-62 — the `patched_llm` fixture; it does the actual monkeypatch of `construct.llm.ask_domain.ChatAnthropic` at line 59)
    - tests/contract/test_ask_domain_mocked.py (line 21 — an inline monkeypatch of `construct.llm.ask_domain.ChatAnthropic`)
  </read_first>
  <action>In `ask_domain.py`: replace the top-level `from langchain_anthropic import ChatAnthropic` import with `from construct.llm import factory`, and replace the `ChatAnthropic(...)` construction inside `llm_synthesize` (lines 228-232) with `factory.build_chat_model(provider_cfg, temperature=0.2)`. The node needs the resolved `ProviderConfig`: thread it through `AskDomainState` — add a `provider_cfg: ProviderConfig` (or the fields `provider_type`/`max_tokens`) entry, populate it in `run_gate`'s `initial_state` from the already-resolved `provider_cfg` (line 374), and reconstruct/read it in `llm_synthesize`. Call the factory via the module attribute (`factory.build_chat_model`) so a single monkeypatch on `construct.llm.factory.build_chat_model` patches BOTH gates (this is the shared seam research_score.py will reuse). Keep all other ask.domain behavior identical (empty-context guard, structured-output, citations, cache). Repoint ALL THREE textual occurrences of `construct.llm.ask_domain.ChatAnthropic` to the new factory seam — there is NO fourth site (verified `grep -rn "construct.llm.ask_domain.ChatAnthropic" tests/` returns exactly these three): (a) the `patched_llm` fixture in tests/llm/test_ask_domain.py:55-62 (monkeypatch line 59) — repoint to `construct.llm.factory.build_chat_model` returning the existing MockChatAnthropic instance; (b) the inline monkeypatch in tests/contract/test_ask_domain_mocked.py:21 — repoint the same way; (c) the `MockChatAnthropic` docstring usage example in tests/llm/conftest.py:30 — update the example string to the new `construct.llm.factory.build_chat_model` seam so the docstring stays accurate (and to satisfy the exhaustive grep). The mock's `build_chat_model` replacement must accept `(cfg, *, temperature=...)` and return a MockChatAnthropic. (No new shared conftest fixture is introduced — `patched_llm` stays where it is in test_ask_domain.py; if a future shared fixture is wanted, that would be a NEW fixture, not part of this repoint.)</action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/llm/test_ask_domain.py tests/contract/test_ask_domain_mocked.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "factory.build_chat_model" src/construct/llm/ask_domain.py` returns a hit.
    - `grep -n "ChatAnthropic(" src/construct/llm/ask_domain.py` returns NOTHING (construction now goes through the factory).
    - `grep -rn "construct.llm.ask_domain.ChatAnthropic" tests/` returns NOTHING (all three textual occurrences repointed: test_ask_domain.py:59, test_ask_domain_mocked.py:21, conftest.py:30 docstring).
    - `grep -rn "construct.llm.factory.build_chat_model" tests/` returns at least the `patched_llm` fixture site in tests/llm/test_ask_domain.py.
    - ask.domain mocked tests pass with the new seam.
  </acceptance_criteria>
  <done>ask.domain constructs its model via the factory, all three monkeypatch occurrences target the shared factory seam, and ask.domain tests are green.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Full-suite regression guard (253-test baseline)</name>
  <files>(no source changes — verification gate only)</files>
  <read_first>
    - .planning/phases/09-llm-provider-factory-research-score/09-RESEARCH.md (## Validation Architecture — baseline 253 tests must stay green after the D-01 retrofit)
  </read_first>
  <action>Run the entire test suite to prove the D-01 retrofit introduced no regression anywhere in the codebase (ask.domain, contract, CLI, MCP, ingestion, graph, etc.). This is a BLOCKING gate: if the suite drops below the 253-test baseline (collection error) or any previously-green test fails, STOP and fix the retrofit before the plan can be considered complete. Do not delete/skip tests to make this pass.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `./.venv/bin/python -m pytest -q` collects >= 253 tests (the new test_factory.py raises the count) and reports 0 failures, 0 errors.
    - No test was deleted or marked xfail/skip to achieve green (`git diff --stat` shows no removed test functions in pre-existing test files beyond the monkeypatch repoint).
  </acceptance_criteria>
  <done>Full suite is green at or above the 253-test baseline; the retrofit is regression-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env -> factory | Provider API keys (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`) are read from the environment by the constructed client; the factory must never log or echo them. |
| PyPI -> build | The new `llm-openai` optional extra introduces a third-party (first-party LangChain) package into the dependency surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Information Disclosure | factory.build_chat_model / config | mitigate | Factory constructs the client only; keys are read from env by the SDK. No key value is logged, echoed, or placed in error messages. GATE_PROVIDER_ERROR strings contain no secrets. |
| T-09-SC | Tampering | pyproject `llm-openai` extra (langchain-openai) | mitigate | Task 1 blocking-human legitimacy checkpoint (pypi.org verification); package is first-party LangChain, declared as optional, never installed in the offline test path. |
</threat_model>

<verification>
- factory.build_chat_model unit tests pass (type map, lazy-import error, unknown-provider error).
- ask.domain behaves identically through the factory (mocked tests green).
- Full suite green at >= 253 tests (BLOCKING regression gate).
- No `ChatAnthropic(` construction remains in ask_domain.py; no `init_chat_model` anywhere.
</verification>

<success_criteria>
- One factory path constructs models for every gate (spec-line-546 anti-pattern resolved).
- `langchain_openai` absence degrades to a clear GATE_PROVIDER_ERROR, not an import crash.
- D-01 retrofit leaves the 253-test baseline green.
</success_criteria>

<output>
Create `.planning/phases/09-llm-provider-factory-research-score/09-01-SUMMARY.md` when done.
</output>
