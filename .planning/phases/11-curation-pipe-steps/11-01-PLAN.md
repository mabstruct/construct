---
phase: 11-curation-pipe-steps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/llm/test_curation_run.py
  - tests/contract/test_curation_run_cli_mcp.py
  - tests/llm/conftest.py
autonomous: true
requirements: [CUR-01]
must_haves:
  truths:
    - "A failing pytest suite exists that pins the curation.run contract before any production code is written"
    - "The llm suite fails because src/construct/llm/curation_run.py does not yet exist (import error in test body)"
    - "The contract suite fails because curation.run / curation.inspect are not yet registered"
    - "Tests can sample every CUR-01 distinction: concrete findings, completed-vs-degraded, skipped-deferred, threshold honoring, no-canonical-writes, anti-placeholder"
  artifacts:
    - path: "tests/llm/test_curation_run.py"
      provides: "node + graph + scan-threshold + status-aggregation + no-write + anti-placeholder red tests"
      min_lines: 120
    - path: "tests/contract/test_curation_run_cli_mcp.py"
      provides: "registry presence, dual-mode shim, MCP auto-discovery, CLI/MCP schema parity, offline smoke"
      min_lines: 80
    - path: "tests/llm/conftest.py"
      provides: "write_card extended with created/last_verified; new decay/orphan workspace fixture"
      contains: "def write_card"
  key_links:
    - from: "tests/llm/test_curation_run.py"
      to: "construct.llm.curation_run.build_curation_run_graph"
      via: "lazy in-body import"
      pattern: "from construct.llm.curation_run import"
    - from: "tests/contract/test_curation_run_cli_mcp.py"
      to: "curation.run capability"
      via: "registry.get"
      pattern: "curation\\.run"
---

<objective>
Create the Wave-0 failing test suite that pins the `curation.run` contract BEFORE the
production module exists (Nyquist: red tests first). The suite is the executable
specification for Plan 02 (the module) and Plan 03 (the CLI/MCP wiring).

Purpose: Every CUR-01 distinction — real findings vs placeholder, completed vs degraded,
skipped-deferred visibility, governance-threshold honoring, no canonical writes — must be
sampleable by a test that can FAIL if a step regresses to fake-success. This phase exists to
kill v0.3 placeholder no-ops; the tests are the guard.

Output: `tests/llm/test_curation_run.py`, `tests/contract/test_curation_run_cli_mcp.py`, and
extended fixtures in `tests/llm/conftest.py`. All curation tests RED until Plans 02–03 land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-curation-pipe-steps/11-RESEARCH.md
@.planning/phases/11-curation-pipe-steps/11-PATTERNS.md

<interfaces>
<!-- Contract the tests must pin. These names become the public surface Plan 02 implements. -->
<!-- Module: src/construct/llm/curation_run.py (NEW, created in Plan 02) -->
- build_curation_run_graph(checkpointer) -> compiled StateGraph
- _initial_state(inp: CurationRunInput, run_id: str) -> dict   (or _initial_state(inp) — mirror research_run; confirm against Plan 02)
- run_curation_run(inp: CurationRunInput) -> CurationRunResult
- inspect_curation_run(inp: CurationInspectInput) -> CurationRunResult
- CurationRunInput(workspace_path: str, run_id: str | None = None)
- CurationInspectInput(workspace_path: str, run_id: str)
- CurationStepResult(step, status: completed|skipped|failed, required: bool, findings: dict, summary: str, reason: str|None)
- CurationRunResult(status: completed|degraded|failed, run_id, steps: list[CurationStepResult], events: list[str], message)

<!-- Node sequence (spec §4.3) the integration test asserts on result["steps"] -->
integrity_check → decay_scan → orphan_scan → promotion_review(SKIP) → connection_maintenance
  → process_inbox(SKIP) → compile_report → views_refresh_hook(SKIP)

<!-- Capability ids / surface names the contract test pins -->
curation.run  → cli_name="curation.run"  → mcp_tool_name="construct_curation_run"
curation.inspect → cli_name="curation.inspect" → mcp_tool_name="construct_curation_inspect"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend conftest fixtures for decay/orphan determinism</name>
  <files>tests/llm/conftest.py</files>
  <read_first>
    - tests/llm/conftest.py (current write_card hardcodes `created: 2025-01-01`, has no created/last_verified params, no orphan fixture)
    - tests/llm/test_research_run.py (fixture-usage idiom to mirror)
    - .planning/phases/11-curation-pipe-steps/11-RESEARCH.md (Validation Architecture → Wave 0 Gaps; Pitfalls 2 & 3 for date/degree rules)
    - src/construct/schemas/workspace.py (ConnectionRecord — `from_` aliased `from`, `to`, `connection_type`; ConnectionsFile shape for writing connections.json)
  </read_first>
  <action>
    Extend `write_card` in tests/llm/conftest.py to accept two new keyword params: `created: str = "2025-01-01"`
    (already the hardcoded value — promote it to a param) and `last_verified: str | None = None` (emit a
    `last_verified:` frontmatter line only when provided). Do NOT change existing call sites — keep defaults so
    `test_workspace` and all current tests behave identically.

    Add a new fixture `curation_workspace(tmp_path)` that builds a workspace via `create_test_workspace`, then
    writes a deterministic card set for threshold testing: (a) one FRESH card created today-ish (recent date) that
    is NOT a decay candidate; (b) one STALE card with `created` far past the default 28-day decay window and no
    connections (degree 0) — this is both a decay AND an orphan candidate; (c) one CONNECTED stale card that is
    decay-eligible but NOT an orphan; (d) one `lifecycle="archived"` stale card that must be EXCLUDED from both
    scans. Then write a `connections/connections.json` (ConnectionsFile shape) containing exactly one
    ConnectionRecord linking the connected stale card to the fresh card, so degree-counting must count BOTH `from_`
    and `to` (Pitfall 3). Compute dates relative to `date.today()` so the fixture stays valid over time — do not
    hardcode absolute stale dates that could drift inside the window.
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/llm/test_research_run.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `write_card(...)` accepts `created=` and `last_verified=` kwargs; calling it without them produces byte-identical output to the pre-change version (existing research-run tests still pass — regression guard).
    - `curation_workspace` fixture yields a workspace path containing 4 cards (fresh, stale-orphan, stale-connected, stale-archived) and a `connections/connections.json` with exactly 1 ConnectionRecord.
    - Stale dates are computed as `date.today() - timedelta(days=...)` past the 28-day window, not hardcoded literals.
    - `pytest tests/llm/test_research_run.py -q` still green (the additive fixture/param change breaks nothing).
  </acceptance_criteria>
  <done>conftest extended additively; existing llm suite still green; new curation_workspace fixture available.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/llm/test_curation_run.py red suite</name>
  <files>tests/llm/test_curation_run.py</files>
  <read_first>
    - tests/llm/test_research_run.py (mirror structure: lazy in-body imports, build graph directly, assert on result["steps"])
    - .planning/phases/11-curation-pipe-steps/11-RESEARCH.md (Validation Architecture → "What real-vs-placeholder distinctions" 1–6; Phase Requirements → Test Map rows)
    - .planning/phases/11-curation-pipe-steps/11-PATTERNS.md ("Sampling points the suite MUST be able to FAIL on")
    - tests/llm/conftest.py (fixtures available after Task 1: curation_workspace, write_card, sqlite_checkpointer, test_workspace)
  </read_first>
  <action>
    Create tests/llm/test_curation_run.py mirroring tests/llm/test_research_run.py structure (module-level
    docstring, lazy `from construct.llm.curation_run import ...` INSIDE each test body so collection succeeds and
    tests fail red on ImportError, not collection error). Implement exactly these test functions, named to match
    the RESEARCH Test Map:
    - `test_full_run_offline_real_findings` — build graph via `build_curation_run_graph(saver)` over a tmp
      checkpointer, `graph.invoke(_initial_state(...), {"configurable": {"thread_id": run_id}})` against
      `curation_workspace`; assert result["steps"] contains entries for integrity_check, decay_scan, orphan_scan,
      connection_maintenance, compile_report each with non-empty `findings` carrying the expected keys (counts +
      candidate_ids), NOT a bare success string.
    - `test_run_status_degraded_on_step_failure` — drive `run_curation_run` (or assemble steps) where one REQUIRED
      step has status="failed"; assert run-level status == "degraded". Pair with a clean-run assertion that status
      == "completed" (NOT degraded despite the 3 deferred skips — Pitfall 5).
    - `test_deferred_nodes_visible_skipped` — assert promotion_review, process_inbox, views_refresh_hook appear in
      result["steps"] with status="skipped", required=False, and reason containing "deferred to Phase 12".
    - `test_steps_return_concrete_findings` — assert NO real step's `summary`/`message` contains the substring
      "placeholder"; each real step's findings dict is non-empty.
    - `test_scans_use_governance_thresholds` — run twice against the same workspace with two different
      `decay_window_days` / `orphan_tolerance_days` governance values (write/patch governance.yaml); assert the
      decay/orphan candidate set changes (proves D-05 wiring, not hardcoding).
    - `test_no_canonical_writes` — snapshot bytes of `cards/`, `refs/`, `connections/connections.json`,
      `search-seeds.json` before/after a run; assert byte-identical. Do NOT assert on `log/` or `views/`
      (bridge_detect writes derived artifacts there — Pitfall 1).
    - `test_auto_archive_reported_not_acted` — set `decay.auto_archive_on_decay: true` in governance.yaml; assert
      the decay step's summary/findings reports the flag AND notes archiving deferred to Phase 12, and that no card
      lifecycle flipped to archived (D-06).
    - `test_inspect_no_rerun` — after a run, `inspect_curation_run` reads persisted terminal state and does NOT
      re-execute nodes (assert it returns the same run_id/steps without mutating the workspace).
    Use `date.today()`-relative assertions consistent with the curation_workspace fixture.
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/llm/test_curation_run.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File defines the 8 test functions named exactly as above (matches RESEARCH Test Map → enables per-task sampling in Plan 02).
    - Every test imports `construct.llm.curation_run` lazily inside the body; `pytest --collect-only tests/llm/test_curation_run.py` succeeds (no collection error).
    - Running the suite is RED: every test fails with ImportError/AttributeError on the missing curation_run module — proves the contract is unimplemented, not falsely passing.
    - `test_no_canonical_writes` asserts on cards/refs/connections.json/search-seeds.json only; grep confirms NO assertion references `log/` or `views/`.
    - No test contains the substring "placeholder" as an EXPECTED value (it appears only inside the negative `assert "placeholder" not in ...` guard).
  </acceptance_criteria>
  <done>8 red tests collected and failing on the missing module; sampling points 1–6 each have a dedicated failing test.</done>
</task>

<task type="auto">
  <name>Task 3: Write tests/contract/test_curation_run_cli_mcp.py red suite</name>
  <files>tests/contract/test_curation_run_cli_mcp.py</files>
  <read_first>
    - tests/contract/test_research_run_cli_mcp.py (full file — direct mirror; rename research→curation, drop the offline-LLM seam)
    - src/construct/mcp/server.py (to write the "no hardcoded curation string" assertion against its source)
    - .planning/phases/11-curation-pipe-steps/11-PATTERNS.md (tests/contract section: exact test shapes + _CAPS map)
  </read_first>
  <action>
    Create tests/contract/test_curation_run_cli_mcp.py mirroring tests/contract/test_research_run_cli_mcp.py.
    Define `_CAPS = {"curation.run": "construct_curation_run", "curation.inspect": "construct_curation_inspect"}`.
    Implement: `test_registered` (each cap present in registry with `cap.mcp_tool_name == mcp_name` and
    `cap.cli_name == cap_id`); `test_shims_reject_positional_args` (`registry.get(cap_id).handler("positional")`
    raises TypeError); `test_in_mcp_tool_list` / `test_mcp_server_exposes_curation` (registry auto-discovery lists
    the curation tools); `test_mcp_no_hardcoded_curation` (assert `"construct_curation_run"` and `"curation.run"`
    do NOT appear in the source of src/construct/mcp/server.py — parity is registry-driven, no server edit);
    `test_cli_commands_present` (`CliRunner().invoke(app, ["curation", sub, "--help"])` exits 0 for sub in
    {run, inspect}); `test_cli_mcp_schema_parity` (CLI `--json` output keys == MCP-serialized handler `data` keys ==
    `CurationRunResult.model_fields.keys()`). The parity/smoke test MUST run fully offline with no
    ANTHROPIC_API_KEY set (connection-health degrades to L1/L2 deterministically).
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/contract/test_curation_run_cli_mcp.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `_CAPS` maps both curation capabilities to their MCP tool names exactly as specified.
    - Suite is RED: registry-presence and CLI-command tests fail because curation.run/curation.inspect are not registered and the `curation` Typer sub-app does not exist yet.
    - `test_mcp_no_hardcoded_curation` is written to assert ABSENCE in mcp/server.py source (will stay GREEN — it is the guardrail that Plan 03 must not edit mcp/server.py).
    - `test_cli_mcp_schema_parity` sets/clears env so it runs with no ANTHROPIC_API_KEY (grep confirms no key requirement).
  </acceptance_criteria>
  <done>Contract suite collected and RED on missing registration + missing CLI sub-app; mcp-no-hardcoded guard in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production contract | Tests encode the security/behavioral invariants the production code must satisfy; a missing test is an unguarded invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | curation.run vs canonical SOT (D-06) | mitigate | `test_no_canonical_writes` asserts cards/refs/connections.json/search-seeds.json byte-identical before/after; excludes derived log/ + views/ (Pitfall 1). This test is the executable D-06 invariant. |
| T-11-02 | Denial of Service | malformed-workspace robustness | mitigate | `test_run_status_degraded_on_step_failure` proves a required-step failure surfaces as `degraded` (honest), not a silent crash or false `completed`. |
| T-11-PL | Spoofing (fake success) | placeholder regression | mitigate | `test_steps_return_concrete_findings` fails if any real step emits a "placeholder"/bare-success message — the anti-fake-success guard (criterion #3). |
| T-11-SC | Tampering | npm/pip installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A — all deps shipped+vetted in Phases 8–10). No supply-chain surface introduced. |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest tests/llm/test_curation_run.py tests/contract/test_curation_run_cli_mcp.py -q` → curation suites RED for the right reasons (missing module / missing registration).
- `./.venv/bin/python -m pytest tests/llm/test_research_run.py -q` → still GREEN (conftest changes are additive).
- `./.venv/bin/python -m pytest --collect-only tests/llm/test_curation_run.py tests/contract/test_curation_run_cli_mcp.py` → no collection errors (lazy imports).
</verification>

<success_criteria>
- Two new test files exist with the named test functions from the RESEARCH Test Map.
- conftest `write_card` extended additively (created/last_verified) + `curation_workspace` fixture added.
- All curation tests RED; all pre-existing tests GREEN.
- The no-canonical-writes test scopes D-06 to canonical SOT only (not derived log/views).
</success_criteria>

<output>
Create `.planning/phases/11-curation-pipe-steps/11-01-SUMMARY.md` when done
</output>
