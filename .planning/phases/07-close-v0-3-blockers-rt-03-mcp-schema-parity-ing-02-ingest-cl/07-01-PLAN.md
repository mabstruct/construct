---
phase: 07-close-v0-3-blockers-rt-03-mcp-schema-parity-ing-02-ingest-cl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/capabilities/catalog.py
  - src/construct/services/help.py
  - tests/contract/test_mcp_contracts.py
autonomous: true
requirements: [RT-03, ING-05]

must_haves:
  truths:
    - "Every MCP tool handler can be invoked with its advertised input_model field names without raising TypeError"
    - "construct_create_card, construct_edit_card, construct_add_connection, construct_validate, construct_ingest_source return an OperationResult/ValidationReport when called with schema-shaped kwargs"
    - "graph.status capability returns the real graph-health report (cards/connections/domains) instead of a 'Not yet implemented' stub"
    - "help.suggest surfaces graph.status data instead of silently swallowing its failure"
    - "A contract test invokes every MCP tool handler with schema-shaped kwargs and fails if any raises TypeError"
  artifacts:
    - path: "src/construct/capabilities/catalog.py"
      provides: "lambda **kwargs adapter shims mapping schema fields to service-fn args for validate, knowledge.card.create, knowledge.card.edit, knowledge.connection.add, ingest.source; graph.status wired to graph_status()"
      contains: "graph_status"
    - path: "tests/contract/test_mcp_contracts.py"
      provides: "handler-invocation contract test"
      contains: "handler("
  key_links:
    - from: "src/construct/capabilities/catalog.py graph.status handler"
      to: "src/construct/pipelines/graph_status.py graph_status"
      via: "adapter shim invoking graph_status(workspace)"
      pattern: "graph_status\\("
    - from: "src/construct/services/help.py"
      to: "graph.status capability handler"
      via: "registry handler call surfacing graph_result.data"
      pattern: "graph\\.status"
---

<objective>
Close RT-03 (MCP schema parity) and ING-05 (graph.status wiring) — both localized to `catalog.py` handler bindings plus a `help.py` consumption fix. Add adapter shims so the five broken MCP capabilities accept their advertised schema fields and marshal them into the real service-function signatures, mirroring the working `ask.domain`/`bridge.detect`/`workflow.run` adapters already in the file. Wire the existing `graph_status()` implementation into the `graph.status` stub. Add the handler-invocation contract test that closes the CI blind spot which hid RT-03.

Purpose: Restore the agentic MCP knowledge-write path and graph-health reporting that v0.3 advertised but that fail at runtime with TypeError / stub responses.
Output: Working MCP tool handlers, wired graph.status, surfaced graph-health in help.suggest, and a contract test that prevents recurrence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-close-v0-3-blockers-rt-03-mcp-schema-parity-ing-02-ingest-cl/07-CONTEXT.md
@.planning/v0.3-MILESTONE-AUDIT.md

<interfaces>
<!-- Service-fn signatures the shims must marshal INTO (confirmed from source) -->

From src/construct/services/knowledge.py:
- create_card(workspace_root, card_data: dict, author: CardAuthor = construct, body: str | None = None) -> OperationResult
- edit_card(workspace_root, card_id, updates: dict, author: CardAuthor = curator) -> OperationResult
- add_connection(workspace_root, from_id, to_id, conn_type: ConnectionType, note=None, created_by: ConnectionAuthor = construct) -> OperationResult

From src/construct/services/validation.py:
- validate_workspace(root) -> ValidationReport

From src/construct/pipelines/ingestion.py:
- ingest_source(...) — already keyword-compatible with IngestSourceInput fields (workspace, source, domain_hint, author, title, relevance, source_tier, key_findings, content_categories, year, venue, search_cluster)

From src/construct/pipelines/graph_status.py:
- graph_status(workspace: str | Path) -> OperationResult  (complete; returns data with cards/connections/domains keys)

<!-- Advertised input_model field names (from catalog.py) the MCP/UI pass as kwargs -->
- CardCreateInput: workspace, title, epistemic_type, domains, confidence, source_tier, content_categories, author, summary
- CardEditInput: card_id, workspace, title, confidence, source_tier, lifecycle, summary, author
- ConnectionAddInput: from_id, to_id, conn_type, note, workspace, created_by
- WorkspacePathInput (validate): path
- GraphStatusInput: workspace

<!-- The CLI already marshals these correctly — mirror its dict construction (cli.py:754-810) -->
<!-- create: card_data={title,epistemic_type,domains,confidence,source_tier,content_categories,author}; summary→card_data["_summary"]; calls handler(workspace, card_data, author=CardAuthor(author)) -->
<!-- edit: updates dict from provided fields; summary→updates["_summary"]; calls handler(workspace, card_id, updates, author=CardAuthor(author)) -->
</interfaces>

<working_adapter_pattern>
<!-- Mirror these EXISTING working adapters in catalog.py (do NOT invent a new abstraction): -->
<!-- ask.domain (catalog.py:316), bridge.detect (catalog.py:334), workflow.run (catalog.py:271): lambda **kwargs / lambda <named args> that re-shape inputs before calling the real fn. -->
</working_adapter_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RT-03 adapter shims for the five broken capabilities</name>
  <read_first>
    - src/construct/capabilities/catalog.py (the registrations being modified AND the working ask.domain/bridge.detect/workflow.run adapters at :271-337 to mirror)
    - src/construct/cli.py:748-811 (the correct card_data/updates dict marshalling to replicate)
    - src/construct/services/knowledge.py:136-226,347-354 (create_card/edit_card/add_connection signatures)
    - src/construct/services/validation.py:205 and the validate_workspace signature
  </read_first>
  <action>
    In `create_registry()` in catalog.py, replace the raw `handler=` bindings for these five capability records with `lambda **kwargs:` adapter shims that map advertised input_model field names to the service-fn parameter names — mirror the marshalling the CLI already does (cli.py:754-810), NOT a new abstraction.

    - `workspace.validate` (mcp `construct_validate`): shim maps `path` → `validate_workspace(root=...)`. (input_model is WorkspacePathInput with field `path`; service param is `root`.)
    - `knowledge.card.create` (`construct_create_card`): shim builds a `card_data` dict from {title, epistemic_type, domains, confidence, source_tier, content_categories} plus `_summary` from `summary` when non-empty, then calls `create_card(workspace, card_data, author=CardAuthor(author))`. Map schema `workspace` → positional workspace_root.
    - `knowledge.card.edit` (`construct_edit_card`): shim builds an `updates` dict from only the provided non-None fields {title, confidence, source_tier, lifecycle} plus `_summary` from `summary`, then calls `edit_card(workspace, card_id, updates, author=CardAuthor(author))`.
    - `knowledge.connection.add` (`construct_add_connection`): shim maps schema `workspace` → workspace_root, coerces `conn_type` → ConnectionType and `created_by` → ConnectionAuthor, passes from_id/to_id/note, then calls `add_connection(...)`.
    - `ingest.source` (`construct_ingest_source`): confirm IngestSourceInput field names already match `ingest_source` keyword params; if they do, a thin `lambda **kwargs: ingest_source(**kwargs)` shim suffices. If any field name diverges, map it. Do NOT change ingest_source.

    Do NOT modify any service-layer signature (knowledge.py, validation.py, ingestion.py) and do NOT touch the CLI. Reuse the existing imports already at the top of catalog.py (CardAuthor, ConnectionAuthor are imported; import ConnectionType if not already in scope).
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && python -c "from construct.capabilities.catalog import get_registry; r=get_registry(); [r.get_by_mcp_name(n).handler for n in ('construct_validate','construct_create_card','construct_edit_card','construct_add_connection','construct_ingest_source')]" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Invoking the `construct_create_card` handler with `{workspace, title, epistemic_type, domains, confidence, source_tier}` keyword args returns an OperationResult (not TypeError).
    - Invoking `construct_edit_card` handler with `{card_id, workspace, title}` returns an OperationResult (not TypeError).
    - Invoking `construct_add_connection` handler with `{from_id, to_id, conn_type, workspace}` returns an OperationResult (not TypeError).
    - Invoking `construct_validate` handler with `{path}` returns a ValidationReport/result (not TypeError).
    - Invoking `construct_ingest_source` handler with `{workspace, source}` returns an OperationResult (not TypeError).
    - No edits to knowledge.py, validation.py, ingestion.py, or cli.py.
  </acceptance_criteria>
  <done>All five MCP capabilities accept their advertised schema fields and marshal into the real service signatures without TypeError; service layer and CLI untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire graph.status to graph_status() and surface it in help.suggest (ING-05)</name>
  <read_first>
    - src/construct/capabilities/catalog.py:247-255 (the stub handler to replace) and the imports block at top
    - src/construct/pipelines/graph_status.py:12 (the complete graph_status(workspace) to wire in — wiring only, do NOT rewrite)
    - src/construct/services/help.py:119-130 (where the handler result is consumed; note it calls cap.handler(workspace_id) POSITIONALLY)
  </read_first>
  <action>
    In catalog.py, add `from construct.pipelines.graph_status import graph_status` to the imports, and replace the `graph.status` record's stub handler (`lambda **kwargs: OperationResult(success=False, message="Not yet implemented — see Plan 02")`) with an adapter that calls `graph_status(workspace)`. The shim MUST accept the `workspace` value whether passed by keyword (`workspace=`, from GraphStatusInput / MCP) or positionally (`help.py:126` calls `cap.handler(workspace_id)`). Use a signature like `lambda workspace: graph_status(workspace)` so both call styles work; confirm help.py's positional call resolves against it.

    In help.py:124-130, keep the try/except but ensure the wired result's `.data` is assigned to `graph_data` on success (the existing code already does this once the handler returns a real OperationResult). Do not broaden the bare `except (KeyError, Exception)` semantics beyond what is needed; the point is that the success branch now actually populates graph_data because the handler no longer returns success=False.

    Do NOT rewrite graph_status(). Leave `views.generate_data` stub untouched (out of scope).
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && python -c "from construct.capabilities.catalog import get_registry; r=get_registry(); res=r.get('graph.status').handler('test-ws/my-construct'); assert res.success, res.message; assert set(['cards','connections','domains']).issubset(res.data.keys()); print('OK', list(res.data.keys()))"</automated>
    <automated>cd /Users/mab/dev/mabstruct/construct && python -c "from construct.capabilities.catalog import get_registry; r=get_registry(); hs=r.get('help.suggest').handler('test-ws/my-construct'); assert hs.success, hs.message; assert 'graph_status' in hs.data, 'help.suggest did not surface graph_status — graph.status result was swallowed'; print('help surfaces graph:', bool(hs.data['graph_status']))"</automated>
  </verify>
  <acceptance_criteria>
    - `get_registry().get('graph.status').handler('test-ws/my-construct')` returns OperationResult(success=True) with `data` containing `cards`, `connections`, and `domains` keys.
    - The handler accepts both `handler('test-ws/my-construct')` (positional, as help.py calls it) and `handler(workspace='test-ws/my-construct')` (keyword, as MCP calls it) without TypeError.
    - `construct graph.status -w test-ws/my-construct` (via registry dispatch) returns graph-health data, not "Not yet implemented".
    - help.suggest surfaces graph health: `get_registry().get('help.suggest').handler('test-ws/my-construct').data` contains a `graph_status` key (per help.py:168-169, this key is present ONLY when graph.status succeeds — so its presence machine-proves the swallow is fixed). This assertion is added to the contract test in Task 3.
    - graph_status.py is unchanged; views.generate_data stub is unchanged.
  </acceptance_criteria>
  <done>graph.status returns the real report via both positional and keyword invocation; help.suggest surfaces it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Handler-invocation contract test (hard done-gate D-08.1)</name>
  <read_first>
    - tests/contract/test_mcp_contracts.py (existing schema-only tests to extend — note they NEVER call a handler, which is why RT-03 passed CI)
    - src/construct/capabilities/catalog.py (the now-fixed handlers and their input_models)
    - src/construct/mcp/server.py:30-44 (how the live MCP server calls capability.handler(**kwargs))
  </read_first>
  <behavior>
    - Test: for every registry capability with an mcp_tool_name, invoking its handler with schema-shaped kwargs (one representative valid payload per tool, against test-ws/my-construct where a workspace is needed) raises NO TypeError.
    - Test: the five previously-broken tools (construct_validate, construct_create_card, construct_edit_card, construct_add_connection, construct_ingest_source) each return a result object (OperationResult/ValidationReport), asserted not to be a TypeError.
    - Test: construct_graph_status returns success with cards/connections/domains keys.
    - Test: help.suggest surfaces graph health — `get_registry().get('help.suggest').handler('test-ws/my-construct').data` contains a `graph_status` key (machine-proves the ING-05 swallow at help.py:124-130 is fixed, since that key is populated only when graph.status succeeds).
    - Edge: a tool invoked with a clearly-wrong kwarg name still must not raise TypeError from the shim layer for the correct advertised fields (the test only asserts the advertised schema fields work).
  </behavior>
  <action>
    Extend test_mcp_contracts.py (or add a sibling test module — Claude's discretion per D-09) with a handler-invocation test that iterates `get_registry().list_mcp_tools()` (which returns a list of dicts with a `name` key — NOT capability records), resolves each capability via `get_registry().get_by_mcp_name(tool["name"])` to obtain the `.handler`, derives a representative valid kwargs payload per tool from its input_model fields (use test-ws/my-construct as the workspace/path; pick minimal valid values for required fields like epistemic_type, domains, confidence, source_tier, conn_type, from_id/to_id), invokes `capability.handler(**payload)`, and asserts the call does not raise TypeError. (Mirror the resolution pattern already used in Task 1's verify command: `get_by_mcp_name(n).handler`.) For write-capable tools that would mutate the fixture, either invoke against a tmp copy of a workspace (pytest tmp_path) or assert only that the call returns a result/known-validation-failure rather than a TypeError. The test MUST fail if any handler raises TypeError for its advertised schema fields. Keep the existing schema-shape tests.

    This test is the structural mechanism preventing RT-03 recurrence — it must call handlers, not just inspect schemas.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && pytest tests/contract/test_mcp_contracts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/contract/test_mcp_contracts.py` exits 0.
    - The new test invokes `capability.handler(**kwargs)` for every mcp tool (assert by grep: `handler(` appears in the test file and the test iterates list_mcp_tools()).
    - If Task 1's shims are reverted, this test FAILS with a TypeError-related assertion (the test genuinely exercises the handler path, not schema shape).
    - The full suite still passes: `pytest -q` exits 0 with no regressions.
  </acceptance_criteria>
  <done>A contract test invokes every MCP tool handler with schema-shaped kwargs and fails on any TypeError; the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP agent → capability handler | Untrusted agent-supplied kwargs cross into service functions via the shim layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | shim kwargs → service args | mitigate | Shims forward only advertised input_model fields; Pydantic input_model already validates types/ranges (confidence ge1 le5, source_tier ge1 le5) before dispatch; service layer keeps its own write-time validation. |
| T-07-02 | Denial of Service | graph.status over large workspace | accept | Read-only traversal of local files; single-user local-first; no remote exposure. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (catalog wiring + test only); slopcheck N/A. |
</threat_model>

<verification>
- `pytest tests/contract/test_mcp_contracts.py -x -q` exits 0 (hard done-gate D-08.1).
- `pytest -q` exits 0 (no regressions across the 209-test suite).
- graph.status returns real data via both positional and keyword invocation.
</verification>

<success_criteria>
- All five previously-broken MCP tool handlers invoke without TypeError on their advertised schema.
- graph.status returns the real cards/connections/domains report; help.suggest surfaces it.
- Handler-invocation contract test exists, calls handlers, and gates the suite.
- No service-layer or CLI signatures changed.
</success_criteria>

<output>
Create `.planning/phases/07-close-v0-3-blockers-rt-03-mcp-schema-parity-ing-02-ingest-cl/07-01-SUMMARY.md` when done.
</output>
