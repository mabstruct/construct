---
phase: 14-durable-state-config-truth
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - CONSTRUCT-CLAUDE-spec/workspace-contract.md
  - CONSTRUCT-CLAUDE-spec/config-topology.md
autonomous: true
requirements: [DOC-03, FIX-02]
must_haves:
  truths:
    - "workspace-contract.md lists .construct/workflow/*.sqlite, .construct/search.yaml, and WORKSPACE.md among its workspace artifacts."
    - ".construct/workflow/*.sqlite is classified under a fourth artifact class (durable orchestration state) whose preamble does not deny that it holds workspace truth — filing it under Support or Derived would reintroduce, in the document being corrected, the exact falsehood this phase removes."
    - "Every surviving mention of model-routing.yaml in workspace-contract.md and config-topology.md carries an explicit deprecation marker; none describes it as authoritative or routing-controlling."
    - "The workspace-contract.md canonical shape tree shows .construct/ with search.yaml and workflow/, plus WORKSPACE.md."
  artifacts:
    - "CONSTRUCT-CLAUDE-spec/workspace-contract.md (modified: shape tree 21-34, Derived table, Support table, new fourth artifact-class section)"
    - "CONSTRUCT-CLAUDE-spec/config-topology.md (modified: template tree line 56, comparison table line 135)"
  key_links:
    - "workspace-contract.md fourth artifact class -> adrs/adr-0004-durable-workflow-checkpoints.md — D-12 requires any new class to be defined in the ADR; the contract cites it rather than re-deriving it"
    - "workspace-contract.md <-> src/construct/schemas/workspace.py REQUIRED_PATHS — the doc describes what the schema enforces; the schema is NOT modified in this phase"
    - "config-topology.md -> src/construct/llm/config.yaml — the named LLM configuration authority, consistent with nfrs.md §3 (plan 14-02)"
  prohibitions:
    - statement: "MUST NOT file .construct/workflow/*.sqlite under the existing Support class, whose own preamble states that such artifacts do not define workspace truth — that sentence is the precise falsehood DOC-03 exists to remove, and filing the checkpoint beneath it would let criterion 2 pass textually while contradicting criterion 1 three files over."
      category: transparency
    - statement: "MUST NOT change what the workspace contract actually requires — no artifact may be added to, removed from, or reclassified within REQUIRED_PATHS, and src/construct/schemas/, storage/, and services/ must remain untouched. This phase documents the existing contract; silently altering the enforced workspace format under cover of a documentation fix is the failure mode v0.4.1 lists Out of Scope."
      category: integrity
---

<objective>
Make `workspace-contract.md` list the artifacts a real workspace actually contains, classified honestly
(D-12/Q3/Q4), and give `model-routing.yaml` its one recorded fate — deprecated but scaffolded — in the
three remaining fence targets (D-01/D-02).

Purpose: criterion 2 needs three missing artifacts listed; criterion 4 needs every doc that currently
calls `model-routing.yaml` authoritative to say otherwise.

Output: two modified spec documents. No schema, storage, or service code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-durable-state-config-truth/14-CONTEXT.md
@.planning/phases/14-durable-state-config-truth/14-RESEARCH.md
@.planning/phases/14-durable-state-config-truth/14-PATTERNS.md
@.planning/phases/14-durable-state-config-truth/14-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| `### Durable orchestration state` | new doc section in `workspace-contract.md` | fourth artifact-class heading, preamble, three-column table, trailing rule sentence |
| `durable orchestration state` | new class value | the `Class` cell value for `.construct/workflow/*.sqlite` |

New identifiers referenced but created by plan 14-01 (exclude from drift checks):
`adrs/adr-0004-durable-workflow-checkpoints.md`, `ADR-0004`.

<d03_collision_note>
**Recorded per D-03 — carried verbatim into this plan so a future executor does not misread it.**

Phase 17 (DOC-02) may delete or rewrite `config-topology.md` wholesale — REQUIREMENTS DOC-02 reads
"`config-topology.md` is either corrected against the real layout or deleted." Phase 14's two edits to
that file may therefore be discarded by Phase 17. **This is accepted.** No cross-phase dependency is
created and Phase 14 must not block on Phase 17. A Phase 17 executor should treat the deprecation notes
added here as **discardable, not load-bearing** — the load-bearing record of `model-routing.yaml`'s fate
lives in `nfrs.md` §3 (plan 14-02) and `workspace-contract.md` (Task 2 below), both of which Phase 17
does not own.
</d03_collision_note>

<tasks>

<task type="auto">
  <name>Task 1: Add the three missing workspace artifacts and introduce the durable-orchestration-state class</name>

  <files>CONSTRUCT-CLAUDE-spec/workspace-contract.md</files>

  <read_first>
    - `CONSTRUCT-CLAUDE-spec/workspace-contract.md` — lines 1-100. Read the canonical shape tree
      (21-34) and all three artifact-class tables (44-56 Canonical, 58-67 Derived, 69-78 Support)
      including each table's `###` heading, defining preamble sentence, column count, and any trailing
      rule sentence. The preambles matter: the Support preamble says its artifacts do not define
      workspace truth, and the Derived section states derived artifacts are never canonical inputs —
      both are false of a checkpoint holding the only copy of pending decisions.
    - `CONSTRUCT-CLAUDE-spec/adrs/adr-0004-durable-workflow-checkpoints.md` — created by plan 14-01.
      Its `## Durable orchestration state (artifact class)` section is the authoritative definition;
      this task's table cites it rather than re-deriving it.
    - `src/construct/schemas/workspace.py` — lines 15-26 (`REQUIRED_PATHS`). **READ-ONLY.** Confirm that
      `.construct/workflow/` is NOT a required path and that `inbox/` IS one. Do not modify this file.
    - `src/construct/services/init.py` — lines 58-59 and 161-176. Confirm `.construct/search.yaml` is
      scaffolded and `WORKSPACE.md` is generated prose. **READ-ONLY.**
    - `src/construct/llm/research_run.py` — line 891, and `src/construct/llm/curation_run.py` line 287.
      Confirm the checkpoint directory is created lazily via `mkdir(parents=True, exist_ok=True)` at
      first checkpointer construction. **READ-ONLY.**
    - `.planning/phases/14-durable-state-config-truth/14-PATTERNS.md` — section
      "CONSTRUCT-CLAUDE-spec/workspace-contract.md" for the verbatim table shapes, the fourth-class
      template, and the exact on-disk form of the shape tree.
  </read_first>

  <action>
    Three edits to `CONSTRUCT-CLAUDE-spec/workspace-contract.md`. No other file may change in this task.

    **Edit 1 — a new fourth artifact-class section.** Add it after the Support artifacts table, copying
    the Derived table's structure exactly: a `###` heading, a one-sentence preamble that *defines the
    class*, a three-column `Path | Class | Role` table, and a trailing rule sentence.

    - Heading: `### Durable orchestration state`
    - Preamble: define the class as artifacts holding in-flight workflow decisions that are not
      reconstructible from the canonical source-of-truth files — neither canonical knowledge nor derived
      from it.
    - One row: path `` `.construct/workflow/*.sqlite` ``; `Class` cell value `durable orchestration state`
      (a lowercase noun phrase matching the heading, as the other three tables do); `Role` cell naming
      LangGraph checkpoint state holding pending human-review decisions for `research.run` and
      `curation.run`.
    - Trailing rule sentence: state that this directory is **not** in `REQUIRED_PATHS`, is not scaffolded
      at init, is created lazily at first checkpointer construction, and may legitimately be absent from
      a valid workspace. Cite the ADR as a plain backticked path —
      `` `adrs/adr-0004-durable-workflow-checkpoints.md` `` — for the full decision.

    Introducing a fourth class rather than reusing an existing one is required by D-12/Q3: the Support
    preamble denies that its members define workspace truth and the Derived section denies that its
    members are canonical inputs, and both denials are false of this artifact. A `.sqlite` row appearing
    directly beneath the `model-routing.yaml` row in the Support table is the failure signal.

    **Edit 2 — the other two D-12 artifacts, into the existing tables.**
    - `WORKSPACE.md` → the **Derived** table. It is generated prose produced by `services/init.py`.
      Role cell: generated workspace orientation document describing the scaffolded layout.
    - `` `.construct/search.yaml` `` → the **Support** table. It is configuration scaffolded at init and
      sits naturally beside the existing `model-routing.yaml` row. Role cell: search-provider
      configuration, naming that the shipped template defaults to the `mock` provider.

    Match each destination table's existing column count and cell style.

    **Edit 3 — the canonical shape tree (currently lines 21-34).** The tree currently omits `.construct/`
    entirely. Add entries for the three D-12 artifacts: `WORKSPACE.md`, and a `.construct/` subtree
    containing `search.yaml` and `workflow/`. Preserve the tree's existing conventions exactly — one
    entry per line, `│   └──` for nesting, and no trailing comments (unlike `config-topology.md`'s tree).
    The final entry currently uses `└──`; adding entries after it requires changing that connector to
    `├──` so the last line in the tree is the only `└──` at its level.

    **Leave `inbox/` out of the tree.** It is in `REQUIRED_PATHS` and its omission here is real,
    pre-existing drift — but it is unrelated to this phase and fixing it would make the diff
    unreviewable against the five criteria. Add a short note beneath the tree recording that `inbox/` is
    a known omission tracked separately, or record it in the plan SUMMARY; do not add the entry.

    Do not modify `src/construct/schemas/workspace.py`, `src/construct/storage/`, or
    `src/construct/services/` in any way. This task documents the existing contract; it does not change it.
  </action>

  <acceptance_criteria>
    - `grep -cF '.construct/workflow/' CONSTRUCT-CLAUDE-spec/workspace-contract.md` returns ≥ 2 (table row + tree) — ROADMAP criterion 2a
    - `grep -cF '.construct/search.yaml' CONSTRUCT-CLAUDE-spec/workspace-contract.md` returns ≥ 1 — criterion 2a
    - `grep -cF 'WORKSPACE.md' CONSTRUCT-CLAUDE-spec/workspace-contract.md` returns ≥ 1 — criterion 2a
    - `grep -cE '^### Durable orchestration state' CONSTRUCT-CLAUDE-spec/workspace-contract.md` returns 1
    - The line containing `.construct/workflow/*.sqlite` in a table row contains `durable orchestration state`
    - The `.construct/workflow/*.sqlite` row does NOT appear inside the Support table:
      `awk '/^### Support artifacts/,/^### /' CONSTRUCT-CLAUDE-spec/workspace-contract.md | grep -c 'sqlite'` returns 0 (Pitfall 2 guard)
    - `grep -cF 'adr-0004' CONSTRUCT-CLAUDE-spec/workspace-contract.md` returns ≥ 1
    - The new section states the directory is not required / may be absent:
      `awk '/^### Durable orchestration state/,0' CONSTRUCT-CLAUDE-spec/workspace-contract.md | grep -ciE 'REQUIRED_PATHS|may be absent|not scaffolded'` returns ≥ 1
    - `git status --porcelain -- src/construct/schemas/ src/construct/storage/ src/construct/services/` returns empty
    - `.venv/bin/python -m pytest tests/unit/test_workspace_contracts.py -q` exits 0
  </acceptance_criteria>

  <verify>
    <automated>F=CONSTRUCT-CLAUDE-spec/workspace-contract.md && [ "$(grep -cF '.construct/workflow/' $F)" -ge 2 ] && grep -qF '.construct/search.yaml' $F && grep -qF 'WORKSPACE.md' $F && [ "$(grep -cE '^### Durable orchestration state' $F)" = "1" ] && grep -F '.construct/workflow/*.sqlite' $F | grep -qF 'durable orchestration state' && [ "$(awk '/^### Support artifacts/{f=1;next}/^### /{f=0}f' $F | grep -c sqlite)" = "0" ] && grep -qF 'adr-0004' $F && [ -z "$(git status --porcelain -- src/construct/schemas/ src/construct/storage/ src/construct/services/)" ] && .venv/bin/python -m pytest tests/unit/test_workspace_contracts.py -q && echo WORKSPACE-CONTRACT-OK</automated>
  </verify>

  <done>
    All three D-12 artifacts appear in `workspace-contract.md`; `.construct/workflow/*.sqlite` is
    classified under a new `### Durable orchestration state` section that cites adr-0004 and records the
    artifact's optional, lazily-created nature; the shape tree shows `.construct/` and `WORKSPACE.md`;
    `inbox/` is untouched and noted; no schema, storage, or service file changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark model-routing.yaml deprecated in the workspace-contract Support table</name>

  <files>CONSTRUCT-CLAUDE-spec/workspace-contract.md</files>

  <read_first>
    - `CONSTRUCT-CLAUDE-spec/workspace-contract.md` — lines 69-80. Read the Support table heading,
      preamble, and the `model-routing.yaml` row (line 78) as it currently stands.
    - `src/construct/schemas/workspace.py` — line 25. **READ-ONLY.** This is why the file stays
      scaffolded: it is a `REQUIRED_PATHS` entry, so removing it is a workspace-format change, which
      v0.4.1 lists Out of Scope. This is the reversal recorded in D-01 — do not re-litigate it.
    - `src/construct/services/init.py` — line 58. **READ-ONLY.** Confirms the scaffolding line stays.
    - `.planning/phases/14-durable-state-config-truth/14-PATTERNS.md` — section
      "CONSTRUCT-CLAUDE-spec/workspace-contract.md" for the Support table's three-column shape.
  </read_first>

  <action>
    One edit to the `model-routing.yaml` row in the Support artifacts table of
    `CONSTRUCT-CLAUDE-spec/workspace-contract.md` (currently line 78). This is one of the four D-02
    fence targets.

    **Keep the row.** Under D-01 the file remains scaffolded — it is a `REQUIRED_PATHS` entry with a
    loader, a validation branch, generated `WORKSPACE.md` prose, a golden fixture entry, and roughly
    twelve test assertions across five test files. Deleting the row would make the contract document
    disagree with the contract the schema enforces.

    Rewrite the `Role` cell so it:
    - Carries an explicit deprecation marker — the word "deprecated" must appear in the row.
    - States that the file is inert and retained only for workspace-contract stability.
    - Names `src/construct/llm/config.yaml` as the actual LLM configuration authority, resolved by
      `src/construct/llm/config.py`.
    - No longer describes the file as providing runtime or provider routing guidance.

    Keep the three-column shape and the `support` value in the `Class` cell. Do not modify the Support
    table's heading, preamble, or its other rows. Do not modify `services/init.py`, `schemas/workspace.py`,
    or any test.
  </action>

  <acceptance_criteria>
    - Every surviving mention of `model-routing` in the file carries a deprecation marker:
      `grep -i 'model-routing' CONSTRUCT-CLAUDE-spec/workspace-contract.md | grep -civ deprecat` returns 0 — ROADMAP criterion 4b
    - `grep -ciF 'model-routing.yaml' CONSTRUCT-CLAUDE-spec/workspace-contract.md` returns ≥ 1 — the row was annotated, not deleted
    - The `model-routing.yaml` row references `llm/config.yaml`
    - D-01 respected — scaffolding and schema intact:
      `grep -qF 'model-routing.yaml' src/construct/services/init.py && grep -qF '.construct/model-routing.yaml' src/construct/schemas/workspace.py` — ROADMAP criterion 4e
    - `git status --porcelain -- src/ tests/` returns empty for this task
    - `.venv/bin/python -m pytest tests/unit -q` exits 0
  </acceptance_criteria>

  <verify>
    <automated>F=CONSTRUCT-CLAUDE-spec/workspace-contract.md && [ "$(grep -i 'model-routing' $F | grep -civ deprecat)" = "0" ] && [ "$(grep -ciF 'model-routing.yaml' $F)" -ge 1 ] && grep -i 'model-routing' $F | grep -qF 'llm/config.yaml' && grep -qF 'model-routing.yaml' src/construct/services/init.py && grep -qF '.construct/model-routing.yaml' src/construct/schemas/workspace.py && .venv/bin/python -m pytest tests/unit -q && echo WC-DEPRECATION-OK</automated>
  </verify>

  <done>
    The Support table still lists `model-routing.yaml`, now marked deprecated and inert, naming
    `src/construct/llm/config.yaml` as the real authority; the scaffolding in `services/init.py` and the
    `REQUIRED_PATHS` entry in `schemas/workspace.py` are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark model-routing.yaml deprecated in both config-topology.md references</name>

  <files>CONSTRUCT-CLAUDE-spec/config-topology.md</files>

  <read_first>
    - `CONSTRUCT-CLAUDE-spec/config-topology.md` — lines 45-70 (the §1 fenced template-directory tree,
      including the sibling lines that establish the comment alignment column) and lines 125-145 (the §3
      "Comparison to Python Approach Workspace" table, including its header and the sibling rows that
      establish the emoji-marker vocabulary).
    - `src/construct/llm/config.py` — lines 49-86. **READ-ONLY.** The authority both edits must name.
    - `.planning/phases/14-durable-state-config-truth/14-PATTERNS.md` — section
      "CONSTRUCT-CLAUDE-spec/config-topology.md" for the exact tree alignment and table marker conventions.
  </read_first>

  <action>
    Two edits to `CONSTRUCT-CLAUDE-spec/config-topology.md`. These are the remaining two D-02 fence
    targets. Both are annotations — the file itself stays scaffolded per D-01.

    **Edit 1 — the template-directory tree entry (currently line 56).** This line sits inside a fenced
    ASCII tree whose trailing comments are column-aligned short noun phrases with no terminal period.
    Replace the trailing comment with a **short in-tree deprecation marker** that fits the existing
    comment column and names the real authority — the marker must contain the word `DEPRECATED` and
    point at `llm/config.yaml`. Do not write a full sentence here; a long comment breaks the column
    alignment of every sibling line. If more explanation is warranted, place it as prose **below** the
    closing fence, not inside the tree.

    Do not alter any sibling tree line, the fence markers, or the alignment column.

    **Edit 2 — the §3 comparison-table row (currently line 135).** Keep the four-column shape and the
    marker-plus-short-parenthetical form used by every sibling row.

    - Leave the **Python-approach column** alone. This table compares against the Python approach, where
      the file genuinely did control routing; that cell is historically accurate and rewriting it would
      make the comparison false.
    - Rewrite the **Claude-approach column** so it carries an explicit deprecation marker rather than
      describing the file as merely informational. Use the table's existing marker vocabulary
      (`✅` / `❌` / `🟡`) with a short parenthetical naming the deprecation.
    - Rewrite the rightmost rationale column to name `llm/config.yaml` as the LLM configuration authority.

    Do not touch the sibling `db/`, `views/`, `inbox/`, or `workflows/` rows. The `db/` row asserts
    "No persistent index" — adjacent to, but outside, this phase's fence, and owned by Phase 17.

    **Record the D-03 collision** (see `<d03_collision_note>` above) in the plan SUMMARY: Phase 17
    (DOC-02) may rewrite or delete this file wholesale, so these two edits are accepted as potentially
    discardable. No dependency on Phase 17 is created.
  </action>

  <acceptance_criteria>
    - Every surviving mention of `model-routing` carries a deprecation marker:
      `grep -i 'model-routing' CONSTRUCT-CLAUDE-spec/config-topology.md | grep -civ deprecat` returns 0 — ROADMAP criterion 4a/4b
    - `grep -ciF 'model-routing.yaml' CONSTRUCT-CLAUDE-spec/config-topology.md` returns 2 — both references annotated, neither deleted
    - Both references name the real authority:
      `grep -i 'model-routing' CONSTRUCT-CLAUDE-spec/config-topology.md | grep -cF 'llm/config.yaml'` returns 2
    - Tree alignment preserved — the edited tree line's comment starts at the same column as its
      siblings: `awk '/├── domains.yaml/{d=index($0,"#")} /model-routing/{m=index($0,"#")} END{exit !(d==m)}' CONSTRUCT-CLAUDE-spec/config-topology.md`
    - The comparison table row keeps four columns:
      `grep -F 'model-routing.yaml' CONSTRUCT-CLAUDE-spec/config-topology.md | grep -c '^|.*|.*|.*|.*|$'` returns 1
    - Sibling rows untouched: `git diff CONSTRUCT-CLAUDE-spec/config-topology.md | grep -cE '^[-+].*(`db/`|`views/`|`inbox/`|`workflows/`)'` returns 0
    - `.venv/bin/python -m pytest -q` exits 0 with ≥ 439 passed
  </acceptance_criteria>

  <verify>
    <automated>F=CONSTRUCT-CLAUDE-spec/config-topology.md && [ "$(grep -i 'model-routing' $F | grep -civ deprecat)" = "0" ] && [ "$(grep -ciF 'model-routing.yaml' $F)" = "2" ] && [ "$(grep -i 'model-routing' $F | grep -cF 'llm/config.yaml')" = "2" ] && awk '/├── domains.yaml/{d=index($0,"#")} /model-routing/{if(index($0,"#")>0)m=index($0,"#")} END{exit !(d==m)}' $F && [ "$(git diff $F | grep -cE '^[-+].*(`db/`|`views/`|`inbox/`|`workflows/`)')" = "0" ] && .venv/bin/python -m pytest -q && echo CONFIG-TOPOLOGY-OK</automated>
  </verify>

  <done>
    Both `model-routing.yaml` references in `config-topology.md` carry a deprecation marker and name
    `llm/config.yaml` as the authority; the tree's comment alignment and the comparison table's
    four-column shape are preserved; the historically-accurate Python-approach cell and every sibling row
    are unchanged; the D-03 collision is recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract document → workspace validator / implementer | `workspace-contract.md` describes what `schemas/workspace.py` enforces. Divergence in either direction produces workspaces that fail their own contract |
| contract document → operator planning backup or migration | The artifact classification tells an operator what may be safely dropped; misclassifying durable state as "support" invites data loss |
| config document → developer configuring LLM behavior | Names which file is authoritative; a wrong answer sends configuration into an inert file |

No untrusted input, no network, no auth, no crypto surface is touched by this plan.

## STRIDE Threat Register (ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03-01 | Tampering | `schemas/workspace.py` `REQUIRED_PATHS` and the scaffolding in `services/init.py` | high | mitigate | A documentation task that "tidies" the enforced contract would produce workspaces failing their own required-paths check. Tasks 1 and 2 assert `git status --porcelain -- src/construct/schemas/ src/construct/storage/ src/construct/services/` is empty and re-run `tests/unit`, so any code-side change hard-fails the task |
| T-14-03-02 | Information Disclosure | operator misreading artifact class | medium | mitigate | Classifying `.construct/workflow/*.sqlite` under Support would tell an operator it holds no workspace truth, inviting deletion of the only copy of pending review decisions. Task 1 asserts no `sqlite` row exists within the Support table section and requires the fourth-class section |
| T-14-03-03 | Tampering | `model-routing.yaml` deprecation completeness | medium | mitigate | A partial deprecation leaves one doc still calling the file authoritative. Tasks 2 and 3 use an exhaustive assertion — every `model-routing` mention in each file must carry a deprecation marker — rather than checking known line numbers |
| T-14-03-04 | Repudiation | historical accuracy of the Python-comparison table | low | accept | Task 3 deliberately preserves the Python-approach column, which is historically correct. Rewriting it would falsify the comparison to make the row read uniformly |
| T-14-03-05 | Information Disclosure | `.construct/search.yaml` row | low | accept | The Support row names the file and its default `mock` provider; it reproduces no `api_key_env` value or secret |

**Supply chain:** not applicable — this plan installs no npm/pip/cargo packages.
</threat_model>

<verification>
Run from the repository root after all three tasks:

1. Criterion 2a — three artifacts listed:
   `for a in '.construct/workflow/' '.construct/search.yaml' 'WORKSPACE.md'; do grep -qF "$a" CONSTRUCT-CLAUDE-spec/workspace-contract.md || exit 1; done`
2. Criterion 4a/4b — exhaustive deprecation, both files:
   `for f in CONSTRUCT-CLAUDE-spec/workspace-contract.md CONSTRUCT-CLAUDE-spec/config-topology.md; do [ "$(grep -i 'model-routing' $f | grep -civ deprecat)" = "0" ] || exit 1; done`
3. Criterion 4e — D-01 respected, scaffolding intact:
   `grep -qF 'model-routing.yaml' src/construct/services/init.py && grep -qF '.construct/model-routing.yaml' src/construct/schemas/workspace.py`
4. Contract unchanged in code:
   `git status --porcelain -- src/ tests/` returns empty
5. Criterion 5 — non-regression: `.venv/bin/python -m pytest -q` → exit 0, ≥ 439 passed
6. Edit fence (D-02) held:
   `git status --porcelain -- AGENTS.md USER-TEST-PLAYBOOK-v03.md CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md CONSTRUCT-CLAUDE-spec/migrations/ .planning/milestones/` returns empty
</verification>

<success_criteria>
- `workspace-contract.md` lists all three D-12 artifacts across its tables and shape tree.
- `.construct/workflow/*.sqlite` is classified under a new `### Durable orchestration state` section
  citing adr-0004 — not under Support and not under Derived.
- Every `model-routing` mention in `workspace-contract.md` and `config-topology.md` carries a
  deprecation marker and names `llm/config.yaml` as the authority.
- `services/init.py` scaffolding and `schemas/workspace.py` `REQUIRED_PATHS` are unchanged (D-01).
- `inbox/` is deliberately left alone and the omission is recorded.
- The D-03 Phase 17 collision is recorded in the SUMMARY.
- Full pytest suite green at ≥ 439.
</success_criteria>

<output>
Create `.planning/phases/14-durable-state-config-truth/14-03-SUMMARY.md` when done.
</output>
