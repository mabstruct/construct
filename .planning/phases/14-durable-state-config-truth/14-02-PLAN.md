---
phase: 14-durable-state-config-truth
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - CONSTRUCT-CLAUDE-spec/nfrs.md
autonomous: true
requirements: [DOC-03, FIX-02]
must_haves:
  truths:
    - "nfrs.md §2's Rebuild-guarantee row no longer denies that any database or required derived state exists; it scopes the guarantee to knowledge state and names .construct/workflow/*.sqlite as the carve-out."
    - "nfrs.md §2's 'No Hidden State' section still asserts markdown-as-truth portability for knowledge (adr-0001's load-bearing claim survives) while naming workflow checkpoints as the one deliberate exception."
    - "nfrs.md §4 names Tavily as an optional, opt-in third-party search API with its data-egress condition, instead of asserting no third-party APIs are used."
    - "nfrs.md §3 names src/construct/llm/config.yaml as the LLM configuration authority and marks model-routing.yaml deprecated."
  artifacts:
    - "CONSTRUCT-CLAUDE-spec/nfrs.md (modified: §2 table row 43, §2 prose block 46-52, §3 line 72, §4 table row 83)"
  key_links:
    - "nfrs.md §2 -> adrs/adr-0004-durable-workflow-checkpoints.md — the scoped invariant cites the ADR rather than restating its argument"
    - "nfrs.md §2 <-> architecture-overview.md §8.2 (plan 14-01) — both must tell one consistent story anchored on adr-0004"
    - "nfrs.md §3 -> src/construct/llm/config.yaml — the named authority, matching llm/config.py's DEFAULT_CONFIG_PATH"
  prohibitions:
    - statement: "MUST NOT over-correct the §4 privacy row into implying CONSTRUCT sends user data to Tavily by default — the shipped .construct/search.yaml sets default_provider: mock, so an unconditional egress claim replaces one false statement with another and misinforms a privacy-sensitive reader."
      category: privacy
    - statement: "MUST NOT resolve the §2 contradiction by deleting the 'No Hidden State' section or its still-true bullets — adr-0001 depends on the markdown-as-truth portability claim, and removing a true, load-bearing guarantee to make a document self-consistent trades a visible error for an invisible hole."
      category: transparency
---

<objective>
Make `nfrs.md` true. Four edits: scope §2's rebuild guarantee to knowledge state and name the workflow
checkpoint carve-out (D-04); rewrite the "No Hidden State" block scoped rather than deleting it (D-05);
name the real LLM config authority and mark `model-routing.yaml` deprecated at §3 (D-02/FIX-02); and
replace §4's no-third-party-APIs claim with an honest, conditional Tavily row (D-08).

Purpose: `nfrs.md` §2 is where the contradicted invariant lives. Until it is scoped, the recorded
architecture denies the durable state the code actually keeps.

Output: one modified spec document, citing adr-0004 as the anchor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-durable-state-config-truth/14-CONTEXT.md
@.planning/phases/14-durable-state-config-truth/14-RESEARCH.md
@.planning/phases/14-durable-state-config-truth/14-PATTERNS.md
@.planning/phases/14-durable-state-config-truth/14-01-SUMMARY.md
</context>

## Artifacts this phase produces

Plan 14-02 creates no new symbols or files — it modifies four regions of an existing document. New
identifiers it *references* (created by plan 14-01, exclude from drift checks):
`adrs/adr-0004-durable-workflow-checkpoints.md`, `ADR-0004`.

<tasks>

<task type="auto">
  <name>Task 1: Scope the §2 rebuild guarantee and rewrite the "No Hidden State" block</name>

  <files>CONSTRUCT-CLAUDE-spec/nfrs.md</files>

  <read_first>
    - `CONSTRUCT-CLAUDE-spec/nfrs.md` — lines 37-56. Read the §2 Reliability table and the prose
      subsection beneath it in full before editing; the current cell text and bullet text are the edit
      targets and must be seen, not assumed.
    - `CONSTRUCT-CLAUDE-spec/adrs/adr-0004-durable-workflow-checkpoints.md` — created by plan 14-01.
      §2's replacement prose must agree with this ADR's Decision section and cite it rather than
      re-argue it.
    - `CONSTRUCT-CLAUDE-spec/adrs/adr-0001-claude-native-approach.md` — the markdown-as-truth claim
      that the rewritten block must preserve.
    - `.planning/phases/14-durable-state-config-truth/14-PATTERNS.md` — section
      "CONSTRUCT-CLAUDE-spec/nfrs.md" for the verbatim §2 table shape (three columns, sentence
      fragments, no terminal periods) and the prose block shape (`###` heading, colon lead-in, flat bullets).
  </read_first>

  <action>
    Two edits inside §2 Reliability of `CONSTRUCT-CLAUDE-spec/nfrs.md`.

    **Edit 1 — the Rebuild-guarantee table row (currently line 43).** Keep the row, its position, and
    the three-column shape. The Requirement cell stays `Rebuild guarantee`. Rewrite the Target and
    Implementation cells so the guarantee is **scoped to knowledge state** rather than absolute.

    Concretely, the row must after this edit:
    - Scope the guarantee by naming knowledge state — `cards/`, `refs/`, `connections.json`,
      `search-seeds.json`, `log/events.jsonl`, `digests/`.
    - Carry a pointer to the carve-out rather than the full argument. Sibling cells in this table are
      short sentence fragments with no terminal periods; match that. The detail belongs in the prose
      subsection below (Edit 2), which is the established spillover location.
    - No longer contain the current Implementation cell's blanket denial of databases, caches, and
      required derived state. That claim is the one contradicted by `.construct/workflow/*.sqlite` and
      must not survive in any form.

    Do not alter the three sibling rows (Workspace integrity, Partial failure, Event log durability) or
    the table header.

    **Edit 2 — the prose subsection beneath the table (currently lines 46-52).** Rewrite it **scoped**;
    do not delete it. Its underlying claim — markdown-as-truth portability — is still true and
    `adr-0001` depends on it. A diff showing net-negative lines here with no replacement prose is the
    failure signal.

    The `###` heading may stay as-is or be scoped in wording; either is acceptable. The block must after
    this edit:
    - Keep the shape: heading, one lead-in sentence ending in a colon, then a flat unordered list.
    - Scope the lead-in sentence to **knowledge state** — it currently asserts the system has no derived
      state that can get out of sync, without qualification, which is the strongest false claim in the block.
    - **Preserve the two bullets that remain true**: the one about no NetworkX graph to recompute, and
      the one about no `views/` directory to refresh. Neither is contradicted by anything; carry them
      through unchanged.
    - Replace the two bullets that are contradicted — the first (asserting there is no such index to
      rebuild) and the last (asserting the files alone fully determine system correctness) — with
      scoped equivalents that are true of knowledge state.
    - Add a bullet or closing sentence naming the one deliberate exception: `.construct/workflow/*.sqlite`
      holds pending human-review decisions that are **not reconstructible from layer 1**. Keep this
      specific — generic phrasing such as "some state is durable" fails DOC-03's purpose of unblocking
      v0.5 design. State the blast radius honestly: losing it costs a completed search+scoring cycle and
      any entered decisions; it never corrupts or loses canonical knowledge. Do not claim the file is
      protected, backed up, or recoverable.
    - Cite the ADR as a plain backticked path — `` `adrs/adr-0004-durable-workflow-checkpoints.md` ``.
      Spec documents cite ADRs as backticked paths, not markdown links.

    Touch nothing outside lines 37-56 in this task.
  </action>

  <acceptance_criteria>
    - `grep -c "No databases, no caches" CONSTRUCT-CLAUDE-spec/nfrs.md` returns 0 (ROADMAP criterion 1a)
    - `grep -c "SQLite index to rebuild" CONSTRUCT-CLAUDE-spec/nfrs.md` returns 0 (criterion 1b)
    - `grep -cF 'Rebuild guarantee' CONSTRUCT-CLAUDE-spec/nfrs.md` returns 1 — the row is scoped, not deleted
    - `grep -cF 'NetworkX' CONSTRUCT-CLAUDE-spec/nfrs.md` returns 1 — the still-true bullet survived
    - `grep -cF 'views/ directory' CONSTRUCT-CLAUDE-spec/nfrs.md` returns 1 — the still-true bullet survived
    - `grep -cF '.construct/workflow/' CONSTRUCT-CLAUDE-spec/nfrs.md` returns ≥ 1 (criterion 1c)
    - `grep -ciE 'not reconstructible|pending human-review' CONSTRUCT-CLAUDE-spec/nfrs.md` returns ≥ 1 (criterion 1c)
    - `grep -cF 'adr-0004' CONSTRUCT-CLAUDE-spec/nfrs.md` returns ≥ 1 (criterion 1e)
    - `sed -n '37,60p' CONSTRUCT-CLAUDE-spec/nfrs.md` still contains a `###` heading followed by a bulleted list — the section was rewritten, not removed (Pitfall 3 guard)
    - `git diff --numstat CONSTRUCT-CLAUDE-spec/nfrs.md` shows added lines > 0 — not a pure deletion
  </acceptance_criteria>

  <verify>
    <automated>F=CONSTRUCT-CLAUDE-spec/nfrs.md && [ "$(grep -c 'No databases, no caches' $F)" = "0" ] && [ "$(grep -c 'SQLite index to rebuild' $F)" = "0" ] && [ "$(grep -cF 'Rebuild guarantee' $F)" = "1" ] && grep -qF 'NetworkX' $F && grep -qF '.construct/workflow/' $F && grep -qiE 'not reconstructible|pending human-review' $F && grep -qF 'adr-0004' $F && [ "$(git diff --numstat $F | cut -f1)" -gt 0 ] && echo NFRS-SEC2-OK</automated>
  </verify>

  <done>
    §2's rebuild guarantee is scoped to knowledge state with the workflow-checkpoint carve-out named and
    the ADR cited; the "No Hidden State" section survives as scoped prose with its two still-true bullets
    intact; neither contradicted claim remains anywhere in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Name the real LLM config authority at §3 and correct the §4 third-party-API claim</name>

  <files>CONSTRUCT-CLAUDE-spec/nfrs.md</files>

  <read_first>
    - `CONSTRUCT-CLAUDE-spec/nfrs.md` — line 72 (the §3 prose paragraph) and lines 76-83 (the §4 Privacy
      table). Read both regions before editing.
    - `src/construct/llm/config.py` — lines 49-86. `DEFAULT_CONFIG_PATH` and `load_llm_config`'s
      resolution order establish which file is actually authoritative; the §3 sentence must agree with it.
    - `CONSTRUCT-CLAUDE-impl/construct/templates/search.yaml` — the shipped template. Confirm
      `default_provider: mock` and that `tavily` is a configured-but-not-default provider keyed on
      `api_key_env: TAVILY_API_KEY`. The §4 correction must be conditional because of this.
    - `.planning/phases/14-durable-state-config-truth/14-PATTERNS.md` — section
      "CONSTRUCT-CLAUDE-spec/nfrs.md" for the §4 table shape (two columns; policy cells are full
      sentences with terminal periods, unlike §2) and the `Telemetry` row as the stylistic model for a
      conditional correction.
  </read_first>

  <action>
    Two edits, in two different sections of `CONSTRUCT-CLAUDE-spec/nfrs.md`.

    **Edit 1 — §3, the prose paragraph at line 72.** This is one of the four D-02 fence targets. The
    paragraph currently bundles `governance.yaml` and `model-routing.yaml` together and describes both
    as merely informational.

    Rewrite **only the `model-routing.yaml` clause**. It must after this edit:
    - Name `src/construct/llm/config.yaml` as the actual LLM configuration authority, and state the
      resolution order performed by `src/construct/llm/config.py`: explicit argument, then the
      `CONSTRUCT_LLM_CONFIG` environment variable, then the packaged default.
    - Carry an explicit deprecation marker for `model-routing.yaml` — the word "deprecated" must appear.
    - State that `model-routing.yaml` is inert and is retained only for workspace-contract stability
      (it remains a `REQUIRED_PATHS` entry, so removing it is a workspace-format change).
    - No longer describe `model-routing.yaml` as informational-but-live or as controlling routing.

    **Leave `governance.yaml` alone.** Do not restructure the sentence in a way that changes what is
    claimed about it. Calling `governance.yaml` informational is itself questionable — it is a
    `REQUIRED_PATHS` source-of-truth artifact per `workspace-contract.md` — but that is a separate,
    untracked defect outside this phase's fence. Record it as an observation in the plan SUMMARY; do not
    fix it here.

    **Edit 2 — §4 Privacy table, the third-party-API row (currently line 83).** Keep the row and its
    `Third-party APIs` label in the Aspect column. Replace the Policy cell.

    The replacement must:
    - Name Tavily explicitly (ROADMAP criterion 2).
    - Be **conditional**, because the shipped `.construct/search.yaml` sets `default_provider: mock`.
      State that Tavily is available and opt-in via `.construct/search.yaml`, and that search queries
      egress to Tavily **when it is enabled**. A cell with no conditional is the failure signal — an
      unconditional egress claim is as false as the claim it replaces, in the opposite direction.
    - Match the §4 cell style: full sentences with terminal periods. The `Telemetry` row is the closest
      stylistic model — a short declarative followed by a qualifying second sentence.

    Do not modify the three sibling §4 rows or the table header. Do not touch §2 in this task.
  </action>

  <acceptance_criteria>
    - `grep -c 'Third-party APIs | None' CONSTRUCT-CLAUDE-spec/nfrs.md` returns 0 (ROADMAP criterion 2b)
    - `grep -ci 'tavily' CONSTRUCT-CLAUDE-spec/nfrs.md` returns ≥ 1 (criterion 2c)
    - The Tavily row contains a conditional marker — `grep -i 'tavily' CONSTRUCT-CLAUDE-spec/nfrs.md | grep -ciE 'opt-in|optional|when enabled|if enabled'` returns ≥ 1 (Pitfall 4 guard)
    - The Tavily row references `.construct/search.yaml`
    - `grep -cF 'Third-party APIs' CONSTRUCT-CLAUDE-spec/nfrs.md` returns 1 — the row label survives
    - `grep -cF 'llm/config.yaml' CONSTRUCT-CLAUDE-spec/nfrs.md` returns ≥ 1 (criterion 4c)
    - `grep -cF 'CONSTRUCT_LLM_CONFIG' CONSTRUCT-CLAUDE-spec/nfrs.md` returns ≥ 1
    - `sed -n '68,76p' CONSTRUCT-CLAUDE-spec/nfrs.md | grep -ci deprecat` returns ≥ 1 (criterion 4b)
    - `grep -cF 'governance.yaml' CONSTRUCT-CLAUDE-spec/nfrs.md` is unchanged from its pre-edit value — the adjacent claim was not opened up
  </acceptance_criteria>

  <verify>
    <automated>F=CONSTRUCT-CLAUDE-spec/nfrs.md && [ "$(grep -c 'Third-party APIs | None' $F)" = "0" ] && grep -qi 'tavily' $F && [ "$(grep -i 'tavily' $F | grep -ciE 'opt-in|optional|when enabled|if enabled')" -ge 1 ] && grep -i 'tavily' $F | grep -qF '.construct/search.yaml' && [ "$(grep -cF 'Third-party APIs' $F)" = "1" ] && grep -qF 'llm/config.yaml' $F && grep -qF 'CONSTRUCT_LLM_CONFIG' $F && [ "$(grep -ci deprecat $F)" -ge 1 ] && echo NFRS-SEC34-OK</automated>
  </verify>

  <done>
    §3 names `src/construct/llm/config.yaml` as the LLM configuration authority with its resolution
    order, marks `model-routing.yaml` deprecated and inert, and leaves the `governance.yaml` claim
    untouched; §4's third-party-API row names Tavily with an explicit opt-in condition and a pointer to
    `.construct/search.yaml`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec document → privacy-sensitive reader | §4 is a privacy policy statement; a reader decides whether to deploy based on it. Both under- and over-claiming egress misinform |
| spec document → developer configuring LLM behavior | §3 tells a developer which file to edit; a wrong answer sends configuration changes into an inert file |

No untrusted input, no network, no auth, no crypto surface is touched by this plan.

## STRIDE Threat Register (ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02-01 | Information Disclosure | `nfrs.md` §4 privacy table | medium | mitigate | The current row understates egress by denying third-party APIs while Tavily is shipped and configurable. Task 2 names Tavily with the opt-in condition; acceptance asserts both the name and a conditional marker are present, so neither the understatement nor an overstatement survives |
| T-14-02-02 | Tampering | `nfrs.md` §3 configuration authority | medium | mitigate | A developer following the current text edits an inert file, leaving intended LLM configuration silently unapplied. Task 2 names `src/construct/llm/config.yaml` and the `CONSTRUCT_LLM_CONFIG` override; acceptance greps both |
| T-14-02-03 | Repudiation | `nfrs.md` §2 reliability guarantees | medium | mitigate | Scoping a guarantee could be used to quietly weaken it. Acceptance asserts the `Rebuild guarantee` row and the still-true `NetworkX` / `views/` bullets survive, and that the diff is not a pure deletion |
| T-14-02-04 | Information Disclosure | `.construct/search.yaml` contents | low | accept | The §4 row references the config file by path and names the provider; it does not reproduce `api_key_env` values or any secret. Reading the path name discloses nothing not already in the shipped template |

**Supply chain:** not applicable — this plan installs no npm/pip/cargo packages.
</threat_model>

<verification>
Run from the repository root after both tasks:

1. Criterion 1a/1b — contradicted claims gone:
   `[ "$(grep -c 'No databases, no caches' CONSTRUCT-CLAUDE-spec/nfrs.md)" = 0 ] && [ "$(grep -c 'SQLite index to rebuild' CONSTRUCT-CLAUDE-spec/nfrs.md)" = 0 ]`
2. Criterion 1c/1e — carve-out named and ADR cited:
   `grep -qF '.construct/workflow/' CONSTRUCT-CLAUDE-spec/nfrs.md && grep -qiE 'not reconstructible|pending human-review' CONSTRUCT-CLAUDE-spec/nfrs.md && grep -qF adr-0004 CONSTRUCT-CLAUDE-spec/nfrs.md`
3. Criterion 2b/2c — Tavily correction:
   `[ "$(grep -c 'Third-party APIs | None' CONSTRUCT-CLAUDE-spec/nfrs.md)" = 0 ] && grep -qi tavily CONSTRUCT-CLAUDE-spec/nfrs.md`
4. Criterion 4b/4c — authority named, deprecation marked:
   `grep -qF 'llm/config.yaml' CONSTRUCT-CLAUDE-spec/nfrs.md && grep -qi deprecat CONSTRUCT-CLAUDE-spec/nfrs.md`
5. Non-regression: `.venv/bin/python -m pytest -q` → exit 0, ≥ 439 passed
6. Edit fence (D-02) held:
   `git status --porcelain -- AGENTS.md USER-TEST-PLAYBOOK-v03.md CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md CONSTRUCT-CLAUDE-spec/migrations/ .planning/milestones/` returns empty
</verification>

<success_criteria>
- Neither contradicted §2 claim survives anywhere in `nfrs.md`.
- §2 names `.construct/workflow/*.sqlite` as intentional durable state holding pending human-review
  decisions not reconstructible from layer 1, and cites adr-0004.
- The "No Hidden State" section still exists, scoped, with its two still-true bullets intact.
- §3 names `src/construct/llm/config.yaml` as the authority (including the `CONSTRUCT_LLM_CONFIG`
  override) and marks `model-routing.yaml` deprecated; `governance.yaml`'s claim is untouched.
- §4 names Tavily with an explicit opt-in condition, not an unconditional egress claim.
- Full pytest suite green at ≥ 439.
</success_criteria>

<output>
Create `.planning/phases/14-durable-state-config-truth/14-02-SUMMARY.md` when done.
</output>
