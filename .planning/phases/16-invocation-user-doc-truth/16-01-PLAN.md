---
phase: 16-invocation-user-doc-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/contract/test_doc_command_references.py
  - tests/contract/test_skill_migration.py
autonomous: true
requirements: [FIX-03, DOC-04, DEC-01]

must_haves:
  truths:
    - "A document named in `_MUST_CARRY_INVOCATIONS` that yields zero extractable `construct ...` invocations fails `test_key_docs_are_not_vacuous`; a document yielding exactly one invocation passes (the threshold is >= 1, not > 1)."
    - "A globbed document NOT named in `_MUST_CARRY_INVOCATIONS` (e.g. `construct/workflows/co-authorship.md`, `construct/workflows/cold-start.md`) may yield zero invocations without failing any test — silence is only a defect for the named set."
    - "`_MUST_CARRY_INVOCATIONS` reads each document directly from `_REPO_ROOT`, independent of `_DOC_GLOBS`, so a document can be asserted non-vacuous before it is globbed."
    - "A path listed in `_MUST_CARRY_INVOCATIONS` that does not exist on disk fails loudly with an is-file assertion rather than passing vacuously."
    - "`_allowed_tools_text` returns the frontmatter tool declaration for BOTH inline style (`allowed-tools: A, B, C`) and list style (`allowed-tools:` followed by indented `- ` lines); for every skill in `_MIGRATED_SKILLS` the returned text carries at least one tool token after the colon."
  artifacts:
    - "tests/contract/test_doc_command_references.py — `_MUST_CARRY_INVOCATIONS` tuple and `test_key_docs_are_not_vacuous`"
    - "tests/contract/test_skill_migration.py — `_allowed_tools_text` and `test_allowed_tools_text_is_not_vacuous`"
  key_links:
    - "`test_key_docs_are_not_vacuous` -> `_invocations()` (the same extractor `test_documented_commands_resolve` uses) — a regex regression fails both together, never one silently"
    - "`_MIGRATED_SKILLS` -> `_allowed_tools_text` -> `test_skill_drops_forbidden_tools` — the parser is the only thing standing between a list-style skill and a vacuous forbidden-tool assertion"
  prohibitions:
    - statement: "The guard must not be made to pass by narrowing what it scans — `_DOC_GLOBS` entries and `_MUST_CARRY_INVOCATIONS` entries may be swapped or added, never dropped to silence a failure."
      status: unverified
      verification: flagged
    - statement: "`_resolves()`'s leaf-vs-group logic must not be loosened to make a documented string resolve; a broken string is fixed in the document, never in the resolver."
      status: unverified
      verification: flagged
    - statement: "The lower-bound assertion `len(VALID_PATHS) > 25` must not be converted into an equality against a specific command count."
      status: unverified
      verification: flagged
    - statement: "`construct-synthesis`'s frontmatter must not be reshaped to fit the single-line parser; the parser is fixed so the data stays as authored."
      status: unverified
      verification: flagged
---

<objective>
Wave 0 RED guards. Close the two demonstrated ways this phase's guards can go silently vacuous, and pin `knowledge card list` into the discoverability assertion — all BEFORE the changes they guard land.

Purpose: RESEARCH Pitfalls 2 and 4 are both false-greens produced *by* the very changes this phase makes. `test_docs_contain_invocations` asserts a repo-wide total (~60), so a newly-globbed document whose CLI column the extractor misses sails through. `_allowed_tools_line()` reads one line, so adding `construct-synthesis` (list-style frontmatter) to `_MIGRATED_SKILLS` makes the forbidden-tool assertions pass whether or not the grants are removed. Both guards must be proven capable of failing before their subjects change.

Output: Three new/extended assertions that are EXPECTED TO BE RED at the end of this plan. They turn green in 16-03 (`card list` lands), 16-04 (synthesis grants removed), and 16-05 (CLI columns land).

Per D-11, D-14, D-16 and the orchestrator's carried-forward decision to fix the parser rather than normalise the data.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md
@.planning/phases/16-invocation-user-doc-truth/16-PATTERNS.md
@.planning/phases/16-invocation-user-doc-truth/16-VALIDATION.md
</context>

## Artifacts this phase produces

Symbols created across Phase 16 (excluded from drift verification — all are new):

| Symbol | Kind | Location |
|---|---|---|
| `_MUST_CARRY_INVOCATIONS` | module constant (tuple[str, ...]) | `tests/contract/test_doc_command_references.py` |
| `test_key_docs_are_not_vacuous` | test function | `tests/contract/test_doc_command_references.py` |
| `_allowed_tools_text` | function (renamed from `_allowed_tools_line`) | `tests/contract/test_skill_migration.py` |
| `test_allowed_tools_text_is_not_vacuous` | test function | `tests/contract/test_skill_migration.py` |
| `list_cards` | service function | `src/construct/services/knowledge.py` |
| `_json_safe` | private helper function | `src/construct/services/knowledge.py` |
| `CardListInput` | Pydantic model | `src/construct/capabilities/catalog.py` |
| `knowledge.card.list` | capability id (registry record) | `src/construct/capabilities/catalog.py` |
| `construct_list_cards` | MCP tool name | `src/construct/capabilities/catalog.py` |
| `card_list` | Typer callback registered as `list` on `card_app` | `src/construct/cli.py` |
| `--domain` / `-d`, `--include-archived`, `--workspace` / `-w`, `--json` / `-j` | CLI flags on `knowledge card list` | `src/construct/cli.py` |
| `TestCardList` | test class | `tests/unit/test_knowledge_operations.py` |
| `tests/contract/test_card_list_cli_mcp.py` | new test file | repo |
| `USER-TEST-PLAYBOOK-v041.md` | new document | repo root |
| CLI-invocation column | new table column | `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md`, `CONSTRUCT-CLAUDE-impl/construct/references/commands.md` |

<tasks>

<task type="auto">
  <name>Task 1: Add the per-doc non-vacuity guard to test_doc_command_references.py</name>
  <read_first>
    - tests/contract/test_doc_command_references.py (full file — `_DOC_GLOBS` at :41, `_invocations` at :108, `_doc_files` at :131, `test_docs_contain_invocations` at :180, the parametrize idiom at :188)
    - .planning/phases/16-invocation-user-doc-truth/16-PATTERNS.md (section "tests/contract/test_doc_command_references.py — three in-place changes")
    - .planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md (Finding 2 and Pitfall 2)
  </read_first>
  <files>tests/contract/test_doc_command_references.py</files>
  <action>
Add a module-level constant `_MUST_CARRY_INVOCATIONS` placed immediately after `_DOC_GLOBS`, holding repo-root-relative path strings for the three documents whose whole purpose is to carry executable invocations:
`"CONSTRUCT-CLAUDE-impl/USER_GUIDE.md"`, `"CONSTRUCT-CLAUDE-impl/construct/references/commands.md"`, `"USER-TEST-PLAYBOOK-v03.md"`.

Resolve each against `_REPO_ROOT` inside the test, NOT via `_doc_files()`. This independence is deliberate and load-bearing: `USER_GUIDE.md` and `commands.md` are not in `_DOC_GLOBS` yet (16-07 adds them), and a guard that could only see globbed documents would be unable to go RED now — which is exactly the proof this task exists to produce.

Add `test_key_docs_are_not_vacuous`, parametrized over `_MUST_CARRY_INVOCATIONS` with `pytest.param(rel, id=rel)`. Each case must:
1. Build `path = _REPO_ROOT / rel` and assert `path.is_file()` with a message naming the missing path — a renamed or deleted document must fail loudly, never disappear into a passing empty set.
2. Call `_invocations(path.read_text(encoding="utf-8"))` and assert the result is non-empty, with a failure message stating that the document is expected to carry at least one executable `construct ...` invocation.

Explicitly KEEP `test_docs_contain_invocations` unchanged. It guards the extractor regex globally; the new test guards per-document coverage. They are complements, and deleting the global one to "replace" it would reopen the regex-regression hole.

Add a short module-docstring paragraph (or a comment block above `_MUST_CARRY_INVOCATIONS`) explaining why a repo-wide total is insufficient: a document added to the glob set whose command strings the extractor misses passes `test_documented_commands_resolve` trivially on an empty set while the global total stays far above its floor.

Expected state at end of this task: two of the three parametrized cases FAIL (`USER_GUIDE.md` and `commands.md` currently yield zero invocations — verified empirically in RESEARCH Finding 1); the `USER-TEST-PLAYBOOK-v03.md` case passes (22 invocations). That asymmetry is the proof the guard can fail.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous" -q; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous" -v` collects exactly 3 parametrized cases with ids `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md`, `CONSTRUCT-CLAUDE-impl/construct/references/commands.md`, `USER-TEST-PLAYBOOK-v03.md`
    - That run reports 1 passed and 2 failed (the playbook case passes; the two NL-first docs fail)
    - `.venv/bin/python -c "from tests.contract.test_doc_command_references import _MUST_CARRY_INVOCATIONS as m; assert len(m) == 3, m"` exits 0
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_docs_contain_invocations" -q` exits 0 (the global guard still exists and still passes)
    - `.venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q --co` collects the pre-existing `test_documented_commands_resolve` and `test_known_broken_entries_are_still_broken` parametrizations unchanged
  </acceptance_criteria>
  <done>A named set of documents is asserted individually non-vacuous, the assertion currently fails for the two documents 16-05 will populate, and the global extractor guard is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Pin knowledge card list into the discoverability assertion</name>
  <read_first>
    - tests/contract/test_doc_command_references.py (`test_command_surface_is_discoverable` at :170-177, `_command_paths` at :64, `_resolves` at :123)
    - .planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md (D-12 — no hard command counts)
  </read_first>
  <files>tests/contract/test_doc_command_references.py</files>
  <action>
In `test_command_surface_is_discoverable`, add `assert ("knowledge", "card", "list") in VALID_PATHS` alongside the existing `("knowledge", "card", "create")` assertion.

Leave `assert len(VALID_PATHS) > 25` exactly as it is. It is a lower bound guarding against introspection collapse, and per D-12 it must not become an equality against a specific surface size — an equality would rot on the next command exactly as the "25-command surface" prose did.

Do NOT touch `_resolves()`. Its leaf-vs-group distinction is what correctly refuses `workflow run`; loosening it to make a documented string resolve would destroy an entire defect class (RESEARCH Pitfall 6).

Expected state at end of this task: `test_command_surface_is_discoverable` FAILS. It turns green in 16-03 when the command is registered.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_command_surface_is_discoverable" -q; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_command_surface_is_discoverable" -q` exits 1 with an assertion failure naming the `knowledge card list` tuple
    - `.venv/bin/python -c "import inspect; from tests.contract import test_doc_command_references as m; s=inspect.getsource(m._resolves); assert 'any(path[:i] in LEAF_COMMANDS' in s, s"` exits 0 (resolver logic unchanged)
    - `.venv/bin/python -c "import inspect; from tests.contract import test_doc_command_references as m; s=inspect.getsource(m.test_command_surface_is_discoverable); assert 'len(VALID_PATHS) > 25' in s"` exits 0 (lower bound preserved, not converted to equality)
  </acceptance_criteria>
  <done>The discoverability meta-guard names `knowledge card list` and fails until the command exists; the resolver and the lower-bound count assertion are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the multi-line frontmatter parser and bring construct-synthesis into scope</name>
  <read_first>
    - tests/contract/test_skill_migration.py (full file — `_MIGRATED_SKILLS` at :27, `_FORBIDDEN_TOOLS` at :34, `_allowed_tools_line` at :37-44, both consumers at :47-60)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md (frontmatter lines 1-9 — list style, one tool per line)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-gap-analysis/SKILL.md (frontmatter line 3 — inline style, for parser-compatibility comparison)
    - .planning/phases/16-invocation-user-doc-truth/16-PATTERNS.md (section "tests/contract/test_skill_migration.py — multi-line parser")
  </read_first>
  <files>tests/contract/test_skill_migration.py</files>
  <action>
Rename `_allowed_tools_line` to `_allowed_tools_text`, keeping the `(skill: str) -> str` signature so both existing consumers (`test_skill_drops_forbidden_tools`, `test_skill_still_delegates_to_cli`) keep working unchanged via substring containment over a longer string. Update both call sites.

New behaviour: after locating the line whose stripped form starts with `allowed-tools:`, continue consuming subsequent lines whose stripped form starts with `- ` (a YAML list item) and join them onto the returned string with newlines. Stop at the first line that is neither. This handles both frontmatter dialects present in the tree:
- inline (`construct-gap-analysis`, `construct-research-cycle`): the next line is the closing `---`, which is not a list item, so the returned text is the single line — byte-identical to today's behaviour.
- list style (`construct-synthesis`): the five `- Read` / `- Bash(construct)` / `- MCP(connect)` / `- WebSearch` / `- WebFetch` lines are captured.

Do not add a YAML dependency; a line-oriented scan is sufficient and keeps the guard dependency-free.

Add `"construct-synthesis"` to `_MIGRATED_SKILLS`, after `"construct-card-evaluate"`.

Add a meta-guard `test_allowed_tools_text_is_not_vacuous`, parametrized over `_MIGRATED_SKILLS`: split the returned text on the first `:` and assert the remainder, stripped, is non-empty. This is the local convention from `test_doc_command_references.py` (every guard carries a meta-guard asserting it is still looking) and it is the assertion that would have caught the exact vacuity trap this task closes: a parser returning a bare `allowed-tools:` makes every `tool not in text` assertion pass unconditionally.

Update the module docstring: it currently describes a three-skill Phase 12 scope and a RED state for `construct-research-cycle` that no longer exists. Replace with the current contract — four skills in scope, `construct-synthesis` added by Phase 16 DEC-01, RED until its `WebSearch` / `WebFetch` grants are removed in 16-04.

Expected state at end of this task: `test_skill_drops_forbidden_tools[construct-synthesis]` FAILS (the grants are still present); `test_skill_still_delegates_to_cli[construct-synthesis]` PASSES (the fixed parser now sees `- Bash(construct)`); `test_allowed_tools_text_is_not_vacuous` passes for all four skills. That failing-plus-passing pair on the same skill is the signature RESEARCH names as the tell that the parser is genuinely reading the grants.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest tests/contract/test_skill_migration.py -q; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/contract/test_skill_migration.py::test_skill_drops_forbidden_tools" -v` reports 3 passed and 1 failed, the failure being the `construct-synthesis` parameter
    - `.venv/bin/python -m pytest "tests/contract/test_skill_migration.py::test_skill_still_delegates_to_cli" -q` exits 0 (all four skills, including synthesis, pass)
    - `.venv/bin/python -m pytest "tests/contract/test_skill_migration.py::test_allowed_tools_text_is_not_vacuous" -q` exits 0 with 4 parametrized cases
    - `.venv/bin/python -c "from tests.contract.test_skill_migration import _allowed_tools_text as f; t=f('construct-synthesis'); assert 'WebSearch' in t and 'Bash(construct)' in t, t"` exits 0 (parser genuinely reads list-style grants)
    - `.venv/bin/python -c "from tests.contract.test_skill_migration import _allowed_tools_text as f; t=f('construct-gap-analysis'); assert t.count(chr(10)) == 0, t"` exits 0 (inline style still returns a single line — no behaviour change for the pre-existing dialect)
    - `.venv/bin/python -c "from tests.contract.test_skill_migration import _MIGRATED_SKILLS as s; assert 'construct-synthesis' in s and len(s) == 4, s"` exits 0
    - `CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md` is unmodified by this task (`git diff --name-only` does not list it)
  </acceptance_criteria>
  <done>The frontmatter parser reads both dialects, `construct-synthesis` is in scope, a meta-guard proves the parser is not returning an empty tool declaration, and the forbidden-tool assertion is RED for synthesis with the grants still in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> executing agent/user | An agent reads a `construct ...` string from a doc and executes it verbatim; a false-green guard is the only thing between a wrong string and a shell |
| skill frontmatter -> Claude tool permission layer | `allowed-tools` grants are the enforcement point for what a skill may do; a vacuous guard silently re-permits removed capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | `_DOC_GLOBS` / `_KNOWN_BROKEN` in `test_doc_command_references.py` | high | mitigate | Task 1's `_MUST_CARRY_INVOCATIONS` is glob-independent: shrinking the scan surface cannot silence it, closing the D-16 "empty, not merely unscanned" hole one level below the glob set |
| T-16-05 | Elevation of Privilege | `construct-synthesis` `allowed-tools` (`WebSearch`, `WebFetch`) | medium | mitigate | Task 3's multi-line parser plus `test_allowed_tools_text_is_not_vacuous` make the forbidden-tool assertion capable of failing before 16-04 removes the grants; a vacuous guard would leave web-egress grants re-addable undetected |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs zero external packages; `pyproject.toml` is untouched and RESEARCH's Package Legitimacy Audit records "not applicable — no package names appear in any recommendation" |
</threat_model>

<verification>
This plan ends deliberately RED. Confirm the failure set is exactly the three expected assertions and nothing else:

```
.venv/bin/python -m pytest tests/contract/test_doc_command_references.py tests/contract/test_skill_migration.py -q
```

Expected failures (3): `test_key_docs_are_not_vacuous[CONSTRUCT-CLAUDE-impl/USER_GUIDE.md]`, `test_key_docs_are_not_vacuous[CONSTRUCT-CLAUDE-impl/construct/references/commands.md]`, `test_command_surface_is_discoverable`, plus `test_skill_drops_forbidden_tools[construct-synthesis]` (4 total across both files).

No other test in the repository may change status:

```
.venv/bin/python -m pytest -q
```

Baseline before this plan is **489 passed**. After this plan: 489 + 5 new passing cases (1 non-vacuity case, 4 meta-guard cases) minus the 4 expected failures. Any failure outside the expected set is a regression and must be fixed before the plan is considered complete.
</verification>

<success_criteria>
- `_MUST_CARRY_INVOCATIONS` exists with 3 entries and is resolved independently of `_DOC_GLOBS`
- `test_key_docs_are_not_vacuous` fails for `USER_GUIDE.md` and `commands.md`, passes for the playbook
- `test_command_surface_is_discoverable` fails on the `knowledge card list` tuple
- `_allowed_tools_text` reads both frontmatter dialects; `construct-synthesis` is in `_MIGRATED_SKILLS`
- `test_skill_drops_forbidden_tools[construct-synthesis]` fails while `test_skill_still_delegates_to_cli[construct-synthesis]` passes
- `_resolves()`, `test_docs_contain_invocations`, and the `> 25` lower bound are unmodified
- No skill or documentation file is modified by this plan
</success_criteria>

<output>
Create `.planning/phases/16-invocation-user-doc-truth/16-01-SUMMARY.md` when done. Record the exact list of failing test ids so 16-03, 16-04, and 16-05 can each confirm they turned the right one green.
</output>
