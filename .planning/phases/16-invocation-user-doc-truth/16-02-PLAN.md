---
phase: 16-invocation-user-doc-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/llm/curation_run.py
  - tests/llm/test_curation_run.py
  - tests/llm/test_views_refresh.py
  - README.md
  - AGENTS.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOC-04]

must_haves:
  truths:
    - "`curation.run`'s decay_scan summary, when `decay.auto_archive_on_decay` is true, describes the runtime's actual behaviour (reports candidates, archives nothing) and names no future phase as the owner of work already shipped."
    - "`README.md`'s product-lineage block shows v0.4 as shipped and v0.4.1 as in flight — no line asserts a milestone is 'next' that has already landed."
    - "`AGENTS.md:284`'s Layer 3 description enumerates the CLI by capability group and cites `tests/contract/test_doc_command_references.py` as the live authority, asserting no numeric command count (D-12)."
    - "`AGENTS.md`'s two `model-routing.yaml` references describe it as deprecated and inert, and name `src/construct/llm/config.yaml` as the LLM configuration authority, matching Phase 14's recorded decision."
    - "`.planning/REQUIREMENTS.md`'s milestone-framing test baseline reads 489, the figure Phase 15 left the suite at, so an executor comparing against it can distinguish a real regression from an expected count."
    - "ROADMAP Phase 16 success criterion 5 describes the command surface without asserting a number."
  artifacts:
    - "src/construct/llm/curation_run.py — corrected decay_scan auto-archive summary"
    - "README.md — corrected product-lineage block and repo-tree annotations"
    - "AGENTS.md — corrected Layer 3 CLI description and model-routing references"
    - ".planning/REQUIREMENTS.md — corrected test baseline"
    - ".planning/ROADMAP.md — corrected Phase 16 criterion 5"
  key_links:
    - "`curation_run.py` decay summary -> `tests/llm/test_curation_run.py::test_auto_archive_reported_not_acted` — the test positively asserts the summary substring, so the string and its test must change together or the suite breaks"
    - "`tests/llm/test_views_refresh.py::test_deferred_step_placeholder_is_gone` -> its region-scoping comment, which explains an exclusion that this plan makes obsolete"
  prohibitions:
    - statement: "No document edited by this plan may assert a numeric count of commands, capabilities, or MCP tools; the surface is described by capability group and the guard test is cited as the authority."
      status: unverified
      verification: flagged
    - statement: "`.planning/milestones/v0.4-MILESTONE-AUDIT.md` is a sealed historical record and must not be edited to reflect corrected numbers."
      status: unverified
      verification: flagged
    - statement: "A completed phase's recorded success criterion must not be rewritten to match today's numbers; historical criteria that were accurate when met stay as written."
      status: unverified
      verification: flagged
    - statement: "The `decay.auto_archive_on_decay` flag must remain report-only — correcting the summary text must not start archiving cards."
      status: unverified
      verification: flagged
---

<objective>
Correct four stale facts that live outside the invocation guard's reach: a runtime summary string that names a shipped phase as future work, `README.md`'s product lineage, `AGENTS.md`'s two-command CLI description and its `model-routing.yaml` references, and the planning documents' stale test baseline and command count.

Purpose: This milestone exists to clear the audit-trail-that-lies defect class. `curation_run.py`'s decay summary tells an operator that archiving is "deferred to Phase 12" — Phase 12 shipped. `AGENTS.md:284` describes a two-command CLI. `REQUIREMENTS.md` asserts a 439-test baseline that Phase 15 moved to 489, which is operationally dangerous: an executor trusting 439 could read a real regression as expected. None of these are reachable by `test_doc_command_references.py`, so they need an explicit pass.

Output: Five corrected files plus two updated tests. Nothing in this plan touches the guard tests or the command surface, so it runs fully parallel with 16-01.

Per D-12, D-13, the Phase 14 D-02 edit fence (which explicitly handed `AGENTS.md:91,134` to this phase), and the orchestrator's resolved decisions 1 and 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md
@.planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md
@.planning/phases/14-durable-state-config-truth/14-CONTEXT.md
</context>

## Artifacts this phase produces

Symbols created across Phase 16 (excluded from drift verification — all are new):
`_MUST_CARRY_INVOCATIONS`, `test_key_docs_are_not_vacuous` (`tests/contract/test_doc_command_references.py`); `_allowed_tools_text`, `test_allowed_tools_text_is_not_vacuous` (`tests/contract/test_skill_migration.py`); `list_cards`, `_json_safe` (`src/construct/services/knowledge.py`); `CardListInput`, capability id `knowledge.card.list`, MCP tool name `construct_list_cards` (`src/construct/capabilities/catalog.py`); `card_list` Typer callback and its `--domain`/`-d`, `--include-archived`, `--workspace`/`-w`, `--json`/`-j` flags (`src/construct/cli.py`); `TestCardList` (`tests/unit/test_knowledge_operations.py`); new file `tests/contract/test_card_list_cli_mcp.py`; new file `USER-TEST-PLAYBOOK-v041.md`; a CLI-invocation table column in `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` and `CONSTRUCT-CLAUDE-impl/construct/references/commands.md`.

This plan creates no new symbols — it is prose and one string literal.

<tasks>

<task type="auto">
  <name>Task 1: Correct the decay_scan auto-archive summary and the two tests that pin it</name>
  <read_first>
    - src/construct/llm/curation_run.py (the `decay_scan` node, lines ~400-430 — the `summary` construction and the `CurationStepResult` it feeds)
    - tests/llm/test_curation_run.py (`test_auto_archive_reported_not_acted`, ~:268-288 — its docstring AND its positive substring assertion)
    - tests/llm/test_views_refresh.py (`test_deferred_step_placeholder_is_gone`, ~:245-258 — the region-scoping comment that names the decay summary as an out-of-scope exclusion)
    - .planning/STATE.md (Blockers/Concerns — the entry routing this decision to Phase 16)
  </read_first>
  <files>src/construct/llm/curation_run.py, tests/llm/test_curation_run.py, tests/llm/test_views_refresh.py</files>
  <action>
<!-- planner-discipline-allow: deferred to Phase 12 -->
In `decay_scan`, the `if auto:` branch appends a clause to `summary` that names a future phase as the owner of archiving. Phase 12 shipped; the clause is now false. Replace it with a clause that states the behaviour truthfully and durably — that `auto_archive_on_decay` is set, that `curation.run` reports decay candidates and does not archive them, and that archival remains an explicit operator action. Do not name any phase number in the replacement: a phase reference is what rotted the first time, and Phase 11's exit-code contract shows this project's convention is to describe behaviour, not delivery schedule.

Behaviour must not change. `decay["findings"]["auto_archive_on_decay"]` stays `True`, and no card lifecycle may flip to archived. This is a string edit inside an existing branch, not a control-flow change.

Then update the two tests that reference the old string:

`tests/llm/test_curation_run.py::test_auto_archive_reported_not_acted` positively asserts the old substring is present in `decay["summary"]`. Retarget that assertion at a distinctive substring of the new clause, and rewrite the docstring's second sentence, which currently states archiving is deferred to a future phase. Keep every other assertion in that test unchanged — the lifecycle-not-flipped loop over `fresh-card` / `stale-orphan-card` / `stale-connected-card` is the actual D-06 contract and is not in scope here.

`tests/llm/test_views_refresh.py::test_deferred_step_placeholder_is_gone` region-scopes its assertion to the `views_refresh_hook` node, with a comment explaining the exclusion exists because a different node carries an unrelated instance of the same string. That exclusion is now obsolete. Keep the region scoping (it is correct discipline and cheap), but update the comment so it no longer describes a live exclusion that has been removed — a comment that documents a condition that no longer holds is the same defect class this task closes.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest tests/llm/test_curation_run.py tests/llm/test_views_refresh.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/llm/test_curation_run.py tests/llm/test_views_refresh.py -q` exits 0
    - `grep -c 'Phase 12' src/construct/llm/curation_run.py` outputs `0`
    - `.venv/bin/python -m pytest "tests/llm/test_curation_run.py::test_auto_archive_reported_not_acted" -q` exits 0 and the test still contains a positive substring assertion against `decay["summary"]` (verified by `grep -c 'decay\["summary"\]' tests/llm/test_curation_run.py` outputting a value of at least 1)
    - `.venv/bin/python -c "import inspect; from construct.llm import curation_run as c; s=inspect.getsource(c); assert 'auto_archive_on_decay' in s"` exits 0 (the flag is still read and reported)
    - `git diff --stat src/construct/llm/curation_run.py` shows changes confined to the summary string (no added or removed `def`, `if`, `for`, or `return` line)
  </acceptance_criteria>
  <done>The decay summary describes what the runtime does rather than what a shipped phase was once expected to do; both tests that referenced the old wording are updated; archival behaviour is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Correct README lineage and AGENTS.md's CLI + model-routing descriptions</name>
  <read_first>
    - README.md (lines 15-52 — the "Where to look" table rows for the runtime layer and GSD state, and the product-lineage code block; lines 103-118 — the repo-tree annotations)
    - AGENTS.md (line ~284 — the Layer 3 "Contains:" bullet; lines ~88-94 — the YAML languages bullet; lines ~130-138 — the Configuration section's model-routing entry)
    - .planning/phases/14-durable-state-config-truth/14-CONTEXT.md (D-02's edit fence, which hands `AGENTS.md:91,134` to this phase)
    - .planning/STATE.md (Phase 14 decisions naming `src/construct/llm/config.yaml` as the LLM config authority and `model-routing.yaml` as deprecated-but-retained for REQUIRED_PATHS stability)
  </read_first>
  <files>README.md, AGENTS.md</files>
  <action>
**README.md — product lineage (the block at ~:33-50).** The chain currently terminates at `v0.4 next            Agent workflows — LangGraph/LangChain; model-agnostic search`. v0.4 shipped 2026-07-07 and v0.4.1 is in flight. Rewrite the tail of the chain so v0.4 reads as shipped (pointing at `.planning/milestones/v0.4-*` the way the v0.3 row points at `.planning/milestones/v0.3-*`), add a v0.4.1 row describing it as the surface-integration and documentation-truth patch milestone pointing at `.planning/REQUIREMENTS.md`, and keep the existing `v0.5 planned` row as the terminus. Preserve the block's fenced `text` format and its arrow layout.

**README.md — the two "Where to look" rows at ~:17-18.** The runtime/API row describes `src/construct/` as the "active v0.3 pipeline, CLI, MCP, LLM gate, and contract tests"; the runtime has since gained the capability registry, the LangGraph research/curation/daily workflows, and the search spine. Drop the version qualifier and describe the layer by what it contains. The GSD-state row reads "v0.3 shipped; v0.4 agent workflows next; v0.5 UI-primary" — update to v0.3 and v0.4 shipped, v0.4.1 in flight, v0.5 planned.

**README.md — the repo-tree annotations at ~:105-115.** The `.planning/` line and the `src/construct/` line both carry the same stale "v0.3 ... v0.4 next" framing. Bring them into agreement with the rows above. Do not restructure the tree.

**AGENTS.md:284 — the Layer 3 `Contains:` bullet.** It currently reads as a two-command CLI plus planned MCP. Replace with a description of the real surface: name the CLI's capability groups (`ask`, `bridge`, `card`, `curation`, `daily`, `ingest`, `knowledge` with its `card` and `connection` sub-groups, `research`, `spike`, `tag`, `views`, `workflow`) alongside the top-level leaves (`help`, `init`, `mcp`, `status`, `validate`), state that MCP tool definitions are auto-discovered from the capability registry rather than hand-written, and keep HTTP routes marked planned. Per D-12 write NO number: instead cite `tests/contract/test_doc_command_references.py` as the mechanical authority on the exact surface, since it is literally the only introspection path that exists.

**AGENTS.md:~91 — the YAML languages bullet.** It lists `.construct/model-routing.yaml` among the canonical config files. Phase 14 settled that `src/construct/llm/config.yaml` is the LLM configuration authority and that `model-routing.yaml` is deprecated and inert, retained only so `REQUIRED_PATHS` workspace-contract checks stay stable. Rewrite the bullet to list `.construct/governance.yaml` and `domains.yaml` as canonical, name `src/construct/llm/config.yaml` as the LLM config authority, and mark `.construct/model-routing.yaml` as deprecated and inert.

**AGENTS.md:~134 — the Configuration section's model-routing entry.** It presents `.construct/model-routing.yaml` as the live LLM provider and task-tier routing file. Mark it deprecated and inert with a pointer to `src/construct/llm/config.yaml`, keeping the template path reference so the REQUIRED_PATHS rationale stays legible.

Scope fence (D-13): these are the only `AGENTS.md` edits. A full accuracy pass overlaps Phase 17's DOC-01 architecture doc set and is explicitly out of scope. Do not edit `.planning/milestones/v0.4-MILESTONE-AUDIT.md` — it is a sealed historical record per Phase 14 D-09.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest -q && grep -c 'v0.4.1' README.md && grep -c 'config.yaml' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v0.4 next' README.md` outputs `0`
    - `grep -c 'v0.4.1' README.md` outputs a value of at least 1
    - `grep -c 'milestones/v0.4' README.md` outputs a value of at least 1
    - `grep -c 'more in v0.3' AGENTS.md` outputs `0`
    - `grep -c 'test_doc_command_references' AGENTS.md` outputs a value of at least 1
    - `grep -c 'src/construct/llm/config.yaml' AGENTS.md` outputs a value of at least 2
    - `grep -ci 'deprecated' AGENTS.md` outputs a value of at least 2
    - `grep -cE '\b(25|33|34|21|22|27)[ -](commands?|CLI|capabilit|MCP)' README.md AGENTS.md` outputs `0` for both files (D-12: no hard counts)
    - `git diff --stat .planning/milestones/` produces no output (sealed record untouched)
    - `.venv/bin/python -m pytest -q` exits 0 with no change in the failing-test set relative to the plan's starting state
  </acceptance_criteria>
  <done>README's lineage and repo-tree annotations describe the shipped state; AGENTS.md's Layer 3 bullet describes the real CLI by capability group with no count; both model-routing references are marked deprecated and point at the real LLM config authority.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the stale test baseline and the 25-command surface claim in the planning documents</name>
  <read_first>
    - .planning/REQUIREMENTS.md (line ~11 — the "Nature of this milestone" paragraph asserting 439 tests green; line ~30 DOC-04; lines ~84-92 — the mechanical completion criteria)
    - .planning/ROADMAP.md (the Phase 16 section — Goal, Depends on, Requirements, and the five success criteria; criterion 5 asserts a "live 25-command surface")
    - .planning/STATE.md (verify whether any 439 assertion survives here)
    - .planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md (State of the Art table — 489 verified, 33 leaves / 47 paths verified)
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
**`.planning/REQUIREMENTS.md` — the milestone-framing paragraph.** It states the v0.4 runtime is sound with "439 tests green". Phase 15 moved the suite to 489 (verified by execution, recorded in `16-RESEARCH.md`). Correct the figure to 489. This one number stays as a number deliberately: unlike a command count it is a *regression baseline*, and an executor comparing a run against 439 could read a genuine regression as expected. Where the number appears, qualify it as the baseline at the start of v0.4.1 so a reader knows it is a point-in-time anchor rather than an invariant.

**`.planning/ROADMAP.md` — Phase 16 success criterion 5.** It requires `README.md` lineage and `AGENTS.md:284`'s CLI description to "match the live 25-command surface". The number is wrong (33 leaves today, 34 once `knowledge card list` lands) and, per D-12, replacing it with 34 would rot on the next command. Rewrite the criterion to require that both documents describe the live surface by capability group, asserting no count, and name `tests/contract/test_doc_command_references.py` as the mechanical authority. Leave the rest of criterion 5 — the release-validation artifact clause — intact.

**Do NOT edit `.planning/ROADMAP.md`'s earlier phase criteria.** One of them carries a "≥439 tests" clause. That belongs to a phase that is already complete and the criterion was accurate when it was met; rewriting it would falsify a sealed record, which is the same defect class as editing the milestone audit. Verify which phase it belongs to before deciding, and if it is a completed phase, leave it and record the reason in the plan summary.

**Verify `.planning/STATE.md`.** The orchestrator's brief flagged STATE.md as carrying a 439 baseline. Grep it. If no such assertion is present, record that in the summary as verified-clean and make no edit — do not manufacture a change to satisfy an instruction whose premise no longer holds.

Every edit in this task is a scoped replacement. Use `Edit`, never `Write`: a whole-file rewrite of `ROADMAP.md` would destroy phase entries outside the diff window.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && grep -c '25-command' .planning/ROADMAP.md .planning/REQUIREMENTS.md; grep -c '439 tests green' .planning/REQUIREMENTS.md; .venv/bin/python -m pytest -q --co -q | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '25-command' .planning/ROADMAP.md` outputs `0`
    - `grep -c '25-command' .planning/REQUIREMENTS.md` outputs `0`
    - `grep -c '439 tests green' .planning/REQUIREMENTS.md` outputs `0`
    - `grep -c '489' .planning/REQUIREMENTS.md` outputs a value of at least 1
    - `grep -c 'test_doc_command_references' .planning/ROADMAP.md` outputs a value of at least 1
    - `grep -c '### Phase' .planning/ROADMAP.md` returns the same value before and after the edit (no phase entry destroyed by a whole-file write)
    - The Phase 16 section of `.planning/ROADMAP.md` still lists exactly five numbered success criteria
    - `.planning/REQUIREMENTS.md` still lists FIX-03, DOC-04, and DEC-01 as open requirements with their evidence citations intact
  </acceptance_criteria>
  <done>The live regression baseline reads 489 and is labelled as a point-in-time anchor; ROADMAP criterion 5 describes the surface by capability group with the guard test cited as authority; no completed phase's recorded criterion and no sealed audit record was rewritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime summary -> operator reading a curation run | An operator reads `decay_scan`'s summary to decide whether cards were archived; a false summary is a lying audit trail |
| planning documents -> executing agent | An agent trusting a stale test baseline misclassifies a real regression as expected, and ships it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Repudiation | `decay_scan` summary in `src/construct/llm/curation_run.py` | medium | mitigate | Task 1 replaces the phase-referencing clause with a durable behavioural statement; the paired test retains a positive substring assertion so the string cannot silently drift again |
| T-16-07 | Repudiation | `.planning/REQUIREMENTS.md` test baseline | medium | mitigate | Task 3 corrects 439 to the verified 489 and labels it a point-in-time anchor, so a regression cannot hide inside an out-of-date expectation |
| T-16-08 | Tampering | `.planning/milestones/v0.4-MILESTONE-AUDIT.md`, completed-phase ROADMAP criteria | low | mitigate | Explicit do-not-edit fence in Task 3 plus a `git diff --stat .planning/milestones/` acceptance check; sealed records are corrected by superseding documents, never in place |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs zero external packages; `pyproject.toml` is untouched (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
```
.venv/bin/python -m pytest -q
```

Must exit 0 for every test outside 16-01's expected-RED set. This plan changes one runtime string and two tests that pin it; nothing else in the suite should move.

```
grep -rn '25-command\|439 tests green' .planning/ROADMAP.md .planning/REQUIREMENTS.md
grep -rn 'more in v0.3\|v0.4 next' README.md AGENTS.md
```

Both must return no matches.
</verification>

<success_criteria>
- The decay summary names no phase number and describes report-only behaviour; `test_auto_archive_reported_not_acted` passes against the new wording
- `test_deferred_step_placeholder_is_gone` still passes and its comment no longer describes a removed exclusion
- README lineage shows v0.4 shipped and v0.4.1 in flight; the runtime and GSD-state rows and the repo-tree annotations agree with it
- `AGENTS.md:284` describes the CLI by capability group, cites the guard test, and asserts no count
- Both `AGENTS.md` model-routing references are marked deprecated and point at `src/construct/llm/config.yaml`
- `.planning/REQUIREMENTS.md` records a 489-test baseline labelled as a point-in-time anchor
- ROADMAP Phase 16 criterion 5 carries no command count
- No sealed milestone record and no completed phase's success criterion was rewritten
</success_criteria>

<output>
Create `.planning/phases/16-invocation-user-doc-truth/16-02-SUMMARY.md` when done. Record (a) the exact replacement wording of the decay summary, (b) whether `.planning/STATE.md` carried a 439 assertion, and (c) which ROADMAP phase owns the "≥439 tests" criterion and why it was left in place.
</output>
