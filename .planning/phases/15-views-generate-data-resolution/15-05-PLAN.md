---
phase: 15-views-generate-data-resolution
plan: 05
type: execute
wave: 3
depends_on: ["15-03"]
files_modified:
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/run.sh
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/SKILL.md
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/generate.py
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/requirements.txt
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/debounced_hook.py
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/debounced-hook.sh
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md
  - CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md
  - CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md
  - .planning/PROJECT.md
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "The `construct-views-generate-data` skill holds no Python implementation — its entry point invokes the CLI."
    - "No views-refresh instruction remains in `construct-curation-cycle/SKILL.md`, `construct-research-cycle/SKILL.md`, or `workflows/daily-cycle.md`."
    - "No remediation string in any workflow doc names a command that does not resolve against the live CLI."
    - "A reader of the live document set can find the recorded reversal of Phase 13's D-10 without consulting the archived v0.4 milestone."
    - "Both ROADMAP criterion 3 ambiguities — the install-root contract and the deployed-skill-directory import coupling — have an explicit recorded decision in a durable, live document."
    - "The known-broken allowlist still holds exactly the four Phase 16 entries after the doc removals."
  artifacts:
    - CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/run.sh
    - .planning/phases/15-views-generate-data-resolution/COVERAGE.md
  key_links:
    - "skill `run.sh` → `construct views generate --install-root \"$1\"` (the CLI command Plan 03 created)"
    - "`adr-0005` → supersedes Phase 13 D-10, cited from `PROJECT.md` Key Decisions so Phase 17 inherits it"
    - "removal of the three refresh sections → resolves the `run 'construct views generate' manually` remediation string that ROADMAP criterion 4 names"
  prohibitions:
    - statement: "The architecture doc set — `architecture-overview.md`, `artifact-catalog.md`, `config-topology.md` — must not be edited in this phase. Phase 17 owns it under DOC-02/DOC-01, and editing it here would produce two uncoordinated sources of truth for capability and skill counts that this phase is actively changing."
    - statement: "The archived v0.4 milestone documents must not be edited to record the supersession. Archived milestone material is read-only; rewriting a past decision in place destroys the record that the decision was made and later reversed, which is the very thing a supersession note exists to preserve."
---

<objective>
Reduce the views skill to a CLI wrapper, remove the three now-obsolete views-refresh doc sections,
and record the two decisions ROADMAP criterion 3 requires plus the v0.4 reversal that D-11 makes.

Purpose: implements D-09 and D-11 and completes ROADMAP criteria 3 and 4. One implementation now
exists in the Python layer, realising PROJECT.md's "Python is the deterministic enforcement layer;
skills orchestrate flow" principle. The three refresh sections instruct a reader to run
`construct views generate --workspace .` — an option that no longer exists and a value that would
discover zero workspaces — and carry the remediation string criterion 4 names. D-11 removes the
sections rather than fixing them, because the Python capability now owns the refresh.

Output: a wrapper skill, three cleaned docs, `adr-0005`, and a PROJECT.md Key Decisions row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-views-generate-data-resolution/15-CONTEXT.md
@.planning/phases/15-views-generate-data-resolution/15-RESEARCH.md
@.planning/phases/15-views-generate-data-resolution/15-PATTERNS.md
@.planning/phases/15-views-generate-data-resolution/15-03-SUMMARY.md
@.planning/phases/14-durable-state-config-truth/14-CONTEXT.md
</context>

<artifacts_this_phase_produces>
Symbols and paths **created** by Plan 05 (new — exclude from drift verification):

- New document: `CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md`
- New PROJECT.md Key Decisions row recording the views-refresh ownership reversal
- New phase artifact (already written at plan time):
  `.planning/phases/15-views-generate-data-resolution/COVERAGE.md`

Paths **deleted** by Plan 05:
`CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/generate.py`,
`.../requirements.txt`, `.../debounced_hook.py`, `.../debounced-hook.sh`, and the skill's
gitignored `.venv/` if present on disk.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reduce the views skill to a CLI wrapper and delete its orphaned runtime</name>
  <files>
    CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/run.sh,
    CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/SKILL.md,
    CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/generate.py,
    CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/requirements.txt,
    CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/debounced_hook.py,
    CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/debounced-hook.sh
  </files>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/run.sh (the current venv-bootstrap path being retired — read it fully before replacing)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/SKILL.md (its frontmatter, declared tools, and every instruction that assumes a local Python runtime)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/debounced_hook.py (confirm what invokes it and whether anything else references it before deleting)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/generate.py (the skill-side entry point being deleted — confirm it holds nothing the Python layer lacks)
    - src/construct/cli.py (the `views generate` command Plan 03 created — its exact option name and behaviour, so `run.sh` invokes it correctly)
    - .planning/PROJECT.md (the "Python is the deterministic enforcement layer; skills orchestrate flow" principle this task realises)
  </read_first>
  <action>
Rewrite `run.sh` as a thin CLI wrapper. It takes the install root as its first positional argument and
invokes `construct views generate --install-root "$1"`, propagating the CLI's exit status as its own.
Keep whatever shebang and strict-mode settings the current script uses. Delete the per-skill virtual
environment bootstrap entirely — that is the accepted cost D-09 records: the skill stops being
standalone and now requires an installed CONSTRUCT. Add a guard that fails with a clear, actionable
message if the `construct` executable is not on PATH, and a usage message if no argument is given —
those two failures are the ones the removed bootstrap used to prevent, so they must now be explicit
rather than surfacing as a confusing shell error.

Delete four files outright: `generate.py`, `requirements.txt`, `debounced_hook.py`, and
`debounced-hook.sh`. The skill's `lib/` directory was already moved by Plan 01. `requirements.txt`
listed only PyYAML for the retired bootstrap and is now dead. The debounce pair shells out to the
`generate.py` being deleted and carries its own YAML import and path assumptions; re-homing debounce
behaviour in the Python layer would be new runtime capability, which v0.4.1 excludes (RESEARCH OQ-3).
**Before deleting the debounce pair, grep the repository for any registration referencing it** — a
hook configuration, a settings file, or another skill. If a live registration exists, do not silently
break it: remove the registration in the same commit, or if it is outside this phase's scope, keep
the file and record the finding prominently in the summary. Also remove the skill's gitignored
`.venv/` directory from disk if present, since nothing bootstraps or uses it any more.

Update `SKILL.md` so it describes what the skill now is. Every instruction that assumed a local
Python runtime, a bootstrap step, or a direct module invocation must go; what remains is the CLI
invocation and when to use it. If its frontmatter declares tools only needed for the retired local
execution path, narrow the declaration to what the wrapper actually needs. Add a short note recording
that the implementation lives in the Python layer and citing `D-09` — enough that a reader does not
go looking for a deleted `lib/` directory.

Confirm that every `construct ...` string left in `SKILL.md` resolves against the live CLI. The
contract guard scans this file, so a stale invocation here would either fail the suite or pressure
someone into widening the allowlist — which Plan 03's prohibition forbids.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `ls CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/` lists exactly `run.sh` and `SKILL.md`
    - `grep -c 'views generate --install-root' CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/run.sh` outputs `1`
    - `grep -ci 'venv\|virtualenv\|pip install' CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/run.sh` outputs `0`
    - `bash -n CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/run.sh` exits 0
    - Running `run.sh` with no arguments exits non-zero and prints a usage message naming the install-root argument
    - `grep -rc 'debounced.hook' CONSTRUCT-CLAUDE-impl/ .claude/ 2>/dev/null | grep -v ':0$'` returns nothing (no surviving registration)
    - `grep -c 'D-09' CONSTRUCT-CLAUDE-impl/claude/skills/construct-views-generate-data/SKILL.md` is at least `1`
    - `.venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q` exits 0
  </acceptance_criteria>
  <done>The skill directory holds only `run.sh` and `SKILL.md`, the wrapper invokes the real CLI command with clear failure messages, no orphaned runtime or hook registration survives, and every command string in the skill resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the three views-refresh doc sections</name>
  <files>
    CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md,
    CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md,
    CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md
  </files>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md (lines 105-140 — Step 5 Views Refresh Hook, including the remediation string and the surrounding step numbering)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md (lines 100-120 — the equivalent section)
    - CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md (lines 90-125 — §5 Views Refresh and the surrounding section numbering)
    - src/construct/views/refresh.py (the Python implementation that now owns this behaviour — written by Plan 04)
    - .planning/phases/15-views-generate-data-resolution/15-RESEARCH.md § "Pitfall 6"
  </read_first>
  <action>
Remove the views-refresh section from each of the three documents. In `construct-curation-cycle/SKILL.md`
that is Step 5's Views Refresh Hook; in `construct-research-cycle/SKILL.md` the equivalent section
around `:109-113`; in `workflows/daily-cycle.md` the §5 Views Refresh block around `:98-116`. Each
section carries the remediation string telling the reader to run the generate command manually — the
string ROADMAP criterion 4 names — and each invokes the generator with a workspace-named option and a
current-directory value that would discover zero workspaces. **Do not fix the option name in a section
you are deleting.** The Python capability owns this now; the instruction has no correct form here.

Repair the surrounding structure. Each document numbers its steps or sections, so removing one leaves
a gap: renumber the following steps consecutively, and update any cross-reference elsewhere in the
same document that points at a renumbered step or at the removed section by number or name. Grep each
file for references to the removed section before finishing.

Add one short sentence in place of each removed section's role — not a new instruction, but a pointer:
state that the views refresh now runs automatically as part of the workflow capability and needs no
step here, citing `D-11`. This is the cheapest durable defence against someone re-adding the step
later because its absence looked like an oversight. Keep it to a sentence; the full rationale lives in
`adr-0005`.

Do not edit the architecture doc set — `architecture-overview.md`, `artifact-catalog.md`, and
`config-topology.md` are Phase 17's under DOC-01/DOC-02, and this phase's skill and capability changes
are precisely what Phase 17 must reconcile. Do not edit anything in the archived v0.4 milestone
directory; it is read-only, and the supersession belongs in a live document.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ric 'views refresh' CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md` shows at most 1 match per file, and each surviving match is the one-sentence D-11 pointer
    - `grep -rc 'construct views generate' CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md` outputs `0` for all three files
    - `grep -rc 'workspace \.' CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md` outputs `0`
    - `grep -rc 'D-11' CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md CONSTRUCT-CLAUDE-impl/claude/skills/construct-research-cycle/SKILL.md CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md` outputs at least `1` for each file
    - Step and section numbering in each edited file is consecutive with no gaps, and `grep -n '^#\{2,4\} ' <file>` shows an unbroken sequence
    - `.venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q` exits 0
    - The allowlist still holds exactly 4 entries: `.venv/bin/python -c "import tests.contract.test_doc_command_references as m; assert len(m._KNOWN_BROKEN)==4; print('ok')"` prints `ok`
    - `git status --porcelain CONSTRUCT-CLAUDE-spec/` shows no modification to `architecture-overview.md`, `artifact-catalog.md`, or `config-topology.md`
  </acceptance_criteria>
  <done>All three refresh sections are gone with numbering repaired and cross-references updated, each file carries a one-sentence D-11 pointer, no remediation string names a nonexistent command, and the allowlist has not grown.</done>
</task>

<task type="auto">
  <name>Task 3: Record the D-11 supersession and the two criterion-3 decisions durably</name>
  <files>CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md, .planning/PROJECT.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-spec/adrs/adr-0004-durable-workflow-checkpoints.md (the Phase 14 precedent for adding a new numbered ADR rather than amending an existing one — copy its structure, heading set, status line, and citation style exactly)
    - CONSTRUCT-CLAUDE-spec/adrs/adr-0003-v03-pipeline-v04-ui.md (the four-layer model this decision sits inside)
    - .planning/PROJECT.md (lines 85-110 — the Key Decisions table's exact column set and row format, and the "Python is the deterministic enforcement layer" row that stays flagged for revisit)
    - .planning/phases/14-durable-state-config-truth/14-CONTEXT.md (D-07's new-ADR-over-amendment reasoning)
    - .planning/phases/15-views-generate-data-resolution/COVERAGE.md (the capability-surface decisions this ADR should be consistent with)
  </read_first>
  <action>
Create `CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md`. Discretion item resolved:
**a new numbered ADR plus a PROJECT.md Key Decisions row — both.** The ADR set lives at
`CONSTRUCT-CLAUDE-spec/adrs/` and currently runs 0001 through 0004; 0005 is the next free number.
Phase 14's D-07 set the precedent of adding a new ADR rather than amending an existing one, on
discoverability grounds, and this is a comparable reversal.

Follow `adr-0004`'s structure exactly — same heading set, same status-line format, same citation
style. The ADR must record four things:

1. **The reversal.** Phase 13's D-10 held that the parent owns the single views refresh, implemented
   as a skill-owned hook. That is superseded: the Python capability owns the refresh, and every
   workflow capability refreshes. State plainly that this reverses a recorded v0.4 decision and name
   it, so a reader who encounters the old rule in the archived milestone knows it was deliberately
   overturned rather than forgotten. This is the durability requirement D-11 sets — Phase 17 must not
   re-derive the old rule from the archive.
2. **Why every workflow refreshes rather than only a parent.** The simplest rule that needs no
   parent-awareness, which matters because `daily.run` deliberately tracks no parent/child
   relationship (Phase 13 D-09, no parent graph). Record the accepted cost: a daily cycle triggers
   three sweeps, made acceptable by incremental fingerprinting, with `version.json` churning more than
   once per run while the SPA polls it.
3. **The install-root contract** — ROADMAP criterion 3's first half. The capability is deliberately
   install-root scoped because it aggregates every discovered workspace into a single build output;
   `discover_workspaces` scans only the children of its argument, so a single workspace yields zero
   discovered workspaces and silently empty views. Both views CLI commands and the capability input
   model name it `install_root`.
4. **The deployed-skill-directory import coupling** — criterion 3's second half. The views source
   parsers are vendored into the shipped package because the distribution packages only `src/construct`;
   the previous arrangement resolved a path to the repository root and therefore worked only inside a
   dev checkout, raising ImportError on an installed CONSTRUCT. Record that this was a verbatim move,
   not a rewrite, and that the skill is consequently now a CLI wrapper requiring an installed CONSTRUCT.

Add two further notes the ADR is the right home for, both flagged in research for Phase 17's benefit:
that the views group deliberately remains a registry holdout (CLI and MCP reach the generator by two
independent paths, RT-01/RT-02 stays open, and PROJECT.md's registry-coverage revisit note stays
accurate), and that the "deployed SPA is ground truth" premise for the views data contract is
unverifiable in-repo — `spec-v02-data-model.md` §5.1/§5.2 is the durable authority.

Then add one row to PROJECT.md's Key Decisions table recording the views-refresh ownership reversal
and citing `adr-0005`. Match the table's existing column set and row format exactly; read it before
writing. Do not alter the "Python is the deterministic enforcement layer" row's revisit flag — D-03
declines the registry unification, so that flag stays accurate.

Do not add a ROADMAP correction note for Phase 17 into the ADR; put it in the summary instead —
`REQUIREMENTS.md` DOC-01 asserts that `architecture-overview.md` cites a non-existent
`spec-v02-data-model.md`, and that premise is false. The file exists at
`CONSTRUCT-CLAUDE-spec/spec-v02-data-model.md`. Phase 17 must inherit the correction rather than act
on a false premise, but Phase 15 does not edit the architecture doc set.
  </action>
  <verify>
    <automated>test -f CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md && grep -q 'adr-0005' .planning/PROJECT.md && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md` exists
    - `grep -c 'D-10' CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md` is at least `1` (the superseded decision is named)
    - `grep -ci 'supersed' CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md` is at least `1`
    - `grep -c 'install_root' CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md` is at least `1`
    - `grep -ci 'import' CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md` is at least `1` (the coupling decision is recorded)
    - `grep -c 'spec-v02-data-model' CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md` is at least `1`
    - The ADR's heading set matches `adr-0004`: `diff <(grep '^#\{1,3\} ' CONSTRUCT-CLAUDE-spec/adrs/adr-0004-durable-workflow-checkpoints.md | sed 's/[0-9].*//') <(grep '^#\{1,3\} ' CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md | sed 's/[0-9].*//')` shows the same top-level section names
    - `grep -c 'adr-0005' .planning/PROJECT.md` is at least `1`
    - `git diff .planning/PROJECT.md | grep -c '^-' ` shows only the diff header line (a row was added, none removed or rewritten)
    - `.venv/bin/python -m pytest -q` exits 0
  </acceptance_criteria>
  <done>`adr-0005` records the D-11 reversal and both ROADMAP criterion 3 decisions in the live ADR set following the `adr-0004` precedent, and PROJECT.md's Key Decisions table cites it without disturbing existing rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude skill invocation → shell → `construct` CLI | The wrapper passes an agent-supplied argument into a shell command line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-16 | Elevation of Privilege | skill `run.sh` argument interpolation | high | mitigate | An unquoted positional argument in the wrapper would allow word-splitting or injection from whatever invokes the skill. Mitigated by requiring the argument to be passed double-quoted as a single word to the CLI, an explicit missing-argument guard, and `bash -n` syntax validation in the acceptance criteria. The wrapper performs no eval, no string-built command, and no shell expansion of the argument beyond the quoted substitution. |
| T-15-17 | Denial of Service | deleting `debounced_hook.py` / `debounced-hook.sh` | medium | mitigate | A live hook registration pointing at a deleted script would break a file-watch path silently. Mitigated by the pre-deletion repository grep for registrations required in Task 1, with an explicit instruction not to break a live registration silently and to escalate to the summary if one is found outside scope. |
| T-15-18 | Repudiation | recording the D-11 reversal | high | mitigate | A reversed architectural decision that is recorded only in an archived, read-only milestone is effectively lost — Phase 17 would re-derive the superseded rule. Mitigated by `adr-0005` in the live ADR set plus a PROJECT.md Key Decisions row, with acceptance criteria asserting the superseded decision is named by id and that no existing PROJECT.md row was rewritten. |
| T-15-19 | Tampering | editing documents outside phase scope | medium | accept | Phase 15 crosses Phase 14 D-02's edit fence for the release playbook (in Plan 03) and edits three workflow docs here. Accepted deliberately, following the precedent D-03 set for `config-topology.md`. Bounded by the prohibition against touching the architecture doc set and by the `git status` acceptance criterion asserting those three files are unmodified. |

ASVS level 1; blocking threshold `high`. T-15-16 and T-15-18 are both mitigated with automated
acceptance criteria in this plan.
</threat_model>

<verification>
1. `.venv/bin/python -m pytest -q` — full suite green.
2. `.venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q` — passes with the
   allowlist still at exactly 4 entries.
3. The views skill directory contains only `run.sh` and `SKILL.md`.
4. No views-refresh instruction or manual-generate remediation string survives in the three
   workflow docs.
5. `adr-0005` exists and PROJECT.md cites it.
6. `git status --porcelain CONSTRUCT-CLAUDE-spec/` shows no change to the architecture doc set.
</verification>

<success_criteria>
- ROADMAP criterion 3 fully satisfied: both ambiguities have an explicit recorded decision in a
  durable, live document — not an implicit resolution.
- ROADMAP criterion 4 fully satisfied: no remediation message names a command that does not exist,
  because the sections carrying that string are gone and the refresh is owned by the Python layer.
- ROADMAP criterion 5 satisfied at the phase gate: full suite green, allowlist at exactly 4 entries,
  no additions.
- D-09 complete: one implementation, in the Python layer; the skill orchestrates flow only.
- D-11 recorded as a deliberate reversal of Phase 13's D-10, durable enough that Phase 17 does not
  re-derive the old rule.
- The architecture doc set is untouched, preserved for Phase 17.
</success_criteria>

<output>
Create `.planning/phases/15-views-generate-data-resolution/15-05-SUMMARY.md` when done.

Record in the summary, for downstream phases:

- **Phase 17 (DOC-01) fact correction.** `REQUIREMENTS.md` DOC-01 asserts that
  `architecture-overview.md` cites "the non-existent `spec-v02-data-model.md`". **That premise is
  false** — the file exists at `CONSTRUCT-CLAUDE-spec/spec-v02-data-model.md` and this phase relied on
  its §5.1/§5.2 as the authoritative views data contract. Phase 17 must inherit this correction rather
  than re-derive it or act on the false premise.
- **Phase 17 (DOC-02) inventory deltas.** One CLI command added (`views generate`); one MCP tool moved
  from permanent-failure to real (`construct_views_generate_data`); one skill reduced to a CLI wrapper
  with four files deleted; three workflow docs lost a section. `artifact-catalog.md` counts change
  accordingly.
- **Phase 16 (FIX-03) inheritance.** The allowlist stands at exactly 4 entries, all owned by FIX-03.
- **v0.6 backlog candidates surfaced this phase:** align `cards.json` with `spec-v02-data-model` §5.2
  (OQ-1 reading (b)); port the vendored views parsers from PyYAML to ruamel.yaml (F6 Option B);
  registry unification for the views group (RT-01/RT-02, declined by D-03); re-home debounce behaviour
  in the Python layer if wanted (OQ-3); tighten `DomainRecord.metrics` and `cross_domain_links` into
  nested models once legacy `domains.yaml` shapes are known.
- Any live hook registration found for the deleted debounce pair.
</output>
