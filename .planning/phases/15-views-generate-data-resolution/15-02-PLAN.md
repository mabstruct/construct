---
phase: 15-views-generate-data-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/views/models.py
  - tests/integration/test_views_generate.py
  - tests/integration/conftest.py
  - tests/contract/test_views_contracts.py
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "`generate()` against a freshly scaffolded install root returns `success=True` with zero validation errors and zero warnings."
    - "`generate()` against a populated install root containing connected cards returns zero validation errors, exercising `CardRecord.connections`."
    - "`DomainRecord` accepts every field `parse_domains.py` emits — `status`, `created`, `content_categories`, `source_priorities`, `cross_domain_links`, `metrics` — and rejects unknown fields."
    - "`CardRecord.connections` is typed as a list of card-id strings, matching what `denormalize_into_cards` assigns."
    - "`BridgeSummary` accepts `top_domain_pairs` and still accepts the four L3 gate fields that `bridge_detect.py` emits."
    - "All 17 models in `views/models.py` still forbid unknown fields after the reconciliation."
    - statement: "An install root containing zero workspaces, and a workspace containing zero cards, each generate valid empty view data rather than erroring or writing malformed files."
      verification: backstop
    - statement: "A card's denormalised connection list is emitted in a specified, stable, repeatable order across regenerations of identical input."
      verification: backstop
  artifacts:
    - src/construct/views/models.py
    - tests/integration/test_views_generate.py
    - tests/integration/conftest.py
  key_links:
    - "`views/lib/parse_domains.py` domain dict → `DomainRecord` (pass-through adapter at `generate.py:97-98`; the six missing fields are why `domains.json` reports 18 extra_forbidden errors today)"
    - "`views/lib/parse_connections.py::denormalize_into_cards` → `connects_to` → `_FILE_MODEL_MAP` cards adapter → `CardRecord.connections`"
    - "`views/lib/parse_bridges.py::_build_summary` → `BridgeSummary.top_domain_pairs`"
    - "`construct.services.init.initialize_workspace` → scaffolded install root → the D-04 done-bar fixture"
  prohibitions:
    - statement: "`model_config = ConfigDict(extra=\"forbid\")` must not be relaxed, removed, or weakened on any of the 17 models in order to make generation succeed. Relaxing it would produce success=True while gutting the schema-validated-generator guarantee — trading a failing gate for a toothless one. D-02 rejects this explicitly."
    - statement: "Validation errors must not be suppressed, downgraded to warnings, or filtered out of `GenerateReport.validation_errors` to make the done-bar green. The distinction between fatal contract failures and advisory content warnings is the mechanism D-04 depends on."
---

<objective>
Reconcile `views/models.py` with the shape the vendored parsers actually emit, so `generate()`
returns `success=True` with zero validation errors — and prove it with integration tests against
both a fresh and a populated install root.

Purpose: implements D-02 and unblocks D-01 (Plan 03 cannot wire a real handler that always reports
failure). A live probe reproduced 3 validation errors: 18 `extra_forbidden` on `domains.json`, 16
type errors on `cards.json`, and 1 `extra_forbidden` on `bridges.json`. RESEARCH F2 upgraded this
from an editorial choice to a spec-conformance fix — `spec-v02-data-model.md` §5.1/§5.2 corroborates
the parsers field-for-field. The models are a narrower contract than reality; they are corrected to
describe the real shape, and they stay strict.

Output: corrected models, a shared scaffolded-install-root fixture, and integration tests that close
RESEARCH Pitfall 1 (a fresh workspace has zero cards, so the `connections` fix would otherwise go
unverified).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-views-generate-data-resolution/15-CONTEXT.md
@.planning/phases/15-views-generate-data-resolution/15-RESEARCH.md
@.planning/phases/15-views-generate-data-resolution/15-PATTERNS.md
@.planning/phases/15-views-generate-data-resolution/15-VALIDATION.md
@CONSTRUCT-CLAUDE-spec/spec-v02-data-model.md
</context>

<artifacts_this_phase_produces>
Symbols and paths **created** by Plan 02 (new — exclude from drift verification):

- New test file: `tests/integration/test_views_generate.py`
- New test functions: `test_fresh_workspace_generates_clean`,
  `test_populated_workspace_generates_clean`, `test_generated_card_connections_are_id_strings`,
  `test_models_still_forbid_unknown_fields`
- New fixture file / fixture: `tests/integration/conftest.py` providing `scaffolded_install_root`
- New `DomainRecord` fields: `status`, `created`, `content_categories`, `source_priorities`,
  `cross_domain_links`, `metrics`
- New `BridgeSummary` field: `top_domain_pairs`

Symbols **removed** by Plan 02: `DomainRecord.card_count`, `DomainRecord.connection_count`,
`DomainRecord.digest_count`, `DomainRecord.article_count`, `DomainRecord.keywords` (the five phantom
fields — subject to the grep gate in Task 2).

Type **changed** by Plan 02: `CardRecord.connections` from a list of dicts to a list of strings.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Probe-surfaced edges — all 3 unresolved, surfaced not dropped

Phase 15 has no SPEC file, so the deterministic edge probe ran over FIX-01's requirement text. All
three applicable edges returned `unresolved`: per the fallback protocol they are **never**
auto-`backstop`ped and **never** auto-dismissed. They are recorded here as explicit assumptions the
executor and the verifier must see. Two of them additionally have a `verification: backstop` truth
authored in `must_haves` above, because this plan's tests genuinely bear on them — the flag stands
regardless.

| # | Category | Probe question | Why it has real content here | Assumption being made |
|---|---|---|---|---|
| E1 | `adjacency` | When two things are exactly equal or just touch, do they merge, collide, or separate? | Two workspaces can declare the same domain id; `parse_domains.py` resolves this with `declared.setdefault(d_id, meta)` — root-level entries win, per-workspace entries do not overwrite. Bridge candidate pairs can also tie on score. | **Assumed:** existing parser collision semantics are correct and out of scope. This phase moves models, not parsers (D-08/D-02), so no collision behaviour is changed or newly specified. Unverified — no test asserts the setdefault precedence. |
| E2 | `empty` | What is the result for empty, single-element, or null input? | Directly maps to RESEARCH Pitfall 1: a freshly scaffolded workspace has **zero cards**, so `cards.json` is an empty list and `CardRecord.connections` is never instantiated — the done-bar would pass with the bug still present. It is exactly why VALIDATION.md requires BOTH a fresh and a populated workspace test. | **Assumed:** an install root with zero discoverable workspaces produces valid-but-empty view data rather than an error. Partially covered — the fresh-workspace test in Task 1 covers zero-cards; the zero-workspaces case is asserted as a backstop truth, not directly tested. |
| E3 | `ordering` | When elements compare equal, is output order specified and stable? | Maps to `denormalize_into_cards` assigning `sorted(nset)` (`parse_connections.py:79`) and to `domains.sort(key=lambda d: d["id"])` in `parse_domains.py`. Order IS specified today, by sorting. | **Assumed:** the existing sort-based ordering is the intended contract and remains stable across regenerations. Authored as a backstop truth; the populated-workspace test asserts the connection list equals its own sorted form, but no test asserts stability across two successive generate runs. |

**No-silent-drop equality:** 3 probe-surfaced items == 0 authored as `covered` truths + 3 surfaced
as flagged assumptions (E2 and E3 additionally carry `verification: backstop` truths). Nothing was
dropped, dismissed, or silently promoted.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing fresh + populated generation tests</name>
  <files>tests/integration/test_views_generate.py, tests/integration/conftest.py</files>
  <behavior>
    - `generate()` against a scaffolded install root returns `success=True`, `validation_errors == []`, `warnings == []`
    - `generate()` against a populated install root returns `validation_errors == []`
    - Every `connections` entry written into a populated `cards.json` is a string (a card id), never a mapping
    - Every generated card's `connections` list equals its own sorted form (the ordering contract of E3)
    - Both tests run against a `tmp_path`-isolated output directory so the fingerprint cache cannot mask the model change
  </behavior>
  <read_first>
    - tests/integration/test_init_cli.py (the exact analog: header, `FIXTURES_DIR` pattern, `tmp_path: Path` fixture use, `assert result.exit_code == 0, result.stdout` style)
    - tests/conftest.py (existing shared fixtures — check whether an install-root scaffold already exists before adding one)
    - src/construct/services/init.py (the `initialize_workspace(root, DomainInitInput(...))` signature and what it writes)
    - src/construct/views/generate.py (the `generate(install_root)` entry point at :175 and the `GenerateReport` dataclass at :78-88)
    - tests/fixtures/v02/multi-domain-medium/ (the populated-workspace fixture — confirm it has cards with connections before relying on it)
    - .planning/phases/15-views-generate-data-resolution/15-RESEARCH.md § "Pitfall 1", § "Pitfall 2", § "D-04 verification harness"
  </read_first>
  <action>
Add a `scaffolded_install_root` fixture. Put it in `tests/integration/conftest.py` unless
`tests/conftest.py` already provides an equivalent — read that file first and reuse rather than
duplicate. The fixture takes `tmp_path`, creates an install root directory, and calls
`construct.services.init.initialize_workspace(root / "demo", DomainInitInput(domain_id="demo",
display_name="Demo", scope="test scope", taxonomy_seeds=["t1"], source_priorities=["arxiv"],
research_seeds=["seed one"]))`, then yields the **root**, not the workspace. The root-versus-workspace
distinction is the whole point of D-05: `discover_workspaces` scans only the children of its
argument, so passing the workspace itself finds zero workspaces and silently emits empty views.

Create `tests/integration/test_views_generate.py` following `tests/integration/test_init_cli.py`:
a `from __future__ import annotations` header, a module-level `FIXTURES_DIR` resolved from
`Path(__file__).resolve().parents[1] / "fixtures"`, plain module-level `def test_*` functions with
`-> None` annotations and no test classes, arrange/invoke/assert separated by blank lines.

Write four tests:

1. `test_fresh_workspace_generates_clean` — the D-04 done-bar. Call `generate(scaffolded_install_root)`
   and assert `report.success is True`, `report.validation_errors == []`, and `report.warnings == []`.
   Include the report's error and warning lists in the assertion messages so a failure is debuggable
   without a rerun. The zero-warnings expectation is what confirms D-04's rationale for excluding
   `test-ws/` content noise.
2. `test_populated_workspace_generates_clean` — closes Pitfall 1. Copy
   `tests/fixtures/v02/multi-domain-medium/` into a `tmp_path` install root (copy, never generate in
   place — the fixture is shared and generation writes files), then call `generate()` on the copy and
   assert `report.validation_errors == []`. Read the fixture first: if its cards carry no connections,
   the test does not exercise `CardRecord.connections` and you must instead copy `test-ws/` or add
   connected cards to the temporary copy. State in a comment which source was used and why.
3. `test_generated_card_connections_are_id_strings` — read the generated per-workspace `cards.json`
   from the populated run, unwrap it with `construct.views.models.unwrap_payload`, and assert that at
   least one card has a non-empty `connections` list and that every entry across all cards is a `str`.
   The non-empty precondition is load-bearing: without it the test passes vacuously on empty data,
   which is exactly the Pitfall 1 failure mode.
4. `test_models_still_forbid_unknown_fields` — import all 17 models from `construct.views.models`
   and assert each one's `model_config` sets `extra` to forbid. This is the guard for the D-02
   prohibition; it must fail if anyone relaxes strictness to make generation pass.

Add an ordering assertion inside test 3 covering probe edge E3: each card's `connections` list equals
`sorted(connections)`.

These tests are expected to FAIL at this point — that is the RED state. Tests 1, 2 and 3 fail because
the models are still wrong; test 4 should already pass. Commit them failing before starting Task 2.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/test_views_generate.py -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_views_generate.py` exists and `.venv/bin/python -m pytest tests/integration/test_views_generate.py --collect-only -q` collects exactly 4 tests
    - `.venv/bin/python -m pytest tests/integration/test_views_generate.py -q` exits non-zero (RED state confirmed before implementation)
    - `test_models_still_forbid_unknown_fields` passes in isolation: `.venv/bin/python -m pytest tests/integration/test_views_generate.py::test_models_still_forbid_unknown_fields -q` exits 0
    - The failure output for `test_fresh_workspace_generates_clean` names `domains.json` and `bridges.json` (2 of 3 errors — the fresh-workspace signature from RESEARCH Pitfall 1)
    - The failure output for `test_populated_workspace_generates_clean` additionally names `cards` and `connections` (the third error the fresh workspace cannot surface)
    - `grep -c 'tmp_path' tests/integration/test_views_generate.py` is at least `2` (no test asserts against a reused build directory — Pitfall 2)
    - The commit for this task is a `test(...)` commit containing no changes under `src/`
  </acceptance_criteria>
  <done>Four integration tests exist, three fail for the right reasons with error output naming domains, bridges, and card connections respectively, and the model-strictness guard passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile DomainRecord, CardRecord, and BridgeSummary with the parser output</name>
  <files>src/construct/views/models.py</files>
  <behavior>
    - `DomainRecord` validates a real `parse_domains.py` dict with zero `extra_forbidden` errors
    - `CardRecord` validates a card whose `connections` is a list of id strings
    - `BridgeSummary` validates a real `_build_summary` dict containing `top_domain_pairs`
    - Every model still rejects a genuinely unknown field
  </behavior>
  <read_first>
    - src/construct/views/models.py (the full file — `BridgeSummary` at :65-75, `BridgesFile` at :77-83, `DomainRecord` at :91-103, `CardRecord` at :163-177, `unwrap_payload` at :292)
    - src/construct/views/lib/parse_domains.py (lines 28-46 — the exact emitted dict; this is the ground truth, not the prd)
    - src/construct/views/lib/parse_bridges.py (lines 320-343 — `_build_summary` returns exactly `totals` and `top_domain_pairs`)
    - src/construct/views/lib/parse_connections.py (line ~79 — `denormalize_into_cards` assigns `sorted(nset)`, proving `connects_to` is a list of strings)
    - src/construct/views/generate.py (lines 95-165 — `_FILE_MODEL_MAP`; read to confirm which adapters are pass-throughs and which reshape, then leave the map untouched)
    - CONSTRUCT-CLAUDE-spec/spec-v02-data-model.md (§5.1 domains, §5.2 cards — the independent corroboration RESEARCH F2 established)
    - .planning/phases/15-views-generate-data-resolution/15-PATTERNS.md § "src/construct/views/models.py — D-02 field widening"
  </read_first>
  <action>
Edit `src/construct/views/models.py` only. Preserve every invariant in the file's established style:
a one-line docstring, `model_config = ConfigDict(extra="forbid")` as the first body statement,
required fields before defaulted ones, scalars defaulted inline, collections defaulted with
`Field(default_factory=...)` and never a bare mutable default, and PEP-604 unions for optionals.

**`DomainRecord`** — add the six fields `parse_domains.py` emits and the spec §5.1 names:
`status: str = "active"`, `created: str = ""`, `content_categories: list[str]` defaulting to an empty
list, `source_priorities: list[str]` defaulting to an empty list, `cross_domain_links: list[dict]`
defaulting to an empty list, and `metrics: dict` defaulting to an empty dict. Type
`cross_domain_links` as a list of dicts rather than introducing a nested link model: the parser
passes it through with only an `isinstance(list)` guard, so a nested model would be stricter than the
parser guarantees and could reject legacy `domains.yaml` content the parser accepts (RESEARCH
assumption A3). Type `metrics` as a plain dict for the same reason — the parser's `_compute_metrics`
sub-dict carries nine keys including nested `cards_by_lifecycle` and `cards_by_confidence` maps.

Then delete the five phantom fields `card_count`, `connection_count`, `digest_count`,
`article_count`, and `keywords`. The parser emits none of them and spec §5.1 names none of them;
they would be written into `domains.json` as zeros that no consumer reads and that contradict the
real counts inside `metrics`. **Gate this deletion:** before removing them, run a repository-wide
grep for each name restricted to `src/`, `tests/`, and `views/design-example/src/`, and confirm no
site constructs or reads them *on a `DomainRecord`*. Note that `card_count` and `connection_count`
appear in `tests/unit/test_help.py:25-27` and in `src/construct/pipelines/graph_status.py` — those
are the graph-status report, an unrelated structure, and must not be touched. If the grep finds a
genuine `DomainRecord` consumer, keep that field, add a comment recording why, and report the finding
in the summary rather than deleting it.

**`CardRecord`** — change `connections` from a list of dicts to a list of strings, keeping the
empty-list default factory. This is provable from the code, not merely observed: `denormalize_into_cards`
assigns `sorted(nset)` where `nset` is a set of target card ids, so the value is always a list of
strings. Spec §5.2 corroborates, noting that the full edge list with types lives in
`connections.json`. Leave every other `CardRecord` field alone.

**`BridgeSummary`** — add `top_domain_pairs: list[dict]` defaulting to an empty list. **Keep** the
four L3 gate fields `l1_l2_only`, `l3_calls`, `l3_candidates_eligible`, and `l3_candidates_total`:
`src/construct/pipelines/bridge_detect.py:512-515` emits all four, `src/construct/llm/curation_run.py:552`
reads `l1_l2_only`, and `tests/contract/test_views_contracts.py` constructs `BridgeSummary` with all
four at lines 263-266 and 398-401. Deleting them would be a breaking change; they are defaulted and
harmless.

Add a decision-ID comment above each edited model in this codebase's house style (see
`catalog.py:303-305`), citing `D-02` and stating that the field set is derived from the parser output
and corroborated by `spec-v02-data-model.md` §5.1/§5.2.

Do not touch `_FILE_MODEL_MAP`, any parser, or `unwrap_payload`. Do not weaken any `model_config`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/test_views_generate.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/integration/test_views_generate.py -q` exits 0 with 4 passing (GREEN state)
    - `grep -c 'extra="forbid"' src/construct/views/models.py` outputs `17`
    - `grep -c 'extra="ignore"\|extra="allow"' src/construct/views/models.py` outputs `0`
    - `.venv/bin/python -c "from construct.views.models import DomainRecord as D; f=set(D.model_fields); assert {'status','created','content_categories','source_priorities','cross_domain_links','metrics'} <= f, f; assert not ({'card_count','connection_count','digest_count','article_count','keywords'} & f), f; print('ok')"` prints `ok`
    - `.venv/bin/python -c "from construct.views.models import CardRecord as C; import typing; print(C.model_fields['connections'].annotation)"` reports a list of `str`
    - `.venv/bin/python -c "from construct.views.models import BridgeSummary as B; f=set(B.model_fields); assert 'top_domain_pairs' in f; assert {'l1_l2_only','l3_calls','l3_candidates_eligible','l3_candidates_total'} <= f; print('ok')"` prints `ok`
    - `.venv/bin/python -c "from construct.views.models import DomainRecord; DomainRecord(id='x',name='x',description='',nonsense=1)"` raises a validation error (strictness intact)
    - `git diff --stat src/construct/views/generate.py` shows no change from this task (the adapter map is untouched)
    - `grep -c 'D-02' src/construct/views/models.py` is at least `1`
  </acceptance_criteria>
  <done>`generate()` returns `success=True` with zero validation errors on both a fresh and a populated install root, all 17 models still forbid unknown fields, and the adapter map is unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the existing views contract tests and confirm the full suite</name>
  <files>tests/contract/test_views_contracts.py</files>
  <read_first>
    - tests/contract/test_views_contracts.py (the whole file — it constructs `BridgeSummary`, `CardRecord`, and `DomainRecord` directly; the direct-construction sites are near lines 103, 110, 261, 335-337, 398)
    - src/construct/views/models.py (the reconciled models, post-Task 2)
    - .planning/phases/15-views-generate-data-resolution/15-VALIDATION.md § "Model-edit caution (from research)"
  </read_first>
  <action>
Run the full suite and repair every failure that the D-02 reconciliation caused in
`tests/contract/test_views_contracts.py`. The file uses class-based grouping rather than
module-level test functions, so count tests with `pytest --collect-only` rather than by grepping for
function definitions.

Expected repair work: any site constructing `DomainRecord` with one of the five deleted phantom
fields must drop those kwargs; any site constructing or asserting on `CardRecord.connections` with
dict elements must be updated to id strings. Sites constructing `BridgeSummary` with the four L3
fields need no change — those fields were deliberately kept.

Where a test asserted on a phantom field's value, do not simply delete the assertion. Replace it with
an assertion against the equivalent real value inside `metrics` where one exists (the parser's
`metrics` sub-dict carries the counts the phantom scalars were reaching for), and only delete the
assertion outright when no real equivalent exists. Deleting coverage to make a suite green is the
failure mode this task exists to avoid — if you delete any assertion, say so explicitly in the
summary with the reason.

Add one positive assertion to this file covering the widened `DomainRecord`: construct a record from
a realistic `parse_domains.py`-shaped dict including `metrics` and `cross_domain_links` and assert it
validates. This keeps the contract file — which declares itself the views contract guard — honest
about the new field set rather than merely tolerant of it.

Do not modify any other test file, and do not add entries to any known-broken allowlist.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` exits 0
    - The passing count is at least 447 (443 baseline + 4 new integration tests), and no test was removed: `.venv/bin/python -m pytest tests/contract/test_views_contracts.py --collect-only -q | tail -1` reports a collected count greater than or equal to the count on the previous commit
    - `.venv/bin/python -m pytest tests/contract/test_views_contracts.py -q` exits 0
    - `grep -c 'card_count\|connection_count\|digest_count\|article_count' tests/contract/test_views_contracts.py` outputs `0`
    - `grep -c 'l1_l2_only' tests/contract/test_views_contracts.py` is at least `1` (the L3 fields were kept, not collaterally removed)
    - A new assertion exercising `metrics` and `cross_domain_links` exists: `grep -c 'cross_domain_links' tests/contract/test_views_contracts.py` is at least `1`
  </acceptance_criteria>
  <done>Full suite green with no test coverage lost, and the views contract file asserts the widened domain shape positively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace YAML/markdown → parsed dicts → Pydantic validation gate | The models are the enforcement point where untrusted-shape workspace content becomes typed data written into `views/build/data/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Tampering | `views/models.py` `model_config` on all 17 models | high | mitigate | Relaxing `extra="forbid"` would silently admit unvalidated fields into the generated data files while reporting success. Mitigated by `test_models_still_forbid_unknown_fields` (Task 1) plus the acceptance criteria asserting a count of exactly 17 forbid declarations and zero ignore/allow declarations. |
| T-15-06 | Tampering | `DomainRecord.metrics`, `DomainRecord.cross_domain_links` | low | accept | Typed as open `dict` / `list[dict]` rather than nested models, so their interiors are unvalidated. Accepted deliberately (RESEARCH A3): a stricter nested model would be stricter than the parser guarantees and would reject legacy `domains.yaml` the parser accepts. The data is local-first, first-party workspace content. Recorded as a v0.6 tightening candidate. |
| T-15-07 | Information Disclosure | `GenerateReport.validation_errors` surfaced to the caller | low | accept | Validation error strings embed field paths and can embed workspace file paths. Local-first single-user CLI/MCP; no remote consumer. Consistent with existing `views validate` output. |

ASVS level 1 (V5 Input Validation is the applicable category and is this plan's central mechanism);
blocking threshold `high`. T-15-05 is mitigated with an automated guard in this plan.
</threat_model>

<assumption_delta_decision>
**Noun now primary:** none — no generalization occurred.

**Decision:** `no-change`.

**Rationale:** The assumption-delta detector fired `kind: pluralization, term: "second"` on the
ROADMAP phrase *"sequenced second because its outcome dictates Phase 16 and 17 content"*. That is an
ordinal describing phase sequencing, not the introduction of a second variant of a modeled thing. No
singular→plural transition exists in this phase's scope: there is still exactly one generator, one
install root per invocation, one capability record, and one views data contract. The identity model
(`ws_id` as the workspace key, card `id` as the card key) is unchanged and still names the right
noun. No promotion or add-alongside is warranted, and no accepted debt is being recorded.

*(This checkpoint is advisory and non-blocking; it is recorded here rather than skipped so the
decision is explicit.)*
</assumption_delta_decision>

<verification>
1. `.venv/bin/python -m pytest -q` — full suite green, at least 447 passing.
2. `.venv/bin/python -m pytest tests/integration/test_views_generate.py -q` — 4 passing.
3. `grep -c 'extra="forbid"' src/construct/views/models.py` outputs 17.
4. Both `generate()` targets — fresh scaffolded root and populated fixture copy — report
   `validation_errors == []`.
</verification>

<success_criteria>
- `generate()` returns `success=True` with zero validation errors and zero warnings on a freshly
  scaffolded install root (the D-04 done-bar).
- `generate()` returns zero validation errors on a populated install root, with the card
  `connections` fix actually exercised (Pitfall 1 closed).
- `DomainRecord` describes the parser's real field set; the five phantom fields are gone (or each
  retention is justified in the summary).
- `CardRecord.connections` is a list of card-id strings.
- `BridgeSummary` gains `top_domain_pairs` and keeps its four L3 gate fields.
- All 17 models still forbid unknown fields.
- Full suite green with no coverage removed.
</success_criteria>

<output>
Create `.planning/phases/15-views-generate-data-resolution/15-02-SUMMARY.md` when done.

Record in the summary: which populated-workspace fixture was used and whether it needed connected
cards added; any phantom `DomainRecord` field that was kept and why; every assertion deleted from
`test_views_contracts.py` with its reason; and RESEARCH OQ-2 for Phase 17's benefit — the "deployed
SPA is ground truth" premise is unverifiable in-repo (`views/build/` does not exist and
`views/design-example/` reads its own bundled data), so `spec-v02-data-model.md` is the durable
authority for the views data contract.
</output>
