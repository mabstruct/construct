---
phase: 01-workspace-canonical-data-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/construct/services/init.py
  - src/construct/cli.py
  - tests/integration/test_init_cli.py
  - tests/fixtures/expected-workspace-tree.txt
autonomous: true
requirements:
  - WORK-01
  - WORK-03
  - CARD-03
must_haves:
  truths:
    - "A user can run `construct init <path>` and receive the full workspace scaffold immediately per D-01 and D-02."
    - "The init interview asks only essential domain inputs and writes defaults for the rest per D-03 and D-04."
    - "A user can run `construct validate` and `construct status` to inspect errors and canonical-vs-derived ownership."
  artifacts:
    - path: "src/construct/services/init.py"
      provides: "Guided workspace creation service"
      contains: "domains/{domain_id}/domain.yaml"
    - path: "src/construct/cli.py"
      provides: "init, validate, and status commands"
      contains: "@app.command"
    - path: "tests/integration/test_init_cli.py"
      provides: "End-to-end CLI coverage for init/validate/status"
      contains: "construct init"
  key_links:
    - from: "src/construct/services/init.py"
      to: "templates/workspace/*.yaml"
      via: "template-backed workspace writing"
      pattern: "model-routing"
    - from: "src/construct/cli.py"
      to: "src/construct/services/validation.py"
      via: "validate/status command delegation"
      pattern: "validate"
---

<objective>
Ship the guided `construct init` workflow and the thin CLI commands that expose init, validate, and status over the shared workspace services.

Purpose: Deliver the Phase 1 user-facing contract — create a full workspace fast, capture one domain through a minimal interview, and prove canonical ownership/validation from the command line.
Output: Init service, wired CLI commands, expected scaffold fixture, and integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-workspace-canonical-data-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-canonical-data-foundation/01-RESEARCH.md
@CONSTRUCT-spec/construct-prd.md
@CONSTRUCT-spec/construct-repo-topology.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement guided workspace init per D-01 through D-07</name>
  <read_first>
    src/construct/cli.py
    src/construct/schemas/config.py
    src/construct/schemas/workspace.py
    src/construct/storage/workspace.py
    .planning/phases/01-workspace-canonical-data-foundation/01-CONTEXT.md
  </read_first>
  <files>src/construct/services/init.py, tests/integration/test_init_cli.py, tests/fixtures/expected-workspace-tree.txt</files>
  <behavior>
    - Test 1: `construct init <path>` creates the full scaffold, including canonical and derived directories, on the first run.
    - Test 2: the guided interview asks only the essential inputs: domain id, display name, scope/description, taxonomy seeds, source priorities, and research seeds.
    - Test 3: the root registry and per-domain `domain.yaml` are both written, with the registry pointing at `domains/{domain_id}/domain.yaml`.
  </behavior>
  <action>Create `src/construct/services/init.py` as the only workspace-writing implementation. It must write the full scaffold from research, including `cards/`, `domains/`, `domains.yaml`, `connections.json`, `governance.yaml`, `model-routing.yaml`, `refs/`, `workflows/`, `log/events.jsonl`, `inbox/`, `digests/`, `db/`, `views/`, `publish/{articles,reports,drafts,exports}`, `.gitignore`, and `WORKSPACE.md` per D-01 and D-02. The guided prompt flow must ask only for the essentials per D-03/D-04: domain slug, display name, scope/description, taxonomy seeds, source priorities, and research seeds. Everything else uses template defaults. Write root `domains.yaml` as the canonical registry and `domains/{domain_id}/domain.yaml` as the domain-local detail file per D-05/D-06/D-07. Add `tests/fixtures/expected-workspace-tree.txt` listing the exact scaffold and write integration tests first in `tests/integration/test_init_cli.py`, then implement until they pass.</action>
  <acceptance_criteria>
    - `src/construct/services/init.py` writes both `domains.yaml` and `domains/{domain_id}/domain.yaml`
    - `src/construct/services/init.py` writes `.gitignore` entries for `db/` and `views/`
    - `src/construct/services/init.py` writes `WORKSPACE.md` explaining canonical vs derived paths
    - `python -m pytest tests/integration/test_init_cli.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/integration/test_init_cli.py -q</automated>
  </verify>
  <done>A new user can initialize a stable Phase 1 workspace with one guided domain and the long-term scaffold already in place.</done>
</task>

<task type="auto">
  <name>Task 2: Wire thin CLI commands for init, validate, and status</name>
  <read_first>
    src/construct/cli.py
    src/construct/services/init.py
    src/construct/services/validation.py
    tests/integration/test_init_cli.py
  </read_first>
  <files>src/construct/cli.py, tests/integration/test_init_cli.py</files>
  <action>Update `src/construct/cli.py` so it stays a thin Typer shell over services: add `@app.command()` handlers for `init`, `validate`, and `status`. `init` must call the init service with the guided prompt values. `validate` must print structured errors/warnings from the validation service and exit non-zero only when hard errors exist. `status` must summarize the workspace categories and explicitly label canonical files versus derived placeholders so users can inspect the contract required by CARD-04. Extend `tests/integration/test_init_cli.py` to assert that `validate` reports warnings without failing, that malformed registry/card fixtures fail with clear errors, and that `status` output names `cards`, `domains.yaml`, `db`, and `views` in the correct ownership categories.</action>
  <acceptance_criteria>
    - `src/construct/cli.py` contains `init`, `validate`, and `status` command handlers
    - `validate` exits non-zero only when the report has hard errors
    - `status` output includes both canonical and derived category labels
    - `python -m pytest tests/integration/test_init_cli.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/integration/test_init_cli.py -q</automated>
  </verify>
  <done>The user-facing Phase 1 CLI contract is complete and backed by integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → init service | User-supplied path and domain answers control filesystem writes. |
| init service → workspace files | The service creates canonical files that later phases will trust. |
| CLI output → user decisions | Validation/status output must accurately distinguish failures from warnings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | `src/construct/services/init.py` | mitigate | Normalize and resolve the target path before writing so `construct init` only creates files inside the requested workspace root. |
| T-01-08 | R | `src/construct/cli.py` | mitigate | Print validation findings with file paths and severity so users can audit why a command succeeded or failed. |
| T-01-09 | I | workspace templates written by init | accept | Default config files intentionally expose editable model names and env-var keys, but never actual credentials or private data. |
</threat_model>

<verification>
- `python -m pytest tests/integration/test_init_cli.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- `construct init <path>` creates the full scaffold and one configured domain.
- `construct validate <path>` clearly separates warnings from hard failures.
- `construct status <path>` makes canonical vs derived ownership inspectable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-canonical-data-foundation/01-04-SUMMARY.md`
</output>
