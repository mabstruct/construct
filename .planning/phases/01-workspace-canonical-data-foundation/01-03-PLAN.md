---
phase: 01-workspace-canonical-data-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/construct/storage/workspace.py
  - src/construct/services/validation.py
  - tests/fixtures/invalid/invalid-card-no-summary.md
  - tests/fixtures/invalid/domains-missing-path.yaml
  - tests/unit/test_validation_service.py
autonomous: true
requirements:
  - CARD-03
  - CARD-04
must_haves:
  truths:
    - "Malformed cards and config files return clear structural errors instead of silent failure."
    - "Non-blocking quality issues appear as warnings."
    - "Validation logic distinguishes canonical files from derived directories."
  artifacts:
    - path: "src/construct/storage/workspace.py"
      provides: "Workspace loading and category-aware path inspection"
      contains: "canonical"
    - path: "src/construct/services/validation.py"
      provides: "Structured validation report with errors and warnings"
      contains: "warnings"
    - path: "tests/unit/test_validation_service.py"
      provides: "Executable validation behavior coverage"
      contains: "invalid"
  key_links:
    - from: "src/construct/storage/workspace.py"
      to: "src/construct/schemas/workspace.py"
      via: "workspace load + scaffold validation"
      pattern: "required_paths"
    - from: "src/construct/services/validation.py"
      to: "src/construct/schemas/card.py"
      via: "card/config validation report"
      pattern: "Validation"
---

<objective>
Implement the workspace-loading and validation services that enforce the canonical contract and report errors/warnings clearly.

Purpose: Deliver CARD-03 and CARD-04 as reusable services before CLI wiring, so every future surface shares one validation path and one canonical-vs-derived interpretation.
Output: Workspace loader, validation service, invalid fixtures, and automated validation tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-workspace-canonical-data-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-canonical-data-foundation/01-RESEARCH.md
@CONSTRUCT-spec/construct-data-schemas.md
@CONSTRUCT-spec/knowledge-card-schema.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the category-aware workspace loader and validation report</name>
  <read_first>
    src/construct/schemas/card.py
    src/construct/schemas/config.py
    src/construct/schemas/workspace.py
    .planning/phases/01-workspace-canonical-data-foundation/01-RESEARCH.md
  </read_first>
  <files>src/construct/storage/workspace.py, src/construct/services/validation.py, tests/unit/test_validation_service.py</files>
  <behavior>
    - Test 1: a valid workspace returns zero errors and preserves any warning-only findings separately.
    - Test 2: malformed YAML, missing required files, and bad registry-to-domain-path links return hard errors.
    - Test 3: empty `db/` and `views/` directories do not count as canonical validation failures because they are derived placeholders, not missing source-of-truth data.
  </behavior>
  <action>Create `src/construct/storage/workspace.py` to discover workspace paths, classify them as canonical/support/derived using the Phase 1 research contract, and load root + per-domain files through the schema models from Plan 02. Create `src/construct/services/validation.py` with a structured report object that exposes `errors`, `warnings`, and per-file findings. Use hard errors for malformed YAML, missing required fields, invalid enums, invalid IDs/filenames, and broken domain-registry links per D-08/D-09. Use warnings for soft quality issues such as missing `## Summary` or unresolved `connects_to` targets per D-10. Write tests first in `tests/unit/test_validation_service.py`, then implement until they pass.</action>
  <acceptance_criteria>
    - `src/construct/services/validation.py` exposes both `errors` and `warnings`
    - `src/construct/storage/workspace.py` contains canonical/support/derived category logic
    - `tests/unit/test_validation_service.py` asserts derived directories are not treated as canonical source files
    - `python -m pytest tests/unit/test_validation_service.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_validation_service.py -q</automated>
  </verify>
  <done>The validation service can be reused by CLI and later runtime paths without duplicating schema/file rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add negative fixtures that prove clear failure modes</name>
  <read_first>
    tests/unit/test_validation_service.py
    CONSTRUCT-spec/knowledge-card-schema.md
    .planning/phases/01-workspace-canonical-data-foundation/01-CONTEXT.md
  </read_first>
  <files>tests/fixtures/invalid/invalid-card-no-summary.md, tests/fixtures/invalid/domains-missing-path.yaml, tests/unit/test_validation_service.py</files>
  <action>Create invalid fixture files that exercise the promised error/warning behavior. `tests/fixtures/invalid/invalid-card-no-summary.md` must be a structurally valid card missing the `## Summary` section so it yields a warning, not a hard failure, per D-10. `tests/fixtures/invalid/domains-missing-path.yaml` must point the root registry at a missing `domains/{domain_id}/domain.yaml` file so it yields a hard error per D-08/D-09. Extend `tests/unit/test_validation_service.py` to assert exact error/warning classification and include the affected file path in the report output.</action>
  <acceptance_criteria>
    - `invalid-card-no-summary.md` lacks `## Summary`
    - `domains-missing-path.yaml` references a missing per-domain file
    - `tests/unit/test_validation_service.py` checks warning vs error classification explicitly
    - `python -m pytest tests/unit/test_validation_service.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_validation_service.py -q</automated>
  </verify>
  <done>The promised validation posture is executable, not just documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user workspace files → validation service | Untrusted markdown/YAML content is parsed and classified. |
| validation service → future CLI/runtime surfaces | Downstream callers trust the report semantics to decide whether work may continue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | `src/construct/services/validation.py` | mitigate | Return explicit per-file findings instead of swallowing parse failures so malformed canonical files cannot pass silently. |
| T-01-06 | D | `src/construct/storage/workspace.py` | mitigate | Treat `db/` and `views/` as derived placeholders so missing rebuildable content does not block canonical validation or cause false failures. |
| T-01-07 | R | test fixtures | mitigate | Assert exact warning/error classification in pytest so regressions are caught automatically. |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_validation_service.py -q`
- `python -m pytest tests/unit -q`
</verification>

<success_criteria>
- Validation logic emits structured errors and warnings.
- Canonical files and derived directories are distinguished consistently.
- Negative fixtures prove the Phase 1 validation promises.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-canonical-data-foundation/01-03-SUMMARY.md`
</output>
