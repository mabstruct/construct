---
phase: 01-workspace-canonical-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/construct/__init__.py
  - src/construct/cli.py
  - tests/conftest.py
  - tests/unit/test_bootstrap.py
autonomous: true
requirements:
  - WORK-01
must_haves:
  truths:
    - "The repository installs an executable `construct` CLI entrypoint."
    - "Phase 1 work can run repeatable automated tests before feature code lands."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project metadata, pytest config, and CLI entrypoint"
      contains: "construct = \"construct.cli:main\""
    - path: "src/construct/cli.py"
      provides: "Typer application shell for later commands"
      contains: "app = typer.Typer"
    - path: "tests/conftest.py"
      provides: "Shared temporary-workspace fixtures"
      contains: "tmp_path"
  key_links:
    - from: "pyproject.toml"
      to: "src/construct/cli.py"
      via: "project.scripts entrypoint"
      pattern: "construct\.cli:main"
---

<objective>
Bootstrap the Python package, executable CLI shell, and test harness required for the workspace foundation plans.

Purpose: Give the later schema/init plans a working package, repeatable pytest loop, and a stable `construct` command surface instead of building Phase 1 on ad-hoc scripts.
Output: Installable package metadata, Typer CLI shell, and bootstrap tests/fixtures.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CONSTRUCT-spec/adrs/adr-0001-python-first-drop-openclaw.md
@CONSTRUCT-spec/construct-repo-topology.md
@CONSTRUCT-spec/construct-development-strategy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Python package and CLI shell</name>
  <read_first>
    .planning/PROJECT.md
    .planning/ROADMAP.md
    CONSTRUCT-spec/adrs/adr-0001-python-first-drop-openclaw.md
    CONSTRUCT-spec/construct-repo-topology.md
  </read_first>
  <files>pyproject.toml, src/construct/__init__.py, src/construct/cli.py</files>
  <action>Create the installable Python project shell for Phase 1. Add `pyproject.toml` with `[project] name = "construct"`, `requires-python = ">=3.11"`, dependencies for `typer`, `pydantic`, `ruamel.yaml`, and `pytest`, and `[project.scripts] construct = "construct.cli:main"`. Create `src/construct/__init__.py` with a version constant. Create `src/construct/cli.py` with `app = typer.Typer(no_args_is_help=True)` plus a `main()` wrapper that invokes the Typer app; leave command implementations thin so later plans can register `init`, `validate`, and `status` without replacing the shell.</action>
  <acceptance_criteria>
    - `pyproject.toml` contains `[project]`, `name = "construct"`, and `[project.scripts]`
    - `pyproject.toml` contains `construct = "construct.cli:main"`
    - `src/construct/cli.py` contains `app = typer.Typer(` and `def main()`
    - `src/construct/__init__.py` exports a `__version__` value
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_bootstrap.py -q</automated>
  </verify>
  <done>The package is installable in editable mode and exposes a `construct` CLI shell that later plans can extend.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared pytest fixtures and bootstrap assertions</name>
  <read_first>
    pyproject.toml
    src/construct/cli.py
    CONSTRUCT-spec/construct-development-strategy.md
  </read_first>
  <files>tests/conftest.py, tests/unit/test_bootstrap.py</files>
  <action>Create the first automated test loop for Phase 1. In `tests/conftest.py`, add reusable fixtures for temporary workspace paths and CLI runner setup. In `tests/unit/test_bootstrap.py`, assert that `construct.cli:main` is importable, the Typer app object exists, and the project metadata points at the same entrypoint. Keep the test file focused on bootstrap invariants so later plans can safely depend on `python -m pytest` as their verify command.</action>
  <acceptance_criteria>
    - `tests/conftest.py` defines at least one temporary workspace fixture
    - `tests/unit/test_bootstrap.py` imports `construct.cli`
    - `tests/unit/test_bootstrap.py` asserts the Typer app and entrypoint wrapper exist
    - `python -m pytest tests/unit/test_bootstrap.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_bootstrap.py -q</automated>
  </verify>
  <done>A repeatable pytest loop exists before schema and init work begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → CLI entrypoint | Untrusted command-line input reaches the Python package entrypoint. |
| test runner → filesystem fixtures | Test helpers create temporary directories and must stay inside controlled paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/construct/cli.py` | mitigate | Keep bootstrap CLI free of implicit filesystem writes and route all future side effects through explicit commands only. |
| T-01-02 | D | `tests/conftest.py` | mitigate | Use pytest temporary directories instead of hard-coded paths so test runs cannot clobber repo files. |
| T-01-03 | R | `pyproject.toml` | accept | Package metadata itself is low-risk; later command implementations provide auditable behavior. |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_bootstrap.py -q`
- `python -m pytest tests/unit -q`
</verification>

<success_criteria>
- The repo exposes a `construct` CLI entrypoint.
- Bootstrap tests pass without requiring any later-plan code.
- Later plans can add command implementations without replacing the package shell.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-canonical-data-foundation/01-01-SUMMARY.md`
</output>
