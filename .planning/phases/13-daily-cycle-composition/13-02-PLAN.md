---
phase: 13-daily-cycle-composition
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - tests/contract/test_daily_run_cli_mcp.py
  - src/construct/capabilities/catalog.py
  - src/construct/cli.py
autonomous: true
requirements: [API-01, API-02, API-03]
must_haves:
  truths:
    - "D-08: daily.run and daily.inspect are registered in the capability registry with Pydantic I/O schemas, handler, cli_name (construct daily run), and mcp_tool_name (construct_daily_run) — mirroring the research.run/curation.run siblings; the deleted workflow.run group is NOT revived"
    - "Both capabilities are invokable through the same registry-backed handler from the construct CLI and the stdio MCP server"
    - "D-08: The MCP tools construct_daily_run / construct_daily_inspect are auto-discovered from the registry — mcp/server.py is never edited (guarded by test_mcp_no_hardcoded_daily)"
    - "CLI --json output keys, MCP-serialized keys, and DailyRunResult.model_fields agree (schema/result parity)"
    - "A degraded daily run still exits 0; only a hard failed maps to success=False (exit-code contract)"
  artifacts:
    - path: "src/construct/capabilities/catalog.py"
      provides: "daily.run + daily.inspect CapabilityRecords and _daily_run_shim/_daily_inspect_shim/_daily_result_to_operation"
      contains: "id=\"daily.run\""
    - path: "src/construct/cli.py"
      provides: "daily Typer sub-app (run/inspect) + _render_daily_result/_emit_daily_result"
      contains: "daily_app"
    - path: "tests/contract/test_daily_run_cli_mcp.py"
      provides: "registration + CLI/MCP parity + no-hardcoded-MCP contract tests (API-01/02/03)"
      contains: "def test_mcp_no_hardcoded_daily"
  key_links:
    - from: "src/construct/capabilities/catalog.py"
      to: "construct.llm.daily_run.run_daily_run / inspect_daily_run"
      via: "shim handler wrapping DailyRunResult into OperationResult"
      pattern: "run_daily_run\\(DailyRunInput"
    - from: "src/construct/cli.py"
      to: "get_registry().get('daily.run')"
      via: "daily run command dispatches through the registry handler"
      pattern: "daily.run"
    - from: "registry auto-discovery"
      to: "construct_daily_run / construct_daily_inspect MCP tools"
      via: "mcp_tool_name on the records (no server.py edit)"
      pattern: "construct_daily_run"
---

<objective>
Register `daily.run` + `daily.inspect` in the capability registry and expose a `daily` Typer sub-app, cloning the curation quartet mechanically. MCP parity is FREE via registry auto-discovery — `mcp/server.py` is never touched. Prove API-01/02/03 with a contract test cloned from the curation contract test.

Purpose: Close API-01 (registered with Pydantic schemas + handler + CLI + MCP metadata), API-02 (invokable via the same registry-backed handler from CLI + stdio MCP), and API-03 (CLI/MCP schema+result parity) for the daily-cycle capabilities.
Output: modified `catalog.py` + `cli.py`, new `tests/contract/test_daily_run_cli_mcp.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-daily-cycle-composition/13-RESEARCH.md
@.planning/phases/13-daily-cycle-composition/13-PATTERNS.md
@.planning/phases/13-daily-cycle-composition/13-01-SUMMARY.md

<interfaces>
<!-- Produced by Plan 13-01 (construct.llm.daily_run). Consume directly — no exploration. -->
From src/construct/llm/daily_run.py:
  class DailyRunInput(BaseModel)      # workspace_path: str, run_id: str | None = None
  class DailyInspectInput(BaseModel)  # workspace_path: str, run_id: str
  class DailyRunResult(BaseModel)     # status/run_id/children/pending_escalations/graph_health/message
  def run_daily_run(inp: DailyRunInput) -> DailyRunResult
  def inspect_daily_run(inp: DailyInspectInput) -> DailyRunResult

<!-- Curation analog locations (verified live 2026-07-06) -->
catalog.py: curation import block L60-66; records L432-452; shims _curation_result_to_operation L594-615, _curation_run_shim L618-624, _curation_inspect_shim L627-633.
cli.py: curation_app L368-373; _render_curation_result L654-671; _emit_curation_result L674-685; curation_run_cmd L688-701; curation_inspect_cmd L704-718.
mcp/server.py L23-28 iterates registry.list_mcp_tools() (auto-discovery). registry.py L24-61 get_by_mcp_name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the daily contract test (RED for registration, GREEN guard for MCP)</name>
  <files>tests/contract/test_daily_run_cli_mcp.py</files>
  <read_first>
    - tests/contract/test_curation_run_cli_mcp.py — verbatim clone target (145 lines): test_registered L47-53, test_shims_reject_positional_args L56-60, test_in_mcp_tool_list L66-68, test_mcp_server_exposes_curation L71-75, test_mcp_no_hardcoded_curation L78-84, test_cli_commands_present L90-96, test_cli_mcp_schema_parity L120-144
    - src/construct/mcp/server.py L23-28 (auto-discovery — the invariant test_mcp_no_hardcoded_daily protects)
    - src/construct/llm/daily_run.py — DailyRunResult.model_fields (the parity target for test_cli_mcp_schema_parity)
    - tests/llm/conftest.py — create_test_workspace L169 + build_chat_model seam L156 (offline-degrade path for the parity test)
  </read_first>
  <action>
    Create `tests/contract/test_daily_run_cli_mcp.py` cloning `test_curation_run_cli_mcp.py`, retargeting curation→daily. Set `_CAPS = {"daily.run": "construct_daily_run", "daily.inspect": "construct_daily_inspect"}`. Clone: `test_registered` (input_model set + mcp_tool_name/cli_name correct), `test_shims_reject_positional_args` (shim `*args` TypeError guard), `test_in_mcp_tool_list` + `test_mcp_server_exposes_daily`, `test_mcp_no_hardcoded_daily` (assert `"construct_daily_run"` AND `"daily.run"` do NOT appear in `Path(mcp_server.__file__).read_text()`), `test_cli_commands_present` (`daily run --help` / `daily inspect --help` exit 0), and `test_cli_mcp_schema_parity` (offline via `monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)`: CLI `--json` keys == `_serialize_result(handler_result)` keys == `DailyRunResult.model_fields.keys()`). Reuse `from tests.llm.conftest import create_test_workspace`. Do NOT clone `test_no_placeholder_curation_path`; do NOT reference a `daily review` command (D-01 — no parent pause). At creation time only `test_mcp_no_hardcoded_daily` is expected GREEN; the registration/CLI tests are RED until Tasks 2-3 land.
  </action>
  <verify>
    <automated>python -m pytest tests/contract/test_daily_run_cli_mcp.py::test_mcp_no_hardcoded_daily -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/contract/test_daily_run_cli_mcp.py::test_mcp_no_hardcoded_daily -x -q` exits 0 (guard GREEN before any registration exists)
    - `python -m pytest tests/contract/test_daily_run_cli_mcp.py --co -q` collects test_registered, test_shims_reject_positional_args, test_in_mcp_tool_list, test_mcp_server_exposes_daily, test_mcp_no_hardcoded_daily, test_cli_commands_present, test_cli_mcp_schema_parity
    - `python -m pytest tests/contract/test_daily_run_cli_mcp.py::test_registered -q` is RED (daily.run not yet registered)
    - File contains no reference to a `daily review` command or `test_no_placeholder`
  </acceptance_criteria>
  <done>Contract test exists, collects the seven named tests, test_mcp_no_hardcoded_daily is GREEN, registration/CLI tests are RED pending Tasks 2-3.</done>
</task>

<task type="auto">
  <name>Task 2: Register daily.run + daily.inspect records and shims in catalog.py</name>
  <files>src/construct/capabilities/catalog.py</files>
  <read_first>
    - src/construct/capabilities/catalog.py — curation import block L60-66; curation.run/inspect records L432-452; _curation_result_to_operation L594-615; _curation_run_shim L618-624; _curation_inspect_shim L627-633; workflow.run removal comment ~L311-315 (DO NOT revive)
    - src/construct/llm/daily_run.py — DailyRunInput/DailyInspectInput/run_daily_run/inspect_daily_run (import targets)
    - .planning/phases/13-daily-cycle-composition/13-PATTERNS.md — catalog.py section A–D + RESEARCH L233-263 (record + shim clone)
  </read_first>
  <action>
    Add a `# ── Daily Run imports (Phase 13) ──` import block: `from construct.llm.daily_run import (DailyInspectInput, DailyRunInput, inspect_daily_run, run_daily_run)`. Register two `CapabilityRecord`s in the registration function mirroring the curation records: `id="daily.run"`, name "Daily Run", description covering "compose research.run → curation.run → graph.status, non-blocking, auto-applies gate recommendations, excludes escalate", `input_model=DailyRunInput`, `output_model=OperationResult`, `handler=_daily_run_shim`, `cli_name="daily.run"`, `mcp_tool_name="construct_daily_run"`; and `id="daily.inspect"`, `input_model=DailyInspectInput`, `output_model=OperationResult`, `handler=_daily_inspect_shim`, `cli_name="daily.inspect"`, `mcp_tool_name="construct_daily_inspect"`. Add ZERO `daily.review` record (D-01 — no parent pause). Clone the shims: `_daily_result_to_operation(cap_id, runner)` (try/except → `OperationResult(success=False, message=f"{cap_id} failed: {type(exc).__name__}", data={"failed": True})`; success path `success = result.status != "failed"` — the degraded-exits-0 contract), `_daily_run_shim(*args, **kwargs)` (guard `if args: raise TypeError("daily.run handler requires keyword arguments")` → `run_daily_run(DailyRunInput(**kwargs))`), `_daily_inspect_shim` (analogous → `inspect_daily_run(DailyInspectInput(**kwargs))`). Do NOT define any Pydantic model here (circular-import discipline). Do NOT edit mcp/server.py.
  </action>
  <verify>
    <automated>python -m pytest tests/contract/test_daily_run_cli_mcp.py::test_registered tests/contract/test_daily_run_cli_mcp.py::test_shims_reject_positional_args tests/contract/test_daily_run_cli_mcp.py::test_in_mcp_tool_list tests/contract/test_daily_run_cli_mcp.py::test_mcp_server_exposes_daily tests/contract/test_daily_run_cli_mcp.py::test_mcp_no_hardcoded_daily -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'id="daily.run"\|id="daily.inspect"\|mcp_tool_name="construct_daily_run"\|mcp_tool_name="construct_daily_inspect"\|cli_name="daily.run"' src/construct/capabilities/catalog.py` returns all five
    - `grep -c 'id="daily.review"' src/construct/capabilities/catalog.py` returns 0 (no parent review — D-01)
    - `git diff --name-only src/construct/mcp/server.py` shows no change (server.py untouched — API-02 invariant)
    - test_registered, test_shims_reject_positional_args, test_in_mcp_tool_list, test_mcp_server_exposes_daily, test_mcp_no_hardcoded_daily all GREEN
  </acceptance_criteria>
  <done>daily.run + daily.inspect registered with schemas/handler/cli_name/mcp_tool_name and keyword-only shims; registration + MCP-discovery contract tests GREEN; mcp/server.py unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add the daily Typer sub-app (run/inspect) and prove CLI/MCP parity</name>
  <files>src/construct/cli.py</files>
  <read_first>
    - src/construct/cli.py — curation_app declaration L368-373; _render_curation_result L654-671; _emit_curation_result L674-685; curation_run_cmd L688-701; curation_inspect_cmd L704-718
    - src/construct/capabilities/catalog.py — daily.run/daily.inspect records (registered in Task 2; the handler the CLI dispatches to)
    - .planning/phases/13-daily-cycle-composition/13-PATTERNS.md — cli.py section A–C + RESEARCH L266-273 (render + commands clone)
  </read_first>
  <action>
    Declare `daily_app = typer.Typer(no_args_is_help=True, name="daily", help="Run the non-blocking daily maintenance cycle (research → curation → graph health).")` and `app.add_typer(daily_app)`, mirroring `curation_app`. Add `_render_daily_result(data)` + `_emit_daily_result(result, json_output)` mirroring the curation render/emit pair — the renderer prints parent `status`/`run_id`, one line per child (capability + status), the `pending_escalations` count, and a graph-health line (cards/connections/domains from `graph_health`). Add `@daily_app.command(name="run")` mirroring `curation_run_cmd` (options `--workspace/-w`, `--json/-j`; look up `get_registry().get("daily.run")`; call `cap.handler(workspace_path=str(workspace))`; `_emit_daily_result`). Add `@daily_app.command(name="inspect")` mirroring `curation_inspect_cmd` (options `--workspace/-w`, required `--run-id`, `--json/-j`; `cap.handler(workspace_path=str(workspace), run_id=run_id)`). Do NOT add a `review` command (D-01). Dispatch ONLY through the registry handler (no direct `run_daily_run` import in cli.py).
  </action>
  <verify>
    <automated>python -m pytest tests/contract/test_daily_run_cli_mcp.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `construct daily --help` lists `run` and `inspect` (and NOT `review`)
    - `construct daily run --help` and `construct daily inspect --help` both exit 0 (test_cli_commands_present GREEN)
    - test_cli_mcp_schema_parity GREEN: CLI `--json` keys == MCP-serialized keys == `DailyRunResult.model_fields.keys()` (API-03)
    - `grep -c "from construct.llm.daily_run import\|run_daily_run(" src/construct/cli.py` returns 0 (CLI dispatches via registry, not direct import)
    - Full `python -m pytest tests/contract/test_daily_run_cli_mcp.py -q` exits 0 (all seven tests GREEN)
  </acceptance_criteria>
  <done>daily sub-app exposes run + inspect through the registry handler; the full daily contract test suite is GREEN, proving API-01/02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP client → registry handler | Untrusted workspace_path/run_id arrive as keyword args; shim rejects positional args |
| capability registry → MCP tool surface | New tools must appear ONLY via auto-discovery, not by editing the transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | MCP surface drift — hardcoding `daily.run`/`construct_daily_run` into mcp/server.py | mitigate | MCP parity is auto-discovered from the registry (`create_server` iterates `list_mcp_tools()`, server.py L23-28). server.py is NEVER edited; guarded by test_mcp_no_hardcoded_daily and a `git diff` acceptance check. |
| T-13-06 | Tampering | Positional-arg invocation bypassing keyword contract | mitigate | `if args: raise TypeError(...)` guard in `_daily_run_shim`/`_daily_inspect_shim`; asserted by test_shims_reject_positional_args. |
| T-13-07 | Information Disclosure | Shim leaking raw exception text on child failure | mitigate | `_daily_result_to_operation` reports `type(exc).__name__` only (clone of curation shim L608); no raw provider text in the OperationResult message. |
| T-13-SC | Tampering | package installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `python -m pytest tests/contract/test_daily_run_cli_mcp.py -q` GREEN (API-01/02/03).
- `git diff` shows `src/construct/mcp/server.py` unchanged.
- `construct daily --help` exposes run + inspect only.
</verification>

<success_criteria>
- daily.run + daily.inspect registered with full metadata (API-01).
- Invokable via registry handler from CLI + auto-discovered MCP, server.py untouched (API-02).
- CLI/MCP schema+result parity proven (API-03).
- Degraded-exits-0 exit-code contract preserved via the shim.
</success_criteria>

<output>
Create `.planning/phases/13-daily-cycle-composition/13-02-SUMMARY.md` when done.
</output>
