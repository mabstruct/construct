---
phase: 13-daily-cycle-composition
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md
autonomous: true
requirements: [API-05]
must_haves:
  truths:
    - "The daily-cycle workflow doc invokes `construct daily run` (the real capability), not the deleted `construct workflow run daily-cycle`"
    - "The daily-cycle skill owns the SINGLE post-run views refresh after the cycle completes (D-10) — the capability itself does no views work"
    - "No dangling references to the removed workflow run/resume command group remain in the daily-cycle doc"
    - "The full v0.3 + v0.4 pytest suite is green after the additive daily.run capability lands (no regressions)"
  artifacts:
    - path: "CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md"
      provides: "Updated daily-cycle skill hook: invokes construct daily run + owns single post-run views refresh (D-10)"
      contains: "construct daily run"
  key_links:
    - from: "CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md"
      to: "construct daily run"
      via: "workflow execution step replacing the removed workflow-runner command"
      pattern: "construct daily run"
    - from: "CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md"
      to: "views regeneration (construct views generate)"
      via: "single post-run refresh owned by the skill, not the capability (D-10)"
      pattern: "views generate"
---

<objective>
Close the daily-cycle at the skill layer and prove v0.3/v0.4 compatibility. Update the daily-cycle workflow doc to invoke the real `construct daily run` capability and own the single post-run views refresh (D-10), then run the full pytest suite as the API-05 regression gate.

Purpose: Complete DAY-01's "user can run a daily-cycle workflow" story end-to-end (the skill currently points at the DELETED `construct workflow run daily-cycle`), keep views-refresh a skill hook per D-10, and prove API-05 (existing v0.3 CLI/MCP/Streamlit/validation/ingestion/graph/ask-domain behavior still passes) with a full-suite-green gate.
Output: updated `CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md`; full `pytest` suite green (~390 baseline + the new daily tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-daily-cycle-composition/13-CONTEXT.md
@.planning/phases/13-daily-cycle-composition/13-02-SUMMARY.md

<notes>
D-10: views-refresh is EXCLUDED from the `daily.run` capability and stays a skill-level hook — the daily-cycle skill triggers a single views regeneration after the run completes. The capability (Plan 13-01) does NO views work.
Phase 12 (D-10/CUR-05) DELETED the `workflow run` / `workflow resume` command group. The daily-cycle doc still references `construct workflow run daily-cycle` (L38) and `construct workflow resume` (L146) — both dangling. The real entrypoint is `construct daily run` (Plan 13-02).
Only edit the SOURCE template `CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md`. Do NOT edit the generated per-workspace copies under `test-ws/**/.construct/workflows/daily-cycle.md` (fixtures).
API-05 is an additive-only change — no edits to mcp/server.py, child modules, or existing records — so the full suite should stay green. Any red test is a regression to fix, not a baseline to weaken.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point the daily-cycle skill at construct daily run + own the single views refresh (D-10)</name>
  <files>CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md — current doc: execution step L35-38 (`construct workflow run daily-cycle` — removed), views-refresh notes L68 / L101-102, error-handling L145-146 (`construct workflow resume` — removed)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-curation-cycle/SKILL.md — L119-125 (the established "skill owns the single post-run `construct views generate`" pattern to mirror)
    - .planning/phases/13-daily-cycle-composition/13-CONTEXT.md — D-08 (capability id/CLI/MCP), D-09 (compose order), D-10 (views-refresh is a skill hook)
  </read_first>
  <action>
    Update `CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md` so the execution step invokes `construct daily run --workspace . --json` (the composed research → curation → graph.status cycle) in place of the removed `construct workflow run daily-cycle`. Keep the views-refresh as a SKILL-owned hook (D-10): after `construct daily run` completes, if `views/build/` exists at the install root AND `.construct/config.yaml` does not set `views.auto_regenerate: false`, run `construct views generate --workspace .` exactly ONCE — mirror the curation-cycle skill's L119-125 wording. Remove/replace the dangling `construct workflow resume` reference in the error-handling section: the daily cycle is non-blocking (D-01) so there is no parent resume — optional review of pending items happens via the children's own `construct research review` / `construct curation review` on a fresh cycle. Do NOT describe any views work inside the capability. Do NOT edit the generated copies under `test-ws/`.
  </action>
  <verify>
    <automated>grep -q "construct daily run" CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md && grep -q "views generate" CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md && ! grep -q "workflow run daily-cycle" CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md && ! grep -q "workflow resume" CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md && echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "construct daily run" CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md` returns >= 1
    - `grep -c "views generate" CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md` returns >= 1 (single post-run refresh, D-10)
    - `grep -c "workflow run daily-cycle\|workflow resume" CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md` returns 0 (no dangling deleted-command refs)
    - No file under `test-ws/` is modified (`git status --porcelain test-ws | grep daily-cycle` is empty)
  </acceptance_criteria>
  <done>The daily-cycle doc invokes `construct daily run`, owns the single post-run views refresh (D-10), and carries no references to the removed workflow run/resume group.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression gate (API-05)</name>
  <files>tests/ (no edits — regression run over the whole suite)</files>
  <read_first>
    - .planning/phases/13-daily-cycle-composition/13-RESEARCH.md — "Validation Architecture" (sampling rate: phase gate = full `pytest` green) + Assumption A4 (API-05 proof = additive change + full-suite green, ~390 baseline)
    - .planning/phases/13-daily-cycle-composition/13-VALIDATION.md — Per-Task Verification Map + full-suite gate row
    - tests/contract/test_mcp_contracts.py — the v0.3 CLI/MCP compat contract that must remain green
  </read_first>
  <action>
    Run the full test suite `python -m pytest -q` and confirm it is green. This is the API-05 regression proof: the daily.run capability is additive (new module + two records + a new CLI sub-app + a new contract test; mcp/server.py and all child modules unchanged), so the pre-existing v0.3+v0.4 suite must continue to pass alongside the new daily tests. If any pre-existing test is red, diagnose whether the daily additions caused it (e.g. an inadvertent import-time side effect in catalog.py) and fix the regression at its source — do NOT modify or skip the failing baseline test to make it pass. Record the final green count in the SUMMARY.
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q` exits 0 with zero failures/errors (full v0.3 + v0.4 suite green)
    - The green total is >= the ~390 pre-phase baseline PLUS the new daily unit + contract tests (no tests deleted or skipped to pass)
    - `git diff --name-only` shows no change to `src/construct/mcp/server.py`, `src/construct/llm/research_run.py`, or `src/construct/llm/curation_run.py` (additive-only invariant)
    - `tests/contract/test_mcp_contracts.py` is included in the green run (v0.3 CLI/MCP compat preserved)
  </acceptance_criteria>
  <done>Full `pytest -q` is green; the daily capability is proven additive with no v0.3/v0.4 regressions (API-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill doc → runtime capability | The skill instructs invocation of `construct daily run`; it must not re-describe capability-owned logic or point at removed commands |
| additive change → existing suite | New capability must not perturb v0.3/v0.4 behavior (regression surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering | Editing the generated per-workspace `test-ws/**/daily-cycle.md` fixtures instead of the source template | mitigate | Task edits only `CONSTRUCT-CLAUDE-impl/construct/workflows/daily-cycle.md`; acceptance check asserts no `test-ws/` daily-cycle file is modified. |
| T-13-09 | Tampering/Repudiation | Weakening or skipping a pre-existing test to force API-05 green | mitigate | Regressions fixed at source only; acceptance requires no deleted/skipped tests and no child-module/server.py diffs; full green count must exceed the baseline. |
| T-13-SC | Tampering | package installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Daily-cycle doc invokes `construct daily run` + owns single post-run views refresh; no removed-command references.
- `python -m pytest -q` GREEN across the whole suite (API-05).
- mcp/server.py + child modules unchanged (additive-only).
</verification>

<success_criteria>
- D-10 skill hook complete: skill invokes the real capability and owns the single views refresh (DAY-01 end-to-end).
- Full v0.3 + v0.4 suite green with the additive daily capability in place (API-05).
</success_criteria>

<output>
Create `.planning/phases/13-daily-cycle-composition/13-03-SUMMARY.md` when done.
</output>
