---
phase: 10-durable-human-review-research-run
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/construct/llm/research_run.py
  - tests/llm/test_research_run.py
autonomous: true
requirements: [RSCH-02, RSCH-03, RSCH-04]
user_setup: []

must_haves:
  truths:
    - "build_research_run_graph compiles a StateGraph with a SqliteSaver checkpointer and the locked topology"
    - "the gate_review node contains ONLY interrupt() — no writes, no events, no non-idempotent prep"
    - "invoking research.run pauses at gate_review returning __interrupt__ with gate_id + per-finding gate_queue (default decision = ingest_action)"
    - "at the paused state NO refs/cards/digests are written and search-seeds.json last_queried is untouched"
    - "score_and_extract catches ResearchScoreOutageError BEFORE the gate → status failed, never pauses"
  artifacts:
    - path: "src/construct/llm/research_run.py"
      provides: "ResearchRunState, I/O models, pre-gate nodes, interrupt gate, graph builder, checkpointer, run-start runner"
      exports: ["build_research_run_graph", "ResearchRunInput", "RunResult"]
      contains: "def gate_review"
  key_links:
    - from: "src/construct/llm/research_run.py"
      to: "construct.llm.research_score.run_gate"
      via: "score_and_extract node calls run_gate('research.score', ...)"
      pattern: "run_gate"
    - from: "src/construct/llm/research_run.py"
      to: "construct.pipelines.research_dedup.normalize_url"
      via: "deduplicate node filters against refs/ + rejected ledger"
      pattern: "normalize_url"
    - from: "build_research_run_graph"
      to: "langgraph.checkpoint.sqlite.SqliteSaver"
      via: "compile(checkpointer=...)"
      pattern: "checkpointer"
---

<objective>
Build the read-side of the durable LangGraph workflow: the `ResearchRunState` channel + in-module I/O models, the pre-gate nodes (`load_config`, `build_queries`, `execute_search`, `deduplicate`, `score_and_extract`), the interrupt-only `gate_review` node, `build_research_run_graph(checkpointer)`, the `_open_checkpointer` wiring under `.construct/`, and a run-start runner that pauses at the gate. Post-gate write nodes exist as skeletons here (the graph must compile and pause); Plan 04 implements their bodies.

Purpose: RSCH-03 (no writes before approval) holds BY CONSTRUCTION — the interrupt is the durability boundary and the gate node re-executes top-to-bottom on resume (RESEARCH Pitfall 1, empirically confirmed), so it must contain only `interrupt()`.
Output: A compiling, checkpointed graph that pauses at a real `interrupt()` with the per-finding `gate_queue`, plus the pre-gate composition of Phase 8 search and Phase 9 scoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-durable-human-review-research-run/10-CONTEXT.md
@.planning/phases/10-durable-human-review-research-run/10-RESEARCH.md
@.planning/phases/10-durable-human-review-research-run/10-PATTERNS.md

<interfaces>
From src/construct/llm/ask_domain.py (the StateGraph idiom to extend):
  - class AskDomainState(TypedDict): ...        # TypedDict, plain serializable data only
  - def build_ask_domain_graph() -> StateGraph: # add_node + linear add_edge + .compile()  (lines 315-335)

From src/construct/llm/research_score.py (the L3 gate to compose, read-only):
  - run_gate(gate_id, input_data: ResearchScoreInput, *, config_path=None) -> ResearchScoreGateOutput
  - class ResearchScoreInput: workspace_path: str; results: list[SearchResult]; provider_override: str | None
  - class ResearchScoreGateOutput: findings: list[ScoredFinding]; gate: GateMetadata; retrieval: dict
  - class ScoredFinding: ... ingest_action: IngestAction (skip|ref_only|ref_and_card)
  - class ResearchScoreOutageError(Exception): .safe_message    (total-outage signal)
  - retrieval dict carries ["degraded"], ["retried"], ["errors"]  (Phase 9 D-08/09 → digest notice)

From src/construct/pipelines/research_search.py:
  - research_search(...) provider-agnostic search (read-only)  — execute_search node seam

From src/construct/pipelines/research_dedup.py (Plan 02):
  - normalize_url, ref_id_for, title_is_near_dup, load_rejected_ledger, rejected_normalized_urls

From src/construct/storage/workspace.py:
  - WorkspaceLoader(path).load_search_seeds() -> SearchSeedsFile   (active SearchCluster, status, last_queried)
  - WorkspaceLoader(path).load_governance().research               (relevance_threshold, card_creation_threshold, max_papers_per_cycle)

From langgraph (installed via Plan 01):
  - from langgraph.graph import StateGraph, START, END
  - from langgraph.types import interrupt, Command
  - from langgraph.checkpoint.sqlite import SqliteSaver
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State schema, I/O models, and pre-gate nodes</name>
  <read_first>
    - src/construct/llm/ask_domain.py (lines 1-49 imports + AskDomainState TypedDict; lines 119-140 per-node return-dict + MCP-safe logging; lines 298-309 WR-04 stderr-only)
    - src/construct/llm/research_score.py (lines 79-115 ResearchScoreInput/Output; 326-389 ResearchScoreOutageError + _safe_scoring_cause; 535-560 run_gate)
    - src/construct/pipelines/research_dedup.py (Plan 02 — normalize_url, ref_id_for, title_is_near_dup, load_rejected_ledger, rejected_normalized_urls)
    - src/construct/storage/workspace.py (lines 113-117 load_search_seeds) + src/construct/schemas/config.py (lines 204-233 SearchCluster/SearchSeedsFile)
    - .planning/phases/10-durable-human-review-research-run/10-RESEARCH.md §Reused Outputs Behind the Gate + §Architecture Patterns (topology)
    - .planning/phases/10-durable-human-review-research-run/10-PATTERNS.md (research_run.py section, lines ~28-135)
  </read_first>
  <behavior>
    - build_queries reads active SearchSeedsFile clusters (status == active), respects governance cap max_papers_per_cycle, and yields a deterministic query list; paused/exhausted clusters are excluded.
    - deduplicate drops any candidate whose normalize_url is in {normalize_url(ref.url) for refs/*.json} ∪ rejected_normalized_urls(ledger) ∪ earlier-in-batch; plus a title fuzzy secondary pass (title_is_near_dup >= 0.90) against existing ref titles.
    - score_and_extract calls research_score.run_gate("research.score", ResearchScoreInput(...)); on ResearchScoreOutageError it returns a failed/degraded marker in state and the graph must NOT reach the interrupt; the retrieval degraded/retried/errors signal is carried into state for the digest.
  </behavior>
  <action>
    Create `src/construct/llm/research_run.py`. Define `ResearchRunState(TypedDict)` holding ONLY plain serializable data (RESEARCH Pitfall 3 — no WorkspaceLoader/LLM client/sqlite conn in channels): inputs (workspace_path, run_id, gate_id, provider_override), processing (queries, search_results, deduped, findings, retrieval, gate_queue, decisions), outputs (status, ingested, digest_path, seed_update, events). Define in-module Pydantic models `ResearchRunInput`, `ReviewInput`, `InspectInput`, `RunResult`, and a per-finding `GateQueueEntry` (finding payload + default decision = ingest_action) — IN this module, not catalog.py (avoid-circular-import rule, RESEARCH §Project Constraints).
    Implement the pre-gate nodes as functions taking `state` and returning a partial-state dict, rebuilding WorkspaceLoader INSIDE each node: `load_config` (governance caps + run_id/gate_id), `build_queries` (D-10 active clusters), `execute_search` (call research_search), `deduplicate` (Plan 02 helpers vs refs/ + ledger + title fuzzy), `score_and_extract` (run_gate; catch ResearchScoreOutageError BEFORE gate → status failed; carry degraded signal). Build the `gate_queue` here with default decision = each finding's ingest_action (D-04). Log to stderr via `logging` only — never `print()` (WR-04 / Pitfall 6).
  </action>
  <acceptance_criteria>
    - `ResearchRunState` is a TypedDict; no field stores a WorkspaceLoader, LLM client, or sqlite connection.
    - `ResearchRunInput`, `ReviewInput`, `InspectInput`, `RunResult`, `GateQueueEntry` are defined in research_run.py (not catalog.py).
    - build_queries excludes non-active clusters and respects max_papers_per_cycle.
    - deduplicate filters against refs/ URLs, the rejected ledger, in-batch dups, and the title-fuzzy pass.
    - score_and_extract catches ResearchScoreOutageError and sets status without raising; `grep -n "ResearchScoreOutageError" src/construct/llm/research_run.py` matches.
    - No `print(` in research_run.py (`grep -c "print(" src/construct/llm/research_run.py` == 0).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/llm/test_research_run.py -x -q -k "build_queries or deduplicate or score_and_extract or models" && test "$(grep -c 'print(' src/construct/llm/research_run.py)" -eq 0</automated>
  </verify>
  <done>State + in-module models + pre-gate nodes implemented; outage caught before gate; degraded signal carried; stderr-only logging; targeted tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interrupt gate, graph builder, checkpointer, run-start runner</name>
  <read_first>
    - .planning/phases/10-durable-human-review-research-run/10-RESEARCH.md §Pattern 1 (interrupt-only gate), §Pattern 2 (_open_checkpointer / from_conn_string footgun), §Pattern 3 (get_state), §Pitfalls 1/2/5, §Code Examples (cross-process resume shape)
    - src/construct/llm/ask_domain.py (lines 315-335 build_*_graph idiom to extend with checkpointer + interrupt)
    - src/construct/pipelines/research_dedup.py (rejected_ledger_path → confirms .construct/ home for the checkpoint DB sibling)
    - tests/unit/test_workflow_runner.py (lines 164-215 r1/r2 two-instance resume idiom → mirror with two SqliteSaver on one DB file)
  </read_first>
  <behavior>
    - gate_review node contains ONLY `interrupt({"gate_id":..., "gate_queue":...})` + return of the resume value; placing any write or event there is a defect (the node re-runs on resume).
    - build_research_run_graph(checkpointer) wires the locked linear topology and compiles with the checkpointer; post-gate nodes are present as skeletons (return {} / no writes) so the graph compiles and pauses.
    - invoking the graph with `thread_id=run_id` runs to gate_review and returns a result containing `__interrupt__` with the gate_id + per-finding gate_queue; get_state(config).next == ("gate_review",).
    - _open_checkpointer opens `SqliteSaver(sqlite3.connect(.construct/workflow/research-run.sqlite, check_same_thread=False))` and returns (saver, conn); the caller closes conn in finally (never `with from_conn_string(...)`).
  </behavior>
  <action>
    Add the `gate_review` interrupt-only node (RESEARCH §Pattern 1) and SKELETON post-gate nodes `ingest_batch`, `compile_digest`, `update_seeds_and_log` that return an empty/no-write partial-state dict — explicitly marked `# IMPLEMENTED IN PLAN 04` so the graph compiles and the pause is testable; they MUST perform no writes in this plan.
    Implement `build_research_run_graph(checkpointer)` extending the ask_domain idiom: add all 9 nodes, linear edges per RESEARCH topology (load_config→build_queries→execute_search→deduplicate→score_and_extract→gate_review→ingest_batch→compile_digest→update_seeds_and_log→END), `return builder.compile(checkpointer=checkpointer)`.
    Implement `_open_checkpointer(workspace: Path) -> tuple[SqliteSaver, sqlite3.Connection]` per RESEARCH §Pattern 2 (DB at `.construct/workflow/research-run.sqlite`, `check_same_thread=False`, mkdir parents).
    Implement a run-start runner (e.g. `run_research_run(inp: ResearchRunInput) -> RunResult`) that opens the checkpointer, builds the graph, invokes with `{"configurable": {"thread_id": run_id}}`, and returns a `RunResult` with status `awaiting_review` + gate_id + the pending gate_queue when `__interrupt__` is present (full completion + review/inspect runners land in Plan 04). Generate run_id as timestamp + short random.
  </action>
  <acceptance_criteria>
    - The gate_review node body contains a single `interrupt(` call and no write/append/event calls (assert via source inspection in the test).
    - build_research_run_graph compiles without error and `get_state(cfg).next == ("gate_review",)` after a paused invoke.
    - test_no_writes_before_approval passes: after the paused invoke, refs/, cards/, digests/ are unchanged and search-seeds.json last_queried is untouched (RSCH-03/SC2).
    - `_open_checkpointer` targets `.construct/workflow/research-run.sqlite` with `check_same_thread=False`; no `with SqliteSaver.from_conn_string` anywhere (`grep -c "from_conn_string" src/construct/llm/research_run.py` == 0).
    - Skeleton post-gate nodes perform zero writes (no `_write_ref_file`/`create_card`/`append_event`/digest writes yet).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/llm/test_research_run.py -x -q -k "no_writes_before_approval or pause or interrupt or checkpointer" && test "$(grep -c 'from_conn_string' src/construct/llm/research_run.py)" -eq 0</automated>
  </verify>
  <done>Graph compiles + pauses at a real interrupt; no-writes-before-approval test green; checkpointer wired under `.construct/` without the from_conn_string footgun; run-start runner returns awaiting_review with gate_queue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human approval gate → SOT writes | The interrupt is the ONLY path to any ref/card/seed/digest write; nothing downstream runs before resume. |
| checkpoint DB (.construct/) → graph state | Persisted orchestration state drives what runs on resume. |
| provider error text → run result/events | Score-gate outage text could carry an API key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation of Privilege | writes bypassing the gate | mitigate | All write nodes are strictly downstream of the interrupt; gate_review contains only `interrupt()`; test_no_writes_before_approval asserts an empty SOT at pause (RSCH-03). |
| T-10-07 | Tampering | gate node re-execution double-firing side effects | mitigate | Gate node has zero side effects (RESEARCH Pitfall 1); skeleton post-gate nodes write nothing in this plan. |
| T-10-08 | Info Disclosure | provider key in outage message | mitigate | score_and_extract catches ResearchScoreOutageError and uses `.safe_message`/`_safe_scoring_cause`; never echoes raw `str(exc)` (Phase 9 T-09-06). |
| T-10-09 | Tampering (protocol) | print() corrupts MCP stdout | mitigate | logging-to-stderr only; grep gate asserts no `print(` in the module (WR-04). |
</threat_model>

<verification>
- `uv run pytest tests/llm/test_research_run.py -q -k "no_writes_before_approval or pause or deduplicate or build_queries"` green.
- `grep -c "from_conn_string" src/construct/llm/research_run.py` == 0; `grep -c "print(" src/construct/llm/research_run.py` == 0.
</verification>

<success_criteria>
A checkpointed StateGraph that composes Phase 8 search + Phase 9 scoring, pauses at a real `interrupt()` exposing a per-finding gate_queue, writes nothing before approval, and handles total outage before the gate.
</success_criteria>

<output>
Create `.planning/phases/10-durable-human-review-research-run/10-03-SUMMARY.md` when done.
</output>
