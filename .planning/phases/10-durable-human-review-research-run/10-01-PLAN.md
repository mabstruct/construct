---
phase: 10-durable-human-review-research-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .planning/REQUIREMENTS.md
  - tests/llm/conftest.py
  - tests/llm/test_research_run.py
autonomous: false
requirements: [RSCH-02, RSCH-03, RSCH-04, RSCH-05]
user_setup: []

must_haves:
  truths:
    - "langgraph-checkpoint-sqlite is installed and SqliteSaver imports successfully"
    - "Checkpoint DB + rejected ledger paths under .construct/ are git-ignored"
    - "REQUIREMENTS.md Out-of-Scope records the D-02 SQLite carve-out for orchestration state only"
    - "A failing/xfail test suite exists naming every RSCH-02..05 behavior + SC5"
  artifacts:
    - path: "tests/llm/test_research_run.py"
      provides: "RSCH-02..05 + SC5 test signatures (Wave 0 gate)"
      contains: "def test_full_run_offline"
    - path: "tests/llm/conftest.py"
      provides: "checkpointer fixture + ScoredFinding batch helper"
      contains: "sqlite"
    - path: "pyproject.toml"
      provides: "langgraph-checkpoint-sqlite dependency pin"
      contains: "langgraph-checkpoint-sqlite"
  key_links:
    - from: ".gitignore"
      to: ".construct/"
      via: "ignore rule"
      pattern: "\\.construct"
---

<objective>
Wave 0 foundation for Phase 10. Installs the one missing dependency (`langgraph-checkpoint-sqlite`, provides `SqliteSaver`), records the D-02 SQLite carve-out in REQUIREMENTS.md, git-ignores the `.construct/` orchestration-state tree, and scaffolds the offline test harness (conftest fixtures + a failing `tests/llm/test_research_run.py`) that the rest of the phase makes green.

Purpose: Every later plan depends on `SqliteSaver` being importable and on the Nyquist test signatures existing first (RESEARCH §Validation Architecture Wave 0 gaps).
Output: Installed dependency, doc carve-out, gitignore rule, and a red test suite enumerating RSCH-02..05 + SC5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-durable-human-review-research-run/10-CONTEXT.md
@.planning/phases/10-durable-human-review-research-run/10-RESEARCH.md
@.planning/phases/10-durable-human-review-research-run/10-VALIDATION.md

<interfaces>
<!-- Mock seams + fixtures the test scaffolding reuses (from tests/llm/conftest.py). -->
From tests/llm/conftest.py:
  - ConfigurableStructuredMock(ScoredFinding(...))  — deterministic mock LLM output
  - make_build_chat_model(mock) — returns a _build(cfg, *, temperature=...) used to monkeypatch construct.llm.factory.build_chat_model
  - create_test_workspace(...) — scaffolds a temp CONSTRUCT workspace
  - sample_search_results() -> list[SearchResult]  (arxiv/blog/shop fixtures)

From src/construct/llm/research_score.py:
  - class ResearchScoreInput(BaseModel): workspace_path: str; results: list[SearchResult]; provider_override: str | None
  - class ScoredFinding(BaseModel): ... ingest_action: IngestAction (skip | ref_only | ref_and_card)
  - class ResearchScoreGateOutput(BaseModel): findings: list[ScoredFinding]; gate: GateMetadata; retrieval: dict
  - run_gate(gate_id, input_data: ResearchScoreInput, *, config_path=None) -> ResearchScoreGateOutput
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — langgraph-checkpoint-sqlite</name>
  <action>Blocking-human legitimacy gate that runs BEFORE any `uv add`. Verify on pypi.org that `langgraph-checkpoint-sqlite` is the first-party langchain-ai artifact (same org as the already-installed `langgraph`/`langgraph-checkpoint`/`langgraph-sdk`), with a mature release history and latest ~2.0.11. Do NOT install until the user types "approved".</action>
  <read_first>
    - .planning/phases/10-durable-human-review-research-run/10-RESEARCH.md §Package Legitimacy Audit (the [ASSUMED] disposition + recommendation)
  </read_first>
  <what-built>Nothing yet — this gate precedes the install. `langgraph-checkpoint-sqlite` is the only new third-party package this phase (slopcheck was unavailable in research; treated as [ASSUMED] per the Package Legitimacy Gate).</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/langgraph-checkpoint-sqlite/ — confirm the project exists, latest is ~2.0.11, and the publisher/home page points at github.com/langchain-ai/langgraph (libs/checkpoint-sqlite). It must be the same langchain-ai org as the already-installed `langgraph`, `langgraph-checkpoint`, `langgraph-sdk`.
    2. Confirm release history is mature (1.0.0 → 2.x), not a brand-new single-release package.
  </how-to-verify>
  <acceptance_criteria>
    - The package page confirms langchain-ai authorship and a mature 1.x→2.x history before install proceeds.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize `uv add langgraph-checkpoint-sqlite`, or describe the concern to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install dependency + git-ignore .construct/ + REQUIREMENTS carve-out</name>
  <read_first>
    - pyproject.toml (existing `langgraph>=0.2` dependency block, line ~14)
    - .gitignore (current rules — has no `.construct` entry)
    - .planning/REQUIREMENTS.md (Out-of-Scope table, the "SQLite, vector database, queue workers..." row, line ~79)
    - .planning/phases/10-durable-human-review-research-run/10-RESEARCH.md §Runtime State Inventory + §Standard Stack Installation
  </read_first>
  <action>
    Run `uv add "langgraph-checkpoint-sqlite>=2.0,<3"` (adds to pyproject.toml + uv.lock and installs). Verify the import works: `uv run python -c "from langgraph.checkpoint.sqlite import SqliteSaver; print('ok')"`.
    Add a `.gitignore` rule for the non-SOT orchestration tree so the SQLite checkpoint DB and rejected ledger are never committed: ignore `.construct/` (covers `.construct/workflow/research-run.sqlite` and `.construct/research/rejected.json`).
    Edit REQUIREMENTS.md Out-of-Scope: amend the SQLite row to record the D-02 carve-out — SQLite as the LangGraph orchestration/checkpoint store under `.construct/` is permitted for `research.run` workflow state ONLY; the canonical SOT (cards/refs/connections/search-seeds/events/digests) stays file-based and "no database owns any facts" is preserved.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from langgraph.checkpoint.sqlite import SqliteSaver"` exits 0.
    - `grep -n "langgraph-checkpoint-sqlite" pyproject.toml` matches; uv.lock updated.
    - `grep -n "construct" .gitignore` includes a `.construct/` ignore rule.
    - REQUIREMENTS.md Out-of-Scope SQLite row mentions the LangGraph checkpoint carve-out under `.construct/` and that SOT stays file-based (RSCH-04 / D-02).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from langgraph.checkpoint.sqlite import SqliteSaver; print('ok')" && grep -q "construct" .gitignore && grep -q "langgraph-checkpoint-sqlite" pyproject.toml && grep -iq "carve\|checkpoint\|orchestration" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>SqliteSaver importable; `.construct/` ignored; REQUIREMENTS.md carve-out recorded; pyproject + uv.lock pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Test scaffolding — conftest fixtures + failing research.run suite</name>
  <read_first>
    - tests/llm/conftest.py (ConfigurableStructuredMock, make_build_chat_model, create_test_workspace, sample_search_results — reuse, do not duplicate)
    - tests/unit/test_workflow_runner.py (r1/r2 two-instance resume idiom — the cross-process template)
    - .planning/phases/10-durable-human-review-research-run/10-RESEARCH.md §Validation Architecture (the Req→Test map: exact test names) + §Offline test construction
    - .planning/phases/10-durable-human-review-research-run/10-PATTERNS.md (tests/llm/test_research_run.py section, lines ~261-287)
  </read_first>
  <action>
    Extend tests/llm/conftest.py with two reusable fixtures: (a) a `tmp_path`-based SQLite checkpointer factory that opens `SqliteSaver(sqlite3.connect(path, check_same_thread=False))` and yields (saver, conn, db_path) with `conn.close()` teardown — usable twice on the same file for cross-process simulation; (b) a `scored_findings_batch` helper that builds a `ResearchScoreGateOutput`/`list[ScoredFinding]` with mixed ingest_action values (skip/ref_only/ref_and_card) for offline review tests.
    Create tests/llm/test_research_run.py with the EXACT test signatures from RESEARCH §Validation Architecture, each marked `@pytest.mark.xfail(reason="implemented in plans 02-05", strict=False)` (or skip) so collection passes while the suite is red: `test_full_run_offline` (RSCH-02/SC1), `test_no_writes_before_approval` (RSCH-03/SC2), `test_per_finding_decisions` (RSCH-03), `test_cross_process_resume` (RSCH-04/SC3), `test_inspect_no_resume` (RSCH-04), `test_idempotent_rerun` (RSCH-05/SC4), `test_partial_batch_resume_safe` (RSCH-05), `test_run_result_fields` asserting status/gate_ids/ref+card counts/digest_path/seed_update/events (SC5). Do NOT import `construct.llm.research_run` at module top (it does not exist yet) — import lazily inside tests so collection does not error.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/llm/test_research_run.py --collect-only -q` lists all 8 named tests with no collection error.
    - Every RSCH id (RSCH-02, RSCH-03, RSCH-04, RSCH-05) and SC5 appears as a test docstring/marker referencing the requirement.
    - conftest exposes a sqlite checkpointer fixture (`grep -n "SqliteSaver\|check_same_thread" tests/llm/conftest.py`) and a findings-batch helper.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/llm/test_research_run.py --collect-only -q 2>&1 | grep -c "test_full_run_offline\|test_no_writes_before_approval\|test_cross_process_resume\|test_idempotent_rerun\|test_partial_batch_resume_safe\|test_inspect_no_resume\|test_per_finding_decisions\|test_run_result_fields"</automated>
  </verify>
  <done>conftest has the checkpointer + findings-batch fixtures; test_research_run.py collects all 8 RSCH/SC tests red (xfail), no import errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → local env | A new third-party package is installed into the venv (supply-chain surface). |
| repo → git | `.construct/` orchestration state could be accidentally committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | `uv add langgraph-checkpoint-sqlite` | mitigate | Blocking-human legitimacy gate (Task 1) verifying the langchain-ai-published artifact on pypi.org before install; pin `>=2.0,<3`. |
| T-10-01 | Info Disclosure | `.construct/` checkpoint DB / ledger | mitigate | `.gitignore` rule (Task 2) keeps SQLite checkpoint + rejected ledger out of git. |
| T-10-02 | Repudiation | Out-of-Scope doc drift | accept | Carve-out recorded in REQUIREMENTS.md so the SQLite exception is auditable, not silent. |
</threat_model>

<verification>
- `uv run python -c "from langgraph.checkpoint.sqlite import SqliteSaver"` exits 0.
- `uv run pytest tests/llm/test_research_run.py --collect-only -q` collects 8 tests with no error.
- `git status` would not stage `.construct/` (ignored).
</verification>

<success_criteria>
SqliteSaver installed + importable; `.construct/` git-ignored; D-02 carve-out documented; red Nyquist suite enumerating RSCH-02..05 + SC5 exists.
</success_criteria>

<output>
Create `.planning/phases/10-durable-human-review-research-run/10-01-SUMMARY.md` when done.
</output>
