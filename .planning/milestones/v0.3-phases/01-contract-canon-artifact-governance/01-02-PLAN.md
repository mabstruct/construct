---
phase: 01-contract-canon-artifact-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/construct/schemas/card.py
  - src/construct/schemas/config.py
  - src/construct/schemas/workspace.py
  - src/construct/storage/workspace.py
  - src/construct/services/init.py
  - src/construct/services/validation.py
  - tests/unit/test_workspace_contracts.py
  - tests/unit/test_artifact_write_gates.py
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-04
must_haves:
  truths:
    - "Runtime schemas enforce the canonical workspace and artifact contract instead of archived assumptions."
    - "Invalid cards, refs, connections, and events are rejected before becoming source-of-truth files."
    - "Workspace initialization and validation reflect the same canonical artifact model as the docs."
  artifacts:
    - path: "src/construct/schemas/workspace.py"
      provides: "Canonical runtime workspace scaffold"
    - path: "src/construct/services/validation.py"
      provides: "Post-write audit rules consistent with write-gate boundaries"
    - path: "tests/unit/test_artifact_write_gates.py"
      provides: "Automated rejection coverage for invalid artifacts"
  key_links:
    - from: "src/construct/services/init.py"
      to: "CONSTRUCT-CLAUDE-impl/construct/templates/"
      via: "template copy path"
      pattern: "construct/templates"
    - from: "src/construct/storage/workspace.py"
      to: "src/construct/schemas/workspace.py"
      via: "WorkspaceScaffold and artifact models"
      pattern: "WorkspaceScaffold|ConnectionsFile"
---

<objective>
Reconcile the dormant Python runtime to the canonical Phase 1 contract and add real write-gate enforcement boundaries.

Purpose: D-04, D-05, and D-06 require runtime code to stop acting as an alternate contract and start enforcing the authoritative one.
Output: Updated schema/load/init/validation code plus unit tests that prove canonical contract alignment and pre-write rejection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-canon-artifact-governance/01-CONTEXT.md
@.planning/phases/01-contract-canon-artifact-governance/01-RESEARCH.md
@.planning/phases/01-contract-canon-artifact-governance/01-01-PLAN.md
@CONSTRUCT-CLAUDE-spec/workspace-contract.md
@CONSTRUCT-CLAUDE-spec/data-schemas.md
@CONSTRUCT-CLAUDE-spec/validation-strategy.md
@src/construct/schemas/card.py
@src/construct/schemas/config.py
@src/construct/schemas/workspace.py
@src/construct/storage/workspace.py
@src/construct/services/init.py
@src/construct/services/validation.py

<interfaces>
From `src/construct/schemas/card.py`:

```python
class KnowledgeCard(BaseModel):
    model_config = ConfigDict(extra="forbid")
```

From `src/construct/storage/workspace.py`:

```python
class WorkspaceLoader:
    def classify(self, relative_path: str) -> str: ...
    def load_connections(self) -> ConnectionsFile: ...
```

From `src/construct/services/validation.py`:

```python
def validate_workspace(root: str | Path) -> ValidationReport: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild runtime contract models around the canonical workspace shape</name>
  <files>src/construct/schemas/config.py, src/construct/schemas/workspace.py, src/construct/storage/workspace.py, tests/unit/test_workspace_contracts.py</files>
  <read_first>src/construct/schemas/config.py, src/construct/schemas/workspace.py, src/construct/storage/workspace.py, CONSTRUCT-CLAUDE-spec/workspace-contract.md, CONSTRUCT-CLAUDE-spec/data-schemas.md</read_first>
  <behavior>
    - Test 1: canonical workspace paths from the Phase 1 contract validate successfully.
    - Test 2: archived-only paths (`domains/{id}/domain.yaml`, root `workflows/`, root `db/`) are no longer treated as canonical defaults unless the canon doc explicitly keeps them.
    - Test 3: template-backed config/artifact files round-trip through the updated models.
  </behavior>
  <acceptance_criteria>
    - `src/construct/schemas/workspace.py` names the canonical, derived, and support paths from Plan 01 rather than the archived Python scaffold.
    - `src/construct/schemas/config.py` stops requiring stale shape details that contradict the Phase 1 contract.
    - `src/construct/storage/workspace.py` loads the canonical artifact files using the updated models and reports category mismatches precisely.
  </acceptance_criteria>
  <action>Write failing tests in `tests/unit/test_workspace_contracts.py` first, then update `src/construct/schemas/workspace.py`, `src/construct/schemas/config.py`, and `src/construct/storage/workspace.py` to match the canonical spec from Plan 01 per D-01, D-02, and D-04. Remove archived assumptions from `REQUIRED_PATHS` and related model validators unless Plan 01 explicitly retained them. Keep the existing strict Pydantic/error-wrapping style and ensure template-backed files parse through the same runtime models used by the loader.</action>
  <verify>
    <automated>python -m pytest tests/unit/test_workspace_contracts.py -k "canon or scaffold" -x</automated>
  </verify>
  <done>Runtime models and loader agree with the authoritative workspace contract and fail fast on drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pre-write artifact rejection and init-path reconciliation</name>
  <files>src/construct/schemas/card.py, src/construct/services/init.py, src/construct/services/validation.py, tests/unit/test_artifact_write_gates.py</files>
  <read_first>src/construct/schemas/card.py, src/construct/services/init.py, src/construct/services/validation.py, CONSTRUCT-CLAUDE-spec/validation-strategy.md, CONSTRUCT-CLAUDE-impl/construct/templates/card.md, CONSTRUCT-CLAUDE-impl/construct/templates/connections.json</read_first>
  <behavior>
    - Test 1: invalid card/connection/event/ref payloads are rejected before any file write helper persists them.
    - Test 2: workspace init copies from the active `CONSTRUCT-CLAUDE-impl/construct/templates/` directory instead of the stale path.
    - Test 3: post-write validation still reports cross-file consistency issues without replacing write-time rejection.
  </behavior>
  <acceptance_criteria>
    - `src/construct/services/init.py` points at the active template directory and scaffolds the canonical layout from Plan 01 per D-01/D-02.
    - `tests/unit/test_artifact_write_gates.py` covers invalid artifact rejection for cards, refs, connections, and events per D-05.
    - `src/construct/services/validation.py` clearly separates pre-write checks from post-write audit logic per D-06.
  </acceptance_criteria>
  <action>Create failing tests in `tests/unit/test_artifact_write_gates.py`, then add deterministic write-gate helpers or service boundaries that validate artifact payloads before persistence per D-05. Update `src/construct/services/init.py` so `TEMPLATE_DIR` and generated scaffold match the active template location and canonical layout, not the archived Python shape. Refactor `src/construct/services/validation.py` so it remains the post-write audit path for cross-file consistency and fixture proof per D-06, rather than the only validation boundary.</action>
  <verify>
    <automated>python -m pytest tests/unit/test_artifact_write_gates.py tests/unit/test_workspace_contracts.py -x</automated>
  </verify>
  <done>Runtime code rejects invalid source-of-truth artifacts before write and initializes/validates workspaces using the same canonical contract as the spec layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→write gate | Untrusted artifact payloads cross into runtime persistence helpers |
| filesystem→loader | Existing workspace files may be malformed or drifted from canon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T | write helpers / init service | mitigate | Validate card/ref/connection/event payloads before persistence and fail before writing per D-05. |
| T-01-04 | I | workspace loader and validators | mitigate | Keep post-write audits for cross-file consistency and emit explicit file-scoped errors per D-06. |
| T-01-05 | D | stale scaffold assumptions | mitigate | Remove archived required-path defaults that make valid Claude-native workspaces fail validation. |
</threat_model>

<verification>
Run `python -m pytest tests/unit/test_workspace_contracts.py tests/unit/test_artifact_write_gates.py -x`.
</verification>

<success_criteria>
- Runtime code follows the canonical contract docs instead of archived defaults.
- Invalid source-of-truth artifacts fail before write.
- Init and validation services use the same canonical artifact map and template source.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-canon-artifact-governance/01-contract-canon-artifact-governance-02-SUMMARY.md`
</output>
