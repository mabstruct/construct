---
phase: 01-contract-canon-artifact-governance
plan: 04
type: execute
wave: 1
depends_on: [01-03]
files_modified:
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md
autonomous: true
gap_closure: true
requirements: [FND-03]
must_haves:
  truths:
    - "The workspace-init skill references a template path that exists in deployed workspaces"
    - "The skill's validation step accepts .construct/templates/ as the valid template source"
  artifacts:
    - path: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md"
      provides: "Corrected template path reference for deployed workspaces"
      contains: ".construct/templates/"
  key_links:
    - from: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md"
      to: "setup-construct.sh:57"
      via: "Deployment copies templates to .construct/templates/ — skill must reference the same path"
      pattern: "\\.construct/templates/"
---

<objective>
Fix the `construct-workspace-init` skill's template path reference so it works in deployed workspaces.

**Purpose:** The skill references `CONSTRUCT-CLAUDE-impl/construct/templates/` (the project-root authoritative source), but `setup-construct.sh` deploys templates to `.construct/templates/`. When the skill runs in a deployed workspace, the project-root path doesn't exist — the skill breaks.

**Output:** Updated SKILL.md with correct deployed-workspace template path and an explanatory note about the project-root ↔ deployed mirror relationship.

**Gap source:** 01-UAT.md — Test 1 (Workspace init creates canonical layout), blocker severity.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contract-canon-artifact-governance/01-UAT.md
@.planning/phases/01-contract-canon-artifact-governance/01-contract-canon-artifact-governance-03-SUMMARY.md

The Phase 1 workspace contract (`CONSTRUCT-CLAUDE-spec/workspace-contract.md`) establishes:
- **Authority set:** `CONSTRUCT-CLAUDE-impl/construct/templates/` is the authoritative template source at the project root (line 6, 85)
- **Deployed mirror:** `.construct/templates/` is the support artifact containing "authoritative initial shapes for canonical and derived artifacts" (line 77)
- **setup-construct.sh line 57** copies `$IMPL_DIR/construct/templates` → `$TARGET/.construct/templates/`

The skill must reference `.construct/templates/` because it runs in deployed workspaces where only the mirror exists.
</context>

<interfaces>
From CONSTRUCT-CLAUDE-spec/workspace-contract.md (relevant sections):

```markdown
## Authority and drift rules
1. `CONSTRUCT-CLAUDE-spec/` defines the contract intent and boundaries.
2. `CONSTRUCT-CLAUDE-impl/construct/templates/` defines the expected initial file shapes.
3. [`artifact-catalog.md`](artifact-catalog.md) records ownership, audit, and sync obligations.
4. Runtime code, validators, and skill procedures must conform to the three layers above.
```

```markdown
### Support artifacts
| `.construct/templates/*` | support | Authoritative initial shapes for canonical and derived artifacts |
```

From setup-construct.sh (line 57):
```bash
cp -r "$IMPL_DIR/construct/templates"  "$TARGET/.construct/"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Fix template path references in construct-workspace-init SKILL.md</name>
  <files>CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md</files>
  <action>
    Apply two corrections to `CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md`:

    **Fix 1 — Procedure step (line 51):**
    Replace the current text:
    ```
    Copy initial file contents from the active templates at `CONSTRUCT-CLAUDE-impl/construct/templates/`.
    ```
    With:
    ```
    Copy initial file contents from the active templates at `.construct/templates/`.

    > **Note:** The authoritative template source is `CONSTRUCT-CLAUDE-impl/construct/templates/` at the project root. When a workspace is deployed via `setup-construct.sh`, templates are mirrored to `.construct/templates/`. The skill references `.construct/templates/` because that is the path that exists in deployed workspaces.
    ```

    **Fix 2 — Validation check (line 103):**
    Replace the current text:
    ```
    - [ ] Templates are sourced from `CONSTRUCT-CLAUDE-impl/construct/templates/` (the active template directory, not `.construct/templates/` or an archived path)
    ```
    With:
    ```
    - [ ] Templates are sourced from `.construct/templates/` (the deployed mirror of the authoritative `CONSTRUCT-CLAUDE-impl/construct/templates/` at the project root)
    ```

    **Why these changes:**
    - `setup-construct.sh` line 57 copies templates to `.construct/templates/`, not `CONSTRUCT-CLAUDE-impl/construct/templates/`
    - The skill runs in deployed workspaces where only `.construct/templates/` exists
    - The validation check was **actively rejecting the correct path** (`.construct/templates/`) while asserting a path (`CONSTRUCT-CLAUDE-impl/construct/templates/`) that doesn't exist in deployed workspaces
    - The explanatory note preserves the link between the project-root authority and the deployed mirror, maintaining traceability per the Phase 1 contract
  </action>
  <verify>
    <automated>bash -c 'grep -c "\.construct/templates/" CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md | xargs test 2 -eq'</automated>
    - Confirms exactly 2 references to `.construct/templates/` (one in procedure, one in validation)
    - Confirms no remaining references to `CONSTRUCT-CLAUDE-impl/construct/templates/` outside the explanatory note
  </verify>
  <done>
    - SKILL.md procedure step references `.construct/templates/` instead of a project-root-only path
    - SKILL.md validation check confirms `.construct/templates/` is the correct deployed-workspace source
    - Explanatory note links the deployed mirror to the authoritative project-root source (`CONSTRUCT-CLAUDE-impl/construct/templates/`)
    - The template path reference now matches what `setup-construct.sh` actually deploys
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill → filesystem | Skill procedure reads template files from a configurable path — an incorrect path reference makes the skill non-functional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | T (tampering) | SKILL.md template path | mitigate | The fix hardens the path to the correct deployed location; the explanatory note preserves traceability to the authoritative source for debugging |
| T-01-04-02 | E (elevation of privilege) | N/A | accept | No privilege boundary — this is a procedure document fixing a path string, not an auth boundary |
</threat_model>

<verification>
1. `bash -c 'grep -c "\.construct/templates/" CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md | xargs test 2 -eq'` — confirms both procedure and validation references use `.construct/templates/`
2. Read the SKILL.md and visually confirm: (a) line 51 references `.construct/templates/` with explanatory note, (b) line 103's validation check accepts `.construct/templates/` as the correct path
3. Verify that `setup-construct.sh` line 57 and the SKILL.md now agree on `.construct/templates/` as the deployed template path
</verification>

<success_criteria>
- SKILL.md procedure step references `.construct/templates/` (not a project-root-only path)
- SKILL.md validation check accepts `.construct/templates/` (not actively rejecting the correct path)
- Explanatory note preserves traceability from deployed mirror to authoritative source
- No remaining references to `CONSTRUCT-CLAUDE-impl/construct/templates/` as a path the skill should use at runtime
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-canon-artifact-governance/01-contract-canon-artifact-governance-04-SUMMARY.md`
</output>
