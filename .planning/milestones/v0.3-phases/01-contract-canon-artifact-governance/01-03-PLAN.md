---
phase: 01-contract-canon-artifact-governance
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-validate/SKILL.md
  - CONSTRUCT-CLAUDE-impl/construct/references/commands.md
  - CONSTRUCT-CLAUDE-impl/construct/references/capabilities.md
  - README.md
  - tests/integration/test_workspace_contract_migration.py
  - CONSTRUCT-CLAUDE-spec/migrations/phase-1-workspace-contract-migration.md
autonomous: true
requirements:
  - FND-03
  - FND-05
  - FND-06
must_haves:
  truths:
    - "User and maintainer can follow workspace-init and workspace-validate behavior that matches the hardened runtime contract."
    - "Maintainer can prove the contract against the checked-in fixture workspaces."
    - "Maintainer has an explicit migration path for existing CONSTRUCT workspaces and repos."
  artifacts:
    - path: "CONSTRUCT-CLAUDE-spec/migrations/phase-1-workspace-contract-migration.md"
      provides: "Explicit migration checklist and safety notes"
    - path: "tests/integration/test_workspace_contract_migration.py"
      provides: "Fixture proof and migration verification"
    - path: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-validate/SKILL.md"
      provides: "Workflow contract aligned with runtime validation behavior"
  key_links:
    - from: "construct-workspace-init/SKILL.md"
      to: "src/construct/services/init.py"
      via: "same scaffold and template semantics"
      pattern: "cards|refs|connections|events"
    - from: "tests/integration/test_workspace_contract_migration.py"
      to: "CONSTRUCT-CLAUDE-spec/migrations/phase-1-workspace-contract-migration.md"
      via: "migration steps proved against fixtures"
      pattern: "migration|fixture"
---

<objective>
Align Claude-native skill/docs behavior to the hardened contract and close Phase 1 with fixture proof plus migration guidance.

Purpose: FND-03, FND-05, and FND-06 are only complete when user-facing workflow docs, fixture validation, and migration steps match the runtime canon.
Output: Updated skill/reference docs, canonical checked-in fixture workspaces, integration proof on those fixtures, and an explicit migration playbook.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-canon-artifact-governance/01-CONTEXT.md
@.planning/phases/01-contract-canon-artifact-governance/01-RESEARCH.md
@.planning/phases/01-contract-canon-artifact-governance/01-01-PLAN.md
@.planning/phases/01-contract-canon-artifact-governance/01-02-PLAN.md
@CONSTRUCT-CLAUDE-spec/workspace-contract.md
@CONSTRUCT-CLAUDE-spec/validation-strategy.md
@CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md
@CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-validate/SKILL.md
@README.md
@.planning/codebase/TESTING.md

<interfaces>
From `CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md`:

```markdown
### Validation
- [ ] connections.json is valid JSON with empty connections array
```

From `CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-validate/SKILL.md`:

```markdown
### Layer 1: Schema Validation
### Layer 5: Audit Trail Check
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align skill and reference docs to the hardened contract</name>
  <files>CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md, CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-validate/SKILL.md, CONSTRUCT-CLAUDE-impl/construct/references/commands.md, CONSTRUCT-CLAUDE-impl/construct/references/capabilities.md</files>
  <read_first>CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-init/SKILL.md, CONSTRUCT-CLAUDE-impl/claude/skills/construct-workspace-validate/SKILL.md, CONSTRUCT-CLAUDE-spec/workspace-contract.md, src/construct/services/init.py, src/construct/services/validation.py</read_first>
  <acceptance_criteria>
    - `construct-workspace-init/SKILL.md` describes the canonical scaffold and template source per D-01/D-02.
    - `construct-workspace-validate/SKILL.md` distinguishes pre-write rejection vs post-write audit per D-05/D-06.
    - `commands.md` and `capabilities.md` describe the same workspace-init/validate behavior as the skills and runtime.
  </acceptance_criteria>
  <action>Rewrite the workspace-init and workspace-validate skill procedures so they describe the canonical artifact model and runtime-backed enforcement from Plans 01 and 02, not the archived layout. Update the relevant entries in `commands.md` and `capabilities.md` so user-facing docs describe the same init/validate behavior, canonical artifact set, and validation layers. Mention D-08 fixture proof expectations where validation docs discuss acceptance targets.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_workspace_contract_migration.py -k skills -x</automated>
  </verify>
  <done>Workflow-facing docs now match the hardened contract and runtime behavior closely enough to be executable references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create and normalize canonical fixture workspaces</name>
  <files>README.md, test-ws/my-construct/, test-ws/ping-eon/, tests/integration/test_workspace_contract_migration.py</files>
  <read_first>README.md, .planning/codebase/TESTING.md, CONSTRUCT-CLAUDE-spec/workspace-contract.md, CONSTRUCT-CLAUDE-spec/validation-strategy.md, .planning/phases/01-contract-canon-artifact-governance/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `test-ws/` exists as the sole canonical checked-in fixture root for Phase 1 proof.
    - Test 2: `test-ws/my-construct/` and `test-ws/ping-eon/` exist with enough real workspace data to exercise canonical validation behavior.
    - Test 3: repo docs point maintainers to the same canonical fixture path and do not leave `test-ws/` vs `testws/` ambiguous.
  </behavior>
  <acceptance_criteria>
    - `test-ws/` exists and is the only canonical fixture root referenced by updated repo-facing docs for Phase 1 proof.
    - `test-ws/my-construct/` and `test-ws/ping-eon/` are checked in with representative workspace contents suitable for validation and migration testing.
    - `README.md` names the same canonical fixture path and, if any alternate spelling existed previously, documents or removes that ambiguity in one direction.
  </acceptance_criteria>
  <action>Write failing integration assertions first, then create and normalize the canonical checked-in fixture root at `test-ws/` per D-08. Populate `test-ws/my-construct/` and `test-ws/ping-eon/` with real workspace artifacts sufficient to exercise the hardened contract, validation flow, and migration checks. Update `README.md` so maintainers have one unambiguous proof target. If any alternate fixture-root spelling exists in docs or scripts, normalize it to the chosen canonical name or document the one-time migration explicitly.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_workspace_contract_migration.py -k fixture_root -x</automated>
  </verify>
  <done>The repo contains real checked-in fixture workspaces under one canonical root, and maintainers know exactly which fixtures Phase 1 uses as proof.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the contract against fixture workspaces</name>
  <files>tests/integration/test_workspace_contract_migration.py, README.md</files>
  <read_first>README.md, .planning/codebase/TESTING.md, CONSTRUCT-CLAUDE-spec/workspace-contract.md, CONSTRUCT-CLAUDE-spec/validation-strategy.md, test-ws/my-construct/, test-ws/ping-eon/</read_first>
  <behavior>
    - Test 1: the checked-in fixture workspaces used for Phase 1 proof are exercised consistently through the canonical validation path.
    - Test 2: fixture workspaces pass the canonical validation path or fail only for enumerated intentional drift covered by migration notes.
    - Test 3: repo-level docs point maintainers to the same proof target and commands.
  </behavior>
  <acceptance_criteria>
    - `tests/integration/test_workspace_contract_migration.py` validates the checked-in fixture workspaces called out by D-08.
    - `README.md` names the same canonical fixture proof target with no competing acceptance target.
    - The test command completes green using the real repo fixtures, not synthetic-only tmp paths.
  </acceptance_criteria>
  <action>After Task 2 creates the canonical fixture workspaces, extend `tests/integration/test_workspace_contract_migration.py` to validate those real checked-in workspaces as the primary Phase 1 proof target per D-08. Keep proof centered on the chosen canonical fixture root and do not restore two equally official acceptance targets.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_workspace_contract_migration.py -k fixtures -x</automated>
  </verify>
  <done>Fixture proof is automated against the real checked-in workspaces and Phase 1 acceptance is backed by executable evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Publish the migration playbook</name>
  <files>CONSTRUCT-CLAUDE-spec/migrations/phase-1-workspace-contract-migration.md, tests/integration/test_workspace_contract_migration.py</files>
  <read_first>CONSTRUCT-CLAUDE-spec/process.md, CONSTRUCT-CLAUDE-spec/workspace-contract.md, CONSTRUCT-CLAUDE-spec/data-schemas.md, tests/integration/test_workspace_contract_migration.py</read_first>
  <acceptance_criteria>
    - `phase-1-workspace-contract-migration.md` lists artifact-by-artifact migration steps, safety checks, and rollback notes per D-07.
    - The migration doc explicitly avoids a broad dual-layout steady state per D-09.
    - Integration tests assert the migration document covers the canonical artifact set and fixture proof command.
  </acceptance_criteria>
  <action>Create `CONSTRUCT-CLAUDE-spec/migrations/phase-1-workspace-contract-migration.md` with a concrete upgrade path for existing CONSTRUCT workspaces and repos per D-07: inventory current artifacts, map old assumptions to the new canon, list one-time rename/path fixes, describe validation commands to run before and after migration, and state rollback expectations. Keep the migration target singular per D-09 — one canonical layout after migration, not indefinite dual support. Extend the integration test file to assert the migration guide mentions the canonical artifact set, proof fixtures, and verification commands.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_workspace_contract_migration.py -k migration -x</automated>
  </verify>
  <done>Maintainers have a precise, test-backed migration story that preserves continuity without freezing the project into parallel contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime contract→skill docs | User-facing procedures can drift from implemented behavior |
| fixture repo state→acceptance claims | Real workspace fixtures may not prove what docs claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | R | skill/reference docs | mitigate | Update skills and user-facing reference docs from the hardened runtime behavior, then verify with integration tests. |
| T-01-07 | I | fixture proof path | mitigate | Choose one canonical fixture target and test against the checked-in workspaces rather than synthetic claims. |
| T-01-08 | D | migration guidance | mitigate | Publish an artifact-by-artifact migration checklist with pre/post validation commands and no indefinite dual-layout support. |
</threat_model>

<verification>
Run `python -m pytest tests/integration/test_workspace_contract_migration.py -x`.
</verification>

<success_criteria>
- Workspace-init and workspace-validate docs match runtime behavior.
- Canonical `test-ws/` fixture workspaces exist in-repo and fixture proof is automated against them.
- A concrete migration playbook exists for current CONSTRUCT users and maintainers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-canon-artifact-governance/01-contract-canon-artifact-governance-03-SUMMARY.md`
</output>
