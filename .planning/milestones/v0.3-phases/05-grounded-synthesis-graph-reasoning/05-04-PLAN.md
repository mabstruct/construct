---
phase: 05-grounded-synthesis-graph-reasoning
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md
autonomous: true
requirements:
  - ING-06
user_setup: []

must_haves:
  truths:
    - The synthesis skill references `construct ask domain` CLI command for grounded Q&A
    - The synthesis skill shows how confidence propagates from source card frontmatter to output annotations
    - The synthesis skill demonstrates inline confidence annotation (hedging when source confidence < 3)
    - The skill documents INPUT (workspace + query/topic) and OUTPUT (synthesis with citations + confidence metadata)
  artifacts:
    - path: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"
      provides: "Updated synthesis skill referencing CLI commands and demonstrating confidence propagation"
      contains: "construct ask"
    - path: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"
      provides: "Confidence propagation documentation in procedure steps"
      contains: "confidence"
  key_links:
    - from: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"
      to: "src/construct/cli.py"
      via: "References construct ask domain and construct knowledge card commands"
    - from: "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"
      to: ".planning/phases/05-grounded-synthesis-graph-reasoning/05-CONTEXT.md"
      via: "D-05 confidence propagation requirements"
---

<objective>
**Synthesis Skill Update — CLI Commands & Confidence Propagation**

Update the Claude-native `construct-synthesis` SKILL.md to call CLI commands (`construct ask domain`, `construct knowledge card`) for grounded data acquisition, and document how confidence propagates from source card frontmatter through to synthesis output. This is a documentation-only change per D-02 (synthesis stays Claude-native).

**Purpose:** ING-06 requires confidence-aware synthesis outputs. The skill demonstrates inline confidence annotation (hedging claims from low-confidence cards) and structured metadata (aggregate + per-citation confidence). The skill is updated to use the new `construct ask domain` CLI for grounded Q&A and `construct knowledge card` for card retrieval, following the same migration pattern established in Phase 4.

**Output:** Updated SKILL.md that agents can follow to produce synthesis outputs with confidence metadata.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-grounded-synthesis-graph-reasoning/05-CONTEXT.md
@.planning/phases/05-grounded-synthesis-graph-reasoning/05-RESEARCH.md

<interfaces>
<!-- Current synthesis skill frontmatter structure -->

From Phase 4 skill migration pattern (04-03-SUMMARY.md):
- allowed-tools restricted to: `Read, Bash(construct), WebSearch, WebFetch, MCP(connect)`
- CLI/MCP invocation replaces inline file I/O
- INPUT/OUTPUT documentation added per WF-02 standard

From CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md:
```yaml
---
description: Create grounded synthesis outputs from workspace knowledge
allowed-tools:
  - Read
  - Bash
  - Edit
  - Write
  - WebSearch
  - WebFetch
---
```

The skill currently uses inline Read for card access and Write for output. We update it to use CLI commands and add confidence propagation documentation.
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Update construct-synthesis SKILL.md with CLI commands and confidence propagation</name>
<files>CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md</files>
<read_first>
  CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md
  CONSTRUCT-CLAUDE-impl/claude/skills/construct-gap-analysis/SKILL.md
</read_first>
<action>
Read the current `construct-synthesis/SKILL.md` first, then apply these changes:

### 1. Update Frontmatter

Follow the Phase 4 skill migration pattern (from `construct-gap-analysis/SKILL.md`): change `allowed-tools` to restrict to CLI/MCP operations:

```yaml
---
description: Create grounded synthesis outputs from workspace knowledge using CLI commands for data acquisition and LLM-driven drafting.
allowed-tools:
  - Read
  - Bash(construct)
  - MCP(connect)
  - WebSearch
  - WebFetch
---
```

### 2. Add INPUT section (after frontmatter, before procedure)

```markdown
## INPUT

- **Workspace:** A CONSTRUCT workspace directory with knowledge cards
- **Query/Topic:** Natural language question or topic to synthesize about
- **Domain (optional):** Target domain ID for bounded synthesis (default: all domains)
- **Max cards (optional):** Maximum cards to consider (default: 20, max: 50)
```

### 3. Add OUTPUT section

```markdown
## OUTPUT

- **Synthesis:** Natural language synthesis grounded in workspace knowledge cards
- **Citations:** Structured references linking claims to specific card IDs
- **Confidence metadata:**
  - Per-citation confidence (1-5) from each source card's frontmatter `confidence` field
  - Overall aggregate confidence (min/mean/weighted across all cited cards)
  - Inline hedging: claims from low-confidence cards (≤ 2) are explicitly noted (e.g., "A speculative source suggests...")
```

### 4. Update Procedure Steps

The procedure becomes a 3-step workflow:

**Step 1: Acquire card knowledge via CLI**

Replace inline `Read cards/*.md` with:
```
construct ask domain --domain "<domain>" --question "<question>" --json
```

This returns structured JSON with answer, citations, and confidence metadata. Store the JSON output for reference.

If no specific question (open-ended synthesis), use:
```
construct knowledge card list --domain <domain> --json
```
to enumerate all cards in a domain, then Read the most promising ones individually.

**Step 2: Analyze confidence metadata**

For each cited card in the ask.domain output, extract the per-citation `confidence` field. Compute aggregate confidence:
- **Min confidence:** The lowest confidence among cited cards
- **Mean confidence:** Average of all cited card confidences
- **Weighted confidence:** Weighted by relevance (heuristic: cards with more body text matching the question get higher weight)

Document these in the synthesis draft metadata.

**Step 3: Draft synthesis with inline confidence annotation**

Using the retrieved card content and confidence metadata, draft the synthesis. Apply these rules:
- Claims from cards with `confidence >= 4`: State directly ("Research shows that...")
- Claims from cards with `confidence == 3`: Use neutral framing ("Current understanding suggests...")
- Claims from cards with `confidence <= 2`: Use explicit hedging ("A speculative source notes..." / "An early-stage finding proposes...")
- Every claim must cite its source card ID parenthetically: "(source: card-xyz-123)"
- If no cards are relevant, state: "The workspace does not contain information about this topic."

End with a structured metadata section:
```markdown
### Synthesis Metadata
- **Cards cited:** {count}
- **Confidence range:** {min} – {max} (per-card confidence)
- **Aggregate confidence:** {mean:.1f} mean | {weighted:.1f} weighted
- **Gate:** ask.domain (L2)
- **Workspace:** {path}
```

### 5. Add Error Handling section

```markdown
## Error Handling

| Error | Cause | Handling |
|-------|-------|----------|
| `construct ask domain` returns no answer | No relevant cards found for the question | Report to user: "Workspace lacks relevant cards." Consider broadening the question or domain. |
| `construct knowledge card list` fails | CLI not available or workspace path wrong | Check workspace path. Fall back to Read cards/*.md for enumeration. |
| Cards cited with confidence < 3 | Source material is speculative or early-stage | Follow hedging rules in Step 3. Never present low-confidence claims as established fact. |
| API key missing for ask.domain | ANTHROPIC_API_KEY not set in environment | Report to user: "LLM gate unavailable. Falling back to card content review." Proceed with manual card review using Read. |
```

### 6. Update allowed-tools rationale

Preserve but add a note following the pattern from Phase 4 migrated skills:
```markdown
> **Note:** Card knowledge acquisition now uses `construct ask domain` CLI (grounded Q&A)
> and `construct knowledge card list` CLI (card enumeration) instead of inline `Read`
> operations. The `Read` tool is still available for individual card inspection and
> config file reading. `Write` is removed — synthesis output is returned via CLI
> structured output, not written to files.
```

### Verify the update

Check the SKILL.md validates against the Phase 4 migration pattern:
- [ ] `allowed-tools` restricted from `Write`/`Edit` to `Bash(construct)`/`MCP(connect)`
- [ ] INPUT section documents workspace + query/topic inputs
- [ ] OUTPUT section documents synthesis + citations + confidence metadata
- [ ] Procedure steps reference CLI commands instead of inline file ops
- [ ] Error handling covers ask.domain failures, confidence < 3, missing API key
</action>
<acceptance_criteria>
- SKILL.md `allowed-tools` includes `Bash(construct)` and `MCP(connect)`; excludes `Write` and `Edit`
- SKILL.md contains `construct ask domain` at least once
- SKILL.md contains `INPUT` section with workspace, query/topic, domain, max_cards
- SKILL.md contains `OUTPUT` section with citations and confidence metadata (per-citation + aggregate)
- SKILL.md contains hedging rules for confidence tiers (>= 4 / == 3 / <= 2)
- SKILL.md contains `Error Handling` section with at least ask.domain and confidence entries
- SKILL.md contains `Synthesis Metadata` block format with confidence range and aggregate
</acceptance_criteria>
<verify>
<automated>grep -c "construct ask" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md" && grep -c "confidence" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md" && grep -c "Bash(construct)" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md" && grep -c "Error Handling" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"</automated>
</verify>
<done>
Synthesis skill updated with CLI command references and confidence propagation documentation. Skill follows Phase 4 migration pattern.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → agent execution | The SKILL.md is a procedure document followed by the Claude agent. No code execution involved. The skill references `Bash(construct)` CLI commands which inherit their security model from the CLI/registry layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Spoofing | SKILL.md procedure — agent may follow outdated instructions | accept | SKILL.md is the living procedure document. Agents use `Read` to load it fresh each session. Outdated instructions are corrected by re-running the skill update (this plan). No automated mitigation needed for documentation drift. |
</threat_model>

<verification>
1. Run: `grep "construct ask" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"` — at least one reference to the ask domain CLI
2. Run: `grep "confidence" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"` — at least 3 references (per-citation, aggregate, hedging rules)
3. Run: `grep "Bash(construct)" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"` — present in allowed-tools
4. Verify: `grep "INPUT" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md" && grep "OUTPUT" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md" && grep "Error Handling" "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md"` — all sections present
</verification>

<success_criteria>
- Synthesis skill references `construct ask domain` and `construct knowledge card list` CLI commands
- Skill documents per-citation confidence (1-5) and aggregate confidence (min/mean/weighted)
- Skill defines hedging rules for three confidence tiers (>= 4 direct, == 3 neutral, <= 2 hedge)
- Skill includes INPUT section (workspace + query/topic + optional domain) and OUTPUT section (synthesis + citations + metadata)
- Skill follows Phase 4 migration pattern: CLI/MCP invocation, restricted allowed-tools, error handling
</success_criteria>

<output>
After completion, create `.planning/phases/05-grounded-synthesis-graph-reasoning/05-04-SUMMARY.md`
</output>