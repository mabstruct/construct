---
phase: 07-close-v0-3-blockers-rt-03-mcp-schema-parity-ing-02-ingest-cl
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/services/init.py
  - CONSTRUCT-CLAUDE-impl/construct/templates/search-seeds.json
  - test-ws/my-construct/search-seeds.json
  - test-ws/ping-eon/search-seeds.json
  - tests/integration/test_ingest_validate_e2e.py
autonomous: true
requirements: [ING-02]

must_haves:
  truths:
    - "A newly initialized workspace has reserved search clusters 'manual-ingest' and 'web-ingest' in search-seeds.json"
    - "construct ingest source then construct validate passes E2E on a clean workspace"
    - "construct validate passes on the test-ws fixtures after ingesting a note (no 'search cluster not defined' error)"
    - "Validation remains strict — refs with an unseeded cluster still fail"
  artifacts:
    - path: "CONSTRUCT-CLAUDE-impl/construct/templates/search-seeds.json"
      provides: "reserved manual-ingest and web-ingest clusters seeded at init"
      contains: "manual-ingest"
    - path: "test-ws/my-construct/search-seeds.json"
      provides: "backfilled reserved clusters on the primary fixture"
      contains: "web-ingest"
    - path: "tests/integration/test_ingest_validate_e2e.py"
      provides: "ingest source then validate E2E test (audit repro, now green)"
      contains: "validate"
  key_links:
    - from: "src/construct/pipelines/ingestion.py search_cluster fallbacks"
      to: "search-seeds.json reserved clusters"
      via: "fallback names manual-ingest/web-ingest now resolve to seeded cluster ids"
      pattern: "manual-ingest|web-ingest"
    - from: "src/construct/services/validation.py:205 cluster-membership rule"
      to: "seeded reserved clusters"
      via: "ingested ref.search_cluster ∈ search_cluster_ids"
      pattern: "search_cluster_ids"
---

<objective>
Close ING-02: governed ingest currently stamps refs with `search_cluster` "manual-ingest"/"web-ingest" (ingestion.py:162,208; knowledge.py:747) but those clusters are never seeded, so validation.py:205 hard-fails every governed ingest. Fix by seeding `manual-ingest` and `web-ingest` as reserved SearchCluster entries in the init template (and thus at every workspace init) and backfilling them into the test-ws fixtures. Keep validation strict and the ingestion fallback names unchanged — make the data conform to the gate rather than weakening the gate.

Purpose: Restore the advertised `ingest source` → `validate` flow without compromising FND-02 write-time strictness.
Output: Reserved clusters seeded at init + in fixtures, and an E2E test reproducing the audit repro (now green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-close-v0-3-blockers-rt-03-mcp-schema-parity-ing-02-ingest-cl/07-CONTEXT.md
@.planning/v0.3-MILESTONE-AUDIT.md

<interfaces>
<!-- SearchCluster schema (config.py:208) — reserved entries must conform: -->
- SearchCluster: id (kebab-case, required), domain (kebab-case, required), terms (list, default []), weight (float 0.0-1.0, required), status (active|paused|exhausted, required), last_queried (datetime | null)
- SearchSeedsFile: version (int >=1), updated (datetime | null), clusters (list[SearchCluster])
- IMPORTANT: SearchCluster.domain is validated kebab-case — reserved clusters need a kebab-safe domain value (e.g. "ingest"), NOT a leading-underscore value. validation.py only checks ref.search_cluster against cluster IDS; the cluster's own domain field is not cross-checked against domains.yaml.

From src/construct/services/init.py:123 (_write_search_seeds):
- loads TEMPLATE_DIR/search-seeds.json (at CONSTRUCT-CLAUDE-impl/construct/templates/search-seeds.json), optionally overrides clusters from domain.research_seeds, validates via validate_search_seeds_write, writes to <root>/search-seeds.json
- NOTE: when domain.research_seeds is provided it REPLACES payload["clusters"] entirely — reserved clusters must survive that path (append, do not let the override drop them).

From src/construct/services/validation.py:139-146,205:
- search_cluster_ids = {cluster.id for cluster in search_seeds.clusters}
- rule: if search_cluster_ids and ref.search_cluster not in search_cluster_ids → error
</interfaces>

<repro>
<!-- Audit repro (was a hard ERROR, must now pass): -->
<!-- construct ingest source 'a test note' -w test-ws/my-construct  then  construct validate test-ws/my-construct -->
</repro>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed reserved manual-ingest/web-ingest clusters at workspace init</name>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/construct/templates/search-seeds.json (the init template — currently clusters: [])
    - src/construct/services/init.py:123-137 (_write_search_seeds; note research_seeds REPLACES clusters)
    - src/construct/schemas/config.py:202-231 (SearchCluster / SearchSeedsFile shapes and kebab-case constraint on id/domain)
    - src/construct/services/validation.py:139-146,205 (the membership rule the seeded clusters satisfy)
  </read_first>
  <action>
    Add two reserved SearchCluster entries — id `manual-ingest` and id `web-ingest` — to the init template `CONSTRUCT-CLAUDE-impl/construct/templates/search-seeds.json` "clusters" array. Each needs a kebab-safe `domain` (e.g. `ingest`), `terms: []`, a valid `weight` (e.g. 0.0), `status: "active"` (per SearchClusterStatus enum — there is no "reserved" status; "active" is the conformant choice), and `last_queried: null`.

    In init.py `_write_search_seeds`, ensure these two reserved clusters are ALWAYS present in the written file even when `domain.research_seeds` is provided (the current code REPLACES payload["clusters"]). Change the research_seeds branch to APPEND the domain seed cluster to the reserved clusters (or re-add the two reserved clusters after the override) so a freshly-init'd workspace with research seeds still validates ingested refs. Keep `validate_search_seeds_write(payload)` in the path. Do NOT change ingestion fallback names or validation.py.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && python -c "import json; c=json.load(open('CONSTRUCT-CLAUDE-impl/construct/templates/search-seeds.json'))['clusters']; ids={x['id'] for x in c}; assert {'manual-ingest','web-ingest'}.issubset(ids), ids; print('OK', ids)"</automated>
  </verify>
  <acceptance_criteria>
    - The init template search-seeds.json contains clusters with ids `manual-ingest` and `web-ingest`, each conforming to SearchCluster (kebab-case domain, weight 0.0-1.0, status in active/paused/exhausted).
    - A workspace initialized WITH research_seeds still has both reserved clusters in its search-seeds.json (the override path appends rather than drops them).
    - `validate_search_seeds_write` accepts the seeded template (no schema error).
    - ingestion.py and validation.py are unchanged.
  </acceptance_criteria>
  <done>Reserved clusters are seeded at init via the template and survive the research_seeds override path.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill reserved clusters into test-ws fixtures</name>
  <read_first>
    - test-ws/my-construct/search-seeds.json (primary fixture, has 3 domain clusters)
    - test-ws/ping-eon/search-seeds.json (second fixture)
    - src/construct/schemas/config.py:208 (SearchCluster shape, to match existing entry formatting)
  </read_first>
  <action>
    Add the same two reserved clusters (`manual-ingest`, `web-ingest`, with the same conformant fields as Task 1) to the existing `clusters` arrays in `test-ws/my-construct/search-seeds.json` and `test-ws/ping-eon/search-seeds.json`, preserving the existing domain clusters. Match the existing JSON formatting/indentation in each file. These are the fixtures the E2E and the audit repro run against. Do NOT alter the existing domain clusters or any ref files.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && python -c "import json; [print(p, {x['id'] for x in json.load(open(p))['clusters']}) or (lambda ids: __import__('sys').exit(0 if {'manual-ingest','web-ingest'}.issubset(ids) else 1))({x['id'] for x in json.load(open(p))['clusters']}) for p in ('test-ws/my-construct/search-seeds.json','test-ws/ping-eon/search-seeds.json')]"</automated>
  </verify>
  <acceptance_criteria>
    - Both `test-ws/my-construct/search-seeds.json` and `test-ws/ping-eon/search-seeds.json` contain clusters with ids `manual-ingest` and `web-ingest` in addition to their existing domain clusters.
    - The existing domain clusters (cosmology-core, etc.) are preserved unchanged.
    - `construct validate test-ws/my-construct` no longer emits a "search cluster 'manual-ingest' is not defined" / "'web-ingest' is not defined" error.
  </acceptance_criteria>
  <done>Both fixtures carry the reserved clusters; their existing clusters and refs are intact.</done>
</task>

<task type="auto">
  <name>Task 3: Ingest-then-validate E2E test (ING-02 audit repro, now green)</name>
  <read_first>
    - tests/integration/ (existing integration test layout/conventions; create dir/file if absent)
    - src/construct/pipelines/ingestion.py:140-218 (note/url ingest paths that stamp web-ingest/manual-ingest)
    - src/construct/services/validation.py:196-227 (the ref cluster check)
  </read_first>
  <action>
    Add `tests/integration/test_ingest_validate_e2e.py` reproducing the audit repro on an isolated copy: copy a fixture workspace (e.g. test-ws/my-construct) into pytest tmp_path (so the test does not mutate the committed fixture), run `ingest_source` for a NOTE source against it, then run `validate_workspace` and assert the report is valid (zero errors). Use the service/pipeline functions directly (not subprocess) for speed and determinism. The test asserts that the ingested ref's manual-ingest cluster resolves and validation passes — failing before this phase's seeding fix and passing after.

    Optionally add a second case ingesting a URL source (stamps web-ingest) to cover both fallback names.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && pytest tests/integration/test_ingest_validate_e2e.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_ingest_validate_e2e.py` exits 0.
    - The test ingests a note (and optionally a URL) into a tmp_path copy of a fixture, then asserts `validate_workspace` returns a report with zero errors.
    - The committed test-ws fixtures are not mutated by the test run (operates on tmp_path copy).
    - `pytest -q` exits 0 (no regressions).
  </acceptance_criteria>
  <done>ingest source → validate passes E2E on a clean workspace copy; the audit repro is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ingested source → ref write gate | Untrusted ingested content produces a ref that must pass write-time validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | weakening validation to admit ingest output | mitigate | Fix seeds the data to conform; validation.py:205 stays strict and unchanged so unseeded clusters still fail. |
| T-07-04 | Spoofing | reserved cluster id collision with a real domain cluster | accept | manual-ingest/web-ingest are reserved kebab ids unlikely to collide; domain seed clusters use `<domain>-seed` naming. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (seeding + test only). |
</threat_model>

<verification>
- `pytest tests/integration/test_ingest_validate_e2e.py -x -q` exits 0.
- `construct ingest source 'note' -w <copy>` then `construct validate <copy>` passes (audit repro).
- `pytest -q` exits 0.
</verification>

<success_criteria>
- Reserved clusters seeded at init and in both fixtures, conforming to SearchCluster.
- ingest → validate passes E2E without weakening validation.
- ingestion fallback names and validation.py unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-close-v0-3-blockers-rt-03-mcp-schema-parity-ing-02-ingest-cl/07-02-SUMMARY.md` when done.
</output>
