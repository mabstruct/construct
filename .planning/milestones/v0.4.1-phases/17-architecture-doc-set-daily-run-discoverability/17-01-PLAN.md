---
phase: 17-architecture-doc-set-daily-run-discoverability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONSTRUCT-CLAUDE-spec/architecture-overview.md
autonomous: true
requirements: [DOC-01]
tags: [documentation, architecture, layer-model]

must_haves:
  truths:
    - "architecture-overview.md presents ADR-0003's L0-L4 runtime model, with the Python pipeline runtime named as Layer 2 (D-01)."
    - "The views cache is referred to as 'derived view data'; the term 'Layer 2' no longer refers to the views cache (D-01)."
    - "No surviving claim that skills are the only legitimate writers to layer 1, at :73 and in the §8.1 decision tree (D-02)."
    - "The Python runtime's write-ownership of layer 1 is visible in the rewritten doc (D-02)."
    - "Invariants I1-I4 remain present, re-anchored to the workspace-SOT / derived-view-data relationship (D-01)."
    - "The five vocabulary-doc citations formerly at :262 resolve to CONSTRUCT-CLAUDE-impl/construct/references/ (real on-disk files) (D-03)."
    - "The spec-v02-data-model.md citations at :6, :102, :259 are preserved — the file exists (D-03)."
    - statement: "A full resolve-sweep of the architecture doc set finds no broken markdown/file citation beyond the five repointed vocab refs."
      verification: backstop
  artifacts:
    - CONSTRUCT-CLAUDE-spec/architecture-overview.md
  key_links:
    - "architecture-overview.md layer vocabulary <-> adr-0003 L0-L4 (must use identical numbering)"
  prohibitions:
    - "Do not delete the spec-v02-data-model.md citations — the file exists (D-03)."
    - "Do not introduce a second 'Layer N' vocabulary — exactly one survives."
    - "Do not edit the sealed v0.4-MILESTONE-AUDIT.md or other sealed planning records."
---

<objective>
Rewrite `CONSTRUCT-CLAUDE-spec/architecture-overview.md` so a v0.5 planner reading it sees the system that actually exists: ADR-0003's L0-L4 runtime layer model (including the Python runtime layer the doc currently omits), with the Python runtime's write-ownership visible, the false single-writer claim gone, and the genuinely-broken vocabulary citations repointed. Satisfies ROADMAP criterion 1.

Purpose: The current doc tells a v0.2 data-flow story ("The Three Layers": canonical -> views cache -> SPA) whose "Layer 2" means the views cache, colliding with ADR-0003's "Layer 2" = Python pipeline runtime. Two "Layer N" vocabularies is exactly the confusion DOC-01 removes.
Output: A rewritten architecture-overview.md with one layer vocabulary, correct write-ownership, and resolving references.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-CONTEXT.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md
@.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- **Modified:** `CONSTRUCT-CLAUDE-spec/architecture-overview.md` — full rewrite of the layer narrative onto ADR-0003's L0-L4 spine; no new symbols, functions, or files. This is prose only.

No new code symbols, tests, or config are created by this plan. (The catalog guard, the daily skill, and the `_MIGRATED_SKILLS` addition are produced by sibling plans 17-02 and 17-03.)

<tasks>

<!-- planner-discipline-allow: skills are the only -->
<!-- planner-discipline-allow: only legitimate writers -->

<task type="auto">
  <name>Task 1: Adopt ADR-0003's L0-L4 as the single layer vocabulary (D-01)</name>
  <files>CONSTRUCT-CLAUDE-spec/architecture-overview.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-spec/architecture-overview.md (the full file — §2 one-sentence pattern, §3 "The Three Layers" :28-89, §5 :108-135, §6 topology variants :137-207, §7 component table :211-224, §8 decision tree/anti-patterns :227-244)
    - CONSTRUCT-CLAUDE-spec/adrs/adr-0003-v03-pipeline-v04-ui.md (the "Layer model (permanent)" block at :136-150 — the canonical L0-L4 spine to adopt VERBATIM in numbering)
    - CONSTRUCT-CLAUDE-spec/adrs/adr-0001-claude-native-approach.md (markdown-as-truth — the claim invariants I1-I4 protect; must survive)
    - .planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md (Finding 2)
  </read_first>
  <action>
    Rewrite the layer narrative so ONE "Layer N" vocabulary survives: ADR-0003's runtime stack — Layer 0 skill specifications (SKILL.md + artifact catalog), Layer 1 workspace SOT (markdown + JSON), Layer 2 Python pipeline runtime (workflows, orchestration, validation, file I/O), Layer 3 invoke surface (CLI first -> MCP -> HTTP, one capability registry), Layer 4 UI shell (v0.5), with LLM gates as a cross-cutting concern invoked only at declared boundaries. Replace the §3 "The Three Layers" ASCII/prose (canonical -> views cache -> SPA) so that the views cache is called "derived view data" and is NEVER labelled "Layer 2". Re-express the one-way data-flow narrative (workspace -> derived view data -> presentation) as a PROPERTY within/across the L0-L4 stack, not as a competing numbering. Update §2's one-sentence pattern, §5 "Why One-Way Flow Matters", §6 topology variants, §7 "Where Components Live Today" (its Layer column), and §8.1 decision-tree items 1-3 to use the L0-L4 numbering and the "derived view data" name. Preserve invariants I1-I4 (§4 :93-104) intact but re-anchor their wording to the workspace-SOT <-> derived-view-data relationship (I1 single-writer, I2 read-only direction, I3 safe-delete, I4 no-novel-data); keep I4's reference to spec-v02-data-model.md (that citation is KEPT per D-03). Do NOT introduce any second layer numbering. Ground the runtime-layer description in the fact that the Python capability runtime (catalog.py registers the live capabilities) performs workflows/orchestration/validation/file I/O.
  </action>
  <verify>
    <automated>grep -c "derived view data" CONSTRUCT-CLAUDE-spec/architecture-overview.md</automated>
    <automated>grep -nE "Layer 2 — DERIVED STATE|the only writer to layer 2" CONSTRUCT-CLAUDE-spec/architecture-overview.md; test $(grep -cE "Layer 2 — DERIVED STATE" CONSTRUCT-CLAUDE-spec/architecture-overview.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "derived view data" CONSTRUCT-CLAUDE-spec/architecture-overview.md` is >= 1 (the views cache is renamed).
    - `grep -c "Layer 2 — DERIVED STATE" CONSTRUCT-CLAUDE-spec/architecture-overview.md` == 0 (no surviving "Layer 2 = views cache" heading).
    - The doc names a "Python" runtime layer as Layer 2 (behavior: `grep -niE "layer 2.*python|python (pipeline )?runtime" architecture-overview.md` returns at least one line).
    - Invariants I1, I2, I3, I4 all still appear (source assertion: the §4 table retains four rows) and I4 still references spec-v02-data-model.md.
    - Exactly one "Layer N" numbering scheme is present (manual review: no section still numbers the views cache as its own "Layer 2").
  </acceptance_criteria>
  <done>architecture-overview.md uses ADR-0003's L0-L4 numbering as the sole layer vocabulary; the views cache is "derived view data"; I1-I4 preserved and re-anchored.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the false single-writer claim; make Python-runtime write-ownership visible (D-02)</name>
  <files>CONSTRUCT-CLAUDE-spec/architecture-overview.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-spec/architecture-overview.md (§3.1 at :73 — the "Edits to layer 1 happen exclusively through Claude ... Skills like card-create ... write to workspace files" prose; §8.1 decision-tree item 4 at :236 — the named false claim; also review :39 and :81 for consistency with the rename)
    - src/construct/capabilities/catalog.py (the registered capabilities whose handlers call services/knowledge.py create_card/edit_card/archive_card/add_connection, research_run, curation_run, daily_run — the ground truth that the Python runtime owns writes)
    - CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md (Python layer owns side effects — supports the write-ownership framing)
    - .planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md (Finding 3)
  </read_first>
  <action>
    Remove the false claim that skills are the sole writers to layer 1. At §3.1 (:73), replace the "Skills like card-create, card-edit, research-cycle, curation-cycle write to workspace files" sentence with framing that Layer 2 (the Python runtime) owns every write to layer 1; skills, CLI, MCP, and the future UI reach it only by invoking registered capabilities through the invoke surface (Layer 3) — the capability registry is the single write contract. At §8.1 decision-tree item 4 (:236), rewrite the item so mutating layer-1 state means adding or modifying a registered capability in the Python runtime and invoking it through the invoke surface — NOT "skills are the only legitimate writers to layer 1". Review :39 ("Edited only by Claude (via skills...)") and the §3.2 single-writer prose (:81) and reconcile them with the corrected write-ownership and the "derived view data" rename. Optionally (D-10, not required) add a one-sentence note in the new Layer 2 description that daily.run is a thin synchronous Python composition over frozen children with no parent LangGraph graph — natural now that the doc describes the Python runtime layer. Keep the recommended D-02 leaning as the framing; exact prose is your discretion but the false claim must be gone and the Python runtime's write role must be visible.
  </action>
  <verify>
    <automated>test $(grep -c "only legitimate writers" CONSTRUCT-CLAUDE-spec/architecture-overview.md) -eq 0</automated>
    <automated>test $(grep -c "skills are the only" CONSTRUCT-CLAUDE-spec/architecture-overview.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "only legitimate writers" CONSTRUCT-CLAUDE-spec/architecture-overview.md` == 0.
    - `grep -c "skills are the only" CONSTRUCT-CLAUDE-spec/architecture-overview.md` == 0.
    - The rewritten :73 region no longer asserts skills write to workspace files as the exclusive path (manual review — the sentence about card-create/card-edit/research-cycle/curation-cycle writing to workspace files is gone or reframed as delegation).
    - The doc states the Python runtime (Layer 2) owns writes to layer 1, reached via registered capabilities through the invoke surface (behavior: a sentence to this effect exists).
  </acceptance_criteria>
  <done>The two false-writer offenders (:73, §8.1 item 4) are gone; Layer 2 Python-runtime write-ownership is stated explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the five broken vocabulary-doc citations; keep spec-v02-data-model.md (D-03)</name>
  <files>CONSTRUCT-CLAUDE-spec/architecture-overview.md</files>
  <read_first>
    - CONSTRUCT-CLAUDE-spec/architecture-overview.md (§9.3 Reference tables at :261-262 — the five citations `references/epistemic-types.md`, `connection-types.md`, `lifecycle-states.md`, `confidence-levels.md`, `source-tiers.md`; and §9.2 :259 + Related list :6 + §4 I4 :102 — the spec-v02-data-model.md citations that are KEPT)
    - CONSTRUCT-CLAUDE-impl/construct/references/ (verify the five files exist there: epistemic-types.md, connection-types.md, lifecycle-states.md, confidence-levels.md, source-tiers.md)
    - .planning/phases/17-architecture-doc-set-daily-run-discoverability/17-RESEARCH.md (Finding 1 — the resolve-sweep table)
  </read_first>
  <action>
    Repoint the five §9.3 vocabulary citations (currently `references/epistemic-types.md`, `connection-types.md`, `lifecycle-states.md`, `confidence-levels.md`, `source-tiers.md`, which resolve relative to CONSTRUCT-CLAUDE-spec/references/ — a directory that does not exist) to their real on-disk home under `../CONSTRUCT-CLAUDE-impl/construct/references/` (relative to CONSTRUCT-CLAUDE-spec/), and note they are workspace-shipped vocabulary docs (deploy to `.construct/references/`). Do NOT touch the spec-v02-data-model.md citations at :6, :102, and :259 — that file EXISTS (verified present, 25,298 bytes) and its citations are load-bearing (:102 anchors invariant I4's per-field derivation traceability). While editing, do a full resolve-sweep of the architecture doc set's cross-tree/relative citations to confirm nothing else is broken (research confirmed only these five are); if another broken link surfaces, flag it in the SUMMARY rather than silently expanding scope. Do NOT fix spec-v04-agentworkflows.md:210's `src/construct/search/config.yaml (new)` — that is a planned config path, outside this plan's fence.
  </action>
  <verify>
    <automated>for f in epistemic-types connection-types lifecycle-states confidence-levels source-tiers; do test -f "CONSTRUCT-CLAUDE-impl/construct/references/$f.md" || { echo "MISSING $f"; exit 1; }; done; echo ok</automated>
    <automated>grep -nE "CONSTRUCT-CLAUDE-impl/construct/references/(epistemic-types|connection-types|lifecycle-states|confidence-levels|source-tiers)" CONSTRUCT-CLAUDE-spec/architecture-overview.md</automated>
    <automated>test $(grep -c "spec-v02-data-model.md" CONSTRUCT-CLAUDE-spec/architecture-overview.md) -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - The five vocab citations now point under `CONSTRUCT-CLAUDE-impl/construct/references/` (or the correct relative equivalent `../CONSTRUCT-CLAUDE-impl/construct/references/`), and each target file exists on disk.
    - `grep -c "spec-v02-data-model.md" architecture-overview.md` >= 3 (the :6, :102, :259 citations are preserved).
    - No `references/epistemic-types.md` bare-relative form remains that would resolve to the non-existent CONSTRUCT-CLAUDE-spec/references/ dir (source assertion).
  </acceptance_criteria>
  <done>The five vocab refs resolve to real files; the spec-v02-data-model.md citations are untouched and present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> spec doc tree | A prose rewrite could introduce a citation that does not resolve, misleading a downstream v0.5 planner. No runtime/network/auth boundary is crossed — this is a Markdown file. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01-01 | Tampering | Repointed vocab citations (:262) | low | mitigate | Task 3 verify asserts each of the five target files exists on disk before/after repointing; grep confirms the new path form. |
| T-17-01-02 | Information disclosure | The layer rewrite could mis-state the write-ownership model | low | mitigate | Grounded in catalog.py + adr-0005 (read_first); grep gates confirm the false claim is removed. |
| T-17-01-SC | Tampering (supply chain) | package installs | low | accept | No npm/pip/cargo installs in this plan; no [ASSUMED]/[SUS] packages. Nothing to gate. |

No high-severity threats. The L1 items considered (input validation, auth, access control, crypto, session) are not applicable — this plan edits one Markdown file and adds no input-parsing, network, or auth surface.
</threat_model>

<verification>
- `grep -c "only legitimate writers" CONSTRUCT-CLAUDE-spec/architecture-overview.md` == 0
- `grep -c "skills are the only" CONSTRUCT-CLAUDE-spec/architecture-overview.md` == 0
- `grep -c "derived view data" CONSTRUCT-CLAUDE-spec/architecture-overview.md` >= 1
- `grep -c "Layer 2 — DERIVED STATE" CONSTRUCT-CLAUDE-spec/architecture-overview.md` == 0
- All five repointed vocab targets exist under CONSTRUCT-CLAUDE-impl/construct/references/
- `grep -c "spec-v02-data-model.md" architecture-overview.md` >= 3 (kept)
- No test regression: `pytest tests/contract/test_doc_command_references.py -q` still green (this doc is not globbed by that guard, but run the contract subset to confirm no collateral).
</verification>

<success_criteria>
architecture-overview.md presents ADR-0003's L0-L4 model with a single layer vocabulary, the Python runtime layer visible with correct write-ownership, no surviving single-writer claim, the five vocab citations resolving, and the spec-v02-data-model.md citations preserved.
</success_criteria>

<output>
Create `.planning/phases/17-architecture-doc-set-daily-run-discoverability/17-01-SUMMARY.md` when done.
</output>
