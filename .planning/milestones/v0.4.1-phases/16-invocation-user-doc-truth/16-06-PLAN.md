---
phase: 16-invocation-user-doc-truth
plan: 06
type: execute
wave: 4
depends_on: ["16-04", "16-05"]
files_modified:
  - USER-TEST-PLAYBOOK-v041.md
  - USER-TEST-PLAYBOOK-v03.md
  - tests/contract/test_doc_command_references.py
autonomous: true
requirements: [DOC-04, FIX-03]

must_haves:
  truths:
    - "A release-validation playbook exists for v0.4.1, organised by capability rather than by v0.3 delivery-phase number, and it is the document the invocation guard scans — the glob entry is swapped, never dropped."
    - "Every step of the playbook that does not carry an explicit credential marker runs against the mock/default search provider and offline paths, so a validator with only a checkout can execute the whole core path."
    - "Steps that require `ANTHROPIC_API_KEY` or a live search key are marked in their heading, following the v0.3 file's own precedent, and are opt-in extras rather than gates on the core run."
    - "Assertions for `curation run`, `research run`, and `daily run` read per-step `completed` / `degraded` / `skipped` state from `--json` output, never a process exit code — a degraded curation run exits 0 by design and an exit-code assertion would green-light it."
    - "The playbook covers the successor flow for the two removed workflow commands — a research run through durable human review to resume, plus curation run, review, and inspect — rather than dropping the section that documented them."
    - "The playbook exercises `daily run`, `daily inspect`, `card evaluate`, `views generate`, `views validate`, and `knowledge card list`."
    - "`_KNOWN_BROKEN` is empty, reached by removing the two broken invocations from a document that REMAINS in `_DOC_GLOBS`; the number of entries in `_DOC_GLOBS` is unchanged by the supersession."
    - "Every `construct ...` invocation in the playbook sits on a single line — the extractor's horizontal-whitespace-only matching cannot see a wrapped command."
    - statement: "No step's pass criterion depends on a numeric threshold, rounding rule, or tie-break that this phase introduces; every playbook assertion is a state or presence check, and the only numeric guards in the test suite remain lower bounds rather than equalities."
      verification: backstop
  artifacts:
    - "USER-TEST-PLAYBOOK-v041.md — the v0.4.1 release-validation artifact"
    - "tests/contract/test_doc_command_references.py — `_DOC_GLOBS` and `_MUST_CARRY_INVOCATIONS` pointing at the v0.4.1 playbook; `_KNOWN_BROKEN` empty"
  key_links:
    - "`USER-TEST-PLAYBOOK-v041.md` -> `_DOC_GLOBS` -> `test_documented_commands_resolve` — the swap is what makes the two `workflow` allowlist deletions honest rather than a shrunken scan"
    - "`USER-TEST-PLAYBOOK-v041.md` -> `_MUST_CARRY_INVOCATIONS` — the non-vacuity guard must follow the rename or it silently starts asserting against a deleted file"
    - "playbook `--json` assertions -> Phase 11's curation exit-code contract — the reason the assertions read status fields"
  prohibitions:
    - statement: "The allowlist must not reach empty by removing a document from the scan surface; the v0.3 playbook is superseded by a v0.4.1 file that takes its place in the glob set, and the glob entry count does not decrease."
      status: unverified
      verification: flagged
    - statement: "No playbook step may require a real API key or live search credential to execute unless its heading explicitly marks it as credentialed; release validation must stay runnable by anyone with a checkout."
      status: unverified
      verification: flagged
    - statement: "No step may assert a process exit code as the verdict for a run that degrades by design; asserting exit 0 as success would silently pass a degraded curation run."
      status: unverified
      verification: flagged
    - statement: "Working sections of the v0.3 playbook must not be dropped during the carry-forward; coverage may be reorganised and extended, never quietly reduced."
      status: unverified
      verification: flagged
    - statement: "No credential, API key, or token value may be written into the playbook, including as an illustrative example."
      status: unverified
      verification: flagged
---

<objective>
Supersede `USER-TEST-PLAYBOOK-v03.md` with `USER-TEST-PLAYBOOK-v041.md` — reorganised by capability, offline-runnable by default, asserting on JSON status fields — and empty `_KNOWN_BROKEN` by removing the two `workflow` invocations from a document that stays under the guard.

Purpose: This is the load-bearing choice of the phase. `.planning/STATE.md` warns that retiring the playbook removes the `workflow run` and `workflow resume` allowlist entries, and that the guard must end **empty, not merely unscanned**. Deleting the file and dropping its glob entry would satisfy FIX-03 on paper while weakening the very test that defines it. Under supersession those two entries die because the commands are gone from a still-scanned document. Separately, `spec-v05-ui-primary.md` already anticipates a v0.5 playbook, so the artifact is designed to live on per version; deleting outright would leave v0.4.1 with no executable release-validation artifact at all.

Output: A v0.4.1 playbook, a swapped glob entry, and an empty allowlist.

Per D-05 (superseded, not deleted; new file stays in `_DOC_GLOBS`), D-06 (coverage carried and reorganised by capability), D-07 (offline by default), D-08 (assert JSON status, not exit codes), D-16.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md
@.planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md
@.planning/phases/16-invocation-user-doc-truth/16-03-SUMMARY.md
@.planning/phases/16-invocation-user-doc-truth/16-05-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created across Phase 16 (excluded from drift verification — all are new):
`_MUST_CARRY_INVOCATIONS`, `test_key_docs_are_not_vacuous` (`tests/contract/test_doc_command_references.py`); `_allowed_tools_text`, `test_allowed_tools_text_is_not_vacuous` (`tests/contract/test_skill_migration.py`); `list_cards`, `_json_safe` (`src/construct/services/knowledge.py`); `CardListInput`, capability id `knowledge.card.list`, MCP tool name `construct_list_cards` (`src/construct/capabilities/catalog.py`); `card_list` Typer callback and its `--domain`/`-d`, `--include-archived`, `--workspace`/`-w`, `--json`/`-j` flags (`src/construct/cli.py`); `TestCardList` (`tests/unit/test_knowledge_operations.py`); new file `tests/contract/test_card_list_cli_mcp.py`; new file `USER-TEST-PLAYBOOK-v041.md`; a CLI-invocation table column in `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` and `CONSTRUCT-CLAUDE-impl/construct/references/commands.md`.

## Invocation form the playbook MUST use

The guard's extractor matches only strings beginning with the literal token `construct` followed by a lowercase token, on one line. Every executable step therefore writes `construct <group> <leaf> ...` inside a fenced block, after the setup section has activated the virtualenv — NOT `.venv/bin/python -m construct.cli ...`, which the extractor cannot see and which would make the playbook invisible to the guard that defines this phase's completion. The virtualenv activation belongs in the setup section, once.

<tasks>

<task type="auto">
  <name>Task 1: Write the v0.4.1 playbook — setup through ingestion</name>
  <read_first>
    - USER-TEST-PLAYBOOK-v03.md (full file, 453 lines — you need §0.1 Tooling including the stale model-routing note at ~:36, §0.2's fresh-smoke-workspace recipe, and §§1-4 in full so nothing working is dropped)
    - .planning/phases/16-invocation-user-doc-truth/16-03-SUMMARY.md (the shipped `knowledge card list` flag surface and JSON envelope)
    - .planning/phases/16-invocation-user-doc-truth/16-05-SUMMARY.md (the settled command-to-capability mapping, so the playbook and the user docs agree)
    - .planning/STATE.md (Phase 14 decisions naming `src/construct/llm/config.yaml` as the LLM config authority, `model-routing.yaml` deprecated and inert)
    - src/construct/services/init.py and CONSTRUCT-CLAUDE-impl/construct/templates/ (to confirm the smoke-workspace scaffolding steps still match what `construct init` produces)
  </read_first>
  <files>USER-TEST-PLAYBOOK-v041.md</files>
  <action>
Create `USER-TEST-PLAYBOOK-v041.md` at the repository root and write its first half.

**Structure.** Organise by capability, not by v0.3 delivery phase. The existing "§1 (Phase 1) … §8 (Phase 6)" numbering encodes a delivery history that maps to nothing a user cares about, and it is why the file drifted. Drop every "(Phase N)" heading suffix. Keep the per-step shape that works: a heading, a fenced block of commands, an **Expected** paragraph, and a **Pass** criterion.

**Section 0 — Prerequisites & setup.** Carry §0.1 forward but correct its LLM note: it currently presents `.construct/model-routing.yaml` as the per-workspace routing authority. Phase 14 settled that `src/construct/llm/config.yaml` is the authority and that `model-routing.yaml` is deprecated and inert. Phase 14 D-02 explicitly fenced this line to this phase, so it must be corrected during the carry rather than assumed gone. State the offline-by-default posture here: the search provider defaults to mock and every section runs without credentials unless its heading says otherwise.

Carry §0.2's fresh-smoke-workspace recipe essentially intact — it is the reusable asset of the whole document, and it correctly warns against testing against the committed `test-ws/` fixtures. Verify the version-check step's expected string against what the package actually reports today rather than carrying `0.3.0` forward.

Activate the virtualenv once, here. Every later section then writes bare `construct ...` invocations, which is what makes them visible to the guard.

**Section — Workspace contract & governance.** Carry the clean-workspace validation, the ownership-categories inspection, and the corruption-rejection negative test. These are unchanged capabilities.

**Section — Governed knowledge operations.** Carry card creation, editing, typed connection creation, connection listing, archive-preserves-connections, and the event-log check. **Add** a card-enumeration step using `construct knowledge card list` with the flags `16-03-SUMMARY.md` records, asserting that the JSON payload carries frontmatter fields and that no object carries a body key — the D-02 contract deserves a release-validation step, not only a unit test.

**Section — Capability registry, CLI & MCP spine.** Carry the MCP stdio server start check. Extend it to confirm the newly registered card-enumeration tool appears on the MCP surface, since D-01's whole point is that parity is free rather than hand-wired.

**Section — Ingestion.** Carry the local-note and file ingestion steps.

Every invocation on one line. No credential value appears anywhere in the file, not even as an illustrative placeholder that looks like a real key.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -c "
from pathlib import Path
from tests.contract.test_doc_command_references import _invocations, _resolves
p = Path('USER-TEST-PLAYBOOK-v041.md')
inv = _invocations(p.read_text(encoding='utf-8'))
bad = sorted(i for i in inv if not _resolves(i))
assert not bad, bad
assert ('knowledge','card','list') in inv, sorted(inv)
assert len(inv) >= 10, len(inv)
print('ok', len(inv))
"</automated>
  </verify>
  <acceptance_criteria>
    - The verify script exits 0 with at least 10 resolving invocations, including the card-enumeration path
    - `grep -c 'Phase [0-9])' USER-TEST-PLAYBOOK-v041.md` outputs `0` (no delivery-phase numbering in headings)
    - `grep -c 'model-routing' USER-TEST-PLAYBOOK-v041.md` outputs `0`, or every occurrence is on a line also containing a deprecation marker
    - `grep -c 'llm/config.yaml' USER-TEST-PLAYBOOK-v041.md` outputs a value of at least 1
    - `grep -cE 'sk-[A-Za-z0-9]|tvly-[A-Za-z0-9]' USER-TEST-PLAYBOOK-v041.md` outputs `0` (no credential-shaped literal)
    - `grep -c 'test-ws/' USER-TEST-PLAYBOOK-v041.md` outputs a value of at least 1 and that line warns against using it (the fixture-protection note survives the carry)
    - `awk '/^```/{f=!f;next} f && /construct /' USER-TEST-PLAYBOOK-v041.md | grep -cE 'construct +[a-z][a-z -]*\\\\$'` outputs `0` (no invocation continued onto a following line)
  </acceptance_criteria>
  <done>The playbook's setup and first four capability sections exist, carry every working v0.3 step, add card enumeration and MCP tool-surface confirmation, correct the stale config reference Phase 14 fenced here, and every invocation resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Write the v0.4.1 playbook — workflows through teardown</name>
  <read_first>
    - USER-TEST-PLAYBOOK-v03.md (§5 in full including §5.1's help suggestion step and the two broken steps at ~:273-297; §6's credentialed-heading precedent at ~:299; §7 including the Phase-15-corrected §7.1; §§8-10 and the results summary)
    - USER-TEST-PLAYBOOK-v041.md (the first half, as written in Task 1 — for section numbering and per-step shape)
    - .planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md (D-06's replacement mapping, D-07, D-08)
    - src/construct/llm/curation_run.py and src/construct/llm/daily_run.py (the step-status vocabulary a `--json` assertion reads: which values a step's status field can take, and where the run-level degraded determination lives)
    - .planning/STATE.md (Phase 11's curation exit-code contract — degraded exits 0 on purpose; Phase 13's daily isolate-and-degrade reporting)
  </read_first>
  <files>USER-TEST-PLAYBOOK-v041.md</files>
  <action>
Write the second half.

**Section — Guided workflow operability (the replacement).** §5.1's workspace-aware suggestion step resolves today and carries forward. §5.2 and §5.3 invoke the two commands Phase 12 removed and are the source of both remaining allowlist entries; they are **replaced**, not deleted, by the real successor flow:
- a research run that pauses at durable human review, then a review step, then a resume — exercising the durable checkpointer that Phase 14's DOC-03 recorded as intentional architecture;
- a curation run followed by review and inspect.

Keep §5.2's honest caveat spirit: where a step degrades offline because a provider or key is absent, say so in the Expected paragraph rather than presenting a degraded run as a failure.

**Assertion style throughout this section and the next — D-08.** Read the per-step `completed` / `degraded` / `skipped` state out of the `--json` payload. Do NOT write a step whose Pass criterion is "exit code 0". Phase 11 established that a degraded curation run exits 0 **on purpose** and that failure surfaces in the status output, the JSON, and the event log; daily's isolate-and-degrade reporting works the same way. A step asserting exit 0 as success would silently green-light a degraded run, and this playbook's job is to be a build signal.

**New section — Daily cycle.** `daily run` and `daily inspect`. This is the flagship v0.4 capability and has never appeared in a release-validation artifact. It degrades each child offline, so it runs credential-free; assert the per-child reported state.

**New section — Card evaluation.** `card evaluate`. Mark the heading as requiring `ANTHROPIC_API_KEY` if the capability needs a live gate; confirm from the source rather than assuming, and if it degrades gracefully offline, keep it in the core path and assert the degraded reporting instead.

**Section — Grounded synthesis & graph reasoning.** Carry forward, keeping its existing credentialed heading marker. That marker is the precedent D-07 generalises: a heading suffix naming the required credential, with everything unmarked guaranteed offline-runnable.

**Section — Derived data & views.** Carry §7 forward. Its first step was corrected by Phase 15 D-07 — preserve that correction, do not regress it. **Add** `views generate` and `views validate` steps using the flag surface Phase 15 shipped. Note in the Expected text that `views validate` does not currently accept every file `views generate` writes; that is a known-open contract question recorded by Phase 15, and a validator hitting it should recognise it rather than file it as a new defect.

**Section — Governed spikes & tag extraction.** Carry the spike listing, tag extraction, and human-gated approval steps.

**Section — Machine-readable output.** Carry forward. This is the natural home for a cross-cutting statement of the D-08 rule: the `--json` envelope is the verdict surface, the exit code is not.

**Section — Teardown.** Carry forward.

**Results summary.** Carry the table forward, re-keyed to the new capability section names.

Every invocation on one line, in a fenced block, after the setup section's activation. No credential values.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -c "
from pathlib import Path
from tests.contract.test_doc_command_references import _invocations, _resolves
p = Path('USER-TEST-PLAYBOOK-v041.md')
inv = _invocations(p.read_text(encoding='utf-8'))
bad = sorted(i for i in inv if not _resolves(i))
assert not bad, bad
for req in [('research','run'),('research','review'),('curation','run'),('curation','review'),('curation','inspect'),('daily','run'),('daily','inspect'),('card','evaluate'),('views','generate'),('views','validate'),('knowledge','card','list')]:
    assert req in inv, req
assert not any(i[0]=='workflow' and i[1] in ('run','resume') for i in inv), sorted(inv)
assert len(inv) >= 22, len(inv)
print('ok', len(inv))
"</automated>
  </verify>
  <acceptance_criteria>
    - The verify script exits 0: every criterion-named command path is present, no removed `workflow` subcommand appears, and the total invocation count is at least 22 (matching or exceeding the v0.3 file's coverage)
    - `grep -ciE 'exit code 0|exit 0' USER-TEST-PLAYBOOK-v041.md` outputs `0` for the curation, research, and daily sections (verify by reading; no Pass criterion in those sections names an exit code)
    - `grep -cE 'completed|degraded|skipped' USER-TEST-PLAYBOOK-v041.md` outputs a value of at least 3
    - `grep -c 'ANTHROPIC_API_KEY' USER-TEST-PLAYBOOK-v041.md` outputs a value of at least 1, and every occurrence in a heading marks that section as credentialed
    - `grep -cE '^#+ ' USER-TEST-PLAYBOOK-v041.md` returns a value of at least 30 (the v0.3 file had 34 headings; coverage was carried and extended, not reduced)
    - `grep -cE 'sk-[A-Za-z0-9]|tvly-[A-Za-z0-9]' USER-TEST-PLAYBOOK-v041.md` outputs `0`
    - The document contains a results-summary table keyed to the new capability section names
  </acceptance_criteria>
  <done>The playbook covers the full v0.4.1 surface including the successor flow for both removed workflow commands, asserts on JSON status rather than exit codes, marks credentialed sections explicitly, and carries every working v0.3 section forward.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the v0.3 playbook, swap the glob entry, and empty the allowlist</name>
  <read_first>
    - tests/contract/test_doc_command_references.py (`_DOC_GLOBS` at ~:41-45, `_MUST_CARRY_INVOCATIONS` as added by 16-01, `_KNOWN_BROKEN` now holding two entries, `_doc_files` at ~:131-138, `test_known_broken_entries_are_still_broken` at ~:212-220)
    - .planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md (D-05 and D-16 — the supersession rationale and the commit-ordering constraint)
    - .planning/STATE.md (the coordination warning: the guard must end empty, not merely unscanned)
    - USER-TEST-PLAYBOOK-v041.md (as completed in Tasks 1-2)
    - grep the repository for other references to the v0.3 playbook filename before deleting it
  </read_first>
  <files>tests/contract/test_doc_command_references.py, USER-TEST-PLAYBOOK-v03.md</files>
  <action>
Perform the three moves as one coherent change, in this order, so the guard is never scanning a file that does not exist and never silently stops scanning a playbook:

1. In `_DOC_GLOBS`, **replace** the v0.3 playbook entry with the v0.4.1 filename. Replace, never remove: the tuple must still hold three entries after this edit. A diff that removes a glob line without adding one is the exact warning sign RESEARCH names for Pitfall 1.
2. In `_MUST_CARRY_INVOCATIONS`, update the playbook entry to the new filename. If this is missed, the non-vacuity guard starts asserting against a deleted path and fails loudly — which is the correct failure mode, but fix it here rather than discovering it in 16-07.
3. Delete `USER-TEST-PLAYBOOK-v03.md`. Before deleting, grep the repository for references to the filename and update any that survive — a dangling link is a smaller version of the same defect this phase closes.
4. Delete both remaining `workflow` keys from `_KNOWN_BROKEN`, leaving an empty dict. Keep the type annotation and the explanatory comment block; the empty allowlist is the phase's terminal state and the comment explains why it can only shrink. `test_known_broken_entries_are_still_broken` parametrized over an empty dict yields zero test cases, which pytest reports as an empty parametrization rather than a failure — that is the intended terminal state, not a problem to work around.

These deletions are legitimate under D-16 specifically because the invocations are gone from a document that **remains** in the scan set, not because the scan set shrank. Record that reasoning in the summary; a future reader must be able to tell the difference between an emptied allowlist and a narrowed guard.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -c "
from tests.contract.test_doc_command_references import _KNOWN_BROKEN, _DOC_GLOBS, _MUST_CARRY_INVOCATIONS, _doc_files
assert _KNOWN_BROKEN == {}, _KNOWN_BROKEN
assert len(_DOC_GLOBS) == 3, _DOC_GLOBS
names = [p.name for p in _doc_files()]
assert 'USER-TEST-PLAYBOOK-v041.md' in names, names
assert 'USER-TEST-PLAYBOOK-v03.md' not in names, names
assert any('v041' in m for m in _MUST_CARRY_INVOCATIONS), _MUST_CARRY_INVOCATIONS
print('ok')
" && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - The verify script exits 0: the allowlist is empty, the glob tuple still holds three entries, the v0.4.1 playbook is in the resolved document set, the v0.3 playbook is not, and the non-vacuity set follows the rename
    - `.venv/bin/python -m pytest -q` exits 0 for the full suite
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_known_broken_entries_are_still_broken" -q` reports zero collected tests without erroring
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous" -q` exits 0 with 3 passing cases
    - `test -f USER-TEST-PLAYBOOK-v03.md` exits non-zero (the file is gone)
    - `grep -rn 'USER-TEST-PLAYBOOK-v03' --include='*.md' --include='*.py' . | grep -v '^./.planning/' | wc -l` outputs `0` (no dangling reference outside the planning archive)
    - `git diff tests/contract/test_doc_command_references.py | grep -c '^-.*_DOC_GLOBS'` and the corresponding `^+` count are equal, or the tuple's length assertion above covers it (no glob line removed without a replacement)
  </acceptance_criteria>
  <done>`_KNOWN_BROKEN` is empty, reached by removing two invocations from a document that remains scanned; the glob set is the same size; the v0.3 playbook is gone with no dangling references; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| playbook step -> validator's shell and workspace | A release validator runs every fenced command verbatim against a scratch workspace |
| playbook step -> external services | Steps that reach a live LLM or search provider cross a network and credential boundary |
| `_DOC_GLOBS` -> the guard's scan surface | What the guard scans defines what FIX-03 means; narrowing it is indistinguishable from fixing it unless the entry count is watched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | `_DOC_GLOBS` / `_KNOWN_BROKEN` | high | mitigate | D-05 supersession keeps the glob count at three, asserted directly in Task 3's verify; the two allowlist deletions are justified by a still-scanned document, not by a shrunken scan |
| T-16-04 | Information Disclosure | credentialed playbook steps | medium | mitigate | D-07: mock/default search provider and offline paths for everything unmarked; credentialed sections carry an explicit heading marker; no key-shaped literal appears in the file, enforced by a pattern check in both writing tasks |
| T-16-15 | Repudiation | `curation run` / `daily run` pass criteria | medium | mitigate | D-08: assertions read per-step `completed` / `degraded` / `skipped` from the `--json` payload. An exit-code assertion would report a degraded run as a passing release validation — a false build signal, which is the worst outcome for this artifact |
| T-16-16 | Denial of Service | smoke-workspace setup | low | accept | The playbook explicitly directs validators to a throwaway directory and warns against the committed `test-ws/` fixtures; carried forward from v0.3 where it has worked |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | Zero external packages installed; `pyproject.toml` untouched (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
```
.venv/bin/python -m pytest -q
```

Must be fully green. The terminal-state checks:

```
.venv/bin/python -c "from tests.contract.test_doc_command_references import _KNOWN_BROKEN as k, _DOC_GLOBS as g; print(len(k), len(g))"
```

Must print `0 3`. Zero allowlist entries with three glob entries is the signature of a guard that ended empty rather than unscanned. Zero and two would mean the opposite and must be treated as a failure of this plan regardless of what the suite reports.

`pytest -v` on `test_documented_commands_resolve` should show a parametrized case for the v0.4.1 playbook and none for v0.3.
</verification>

<success_criteria>
- `USER-TEST-PLAYBOOK-v041.md` exists, is organised by capability, and carries at least 22 resolving invocations
- Every unmarked step runs offline against the mock/default provider; credentialed steps are marked in their headings
- The successor flow replaces both removed workflow commands; `daily run|inspect`, `card evaluate`, `views generate|validate`, and `knowledge card list` are all exercised
- Pass criteria for curation, research, and daily read `--json` status fields, never exit codes
- `_KNOWN_BROKEN` is empty and `_DOC_GLOBS` still holds three entries
- `USER-TEST-PLAYBOOK-v03.md` is deleted with no dangling references outside `.planning/`
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/16-invocation-user-doc-truth/16-06-SUMMARY.md` when done. Record the playbook's section list, its total invocation count, which sections are credentialed, and an explicit statement that the two allowlist deletions came from a still-scanned document rather than a narrowed scan.
</output>
