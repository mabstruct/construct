---
phase: 16-invocation-user-doc-truth
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/construct/services/knowledge.py
  - src/construct/capabilities/catalog.py
  - src/construct/cli.py
  - tests/unit/test_knowledge_operations.py
  - tests/contract/test_card_list_cli_mcp.py
  - tests/contract/test_mcp_contracts.py
  - tests/contract/test_doc_command_references.py
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "`construct knowledge card list` resolves against the live Typer app as a leaf command, and `construct knowledge card list --domain <d> --json` (a leaf plus trailing argument tokens) also resolves; the `knowledge card` group continues to resolve on its own as a group."
    - "`list_cards` against a workspace whose `cards/` directory exists but holds no card files returns success with an empty data list and a message naming zero cards; against a workspace with no `cards/` directory at all it returns an unsuccessful OperationResult carrying an OperationError, never an empty success and never a raised exception."
    - "The `--domain` filter matches by exact string equality against the elements of each card's `domains` list — no case folding, no substring matching, no Unicode normalization — so a card in domain `Cosmology` is not returned by `--domain cosmology`."
    - "`list_cards` returns cards in the order `WorkspaceLoader.iter_cards()` yields them, which is `sorted()` over the `cards/*.md` glob; two consecutive calls against an unchanged workspace return identically ordered lists."
    - "`construct knowledge card list --json` emits parseable JSON for every card: date-valued frontmatter fields are rendered as ISO-8601 strings, so `json.loads` on the CLI's stdout succeeds."
    - "No object returned by `list_cards` carries a `body` key — the enumerate call returns frontmatter only, never card prose (D-02)."
    - "`knowledge.card.list` is reachable from BOTH the Typer CLI and the MCP surface, and `src/construct/mcp/server.py` contains no card-list-specific wiring — parity comes from registry auto-discovery."
  artifacts:
    - "src/construct/services/knowledge.py — `list_cards` handler and the `_json_safe` coercion helper"
    - "src/construct/capabilities/catalog.py — `CardListInput` model and the `knowledge.card.list` CapabilityRecord carrying both `cli_name` and `mcp_tool_name`"
    - "src/construct/cli.py — `card_list` wrapper registered as `@card_app.command(\"list\")`"
    - "tests/unit/test_knowledge_operations.py — `TestCardList`"
    - "tests/contract/test_card_list_cli_mcp.py — CLI/MCP parity contract suite"
  key_links:
    - "`CardListInput.workspace` -> `list_cards(workspace, ...)` — the field name and the handler's first parameter name must match, because MCP calls the handler with keyword arguments while the CLI calls it positionally; a mismatch here is the RT-03 defect class"
    - "`CapabilityRecord.mcp_tool_name` -> `registry.list_mcp_tools()` -> `tests/contract/test_mcp_contracts.py::test_mcp_tool_count` — that test compares against an exact expected set, so a new MCP tool that is not added there fails the suite"
    - "`WorkspaceLoader.load_cards()` -> `list_cards` — `load_cards` deliberately returns `datetime.date` objects and injects a `body` key; both must be handled in the handler, not in `_display_result`"
  prohibitions:
    - statement: "Card bodies must never leave through the enumerate call; an enumerate command that returns the graph's prose is an information-disclosure surface, not merely a verbose one."
      status: unverified
      verification: flagged
    - statement: "The date-serialization fix must be made in the `list_cards` handler, not by adding a `default=` or a custom encoder to `_display_result`, which is shared by every CLI command and would silently change every other command's output contract."
      status: unverified
      verification: flagged
    - statement: "`knowledge card list` must be registered through the capability registry; the hand-written views-group CLI/MCP exception must not be used as a precedent for a second bypass."
      status: unverified
      verification: flagged
    - statement: "Card frontmatter parsing must not be re-implemented in the handler when `WorkspaceLoader.load_cards()` already owns it."
      status: unverified
      verification: flagged
---

<objective>
Implement `knowledge card list` as a real, registry-routed command with full CLI and MCP parity, and delete its entry from `_KNOWN_BROKEN`.

Purpose: The graph already has an enumerate command for edges (`knowledge connection list`) but none for nodes. Two skills document the node half and it does not exist. D-01 closes that asymmetry through the registry rather than by rewriting the skills onto `Read` globs, which would reverse Phase 12's thin-wrapper direction. This is the only code change in the phase and it unblocks the skill rewrites (16-04) and the CLI columns (16-05).

Output: A working `construct knowledge card list` on both surfaces, six unit tests, a parity contract suite, and one fewer allowlist entry.

Per D-01 (registry-routed, both `cli_name` and `mcp_tool_name`), D-02 (full frontmatter, never bodies), D-16 (the allowlist entry is deleted in the same change that makes the command resolve).

**D-04 — surface-count fence.** This plan takes the live surface from 33 to 34 CLI leaves and from 21 to 22 MCP tools. That consequence is recorded for Phase 17's DOC-02, which owns the capability / CLI / MCP inventory in `artifact-catalog.md` and must inventory the post-Phase-16 reality. There is **no blocking dependency in either direction**: this plan does not edit `artifact-catalog.md` and must not, and Phase 17 must not block waiting on this phase. Record the new counts in this plan's summary so Phase 17 has a settled starting number, but write no count into any shipped document (D-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md
@.planning/phases/16-invocation-user-doc-truth/16-PATTERNS.md
@.planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md
@.planning/phases/16-invocation-user-doc-truth/16-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created across Phase 16 (excluded from drift verification — all are new):
`_MUST_CARRY_INVOCATIONS`, `test_key_docs_are_not_vacuous` (`tests/contract/test_doc_command_references.py`); `_allowed_tools_text`, `test_allowed_tools_text_is_not_vacuous` (`tests/contract/test_skill_migration.py`); `list_cards`, `_json_safe` (`src/construct/services/knowledge.py`); `CardListInput`, capability id `knowledge.card.list`, MCP tool name `construct_list_cards` (`src/construct/capabilities/catalog.py`); `card_list` Typer callback and its `--domain`/`-d`, `--include-archived`, `--workspace`/`-w`, `--json`/`-j` flags (`src/construct/cli.py`); `TestCardList` (`tests/unit/test_knowledge_operations.py`); new file `tests/contract/test_card_list_cli_mcp.py` with `_CAPS`, `test_registered`, `test_handler_accepts_workspace_keyword`, `test_in_mcp_tool_list`, `test_mcp_no_hardcoded_card_list`, `test_cli_commands_present`, `test_cli_mcp_schema_parity`; new file `USER-TEST-PLAYBOOK-v041.md`; a CLI-invocation table column in `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` and `CONSTRUCT-CLAUDE-impl/construct/references/commands.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the RED tests for card list — unit behaviour and CLI/MCP parity</name>
  <read_first>
    - tests/unit/test_knowledge_operations.py (`_sample_card_data` at ~:33, the `init_workspace` and `workspace_with_cards` fixtures at ~:95-106, and `TestConnectionList` at ~:335-384 — the class-per-command, one-method-per-filter-dimension shape to copy)
    - tests/contract/test_daily_run_cli_mcp.py (full file — module-level `_CAPS` dict, `CliRunner`, `test_registered`, `test_in_mcp_tool_list`, `test_mcp_no_hardcoded_daily`, `test_cli_commands_present`, `test_cli_mcp_schema_parity`)
    - src/construct/services/knowledge.py (`list_connections` at ~:558-600 and `_get_archived_card_ids` at ~:603-617 — the OperationResult envelope and the archive-filter idiom)
    - src/construct/storage/workspace.py (`iter_cards` and `load_cards` at ~:141-175 — read the `load_cards` docstring in full; it explicitly states dates stay as `datetime.date` and shows `card_data["body"] = body`)
    - src/construct/cli.py (`_display_result` at ~:165-192 — note the bare `json.dumps` with no `default=`)
  </read_first>
  <files>tests/unit/test_knowledge_operations.py, tests/contract/test_card_list_cli_mcp.py</files>
  <behavior>
    - `list_cards(workspace)` on a workspace with two cards returns `success is True` and `len(result.data) == 2`
    - `list_cards(workspace, domain="test-domain")` returns only cards whose `domains` list contains that exact string; a differently-cased domain string returns zero cards
    - `list_cards(workspace, include_archived=False)` omits a card archived via `archive_card`; `include_archived=True` includes it
    - No dict in `result.data` has a `body` key
    - `json.dumps(result.data)` succeeds without a `default=` argument, and the `created` value round-trips as an ISO-8601 string
    - `list_cards` on a workspace whose `cards/` directory exists but is empty returns `success is True` with `result.data == []`
    - `list_cards` on a path with no `cards/` directory returns `success is False` with at least one `OperationError`, and raises nothing
    - Two consecutive `list_cards` calls on an unchanged workspace return the same `[c["id"] for c in result.data]` sequence
  </behavior>
  <action>
Add `TestCardList` to `tests/unit/test_knowledge_operations.py`, immediately after `TestConnectionList`, following that class's shape exactly: reuse the `workspace_with_cards` fixture, one method per behaviour listed above, and `create_card(workspace, _sample_card_data(id=..., ...))` to add cards with distinct domains where a filter dimension needs one. Import `list_cards` from `construct.services.knowledge` alongside the existing imports. The empty-workspace case uses the `init_workspace` fixture (a scaffolded workspace with no cards created); the missing-`cards/`-directory case uses a bare `tmp_path`.

The body-exclusion and JSON-serialization methods are the two highest-value tests in this plan — they pin the two hazards `load_cards()`'s own docstring warns about. The serialization test must call `json.dumps(result.data)` with no `default=` argument, because that is precisely what `_display_result` does; passing a `default=` would make the test agree with itself rather than with the CLI.

Create `tests/contract/test_card_list_cli_mcp.py` modelled on `test_daily_run_cli_mcp.py`:
- module-level `_CAPS = {"knowledge.card.list": "construct_list_cards"}`, `runner = CliRunner()`
- `test_registered` — asserts the record has a non-None `input_model`, `mcp_tool_name` matching `_CAPS`, and `cli_name == "knowledge.card.list"`
- `test_handler_accepts_workspace_keyword` — calls the registered handler with `workspace=<path>` as a keyword and asserts no `TypeError`. This replaces daily's `test_shims_reject_positional_args`, which is the wrong discipline here: `card list` follows `connection list`'s convention where the CLI calls the handler positionally, so the property worth pinning is that the *keyword* form (the MCP path) also binds.
- `test_in_mcp_tool_list` and `test_mcp_server_exposes_card_list` — registry and `create_server()` discovery
- `test_mcp_no_hardcoded_card_list` — reads `Path(mcp_server.__file__).read_text()` and asserts neither the capability id nor the MCP tool name appears in it. This GREEN guard is the mechanical proof that D-01 is registry-routed and not a second hand-written views-style exception.
- `test_cli_commands_present` — `runner.invoke(app, ["knowledge", "card", "list", "--help"])` exits 0
- `test_cli_mcp_schema_parity` — invokes the handler directly, serializes via `_serialize_result`, invokes the CLI with `--workspace <ws> --json`, `json.loads` the stdout, and asserts the key sets match. For this command the parity test doubles as the JSON-serialization regression: `json.loads` fails loudly if date objects leak.

Import the result-carrying symbols lazily where the daily analog does, so the file collects before the capability is registered.

Expected state at end of this task: every new test FAILS (the capability does not exist). Confirm the failures are import/lookup errors against the unbuilt capability, not collection errors in the test files themselves.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest tests/contract/test_card_list_cli_mcp.py "tests/unit/test_knowledge_operations.py::TestCardList" -q; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/contract/test_card_list_cli_mcp.py "tests/unit/test_knowledge_operations.py::TestCardList" -q --co` collects at least 14 tests without collection errors
    - `.venv/bin/python -m pytest tests/contract/test_card_list_cli_mcp.py "tests/unit/test_knowledge_operations.py::TestCardList" -q` exits 1 with every collected test failing or erroring
    - `grep -c 'default=' tests/unit/test_knowledge_operations.py` returns the same value before and after this task (the serialization test uses a bare `json.dumps`)
    - `grep -c 'construct_list_cards' tests/contract/test_card_list_cli_mcp.py` outputs a value of at least 2
    - `.venv/bin/python -m pytest -q --co` collects the full suite without error (no import-time breakage introduced)
  </acceptance_criteria>
  <done>Fourteen or more tests describe the command's contract — enumeration, both filter dimensions, body exclusion, JSON serializability, empty and missing-workspace cases, order stability, and CLI/MCP parity — and all of them fail because the command does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement list_cards, register knowledge.card.list, and add the CLI wrapper</name>
  <read_first>
    - src/construct/services/knowledge.py (`list_connections` at ~:558-600 for the error envelope and return shape; `_get_archived_card_ids` at ~:603-617; the module's existing imports of `WorkspaceLoader`, `OperationResult`, `OperationError`)
    - src/construct/capabilities/catalog.py (`ConnectionListInput` at ~:140-143; `knowledge.connection.add` at ~:275-284 — sets BOTH `cli_name` and `mcp_tool_name`; `knowledge.connection.list` at ~:294-302 — sets only `cli_name`; the `model_config = {"extra": "forbid"}` usage at ~:202; the `from construct.services.knowledge import (...)` block at the top)
    - src/construct/cli.py (`card_app` creation at ~:161-162; `connection_list` at ~:1452-1467; `_display_result` at ~:165-192)
    - src/construct/storage/workspace.py (`load_cards` at ~:147-174 — the docstring is the specification for the two hazards)
    - tests/contract/test_mcp_contracts.py (`test_mcp_tool_count`'s exact `expected` set at ~:54-80; `_payload_for`'s `payloads[tool_name]` lookup at ~:150; `_invalid_path_payload`'s equivalent lookup)
    - .planning/phases/16-invocation-user-doc-truth/16-PATTERNS.md (the MCP parity caveat — `connection.list` is the WRONG template for D-01)
  </read_first>
  <files>src/construct/services/knowledge.py, src/construct/capabilities/catalog.py, src/construct/cli.py, tests/contract/test_mcp_contracts.py</files>
  <action>
**Handler — `src/construct/services/knowledge.py`.**

Add `list_cards(workspace, domain=None, include_archived=False) -> OperationResult`, placed next to `list_connections`.

Name the first parameter `workspace`, NOT `workspace_root`. This is load-bearing: the CLI calls the handler positionally while MCP calls it with keyword arguments derived from `CardListInput`'s field names, so a single parameter named `workspace` binds both callers with no shim. This mirrors the `graph.status` record's `lambda workspace:` precedent recorded in Phase 07-01. `list_connections` gets away with `workspace_root` only because it carries no `mcp_tool_name`.

Body:
1. `root = Path(workspace)`; if `root / "cards"` does not exist, return `OperationResult(success=False, ...)` with an `OperationError` naming the missing directory. Distinguishing "not a workspace" from "a workspace with no cards" is the contract, not an implementation detail — an empty success for a mistyped path would silently look like an empty graph.
2. Call `WorkspaceLoader(root).load_cards()`. Do not re-implement frontmatter parsing; `load_cards` already owns parse-error handling and lifecycle normalization.
3. For each returned dict: `card_data.pop("body", None)`. `load_cards` injects the full card prose under that key and D-02 forbids returning it. This is an information-disclosure control, not a size optimization.
4. Coerce date-typed values to ISO strings via a new module-private helper `_json_safe(value)` that returns `value.isoformat()` when the value is a `datetime.date` or `datetime.datetime` and the value unchanged otherwise. Apply it across each card dict's values. `load_cards`'s docstring is explicit that `created` and `last_verified` stay as `date` objects because the curation decay scans depend on that; the coercion belongs here, on the way out of the enumerate handler. It must NOT be added to `_display_result`, which is shared by every CLI command.
5. Apply `--include-archived`: when false, drop cards whose `lifecycle` value equals the archived lifecycle token. Filter on the card's own normalized `lifecycle` string rather than reusing `_get_archived_card_ids` — that helper exists to filter *connections* by the lifecycle of their endpoint cards and re-reading every file to learn something already present on the record would be wasted work.
6. Apply `--domain`: when set, keep cards where the value is an element of `card_data.get("domains", [])`. Exact string equality, matching the Phase 05-02 precedent that the domain filter checks membership in the plural `domains` list.
7. Return `OperationResult(success=True, message=f"Found {n} card(s)", data=[...])`, mirroring `list_connections`'s message shape. Preserve `load_cards()`'s ordering — it derives from `sorted()` over the `cards/*.md` glob and is the command's stability guarantee.

**Registry — `src/construct/capabilities/catalog.py`.**

Add `CardListInput(BaseModel)` next to `ConnectionListInput` with fields `workspace: Path`, `domain: str | None = None`, `include_archived: bool = False`, plus `model_config = {"extra": "forbid"}`. The `extra: forbid` is not decorative: it is this phase's one applicable ASVS V5 control, and it is NOT inherited — verify by reading the module that only one existing model sets it, so `ConnectionListInput` cannot be copied for this property.

Register a `CapabilityRecord` with `id="knowledge.card.list"`, name and description mirroring the connection-list record, `input_model=CardListInput`, `output_model=OperationResult`, `handler=list_cards`, `cli_name="knowledge.card.list"`, and `mcp_tool_name="construct_list_cards"`.

Copy the shape of `knowledge.connection.add`, which sets both fields — NOT `knowledge.connection.list`, which sets only `cli_name` and is a CLI-only record. Copying the connection-list record verbatim yields a CLI-only command and silently delivers half of D-01. Add `list_cards` to the existing `from construct.services.knowledge import (...)` block.

**CLI — `src/construct/cli.py`.**

Add `card_list` under the existing `card_app` sub-app (no new Typer group is needed; `card_app` already exists and is already attached to `knowledge_app`). Register it as `@card_app.command("list")` with the explicit string name. The explicit name is required: the guard's `_command_paths()` falls back to `callback.__name__.replace("_", "-")`, so a bare decorator on `def card_list` would register the leaf as `card-list` and leave the allowlist entry unresolvable.

Signature mirrors `connection_list`: `ctx: typer.Context`, `domain: Optional[str] = typer.Option(None, "--domain", "-d", help=...)`, `include_archived: bool = typer.Option(False, "--include-archived", help=...)`, `workspace: Path = typer.Option(Path.cwd(), "--workspace", "-w")`, `json_output: bool = typer.Option(False, "--json", "-j")`. Body is the standard 15-line wrapper with zero logic: `get_registry().get("knowledge.card.list")` inside a `try` that echoes the registry-not-found error and exits 1, then `cap.handler(workspace, domain=domain, include_archived=include_archived)`, then `_display_result(result, json_output)`.

**Contract test bookkeeping — `tests/contract/test_mcp_contracts.py`.**

Three edits are mandatory because a new MCP tool participates in three exhaustive iterations:
1. `test_mcp_tool_count` compares `tool_names == expected` on an exact set — add `"construct_list_cards"` to `expected`.
2. `_payload_for` does a direct `payloads[tool_name]` lookup and will raise `KeyError` for an unlisted tool — add an entry supplying the advertised schema field, i.e. the workspace path under the field name `CardListInput` declares.
3. The invalid-path payload builder has the same exhaustive shape — add the matching entry there so the boundary test can exercise the handler with a bogus path.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest tests/contract/test_card_list_cli_mcp.py "tests/unit/test_knowledge_operations.py::TestCardList" tests/contract/test_mcp_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/contract/test_card_list_cli_mcp.py "tests/unit/test_knowledge_operations.py::TestCardList" -q` exits 0
    - `.venv/bin/python -m pytest tests/contract/test_mcp_contracts.py -q` exits 0
    - `.venv/bin/python -c "from construct.capabilities.catalog import get_registry as g; c=g().get('knowledge.card.list'); assert c.cli_name=='knowledge.card.list' and c.mcp_tool_name=='construct_list_cards', (c.cli_name, c.mcp_tool_name)"` exits 0
    - `.venv/bin/python -c "from construct.capabilities.catalog import CardListInput as M; import pydantic; M(workspace='.', bogus=1)"` exits non-zero with a Pydantic validation error (extra=forbid is active)
    - `.venv/bin/python -c "import inspect; from construct.services.knowledge import list_cards; assert list(inspect.signature(list_cards).parameters)[0]=='workspace'"` exits 0
    - `.venv/bin/python -m construct.cli knowledge card list --help` exits 0 and its output names `--domain`, `--include-archived`, `--workspace`, and `--json`
    - `.venv/bin/python -c "from tests.contract.test_doc_command_references import VALID_PATHS, LEAF_COMMANDS as L; assert ('knowledge','card','list') in L and ('knowledge','card') in VALID_PATHS"` exits 0
    - `grep -c 'card.list\|list_cards\|construct_list_cards' src/construct/mcp/server.py` outputs `0` (parity is auto-discovered, never hand-wired)
    - `git diff src/construct/cli.py | grep -c '^[-+].*def _display_result'` outputs `0` (the shared renderer is untouched)
  </acceptance_criteria>
  <done>`construct knowledge card list` runs on the CLI and is exposed as `construct_list_cards` on MCP with no edit to the MCP server; every unit and contract test from Task 1 passes; `_display_result` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the card list entry from _KNOWN_BROKEN</name>
  <read_first>
    - tests/contract/test_doc_command_references.py (`_KNOWN_BROKEN` at ~:152-157 and `test_known_broken_entries_are_still_broken` at ~:212-220 — the paired enforcer that forces this deletion)
    - .planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md (D-16 — the allowlist reaches empty by removal from scanned documents or by the command becoming real, never by narrowing the scan)
    - .planning/phases/16-invocation-user-doc-truth/16-01-SUMMARY.md (the recorded RED test ids, to confirm the right one turns green here)
  </read_first>
  <files>tests/contract/test_doc_command_references.py</files>
  <action>
Remove the `("knowledge", "card", "list")` key from `_KNOWN_BROKEN`. Its paired enforcer asserts each allowlisted path is *still* broken, so with the command now registered that test fails until the entry is deleted — this is the mechanism working as designed, not a workaround.

Delete exactly one key. The three remaining entries stay: `knowledge ref list` is removed in 16-04 and the two `workflow` paths in 16-06, each in the change that genuinely closes them.

Do not touch `_DOC_GLOBS` in this task. Under D-16 the allowlist may only shrink by a command becoming real or by a broken string leaving a document that *remains* scanned; shrinking the scan surface is the failure mode the decision exists to forbid.

Confirm 16-01's `test_command_surface_is_discoverable` now passes, and that the two `test_key_docs_are_not_vacuous` cases for `USER_GUIDE.md` and `commands.md` are still RED — 16-05 owns those.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q --deselect "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from tests.contract.test_doc_command_references import _KNOWN_BROKEN as k; assert len(k)==3 and ('knowledge','card','list') not in k, k"` exits 0
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_known_broken_entries_are_still_broken" -q` exits 0 with 3 parametrized cases
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_command_surface_is_discoverable" -q` exits 0
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_documented_commands_resolve" -q` exits 0 (the two skills documenting `card list` now resolve)
    - `.venv/bin/python -c "from tests.contract.test_doc_command_references import _DOC_GLOBS as g; assert len(g)==3, g"` exits 0 (scan surface unchanged by this plan)
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous" -q` still exits 1 with 2 failures (16-05 owns those)
  </acceptance_criteria>
  <done>The allowlist holds three entries; the command-surface guard is green; the two skills that document `card list` resolve; the scan surface is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP caller -> `--workspace` path | An untrusted or mistyped path reaches a new filesystem read path |
| workspace card store -> `--json` stdout / MCP result | Every card's frontmatter, and potentially its prose body, crosses out of the workspace in one call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Information Disclosure | `list_cards` return payload (`src/construct/services/knowledge.py`) | medium | mitigate | `card_data.pop("body", None)` in the handler; pinned by the `TestCardList` body-exclusion test. D-02 is a confidentiality control — a single enumerate call must not exfiltrate the whole graph's prose |
| T-16-03 | Information Disclosure | `--workspace` option on `knowledge card list` | low | mitigate | Resolution goes through `WorkspaceLoader` on the existing code path and reads only `<workspace>/cards/*.md`; a path with no `cards/` directory returns an error result rather than walking upward or reading arbitrary files |
| T-16-09 | Denial of Service | `CardListInput` field surface | low | mitigate | ASVS V5: `model_config = {"extra": "forbid"}` on `CardListInput` rejects unexpected MCP payload fields at the boundary. Explicitly set on this model — it is not inherited from a module-level config |
| T-16-10 | Tampering | CLI/MCP surface divergence | medium | mitigate | Registration carries both `cli_name` and `mcp_tool_name`; `test_mcp_no_hardcoded_card_list` asserts `mcp/server.py` holds no card-list-specific wiring, so the two surfaces cannot drift apart into a hand-maintained pair |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | Zero external packages installed; `pyproject.toml` untouched (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
```
.venv/bin/python -m pytest -q
```

Expected after this plan: baseline 489 plus 16-01's 5 new passing cases plus this plan's ~14 new passing cases. Remaining failures must be exactly two — `test_key_docs_are_not_vacuous` for `USER_GUIDE.md` and `commands.md` (16-05 owns) — plus `test_skill_drops_forbidden_tools[construct-synthesis]` (16-04 owns). Any other failure is a regression.

Manual smoke against a scratch workspace:

```
.venv/bin/python -m construct.cli knowledge card list --workspace <ws> --json
```

Output must parse with `json.loads`, contain no `body` key, and render date fields as ISO-8601 strings.
</verification>

<success_criteria>
- `construct knowledge card list` is a leaf command on the live Typer app with `--domain`, `--include-archived`, `--workspace`, `--json`
- The capability is registered with both `cli_name` and `mcp_tool_name`, and `mcp/server.py` is unmodified
- `CardListInput` rejects extra fields
- `list_cards`'s first parameter is named `workspace`, binding both the positional CLI call and the keyword MCP call
- Returned card objects carry no `body` key and no non-JSON-serializable value
- A workspace with an empty `cards/` returns an empty success; a path with no `cards/` returns an error result without raising
- `_KNOWN_BROKEN` holds three entries; `_DOC_GLOBS` is unchanged
- `_display_result` is unchanged
</success_criteria>

<output>
Create `.planning/phases/16-invocation-user-doc-truth/16-03-SUMMARY.md` when done. Record the final `CardListInput` field set, the exact CLI flag surface (so 16-05's CLI column can name it correctly), and the JSON envelope shape a caller receives.
</output>
