---
phase: 16-invocation-user-doc-truth
plan: 05
type: execute
wave: 3
depends_on: ["16-03"]
files_modified:
  - CONSTRUCT-CLAUDE-impl/USER_GUIDE.md
  - CONSTRUCT-CLAUDE-impl/construct/references/commands.md
autonomous: true
requirements: [DOC-04]

must_haves:
  truths:
    - "`USER_GUIDE.md`'s Full Command Reference lets a reader invoke `research search`, `research score`, `research run`, `research review`, `research inspect`, `curation run`, `curation review`, `curation inspect`, `daily run`, `daily inspect`, and `card evaluate` directly, each written as a complete single-line `construct ...` invocation."
    - "`construct/references/commands.md` carries a real `construct ...` invocation for every natural-language entry that has one."
    - "Every `construct ...` string added to either document resolves against the live Typer app — verified by running the guard's own extractor and resolver over both files."
    - "A natural-language entry with no CLI equivalent renders as an explicit blank or skill-only marker; no row carries an invented command."
    - "Both documents individually yield at least one extractable invocation, turning the two `test_key_docs_are_not_vacuous` cases that have been RED since 16-01 green."
    - "Neither document asserts a numeric count of commands, capabilities, or MCP tools."
    - "`commands.md`'s views configuration note no longer presents `views.per_card_hooks.*` as a working feature — the debounce pair it configured was deleted in Phase 15-05 and the key is inert."
  artifacts:
    - "CONSTRUCT-CLAUDE-impl/USER_GUIDE.md — a CLI column across the Full Command Reference tables"
    - "CONSTRUCT-CLAUDE-impl/construct/references/commands.md — a CLI column across the group tables, and a corrected views configuration note"
  key_links:
    - "CLI column cells -> `_INVOCATION` regex in `tests/contract/test_doc_command_references.py` — the extractor matches only strings beginning with the literal token `construct` followed by a lowercase token, on ONE line; a wrapped or NL-prefixed cell is invisible to the guard"
    - "CLI column cells -> `_ARG_START` — the path truncates at the first argument-looking token, so placeholders in angle or curly brackets and flags are safe and do not need to resolve"
    - "`16-03-SUMMARY.md` -> the `knowledge card list` row — the flag surface written here must match what actually shipped"
  prohibitions:
    - statement: "A row with no CLI equivalent must be left visibly empty or marked skill-only; a plausible-looking command must never be invented to fill a cell."
      status: unverified
      verification: flagged
    - statement: "The natural-language framing must not be replaced by a CLI-first structure; the CLI column is an addition, and demoting the NL entry path would misrepresent the product's actual interaction model."
      status: unverified
      verification: flagged
    - statement: "No numeric command, capability, or MCP tool count may be written into either document; the surface is described by capability group with the guard test cited as authority."
      status: unverified
      verification: flagged
---

<objective>
Add a CLI-invocation column to `USER_GUIDE.md`'s Full Command Reference and `construct/references/commands.md`'s group tables, so a reader can execute the v0.4 runtime directly from the user-facing doc set.

Purpose: Both documents yield **zero** extractable invocations today — verified empirically. They describe the product entirely in natural language, which is deliberate and correct for the Claude-native interaction model, but it means neither document lets a user or a CI script actually run anything, and neither is reachable by the invocation guard. DOC-04 criterion 4 asks for invocability, which is an addition rather than a replacement. This plan is what makes 16-07's glob extension meaningful — extending the scan before these strings exist would produce a passing test that proves nothing.

Output: Two documents that a reader can execute from, and the two RED `test_key_docs_are_not_vacuous` cases turning green.

Per D-10 (NL-first preserved, CLI column added, honest blanks), D-12 (no hard counts).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md
@.planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md
@.planning/phases/16-invocation-user-doc-truth/16-PATTERNS.md
@.planning/phases/16-invocation-user-doc-truth/16-03-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created across Phase 16 (excluded from drift verification — all are new):
`_MUST_CARRY_INVOCATIONS`, `test_key_docs_are_not_vacuous` (`tests/contract/test_doc_command_references.py`); `_allowed_tools_text`, `test_allowed_tools_text_is_not_vacuous` (`tests/contract/test_skill_migration.py`); `list_cards`, `_json_safe` (`src/construct/services/knowledge.py`); `CardListInput`, capability id `knowledge.card.list`, MCP tool name `construct_list_cards` (`src/construct/capabilities/catalog.py`); `card_list` Typer callback and its `--domain`/`-d`, `--include-archived`, `--workspace`/`-w`, `--json`/`-j` flags (`src/construct/cli.py`); `TestCardList` (`tests/unit/test_knowledge_operations.py`); new file `tests/contract/test_card_list_cli_mcp.py`; new file `USER-TEST-PLAYBOOK-v041.md`; a CLI-invocation table column in `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` and `CONSTRUCT-CLAUDE-impl/construct/references/commands.md`.

## The live leaf surface (post-16-03) — the only commands a CLI cell may name

```
ask domain · bridge detect · card evaluate · curation inspect|review|run ·
daily inspect|run · help · ingest source · init ·
knowledge card archive|create|edit|list · knowledge connection add|list|remove ·
mcp · research inspect|review|run|score|search · spike list|run · status ·
tag approve|extract|list · validate · views generate|validate · workflow status
```

Anything not in that list has no CLI equivalent and its cell stays honestly empty.

<tasks>

<task type="auto">
  <name>Task 1: Add the CLI column to USER_GUIDE.md's Full Command Reference</name>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/USER_GUIDE.md (the `## Full Command Reference` section, ~:219-292 — all eight `###` group tables and the Workflows table, plus the `## Reference Tables` section that follows so you can see where the section ends)
    - tests/contract/test_doc_command_references.py (`_INVOCATION` at :56, `_ARG_START` at :48, `_code_spans` at :101, `_invocations` at :108 — these define exactly what the guard will and will not see in a table cell)
    - .planning/phases/16-invocation-user-doc-truth/16-03-SUMMARY.md (the shipped `knowledge card list` flag surface)
    - .planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md (Finding 3 — extractor behaviour the CLI column must respect)
  </read_first>
  <files>CONSTRUCT-CLAUDE-impl/USER_GUIDE.md</files>
  <action>
Add a third column headed `CLI` to each two-column `| You say | What happens |` table in the Full Command Reference. Keep `You say` as the first column: the NL framing matches the Claude-native interaction model PROJECT.md names as the near-term UX, and reordering CLI-first would demote the product's real entry path.

Each CLI cell holds a single-backtick span containing one complete `construct ...` invocation on ONE line, or is left empty / marked `skill-only` where no command exists. The extractor truncates a path at the first argument-looking token, so placeholders in angle or curly brackets and flags after the command path are safe and need not themselves resolve — but the command path preceding them must.

Populate the existing groups against the live leaf surface listed above:

| Group | Entry | CLI cell |
|---|---|---|
| Entry & orientation | help / what's next? | `construct help` |
| Entry & orientation | init {domain} | `construct init <domain>` |
| Entry & orientation | init {domain} interview | skill-only |
| Research | research {domain} | `construct research run --workspace .` |
| Research | research {topic} | `construct research search --workspace .` |
| Research | search adjust | skill-only |
| Knowledge operations | add card | `construct knowledge card create` |
| Knowledge operations | edit card {id} | `construct knowledge card edit` |
| Knowledge operations | connect {A} to {B} | `construct knowledge connection add` |
| Knowledge operations | evaluate {id} | `construct card evaluate` |
| Knowledge operations | archive {id} | `construct knowledge card archive` |
| Curation | curate {domain} | `construct curation run --workspace .` |
| Curation | bridges | `construct bridge detect` |
| Curation | validate | `construct validate` |
| Analysis | status | `construct status` |
| Analysis | gaps {domain} / gaps {topic} | skill-only |
| Analysis | domains | skill-only |
| Writing | write {topic} / publish | skill-only |
| Views & server | update views / refresh data | `construct views generate` |
| Views & server | scaffold / build / start / stop / reset views | skill-only |

Then ADD the rows criterion 4 names that have no NL entry today. Give each a `You say` phrasing consistent with the existing register:

- Knowledge operations gains a card-enumeration row → `construct knowledge card list --domain <domain> --json`, and a connection-enumeration row → `construct knowledge connection list --json`. Write the flags exactly as `16-03-SUMMARY.md` records them.
- Research gains rows for scoring, reviewing a paused run, and inspecting a completed one → `construct research score`, `construct research review`, `construct research inspect`.
- Curation gains review and inspect rows → `construct curation review`, `construct curation inspect`.
- A new `### Daily cycle` group with two rows → `construct daily run --workspace .` and `construct daily inspect`.
- A new `### Ingestion, spikes & tags` group → `construct ingest source`, `construct spike list`, `construct spike run`, `construct tag extract`, `construct tag list`, `construct tag approve`.
- Views & server gains a validation row → `construct views validate`.
- Entry & orientation gains an MCP row → `construct mcp` — and a workflow-status row → `construct workflow status`.

Note for the daily and curation rows: a degraded run exits 0 by design (Phase 11's recorded contract) and reports its state in the `--json` payload, so where the "What happens" cell describes success, phrase it in terms of the reported per-step state rather than implying an exit code carries the verdict.

Leave the Workflows table (`| Workflow | When to use | Sequence |`) as it is — it describes multi-skill sequences, not commands, and adding a CLI column there would invite an invented composite invocation.

Per D-12, write no count of commands anywhere. If a summary sentence would naturally say how many there are, cite `tests/contract/test_doc_command_references.py` as the live authority instead.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -c "
from pathlib import Path
from tests.contract.test_doc_command_references import _invocations, _resolves
p = Path('CONSTRUCT-CLAUDE-impl/USER_GUIDE.md')
inv = _invocations(p.read_text(encoding='utf-8'))
bad = sorted(i for i in inv if not _resolves(i))
assert not bad, bad
assert len(inv) >= 20, len(inv)
for req in [('research','search'),('research','score'),('research','run'),('research','review'),('research','inspect'),('curation','run'),('curation','review'),('curation','inspect'),('daily','run'),('daily','inspect'),('card','evaluate'),('knowledge','card','list')]:
    assert req in inv, req
print('ok', len(inv))
"</automated>
  </verify>
  <acceptance_criteria>
    - The verify script above exits 0, proving every extracted path resolves and all twelve criterion-4 command paths are present
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous" -q -k USER_GUIDE` exits 0
    - `grep -c '| You say |' CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` returns a value of at least 8, and every such header line is followed by a row containing a third column separator
    - `grep -cE '\| *`construct [^`]*\n' CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` outputs `0` (no invocation is split across lines)
    - `grep -ciE '(25|33|34|21|22|27) (commands?|capabilit|MCP tools?)' CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` outputs `0`
    - The section still opens with `## Full Command Reference` and the first column of every group table is still the natural-language entry
  </acceptance_criteria>
  <done>Every Full Command Reference group table carries a CLI column, the criterion-4 surface is reachable, every written invocation resolves, and rows with no command equivalent are honestly blank.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CLI column to construct/references/commands.md</name>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/construct/references/commands.md (full file — the `## Starting Point` two-column table at ~:13-15 including the bare backtick span at :15, every `| Command | Skill | What it does |` group table, and the `## Natural Language` section at the end)
    - tests/contract/test_doc_command_references.py (`_INVOCATION`, `_ARG_START`, `_code_spans`)
    - .planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md (D-10's note that "Skill" and "CLI" are different axes)
  </read_first>
  <files>CONSTRUCT-CLAUDE-impl/construct/references/commands.md</files>
  <action>
The group tables are already three columns: `| Command | Skill | What it does |`, where `Command` holds the natural-language keyword and `Skill` holds the skill that handles it. Add a fourth column headed `CLI` between `Skill` and `What it does`, holding the real invocation. Do not repurpose the `Skill` column: a skill name and a CLI invocation are different axes, and collapsing them would lose the skill mapping this document exists to provide.

Populate the CLI cells using the same mapping as Task 1 — the two documents describe the same surface and must not disagree. Add the same new rows Task 1 adds where the group exists here (`Knowledge Operations` gains card and connection enumeration; `Research` gains score/review/inspect; `Curation` gains review/inspect; add new `## Daily Cycle` and `## Ingestion, Spikes & Tags` sections; `Views & Server` gains validation).

Leave the `## Starting Point` table alone. Its bare single-backtick `construct` span does not fire the extractor (the regex requires a following lowercase token) and adding an invocation there would duplicate the help row.

Leave the `## Natural Language` section as it is — its `research cosmology` style examples are deliberately NL and carry no `construct` prefix, so they stay invisible to the extractor, which is correct.

**Also correct the views configuration note** (the paragraph beginning "Config:" beneath the Views & Server table). It presents `views.per_card_hooks.*` as scheduling a debounced refresh after direct card-create and card-connect edits. Phase 15-05 deleted the debounce pair together with its two live skill registrations, so that key is now inert and direct per-card edits have no views-refresh path at all. Remove the `per_card_hooks` clause and state plainly that direct card-create and card-connect edits do not trigger a views refresh, with the workflow capabilities (`research run`, `curation run`, `daily run`) named as what does. Keep the other three configuration keys in the sentence — they are still live.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -c "
from pathlib import Path
from tests.contract.test_doc_command_references import _invocations, _resolves
p = Path('CONSTRUCT-CLAUDE-impl/construct/references/commands.md')
inv = _invocations(p.read_text(encoding='utf-8'))
bad = sorted(i for i in inv if not _resolves(i))
assert not bad, bad
assert len(inv) >= 15, len(inv)
for req in [('research','run'),('curation','run'),('daily','run'),('card','evaluate'),('knowledge','card','list'),('views','generate'),('views','validate')]:
    assert req in inv, req
print('ok', len(inv))
"</automated>
  </verify>
  <acceptance_criteria>
    - The verify script above exits 0
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous" -q` exits 0 with all 3 parametrized cases passing
    - `grep -c 'per_card_hooks' CONSTRUCT-CLAUDE-impl/construct/references/commands.md` outputs `0`
    - `grep -c 'auto_regenerate' CONSTRUCT-CLAUDE-impl/construct/references/commands.md` outputs a value of at least 1 (the still-live config keys survive)
    - `grep -c '| Command | Skill |' CONSTRUCT-CLAUDE-impl/construct/references/commands.md` returns a value of at least 6, and each such header now carries a fourth column
    - `grep -ciE '(25|33|34|21|22|27) (commands?|capabilit|MCP tools?)' CONSTRUCT-CLAUDE-impl/construct/references/commands.md` outputs `0`
    - The `## Starting Point` and `## Natural Language` sections are unmodified (`git diff` shows no hunk touching either heading's table or list)
  </acceptance_criteria>
  <done>Every group table carries a CLI column consistent with USER_GUIDE.md, all invocations resolve, the inert per-card-hooks documentation is corrected, and both NL-only sections are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-check the two documents against each other and against the live app</name>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/USER_GUIDE.md (the Full Command Reference as edited in Task 1)
    - CONSTRUCT-CLAUDE-impl/construct/references/commands.md (the group tables as edited in Task 2)
    - tests/contract/test_doc_command_references.py (`LEAF_COMMANDS`, `COMMAND_GROUPS`, `_resolves`)
  </read_first>
  <files>CONSTRUCT-CLAUDE-impl/USER_GUIDE.md, CONSTRUCT-CLAUDE-impl/construct/references/commands.md</files>
  <action>
Two documents describing one surface must not disagree. Extract the invocation set from each and compare:

1. Any command path present in one document and absent from the other is either a deliberate scope difference (`USER_GUIDE.md` is the fuller reference and may legitimately carry more) or an omission. Resolve each difference explicitly — add the missing row or record why the asymmetry is intended.
2. Any leaf command in the live app that appears in NEITHER document is a discoverability gap. List them. Add rows for any that a user would reasonably want to reach; for the rest, record why they are internal-only (the MCP server entry point is the obvious candidate).
3. Confirm no invented commands: every extracted path must be in `LEAF_COMMANDS` or `COMMAND_GROUPS`, or must extend a leaf with argument tokens.
4. Confirm every cell that could not be filled is visibly empty or marked skill-only, and that none was quietly filled with a plausible-looking guess.

Fix what the comparison surfaces. Record the residual differences and the internal-only leaf list in the plan summary so 16-06's playbook and Phase 17's inventory can start from a settled picture.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -c "
from pathlib import Path
from tests.contract.test_doc_command_references import _invocations, _resolves, LEAF_COMMANDS
g = _invocations(Path('CONSTRUCT-CLAUDE-impl/USER_GUIDE.md').read_text(encoding='utf-8'))
c = _invocations(Path('CONSTRUCT-CLAUDE-impl/construct/references/commands.md').read_text(encoding='utf-8'))
assert not [i for i in g|c if not _resolves(i)]
missing = sorted(l for l in LEAF_COMMANDS if l not in g and l not in c)
print('leaves documented in neither:', missing)
assert len(missing) <= 2, missing
" && .venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The verify script exits 0, printing the list of leaf commands documented in neither file, and that list has at most 2 entries
    - `.venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q` exits 0 (including all 3 non-vacuity cases and every `test_documented_commands_resolve` parametrization)
    - `.venv/bin/python -m pytest -q` exits 0 for the full suite
    - Every command path appearing in `commands.md` also appears in `USER_GUIDE.md` (the reference document is a subset of the guide), verified by set comparison
    - The plan summary lists each residual asymmetry with its reason
  </acceptance_criteria>
  <done>The two documents agree on the surface they describe, at most two internal-only leaves are undocumented with a recorded reason, no invented command exists in either file, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-facing documentation -> user's shell | A reader copies a CLI cell verbatim and runs it; an invented or wrong command is executed with the user's privileges in their workspace |
| documentation -> invocation guard | New `construct ...` strings enter the repository; until 16-07 globs these files, only the per-doc non-vacuity guard watches them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-13 | Tampering | CLI cells in `USER_GUIDE.md` / `commands.md` | medium | mitigate | Every cell is validated against the live Typer app by running the guard's own `_invocations` + `_resolves` in each task's verify step, before 16-07 makes the check permanent. Honest blanks are enforced by the same check — an invented command would fail to resolve |
| T-16-14 | Information Disclosure | example invocations carrying credentials or absolute paths | low | mitigate | No CLI cell embeds a key, token, or absolute filesystem path; placeholders use bracketed tokens that the extractor truncates at and that a reader must substitute |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | Zero external packages installed; `pyproject.toml` untouched (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
```
.venv/bin/python -m pytest -q
```

Must be fully green after this plan (assuming 16-04 has also landed). The two `test_key_docs_are_not_vacuous` cases RED since 16-01 must now pass — and critically, they must pass because real invocations were added, not because the guard was weakened. Confirm `_MUST_CARRY_INVOCATIONS` still holds 3 entries and `_DOC_GLOBS` still holds 3.

Spot-run three invocations copied verbatim from each document against a scratch workspace and confirm each executes rather than merely resolving.
</verification>

<success_criteria>
- Both documents carry a CLI column; the natural-language column remains first in every table
- Every criterion-4 command path (`research search|score|run|review|inspect`, `curation run|review|inspect`, `daily run|inspect`, `card evaluate`) is reachable from `USER_GUIDE.md`
- Every extracted invocation in both files resolves against the live Typer app
- Rows with no CLI equivalent are blank or marked skill-only; no invented command exists
- Both `test_key_docs_are_not_vacuous` cases pass
- `commands.md` no longer documents `views.per_card_hooks.*` as functional
- Neither document asserts a numeric command count
- `_DOC_GLOBS` is unchanged — 16-07 owns the glob extension
</success_criteria>

<output>
Create `.planning/phases/16-invocation-user-doc-truth/16-05-SUMMARY.md` when done. Record the per-document invocation counts, the residual asymmetries between the two files with reasons, and the list of live leaf commands documented in neither.
</output>
