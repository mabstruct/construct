---
phase: 16-invocation-user-doc-truth
plan: 07
type: execute
wave: 5
depends_on: ["16-06"]
files_modified:
  - tests/contract/test_doc_command_references.py
autonomous: false
requirements: [DOC-04, FIX-03]

must_haves:
  truths:
    - "`USER_GUIDE.md` and `construct/references/commands.md` are in `_DOC_GLOBS`, so every `construct ...` string in either file is resolved against the live Typer app on every test run — the guard now covers the user-facing doc set, not only skills, workflow docs, and the playbook."
    - "`_doc_files()` returns each document path exactly once; a document reachable both through a directory glob and through a single-file entry does not produce two parametrized cases, and `test_documented_commands_resolve` has no duplicate test id."
    - "`_doc_files()` returns paths in a deterministic order — directory entries come from `sorted()` over the glob and the tuple order of `_DOC_GLOBS` is fixed — so the parametrized test ids are stable across runs and a disappearing id is readable as a dropped document rather than as run-to-run noise."
    - "`_KNOWN_BROKEN` is empty with the full suite green: every `construct ...` string a user or agent executes from CONSTRUCT's documentation resolves against the live registry."
    - "The full suite passes at or above the 489-test baseline Phase 15 left, plus this phase's additions; no pre-existing test was deleted or weakened to reach green."
    - "A human has executed the v0.4.1 playbook's offline sections end to end against a freshly scaffolded smoke workspace and recorded the result — resolution alone is insufficient, because a step can resolve and still fail at runtime."
  artifacts:
    - "tests/contract/test_doc_command_references.py — `_DOC_GLOBS` extended to five entries covering the user-facing doc set"
    - ".planning/phases/16-invocation-user-doc-truth/16-07-SUMMARY.md — the recorded result of the human playbook run"
  key_links:
    - "`_DOC_GLOBS` -> `_doc_files()` -> `test_documented_commands_resolve` parametrization — extending the glob set is what makes 16-05's CLI columns permanently guarded rather than correct-once"
    - "`_MUST_CARRY_INVOCATIONS` -> the newly globbed documents — the per-doc non-vacuity guard, RED since 16-01 and green since 16-05, is what stops this extension from being the vacuous change RESEARCH warns about"
  prohibitions:
    - statement: "A document must not be added to the scan surface while its invocations remain unverified; the extension is only meaningful because the CLI columns already landed and the per-document non-vacuity guard already covers both files."
      status: unverified
      verification: flagged
    - statement: "No test may be deleted, skipped, xfailed, or have an assertion weakened in order to reach a green suite at the end of this phase."
      status: unverified
      verification: flagged
    - statement: "The human playbook run must not be recorded as passed on the basis that the commands resolve; each offline step must actually be executed."
      status: unverified
      verification: flagged
---

<objective>
Close the phase: bring `USER_GUIDE.md` and `construct/references/commands.md` under the invocation guard permanently, prove the allowlist is empty with the suite green, and execute the v0.4.1 playbook's offline sections once against a fresh smoke workspace.

Purpose: 16-05 made both documents executable; without this extension the phase would *create* a fresh crop of unguarded invocation strings — precisely the drift class FIX-04 exists to prevent and precisely where DOC-04's defects came from. The extension is deliberately last: RESEARCH verified that globbing these files before the CLI columns existed would have produced a passing test over an empty set, proving nothing. And D-09 makes completion two-part — mechanical resolution by the guard, plus one human execution, because a step can resolve and still fail at runtime.

Output: A five-entry glob set, an empty allowlist with a green suite, and a recorded playbook run.

Per D-11 (both documents added to `_DOC_GLOBS`), D-09 (two-part definition of done), D-16.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md
@.planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md
@.planning/phases/16-invocation-user-doc-truth/16-05-SUMMARY.md
@.planning/phases/16-invocation-user-doc-truth/16-06-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created across Phase 16 (excluded from drift verification — all are new):
`_MUST_CARRY_INVOCATIONS`, `test_key_docs_are_not_vacuous` (`tests/contract/test_doc_command_references.py`); `_allowed_tools_text`, `test_allowed_tools_text_is_not_vacuous` (`tests/contract/test_skill_migration.py`); `list_cards`, `_json_safe` (`src/construct/services/knowledge.py`); `CardListInput`, capability id `knowledge.card.list`, MCP tool name `construct_list_cards` (`src/construct/capabilities/catalog.py`); `card_list` Typer callback and its `--domain`/`-d`, `--include-archived`, `--workspace`/`-w`, `--json`/`-j` flags (`src/construct/cli.py`); `TestCardList` (`tests/unit/test_knowledge_operations.py`); new file `tests/contract/test_card_list_cli_mcp.py`; new file `USER-TEST-PLAYBOOK-v041.md`; a CLI-invocation table column in `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` and `CONSTRUCT-CLAUDE-impl/construct/references/commands.md`.

<tasks>

<task type="auto">
  <name>Task 1: Extend _DOC_GLOBS to cover the user-facing doc set</name>
  <read_first>
    - tests/contract/test_doc_command_references.py (`_DOC_GLOBS` at ~:41, `_doc_files()` at ~:131-138 — note it handles both the directory-glob form and the single-file form, testing `(root / pattern).is_file()`; the parametrize idiom at ~:188-191 whose ids are built from `p.parent.name` and `p.name`)
    - .planning/phases/16-invocation-user-doc-truth/16-05-SUMMARY.md (the per-document invocation counts, so you know what the new parametrized cases will actually be checking)
    - .planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md (Findings 1-3 — why this task is last)
  </read_first>
  <files>tests/contract/test_doc_command_references.py</files>
  <action>
Add two entries to `_DOC_GLOBS` for `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` and `CONSTRUCT-CLAUDE-impl/construct/references/commands.md`, taking the tuple from three entries to five.

Use the single-file form. `_doc_files()` tests `(root / pattern).is_file()`, so a nested relative path works — pair the implementation root with the relative path rather than inventing a new glob shape. Confirm this by reading `_doc_files()` rather than assuming; a pattern form that silently matches nothing would add zero parametrized cases and reintroduce the vacuity this phase spent a Wave 0 closing.

Two properties must hold after the edit and are worth checking explicitly rather than trusting:
1. **No duplicate.** `_doc_files()` must return each path exactly once. Neither new file sits under `claude/skills/` or `construct/workflows/`, so no directory glob should also reach them — but verify by comparing the list length against the length of the set of resolved paths. A duplicated path produces two parametrized cases with the same id, which pytest tolerates and a reader misreads.
2. **Deterministic order.** Directory entries already come from `sorted()` and `_DOC_GLOBS` is a fixed-order tuple, so the resulting id sequence is stable across runs. This matters because a *disappearing* parametrized id is the D-16 red flag, and that signal is only readable if the id sequence is otherwise stable.

The `test_key_docs_are_not_vacuous` guard added in 16-01 is what makes this extension meaningful: both new files are already in `_MUST_CARRY_INVOCATIONS` and have been green since 16-05, so a formatting change that made the extractor blind to their CLI column would fail loudly instead of passing on an empty set.

Note that `test_documented_script_paths_exist` also iterates `_doc_files()`. Neither new document is expected to reference a skill wrapper script, so its `assert scanned` guard remains satisfied by the existing skill docs — confirm it still passes rather than assuming.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -c "
from tests.contract.test_doc_command_references import _DOC_GLOBS, _doc_files, _invocations
files = _doc_files()
assert len(_DOC_GLOBS) == 5, _DOC_GLOBS
assert len(files) == len({p.resolve() for p in files}), [p.name for p in files]
names = {p.name for p in files}
assert {'USER_GUIDE.md','commands.md','USER-TEST-PLAYBOOK-v041.md'} <= names, sorted(names)
for p in files:
    if p.name in ('USER_GUIDE.md','commands.md'):
        assert _invocations(p.read_text(encoding='utf-8')), p
assert [p.name for p in _doc_files()] == [p.name for p in _doc_files()]
print('ok', len(files))
" && .venv/bin/python -m pytest tests/contract/test_doc_command_references.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The verify script exits 0: five glob entries, no duplicate resolved path, both new documents present and non-empty of invocations, and a stable repeated ordering
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_documented_commands_resolve" -v` collects two more parametrized cases than before this task, with ids naming `USER_GUIDE.md` and `commands.md`, and all pass
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_documented_script_paths_exist" -q` exits 0
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_docs_contain_invocations" -q` exits 0
    - `.venv/bin/python -c "from tests.contract.test_doc_command_references import _KNOWN_BROKEN as k; assert k == {}, k"` exits 0
    - `git diff tests/contract/test_doc_command_references.py` shows only additions to `_DOC_GLOBS`; no removed entry, no change to `_resolves`, `_invocations`, `_INVOCATION`, or `_ARG_START`
  </acceptance_criteria>
  <done>The user-facing doc set is permanently under the invocation guard, every new parametrized case passes on real invocations rather than an empty set, and no extractor or resolver logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the phase's mechanical completion criterion</name>
  <read_first>
    - tests/contract/test_doc_command_references.py (final state — `_DOC_GLOBS`, `_MUST_CARRY_INVOCATIONS`, `_KNOWN_BROKEN`)
    - .planning/REQUIREMENTS.md (the mechanical completion criteria section — FIX-03 is complete when the allowlist is empty with the suite green — and the corrected test baseline from 16-02)
    - .planning/phases/16-invocation-user-doc-truth/16-VALIDATION.md (the observable-signals table and the per-task verification map, which needs filling in)
  </read_first>
  <files>tests/contract/test_doc_command_references.py</files>
  <action>
Run the full suite and record the terminal state against each of the phase's observable signals. This task writes no production code; its deliverable is evidence, and its acceptance criteria are the phase's completion criteria.

Check each signal from `16-VALIDATION.md`:
- the allowlist is an empty dict, source-visible;
- `test_known_broken_entries_are_still_broken` collects zero parametrized cases, confirming the allowlist is genuinely empty rather than bypassed;
- the per-document parametrized id list contains every expected document and has lost none — compare against the list recorded in `16-06-SUMMARY.md`; a *dropped* id is the D-16 red flag and must be investigated, not accepted;
- every document in `_MUST_CARRY_INVOCATIONS` yields at least one invocation;
- the full suite passes at or above the baseline `16-02` corrected into `REQUIREMENTS.md`, plus this phase's additions.

If the suite is green but any signal is off — a missing parametrized id, a glob entry count below five, a non-empty allowlist — treat the phase as incomplete regardless of the exit code. A green suite that is green for the wrong reason is the failure mode this entire phase exists to eliminate.

Fill in `16-VALIDATION.md`'s per-task verification map from the plans' verify commands and set its frontmatter `wave_0_complete` to true, recording the Wave 0 deliverables 16-01 and 16-03 landed. Do not set `nyquist_compliant` or `status: validated` — those are `validate-phase`'s to set, not this plan's.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest -q && .venv/bin/python -c "
from tests.contract.test_doc_command_references import _KNOWN_BROKEN as k, _DOC_GLOBS as g, _MUST_CARRY_INVOCATIONS as m, _doc_files, _invocations
from pathlib import Path
assert k == {}, k
assert len(g) == 5, g
docs = _doc_files()
print('scanned documents:', sorted(p.name for p in docs))
total = sum(len(_invocations(p.read_text(encoding='utf-8'))) for p in docs)
print('total invocations:', total)
assert total > 40, total
for rel in m:
    assert _invocations((Path.cwd()/rel).read_text(encoding='utf-8')), rel
print('ALLOWLIST EMPTY, GUARD COVERING', len(docs), 'DOCUMENTS')
"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` exits 0 with zero failures, zero errors, and zero skips introduced by this phase
    - The reported passing-test count is at least 489 plus this phase's additions, and no pre-existing test was deleted (`git diff --stat` against the phase's starting commit shows no net removal of test functions)
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_known_broken_entries_are_still_broken" -q` collects zero tests without erroring
    - The verify script prints an allowlist-empty line and a total invocation count above 40
    - The scanned-document list contains every document recorded in `16-06-SUMMARY.md` with none missing
    - `.planning/phases/16-invocation-user-doc-truth/16-VALIDATION.md` has its per-task verification map populated and `wave_0_complete: true`; `status` remains `draft` and `nyquist_compliant` remains `false`
    - `grep -rcE 'pytest.mark.(skip|xfail)' tests/contract/test_doc_command_references.py tests/contract/test_skill_migration.py` outputs `0` for both files
  </acceptance_criteria>
  <done>The allowlist is empty with a green suite over a five-document scan surface, every observable signal is recorded, and no test was weakened to get there.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the v0.4.1 playbook's offline sections against a fresh smoke workspace</name>
  <action>Human-run release validation — D-09 part two. See how-to-verify for the exact procedure.</action>
  <what-built>
Phase 16 is mechanically complete: `knowledge card list` ships on CLI and MCP, `construct-synthesis` no longer declares web-search grants, `USER_GUIDE.md` and `commands.md` carry executable CLI columns and are under the invocation guard, `USER-TEST-PLAYBOOK-v041.md` supersedes the v0.3 file, and `_KNOWN_BROKEN` is empty with the full suite green.

What remains is D-09's second half. The guard proves every documented command string **resolves**; it cannot prove a step **runs**. A command can bind to a real Typer leaf and still fail against a real workspace — wrong flag combination, a capability that needs state the previous step did not create, a JSON field that is not where the Expected paragraph says it is. Adding the playbook to CI as an automated smoke suite was considered and rejected as out of scope: that is new test infrastructure, larger than a documentation-truth phase. So this is a one-time human execution.
  </what-built>
  <how-to-verify>
1. Create a throwaway smoke workspace following the setup section of `USER-TEST-PLAYBOOK-v041.md`. Do NOT use `test-ws/` — those are committed fixtures and the playbook says so.

2. Execute every section whose heading does NOT carry a credential marker, in order, from a shell where the repository virtualenv is activated. Set no `ANTHROPIC_API_KEY` and no search-provider key for this run — the point of D-07 is that the core path runs without them.

3. For each step, compare what happened against its **Expected** paragraph and record pass or fail. Pay particular attention to:
   - the research run pausing at durable human review, the review step, and the resume — this is the successor flow replacing the two removed workflow commands and it is the section with no v0.3 precedent;
   - `daily run` and `daily inspect` — never previously covered by any release-validation artifact;
   - `knowledge card list --json` — confirm the payload carries frontmatter fields, that no object carries a body key, and that date fields render as ISO-8601 strings rather than Python repr;
   - every curation, research, and daily step — confirm the Pass criterion you are checking is the per-step `completed` / `degraded` / `skipped` state in the JSON, not the exit code. If any step's Expected text asks you to check an exit code, that is a defect in the playbook and must be reported.

4. Skip the credential-marked sections and record them as skipped-by-design rather than as failures.

5. Tear down the smoke workspace using the teardown section.

Report back: the per-section pass/fail/skipped table, any step whose command ran but whose Expected text did not match reality, and any step that could not be executed offline despite carrying no credential marker. That last category is a D-07 violation and is the most important thing this checkpoint can surface.
  </how-to-verify>
  <resume-signal>Type "approved" with the per-section result table, or describe the failing steps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_DOC_GLOBS` -> the guard's scan surface | The glob set defines what FIX-03 means; every document outside it carries unverified invocation strings |
| playbook -> human validator's shell and workspace | The checkpoint executes every offline step verbatim against a scratch workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | `_DOC_GLOBS` final state | high | mitigate | Task 1 asserts the tuple grows to five with no removed entry and no duplicate resolved path; Task 2 records the full scanned-document list and compares it against `16-06-SUMMARY.md`, so a dropped document is caught rather than read as noise |
| T-16-17 | Repudiation | phase completion evidence | medium | mitigate | Task 2 checks each observable signal independently of the suite exit code, and explicitly treats a green suite with an off signal as incomplete — a green suite that is green for the wrong reason is the defect class this phase exists to eliminate |
| T-16-04 | Information Disclosure | credentialed playbook sections | medium | mitigate | The checkpoint runs with no API key set; credential-marked sections are recorded as skipped-by-design. A step that cannot run offline despite carrying no marker is escalated as a D-07 violation |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | Zero external packages installed across the phase; `pyproject.toml` untouched (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
```
.venv/bin/python -m pytest -q
.venv/bin/python -c "from tests.contract.test_doc_command_references import _KNOWN_BROKEN as k, _DOC_GLOBS as g, _doc_files; print(len(k), len(g), len(_doc_files()))"
```

The second command must print `0 5` followed by the document count. Zero allowlist entries over a five-entry glob set covering more documents than the phase started with is the signature of a guard that ended empty rather than unscanned — the phrase from `.planning/STATE.md` that D-05 and D-16 exist to honour.

Then the human playbook run, whose result is recorded in the plan summary.
</verification>

<success_criteria>
- `_DOC_GLOBS` holds five entries including both user-facing documents; no entry was removed
- `_doc_files()` returns each path exactly once in a deterministic order
- Every parametrized `test_documented_commands_resolve` case passes, including the two new ones, on non-empty invocation sets
- `_KNOWN_BROKEN` is empty
- The full suite is green at or above the corrected baseline with no test deleted, skipped, or weakened
- `16-VALIDATION.md`'s per-task map is populated and `wave_0_complete` is true
- A human has executed the playbook's offline sections against a fresh smoke workspace and the per-section result is recorded
</success_criteria>

<output>
Create `.planning/phases/16-invocation-user-doc-truth/16-07-SUMMARY.md` when done. Record the final glob set, the full scanned-document list with per-document invocation counts, the final suite count, and the human playbook run's per-section result table including any D-07 violation found.
</output>
