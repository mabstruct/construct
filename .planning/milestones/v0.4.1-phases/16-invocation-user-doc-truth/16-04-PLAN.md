---
phase: 16-invocation-user-doc-truth
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-03"]
files_modified:
  - CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md
  - CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md
  - tests/contract/test_doc_command_references.py
autonomous: true
requirements: [DEC-01, FIX-03]

must_haves:
  truths:
    - "`construct-synthesis`'s `allowed-tools` frontmatter declares none of the tools in `_FORBIDDEN_TOOLS`, and still declares `Bash(construct)` — the skill remains a thin CLI delegator rather than losing its delegation path along with its web grants."
    - "`construct-synthesis` documents its `refs/` lookup as a scoped `Read` over the workspace's `refs/` directory, using a tool the skill already grants, so closing the web grants introduces no new tool requirement."
    - "No `construct ...` string in `construct-synthesis/SKILL.md` or `construct-gap-analysis/SKILL.md` fails to resolve against the live Typer app, and neither file is covered by an allowlist entry."
    - "`construct-synthesis/SKILL.md`'s Step 7 views-refresh section survives intact — it is an exemption recorded in `adr-0005-views-refresh-ownership.md`, not a leftover from the Phase 15 sweep."
    - "The `spec-v04-agentworkflows.md` row instructing removal of the web-search grant is marked discharged, naming this phase and requirement, so no live instruction remains to redo work already done."
  artifacts:
    - "CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md — frontmatter without web grants, `refs/` step rewritten onto Read"
    - "CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md — discharged marker on the migration-fallback row"
    - "tests/contract/test_doc_command_references.py — `_KNOWN_BROKEN` reduced to the two playbook entries"
  key_links:
    - "`_MIGRATED_SKILLS` -> `_allowed_tools_text` -> `test_skill_drops_forbidden_tools[construct-synthesis]` — 16-01 made this assertion capable of failing; this plan is what turns it green, and only a real frontmatter edit can do so"
    - "`construct-synthesis/SKILL.md` `refs/` step -> `_KNOWN_BROKEN` — the entry may only be deleted because the broken string left a document that REMAINS in `_DOC_GLOBS` (D-16)"
    - "`construct-synthesis/SKILL.md:~130` -> `adr-0005-views-refresh-ownership.md` — the ADR is why this section is not swept with the rest of the Phase 15 refresh cleanup"
  prohibitions:
    - statement: "The `refs/` lookup step must not be silently deleted; the step's intent — also consult supporting references — is preserved on a tool the skill already grants, not dropped because the command it named does not exist."
      status: unverified
      verification: flagged
    - statement: "The synthesis views-refresh step must not be removed as Phase 15 cleanup residue; it is a recorded ADR exemption and removing it would break the only path a synthesis has to the SPA."
      status: unverified
      verification: flagged
    - statement: "The two model-routing lines elsewhere in `spec-v04-agentworkflows.md` are fenced to Phase 17 and must not be edited here, even though the file is open."
      status: unverified
      verification: flagged
    - statement: "A tool grant must not be removed from the frontmatter while the skill body still depends on it; removal is justified only because the grants are provably unused."
      status: unverified
      verification: flagged
---

<objective>
Close DEC-01: remove `construct-synthesis`'s dead web-search grants, rewrite its one unimplementable command reference onto a tool it already holds, and mark the spec instruction that mandated the removal as discharged.

Purpose: `spec-v04-agentworkflows.md` states the rule itself — remove the web-search grant from skill `allowed-tools` when `research.run` ships. `research.run` shipped in Phase 10; the condition has been satisfied for two milestones. The grants appear only in frontmatter with zero references anywhere in the skill body, so removal costs nothing at runtime and closes the last standing exception to the thin-wrapper claim. Leaving a live "do this when X" instruction after X has happened reproduces exactly the rot this milestone exists to clear.

Output: A synthesis skill with no web-egress grants, a `refs/` step that works, a discharged spec row, and an allowlist down to its final two entries.

Per D-03 (no `ref` sub-app; scoped `Read` instead), D-14 (both grants removed, guard scope extended), D-15 (`spec-v04:436` discharged here; `:211` and `:557` fenced to Phase 17), D-16.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md
@.planning/phases/16-invocation-user-doc-truth/16-RESEARCH.md
@.planning/phases/16-invocation-user-doc-truth/16-01-SUMMARY.md
@.planning/phases/16-invocation-user-doc-truth/16-03-SUMMARY.md
@CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md
</context>

## Artifacts this phase produces

Symbols created across Phase 16 (excluded from drift verification — all are new):
`_MUST_CARRY_INVOCATIONS`, `test_key_docs_are_not_vacuous` (`tests/contract/test_doc_command_references.py`); `_allowed_tools_text`, `test_allowed_tools_text_is_not_vacuous` (`tests/contract/test_skill_migration.py`); `list_cards`, `_json_safe` (`src/construct/services/knowledge.py`); `CardListInput`, capability id `knowledge.card.list`, MCP tool name `construct_list_cards` (`src/construct/capabilities/catalog.py`); `card_list` Typer callback and its `--domain`/`-d`, `--include-archived`, `--workspace`/`-w`, `--json`/`-j` flags (`src/construct/cli.py`); `TestCardList` (`tests/unit/test_knowledge_operations.py`); new file `tests/contract/test_card_list_cli_mcp.py`; new file `USER-TEST-PLAYBOOK-v041.md`; a CLI-invocation table column in `CONSTRUCT-CLAUDE-impl/USER_GUIDE.md` and `CONSTRUCT-CLAUDE-impl/construct/references/commands.md`.

## Flagged planner assumptions (unresolved edges)

The deterministic edge probe surfaced one row against DEC-01 that it could not classify (`{"requirement_id": "DEC-01", "category": "unclassified", "status": "unresolved"}`). It is recorded here rather than dropped or auto-converted into a backstop.

**Assumption (unverified):** DEC-01's edge surface is treated as *binary and static* — a tool grant is either declared in the frontmatter or it is not, and the guard reads a static file. The planner found no boundary, ordering, encoding, or precision dimension in "a skill declares a tool". The one genuine dynamic dimension — whether Claude's runtime honours the narrowed grant at execution time — is outside this repository's test surface and is not asserted by any test in this phase.

**If this assumption is wrong,** the failure mode is that `test_skill_drops_forbidden_tools` proves only that the *declaration* is absent, not that the *capability* is unreachable. That gap is accepted: the declaration is the enforcement point the project controls, and RESEARCH verified the grants have zero references in the skill body, so there is no code path that would exercise them even if the runtime permitted it.

<tasks>

<task type="auto">
  <name>Task 1: Remove the dead web-search grants from construct-synthesis</name>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md (frontmatter lines 1-9 in full — list-style `allowed-tools`, one tool per line; then the whole body, to confirm for yourself that no procedure step invokes a web tool)
    - tests/contract/test_skill_migration.py (post-16-01 state — `_MIGRATED_SKILLS` now includes synthesis, `_FORBIDDEN_TOOLS` at ~:34, `_allowed_tools_text`)
    - CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md (the search-provider table around line 436 — the row stating the removal condition)
    - .planning/phases/16-invocation-user-doc-truth/16-01-SUMMARY.md (which test id is expected to turn green here)
  </read_first>
  <files>CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md</files>
  <action>
Drop the two web-tool entries from the list-style `allowed-tools` block, leaving `Read`, `Bash(construct)`, and `MCP(connect)`. Preserve the list style — do not collapse the block to the inline dialect. 16-01 fixed the parser precisely so the data does not have to be reshaped to fit the test; normalising the file here would make the data fit the test, which is backwards, and would leave the next multi-line skill exposed.

Before editing, confirm from your own read of the body that no procedure step, failure-mode row, or checklist item depends on web access. RESEARCH verified this by grep and found the tools referenced only in frontmatter; the grants are dead, which is what makes removal free rather than a capability reduction.

Verify by reading that the `description:` line, the `Read` grant, `Bash(construct)`, and `MCP(connect)` all survive. `Bash(construct)` in particular is asserted by `test_skill_still_delegates_to_cli` — the skill must stay a thin CLI delegator, not lose its delegation path along with its web grants.

This turns `test_skill_drops_forbidden_tools[construct-synthesis]` — RED since 16-01 — green.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest tests/contract/test_skill_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/contract/test_skill_migration.py -q` exits 0 with all parametrizations passing, including every `construct-synthesis` case
    - `.venv/bin/python -c "from tests.contract.test_skill_migration import _allowed_tools_text as f, _FORBIDDEN_TOOLS as F; t=f('construct-synthesis'); bad=[x for x in F if x in t]; assert not bad, bad"` exits 0
    - `.venv/bin/python -c "from tests.contract.test_skill_migration import _allowed_tools_text as f; t=f('construct-synthesis'); assert 'Bash(construct)' in t and 'Read' in t and 'MCP(connect)' in t, t"` exits 0
    - `.venv/bin/python -c "from tests.contract.test_skill_migration import _allowed_tools_text as f; assert f('construct-synthesis').count(chr(10)) >= 2"` exits 0 (list style preserved, not collapsed to inline)
    - `head -2 CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md | tail -1 | grep -c '^description:'` outputs `1`
  </acceptance_criteria>
  <done>Synthesis declares only the three tools it uses; the delegation grant and the list-style frontmatter dialect both survive; the skill-migration guard is fully green for the first time with synthesis in scope.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the refs lookup onto Read and delete the ref list allowlist entry</name>
  <read_first>
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md (Step 2 around lines 60-75 — the card-enumeration block, the "Also check refs/" sentence, and the fenced block naming a command that does not exist; the failure-mode table around line 159; the validation checklist around line 172; and Step 7 around line 130 — the views-refresh section that must survive)
    - CONSTRUCT-CLAUDE-impl/claude/skills/construct-gap-analysis/SKILL.md (the "Card data" step around line 27 and the checklist around line 148 — determine whether either needs any edit now that the command resolves)
    - CONSTRUCT-CLAUDE-spec/adrs/adr-0005-views-refresh-ownership.md (why synthesis keeps a refresh step when every other skill lost one)
    - .planning/phases/16-invocation-user-doc-truth/16-03-SUMMARY.md (the shipped `knowledge card list` flag surface, so the surviving card-enumeration invocation names real flags)
    - tests/contract/test_doc_command_references.py (`_KNOWN_BROKEN`, now three entries)
  </read_first>
  <files>CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md, tests/contract/test_doc_command_references.py</files>
  <action>
**The `refs/` step.** Step 2's second fenced block invokes a `ref` sub-app that does not exist and, per D-03, will not be built — standing up a whole sub-app for one advisory lookup in one skill is disproportionate. Replace the fenced invocation with an instruction to use `Read` over the workspace's `refs/` directory, scoped to the domain under synthesis. `Read` is already granted (and survives Task 1), so this introduces no tool requirement.

Preserve the step's intent. The sentence introducing it — that the skill should also consult `refs/` for supporting references — stays; only the mechanism changes. Deleting the step because its command does not exist would silently reduce what the skill does, which is the scope-reduction failure this phase is the opposite of.

Keep the surviving card-enumeration invocation in the same step. It names a command that is now real, and its flags should match what 16-03 actually shipped — read `16-03-SUMMARY.md` and correct the flag spelling if the shipped surface differs from what the skill currently writes. Keep the whole invocation on ONE line: the extractor's `[ \t]+` deliberately excludes newlines and will not match a wrapped command.

Review the failure-mode table row and the validation checklist item that reference card enumeration. Both now name a real command, so they need no correction — but confirm rather than assume, and correct the flags if 16-03 diverged.

**Do not touch Step 7 (views refresh).** It is an exemption recorded in `adr-0005-views-refresh-ownership.md`: synthesis has no `synthesis.run` capability, so there is no Python entry point to hang a refresh on, and without this step a synthesis never reaches the SPA. It looks like Phase 15 cleanup residue and is not.

**`construct-gap-analysis/SKILL.md`.** Determine from your read whether it needs any edit. Its card-enumeration invocation should now resolve unchanged. If it does, make no edit and record that in the summary — do not manufacture a diff.

**The allowlist.** Delete the `("knowledge", "ref", "list")` key from `_KNOWN_BROKEN`. This deletion is legitimate under D-16 specifically because the broken string left a document that REMAINS in `_DOC_GLOBS` — the file is still scanned, so the guard proves the string is gone rather than merely unwatched. Leave the two `workflow` entries; 16-06 owns those.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest tests/contract/test_doc_command_references.py tests/contract/test_skill_migration.py -q --deselect "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from tests.contract.test_doc_command_references import _KNOWN_BROKEN as k; assert len(k)==2 and all(p[0]=='workflow' for p in k), k"` exits 0
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_known_broken_entries_are_still_broken" -q` exits 0 with 2 parametrized cases
    - `.venv/bin/python -m pytest "tests/contract/test_doc_command_references.py::test_documented_commands_resolve" -q` exits 0
    - `.venv/bin/python -c "from pathlib import Path; from tests.contract.test_doc_command_references import _invocations, _resolves; p=Path('CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md'); bad=[i for i in _invocations(p.read_text()) if not _resolves(i)]; assert not bad, bad"` exits 0
    - `.venv/bin/python -c "from pathlib import Path; from tests.contract.test_doc_command_references import _invocations; p=Path('CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md'); assert len(_invocations(p.read_text())) >= 2"` exits 0 (the skill still documents real invocations; the step was rewritten, not gutted)
    - `.venv/bin/python -c "from tests.contract.test_doc_command_references import _DOC_GLOBS as g; assert len(g)==3, g"` exits 0 (the entry was deleted by fixing a document, not by narrowing the scan)
    - `grep -c 'adr-0005-views-refresh-ownership' CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md` outputs a value of at least 1 (Step 7 intact)
    - `grep -ci 'refs/' CONSTRUCT-CLAUDE-impl/claude/skills/construct-synthesis/SKILL.md` outputs a value of at least 1 (the supporting-references intent survives)
  </acceptance_criteria>
  <done>Synthesis's `refs/` step works using a tool it already holds, its card-enumeration invocation names the command 16-03 shipped, Step 7 is untouched, and the allowlist is down to the two playbook entries.</done>
</task>

<task type="auto">
  <name>Task 3: Mark the spec's migration-fallback instruction discharged</name>
  <read_first>
    - CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md (the search-provider table at ~:432-440 — locate the migration-fallback row by content, not by line offset; ALSO locate lines ~211 and ~557 so you can confirm you are not editing them)
    - .planning/phases/16-invocation-user-doc-truth/16-CONTEXT.md (D-15 — this row only; the two model-routing lines are fenced to Phase 17)
    - .planning/phases/14-durable-state-config-truth/14-CONTEXT.md (D-02, which deferred the model-routing spec lines to "Phase 16/17")
  </read_first>
  <files>CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md</files>
  <action>
The search-provider table's migration-fallback row carries a conditional instruction: remove the grant from skill `allowed-tools` once `research.run` ships. `research.run` shipped in Phase 10 and Task 1 completed the removal. Rewrite the row's action cell to record the instruction as discharged: state that the grant was removed, name the phase and the requirement (Phase 16 / DEC-01) that closed it, and keep the row itself so the historical rationale for the fallback remains legible. Do not delete the row — a deleted row loses the reasoning; a discharged row keeps it and closes it.

Change nothing else in this file. Lines ~211 and ~557 carry model-routing references that Phase 14 D-02 deferred and D-15 fences to Phase 17's spec pass, so they stay with the rest of the model-routing cleanup where the boundary is legible. Confirm by diff that your edit touches exactly one table row.
  </action>
  <verify>
    <automated>cd /Users/mab/dev/mabstruct/construct && .venv/bin/python -m pytest -q --deselect "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous" && git diff --numstat CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md` shows 1 line added and 1 line removed
    - `grep -c 'DEC-01' CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md` outputs a value of at least 1
    - `grep -ci 'discharged\|closed' CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md` outputs a value of at least 1
    - `grep -c 'model-routing' CONSTRUCT-CLAUDE-spec/spec-v04-agentworkflows.md` returns the same value before and after this task (the Phase 17 fence holds)
    - `.venv/bin/python -m pytest -q --deselect "tests/contract/test_doc_command_references.py::test_key_docs_are_not_vacuous"` exits 0
  </acceptance_criteria>
  <done>The spec row records that its instruction was carried out, by whom and under which requirement; the two model-routing lines fenced to Phase 17 are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill `allowed-tools` -> Claude tool permission layer | The frontmatter declaration is the enforcement point for what capabilities a skill may reach; a grant left in place is standing permission |
| skill procedure -> agent executing it | An agent follows the documented step verbatim; a step naming a nonexistent command fails at runtime with no failure-mode row covering it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Elevation of Privilege | `construct-synthesis` `allowed-tools` web grants | medium | mitigate | Task 1 removes both grants; `test_skill_drops_forbidden_tools[construct-synthesis]`, made non-vacuous in 16-01, keeps them out. The skill retains no unexercised egress capability |
| T-16-11 | Denial of Service | `construct-synthesis` Step 2 `refs/` lookup | low | mitigate | Task 2 rewrites the step onto `Read`, an already-granted tool, so the step succeeds instead of erroring on a nonexistent sub-app; the failure-mode table stays accurate |
| T-16-12 | Repudiation | `spec-v04-agentworkflows.md` migration-fallback row | low | mitigate | Task 3 marks the instruction discharged with phase and requirement attribution rather than deleting it, so the audit trail records both the rationale and its closure |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | Zero external packages installed; `pyproject.toml` untouched (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
```
.venv/bin/python -m pytest -q
```

After this plan the only remaining failures in the entire suite must be the two `test_key_docs_are_not_vacuous` cases for `USER_GUIDE.md` and `commands.md`, which 16-05 owns. `test_skill_migration.py` must be fully green, and `_KNOWN_BROKEN` must hold exactly the two `workflow` entries.

Read `construct-synthesis/SKILL.md` end to end once after editing and confirm: no step names a command that does not exist, the `refs/` intent survives, Step 7 is present, and the frontmatter is still list-style.
</verification>

<success_criteria>
- `test_skill_migration.py` is green across all four skills including `construct-synthesis`
- Synthesis's frontmatter declares only `Read`, `Bash(construct)`, `MCP(connect)`, in list style
- Synthesis's `refs/` step uses `Read` and preserves the original intent
- Every `construct ...` string in both skill files resolves; neither is covered by an allowlist entry
- Synthesis Step 7's ADR-recorded views-refresh exemption is intact
- `_KNOWN_BROKEN` holds exactly the two `workflow` entries; `_DOC_GLOBS` still holds 3 entries
- `spec-v04-agentworkflows.md` shows exactly one changed row and its model-routing references are untouched
</success_criteria>

<output>
Create `.planning/phases/16-invocation-user-doc-truth/16-04-SUMMARY.md` when done. Record whether `construct-gap-analysis/SKILL.md` needed any edit and why, and the exact discharged wording written into the spec row.
</output>
