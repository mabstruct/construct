---
phase: 10-durable-human-review-research-run
plan: 01
subsystem: research-run-foundation
tags: [dependency, checkpoint, sqlite, test-scaffold, wave-0]
requires: []
provides:
  - "SqliteSaver importable (langgraph-checkpoint-sqlite>=2.0,<3)"
  - ".construct/ git-ignored (checkpoint DB + rejected ledger)"
  - "D-02 SQLite carve-out recorded in REQUIREMENTS.md"
  - "Red Nyquist suite: RSCH-02..05 + SC5 (tests/llm/test_research_run.py)"
  - "conftest sqlite_checkpointer fixture + scored_findings_batch helper"
affects:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .planning/REQUIREMENTS.md
  - tests/llm/conftest.py
  - tests/llm/test_research_run.py
tech-stack:
  added:
    - "langgraph-checkpoint-sqlite>=2.0,<3 (provides langgraph.checkpoint.sqlite.SqliteSaver)"
  patterns:
    - "Cross-process resume via two SqliteSaver instances over one tmp DB file (r1/r2 idiom)"
    - "Lazy in-test import of not-yet-existing module so --collect-only stays green while suite is red"
key-files:
  created:
    - tests/llm/test_research_run.py
  modified:
    - pyproject.toml
    - uv.lock
    - .gitignore
    - .planning/REQUIREMENTS.md
    - tests/llm/conftest.py
decisions:
  - "D-02 carve-out: SQLite permitted ONLY as the LangGraph orchestration/checkpoint store under .construct/ for research.run workflow state; canonical SOT stays file-based."
  - "Build stamp src/construct/_build.py (regenerated by the hatch hook during uv add) reverted out of every commit as incidental tooling churn."
metrics:
  duration: ~6m
  completed: 2026-06-28
  tasks: 3
  files: 6
---

# Phase 10 Plan 01: Durable research.run Wave 0 Foundation Summary

Installed `langgraph-checkpoint-sqlite` (provides `SqliteSaver`), git-ignored the `.construct/` orchestration-state tree, recorded the D-02 SQLite carve-out in REQUIREMENTS.md, and scaffolded the offline test harness (conftest checkpointer + findings-batch fixtures and a red 8-test `tests/llm/test_research_run.py` enumerating RSCH-02..05 + SC5) that the rest of Phase 10 makes green.

## Tasks Completed

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | Package legitimacy gate — langgraph-checkpoint-sqlite | (pre-approved by orchestrator; user typed "approved") | — |
| 2 | Install dependency + git-ignore .construct/ + REQUIREMENTS carve-out | 9ecc67e | pyproject.toml, uv.lock, .gitignore, .planning/REQUIREMENTS.md |
| 3 | Test scaffolding — conftest fixtures + failing research.run suite | e2de611 | tests/llm/conftest.py, tests/llm/test_research_run.py |

## What Was Built

- **Dependency:** `langgraph-checkpoint-sqlite>=2.0,<3` pinned in `pyproject.toml` and resolved into `uv.lock`. `uv run --extra dev python -c "from langgraph.checkpoint.sqlite import SqliteSaver"` exits 0.
- **`.gitignore`:** added a `.construct/` rule (covers `.construct/workflow/research-run.sqlite` and `.construct/research/rejected.json`) so the checkpoint DB and rejected ledger are never committed (T-10-01 mitigation).
- **REQUIREMENTS.md:** Out-of-Scope SQLite row amended to record the D-02 carve-out — SQLite is permitted as the LangGraph orchestration/checkpoint store under `.construct/` for `research.run` state only; the canonical SOT (cards/refs/connections/search-seeds/events/digests) stays file-based and "no database owns any facts" is preserved (T-10-02 audit record).
- **conftest fixtures:** `sqlite_checkpointer` (a factory yielding `(saver, conn, db_path)` over a shared tmp DB file with `check_same_thread=False`, all connections closed at teardown — supports the r1/r2 cross-process resume simulation) and `make_scored_findings_batch` / `scored_findings_batch` (a deterministic `ResearchScoreGateOutput` with one finding per ingest_action band: skip / ref_only / ref_and_card).
- **Red suite:** `tests/llm/test_research_run.py` with all 8 required tests — `test_full_run_offline` (RSCH-02/SC1), `test_no_writes_before_approval` (RSCH-03/SC2), `test_per_finding_decisions` (RSCH-03), `test_cross_process_resume` (RSCH-04/SC3), `test_inspect_no_resume` (RSCH-04), `test_idempotent_rerun` (RSCH-05/SC4), `test_partial_batch_resume_safe` (RSCH-05), `test_run_result_fields` (SC5). Each is `xfail(strict=False)` and imports `construct.llm.research_run` lazily inside the test body so `--collect-only` passes while the suite is red.

## Verification

- `uv run --extra dev python -c "from langgraph.checkpoint.sqlite import SqliteSaver; print('ok')"` → `ok`.
- `grep -n "langgraph-checkpoint-sqlite" pyproject.toml` → line 15; `uv.lock` updated.
- `grep -n "construct" .gitignore` → `.construct/` ignore rule present.
- REQUIREMENTS.md Out-of-Scope SQLite row references the LangGraph checkpoint carve-out under `.construct/` and that SOT stays file-based.
- `uv run --extra dev pytest tests/llm/test_research_run.py --collect-only -q` → 8 tests collected, no collection error (verify-script grep count = 8).
- `uv run --extra dev pytest tests/llm/test_research_run.py -q` → `8 xfailed` (suite red as intended).
- `uv run --extra dev pytest tests/llm --collect-only -q` → 52 tests collected (conftest changes did not break existing collection).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Reverted auto-regenerated build stamp from each commit**
- **Found during:** Task 2 (and re-checked at Task 3)
- **Issue:** `uv add` triggers the hatch custom build hook (`hatch_build.py`), which rewrites `src/construct/_build.py` with a fresh timestamp on every editable reinstall. This unrelated churn appeared in `git status`.
- **Fix:** `git checkout -- src/construct/_build.py` before staging each task's files so the build stamp is excluded from the plan's commits (out-of-scope tooling artifact).
- **Files modified:** none committed (reverted)
- **Commit:** n/a

### Test invocation note
Per repo convention, all test runs used `uv run --extra dev pytest` (a bare `uv run pytest` resolves pytest in an isolated env without project deps).

## Known Stubs

None. Plan 01 is intentionally scaffolding-only: the 8 tests are `xfail` placeholders that plans 02-05 will implement and turn green. This is the documented Wave 0 gate (RESEARCH §Validation Architecture Wave 0 gaps), not an unintended stub.

## Threat Flags

None. No new security surface beyond the two boundaries already in the plan's threat model (package registry → local env, mitigated by the Task 1 legitimacy gate + pin; repo → git, mitigated by the `.construct/` ignore rule).

## Self-Check: PASSED
- FOUND: tests/llm/test_research_run.py
- FOUND: tests/llm/conftest.py (modified — sqlite_checkpointer + findings-batch helper)
- FOUND: commit 9ecc67e (Task 2)
- FOUND: commit e2de611 (Task 3)
