---
phase: 13-daily-cycle-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/llm/test_daily_run.py
  - src/construct/llm/daily_run.py
autonomous: true
requirements: [DAY-01, DAY-02, DAY-03]
must_haves:
  truths:
    - "D-09: daily.run composes research.run → curation.run → graph.status by CALLING the child run_* entrypoints (no duplicated child logic)"
    - "D-01: daily.run runs the children to completion in a single non-blocking pass — it never blocks on or interrupts for human review; review stays optional and post-hoc via the children's own review commands"
    - "D-06: A failing or paused child never aborts the cycle — the other child and graph.status still run (isolate + degrade)"
    - "D-07: The result surfaces per-child status, a pending-escalation count, and the final graph-health summary, mirroring curation.run's _aggregate_status vocabulary"
    - "D-07: A run with a failed/degraded child OR pending escalations is reported degraded, never a bare completed (no false completed); child failure detail is sanitized via _sanitize_error"
    - "D-02 / D-03: escalate items receive NO canonical write; recommended promote/connection/archive decisions are auto-applied via the child approve-all resume (D-03a: escalates surfaced as a pending count, not a live paused checkpoint)"
    - "D-04: every auto-applied write is event-logged by the children's existing _emit/append_event exactly as an interactive review would — daily_run adds no logging of its own"
    - "A past run can be inspected read-only from a persisted receipt; a missing run resolves to a failed 'No such daily run.' result"
  artifacts:
    - path: "src/construct/llm/daily_run.py"
      provides: "DailyRunInput/DailyInspectInput/DailyChildStatus/DailyRunResult models, _aggregate_daily_status, run_daily_run, inspect_daily_run"
      contains: "def run_daily_run"
      min_lines: 120
    - path: "tests/llm/test_daily_run.py"
      provides: "RED-first composition/degrade/result-surface/auto-apply unit + integration tests (DAY-01/02/03)"
      contains: "def test_composes_children"
  key_links:
    - from: "src/construct/llm/daily_run.py"
      to: "run_research_run / review_research_run"
      via: "direct function call (research child), branch on status == 'awaiting_review'"
      pattern: "review_research_run\\(.*approve_all=True"
    - from: "src/construct/llm/daily_run.py"
      to: "run_curation_run / review_curation_run"
      via: "direct function call (curation child); pending_escalations counted BEFORE resume"
      pattern: "review_curation_run\\(.*approve_all=True"
    - from: "src/construct/llm/daily_run.py"
      to: "graph_status(workspace).data"
      via: "closing graph-health summary folded into DailyRunResult.graph_health"
      pattern: "graph_status\\("
    - from: "src/construct/llm/daily_run.py"
      to: ".construct/workflow/daily/<run_id>.json"
      via: "receipt persistence after run_id passes _validate_run_id kebab guard"
      pattern: "workflow/daily"
---

<objective>
Build `daily.run` as a THIN synchronous Python composition module that calls the frozen `research.run` and `curation.run` child entrypoints plus `graph.status`, folding all three into a typed `DailyRunResult`. No parent LangGraph graph, no parent checkpointer, no new curation/research/gate logic.

Purpose: Deliver DAY-01/02/03 — a non-blocking daily maintenance cycle that composes stable children, isolates-and-degrades on child failure, auto-applies each child gate's *recommended* decision while excluding escalate, and never reports a false `completed`.
Output: `src/construct/llm/daily_run.py` (composition + models + inspect) proven by RED-first unit/integration tests in `tests/llm/test_daily_run.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-daily-cycle-composition/13-CONTEXT.md
@.planning/phases/13-daily-cycle-composition/13-RESEARCH.md
@.planning/phases/13-daily-cycle-composition/13-PATTERNS.md

<architecture_note>
RESEARCH resolved (HIGH confidence): `daily.run` is THIN Python composition, NOT a LangGraph parent graph. Each child owns its own SQLite checkpointer + thread_id + returns a typed Pydantic model. Do NOT build a parent StateGraph/checkpointer. Isolate-and-degrade is a `try/except` per child call. The spec's "parent graph" sketch (spec-v04 L101/L355/L578) is superseded by D-09.
</architecture_note>

<interfaces>
<!-- Child entrypoints to CALL (frozen — do NOT modify). Verified live 2026-07-06. -->
From src/construct/llm/research_run.py:
  def run_research_run(inp: ResearchRunInput) -> RunResult          # L900 — pauses at gate (status "awaiting_review") or "failed" on total outage
  def review_research_run(inp: ReviewInput) -> RunResult            # L996 — approve_all=True → recommended ingest to END
From src/construct/llm/curation_run.py:
  def run_curation_run(inp: CurationRunInput) -> CurationRunResult  # L1071 — empty gate_queue short-circuits to completed/degraded (no pause)
  def review_curation_run(inp: CurationReviewInput) -> CurationRunResult  # L1134 — approve_all=True → recommended decisions to END; escalate never written
  def inspect_curation_run(inp: CurationInspectInput) -> CurationRunResult  # L1182 — no-such-run branch L1213-1217 (mirror shape)
From src/construct/pipelines/graph_status.py:
  def graph_status(workspace: str | Path) -> OperationResult        # L12 — .data = {cards, connections, domains, workspace}
Reuse verbatim from curation_run.py: _validate_run_id (L61-74), _new_run_id (L214-222), _sanitize_error (L296-305).
CurationRunResult fields (mirror target, L195-208): status, run_id, gate_id, gate_queue, steps, events, message.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED unit + integration tests for daily.run composition</name>
  <files>tests/llm/test_daily_run.py</files>
  <read_first>
    - tests/llm/conftest.py — `create_test_workspace` (L169) fixture + `build_chat_model` monkeypatch seam (L156) reused for offline runs
    - src/construct/search/providers/mock.py — mock search provider for the offline research happy path (SRCH-03)
    - src/construct/llm/curation_run.py L1071-1114 (run_curation_run shape), L1134-1179 (review_curation_run), L855-859 (escalate = review-only, no write), L195-208 (CurationRunResult fields)
    - src/construct/llm/research_run.py L900-954 (run_research_run pause/fail branches), L979-993 (research _build_resume_decisions — no escalate kind)
    - .planning/phases/13-daily-cycle-composition/13-RESEARCH.md — "Validation Architecture" (Wave 0 gaps) + "Result Shape" + Pitfalls 1/2/3/5
  </read_first>
  <behavior>
    - test_composes_children (DAY-01): run_daily_run over an offline mock-provider workspace calls research then curation then graph.status; assert child capabilities appear in result.children in order ["research.run","curation.run","graph.status"] and no child logic is re-implemented (children invoked as functions).
    - test_result_surface (DAY-02): DailyRunResult exposes status, run_id, children (each with capability+status), pending_escalations (int), graph_health (dict with cards/connections/domains), message.
    - test_degrade_on_child_failure (DAY-03): when the research child raises/returns failed, curation + graph.status still run and parent status is "degraded" (never "completed"); child failure message is sanitized (class name only, no raw provider text).
    - test_no_false_completed_on_pending_escalation (DAY-03/D-07): a curation awaiting_review result carrying an escalate-kind proposal yields pending_escalations >= 1 and parent status "degraded", captured BEFORE the approve_all resume (Pitfall 5).
    - test_auto_apply_excludes_escalate (D-02/D-03): recommended promote/connection/archive are applied via review_curation_run(approve_all=True); escalate items are NOT written (assert via child result / event trail), and daily_run emits no events of its own (D-04 inherited).
    - test_inspect_roundtrip: after run_daily_run, inspect_daily_run(run_id) returns the persisted receipt; a missing run_id returns status "failed" message "No such daily run."
    - test_run_id_kebab_guard (V5): DailyRunInput/DailyInspectInput reject a non-kebab run_id (path-traversal guard) before any receipt path join.
  </behavior>
  <action>
    Create `tests/llm/test_daily_run.py` importing `run_daily_run`, `inspect_daily_run`, `DailyRunInput`, `DailyInspectInput`, `DailyRunResult` lazily from `construct.llm.daily_run` (module does not exist yet → these tests are RED until Task 2). Mirror the offline seams used by the curation/research suites: build the workspace with `create_test_workspace`, monkeypatch `build_chat_model`, and use the mock search provider for the research happy path; use the no-API-key path (`monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)`) for the degrade case. Assert the DAY-01/02/03 behaviors listed above. Do NOT assert on raw provider strings. Do NOT call any child's internal nodes directly — drive only the public `run_*`/`review_*` entrypoints through `run_daily_run`.
  </action>
  <verify>
    <automated>python -m pytest tests/llm/test_daily_run.py -x --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/llm/test_daily_run.py --co -q` collects test_composes_children, test_result_surface, test_degrade_on_child_failure, test_no_false_completed_on_pending_escalation, test_auto_apply_excludes_escalate, test_inspect_roundtrip, test_run_id_kebab_guard
    - Running the file (`python -m pytest tests/llm/test_daily_run.py -x`) fails/errors on import of `construct.llm.daily_run` (RED — module unbuilt), proving the tests exercise the real target
    - No test references a raw provider/exception string (grep for `ANTHROPIC` in asserts returns only env-manipulation, not message assertions)
  </acceptance_criteria>
  <done>Test file exists, collects the seven named tests, and is RED because `construct.llm.daily_run` does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement daily_run.py thin composition module (GREEN)</name>
  <files>src/construct/llm/daily_run.py</files>
  <read_first>
    - src/construct/llm/curation_run.py — L61-74 (_validate_run_id), L121-138 (input models), L214-222 (_new_run_id), L195-208 (CurationRunResult), L296-305 (_sanitize_error), L1056-1068 (_aggregate_status), L1071-1114 (run_curation_run), L1117-1131 (_build_resume_decisions — approve-all path), L1134-1179 (review_curation_run), L1182-1227 (inspect_curation_run incl. no-such-run L1213-1217), L855-859 (escalate = review-only, NO write), L308-324 (_emit → append_event, fired by CHILDREN)
    - src/construct/llm/research_run.py — L900-954 (run_research_run pause/fail), L996-1035 (review_research_run), L979-993 (research _build_resume_decisions)
    - src/construct/pipelines/graph_status.py — L12-79 (graph_status → OperationResult; .data shape L52-73)
    - src/construct/services/event_log.py — L13-41 (append_event; non-blocking) — for awareness only; daily_run emits NOTHING
    - .planning/phases/13-daily-cycle-composition/13-PATTERNS.md — sections A–M (exact clone map for daily_run.py)
  </read_first>
  <action>
    Create `src/construct/llm/daily_run.py`. Reuse `_validate_run_id` + `KEBAB_CASE_PATTERN` and `_sanitize_error` from `curation_run` (import, do not re-author). Define `DailyRunInput` (workspace_path, optional run_id + `_check_run_id` validator), `DailyInspectInput` (workspace_path, required run_id + validator), `DailyChildStatus` (capability, status, run_id|None, pending_escalations:int=0, message:str=""), and `DailyRunResult` (status: Literal["completed","degraded","failed"], run_id, children: list[DailyChildStatus], pending_escalations:int, graph_health: dict, message) — all `extra="forbid"`. Add `_new_run_id` mirroring curation's (prefix `daily-`, `%Y%m%d-%H%M%S` dash form to stay kebab-valid). Implement `_aggregate_daily_status(children, pending_escalations)`: `failed` only if ALL children failed; `degraded` if any child status in (failed, degraded, awaiting_review) OR pending_escalations>0; else `completed`.

    Implement `run_daily_run(inp)`: derive `run_id = inp.run_id or _new_run_id()`; give children derived kebab ids `f"{run_id}-research"` / `f"{run_id}-curation"`. RESEARCH child in `try/except`: call `run_research_run`; if status=="awaiting_review" call `review_research_run(approve_all=True)`; on exception record a degraded/failed DailyChildStatus via `_sanitize_error`. CURATION child in `try/except`: call `run_curation_run`; capture `pending_escalations = sum(1 for p in c.gate_queue if p.get("kind")=="escalate")` from the awaiting_review result BEFORE resuming (Pitfall 5); if status=="awaiting_review" call `review_curation_run(approve_all=True)`; else do NOT call review (empty-queue short-circuit, Pitfall 3). GRAPH health: `graph_status(inp.workspace_path)`; fold `.data` into `graph_health`. Fold into `DailyRunResult(status=_aggregate_daily_status(...), ...)`. Persist the receipt JSON to `.construct/workflow/daily/{run_id}.json` (only after run_id passed the kebab validator). Implement `inspect_daily_run(inp)`: read `.construct/workflow/daily/{run_id}.json` → DailyRunResult; missing file → `DailyRunResult(status="failed", run_id=..., message="No such daily run.")`. Use `logging` to stderr only — NEVER `print` (stdout is MCP JSON-RPC transport). Emit NO events (children own the `gate_review_*` + `workflow_step_complete` trail, D-04). Do NOT build a StateGraph, checkpointer, or any gate/apply logic.
  </action>
  <verify>
    <automated>python -m pytest tests/llm/test_daily_run.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/llm/test_daily_run.py -x -q` exits 0 (all seven Task-1 tests GREEN)
    - `grep -n "def run_daily_run\|def inspect_daily_run\|def _aggregate_daily_status\|class DailyRunResult\|class DailyChildStatus" src/construct/llm/daily_run.py` returns all five
    - `grep -vn '^\s*#' src/construct/llm/daily_run.py | grep -c "StateGraph\|Checkpointer\|_open_checkpointer\|graph.invoke"` returns 0 (no parent graph/checkpointer)
    - `grep -vn '^\s*#' src/construct/llm/daily_run.py | grep -c "print("` returns 0 (stderr logging only)
    - `grep -c "append_event\|_emit(" src/construct/llm/daily_run.py` returns 0 (daily_run emits no events — inherited from children, D-04)
    - `review_curation_run` and `review_research_run` are both called with `approve_all=True` (grep confirms)
  </acceptance_criteria>
  <done>daily_run.py implements thin composition with models, _aggregate_daily_status, run_daily_run, inspect_daily_run + receipt persistence; the full test_daily_run.py suite is GREEN; no parent graph, no self-emitted events, no print.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP client → daily.run handler | Untrusted `workspace_path` / `run_id` cross here (run_id becomes the receipt file path) |
| daily.run → child capabilities (research/curation) | Auto-apply resume writes to canonical SOT on the user's behalf (D-05 departure) |
| child → external search/LLM provider | Owned by the frozen children; exceptions bubble to daily.run as opaque objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Elevation/Tampering | run_daily_run auto-apply (D-05 unattended canonical write, departs from T-12-09 spine) | mitigate | Bounded by construction: (a) escalate items never written — enforced by child apply_promotions L855-859; (b) only the gate's *recommended* decision applied via `review_*_run(approve_all=True)` → child `_build_resume_decisions`, not a blanket approve; (c) every write event-logged by the children's `_emit` exactly as interactive review (D-04); (d) children's own direct-invocation HITL contract untouched — auto-apply lives ONLY in daily_run.py. Test: test_auto_apply_excludes_escalate + test_no_false_completed_on_pending_escalation. |
| T-13-02 | Tampering | `.construct/workflow/daily/<run_id>.json` receipt path derived from run_id | mitigate | `_validate_run_id` (KEBAB_CASE_PATTERN) on DailyRunInput/DailyInspectInput MUST run before any path join. Test: test_run_id_kebab_guard. |
| T-13-03 | Information Disclosure | child-failure detail in DailyChildStatus.message | mitigate | `_sanitize_error(exc)` (class name + first line only) for any caught child exception; never echo raw provider text. Test: test_degrade_on_child_failure. |
| T-13-04 | Information Disclosure | stdout writes breaking MCP JSON-RPC / leaking secrets | mitigate | `logging` to stderr only; grep gate asserts no `print(` in daily_run.py. |
| T-13-SC | Tampering | package installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A — reuses vendored pydantic/typer/langgraph/stdlib). |
</threat_model>

<verification>
- `python -m pytest tests/llm/test_daily_run.py -q` GREEN (DAY-01/02/03 unit + integration).
- Composition proven: children invoked as functions, isolate-and-degrade holds, no false completed, escalate excluded, receipt round-trips.
- Guards: no StateGraph/checkpointer, no self-emitted events, no `print`, run_id kebab-validated before path join.
</verification>

<success_criteria>
- `daily.run` composes research → curation → graph.status via the child entrypoints (DAY-01).
- Result surfaces per-child status + pending-escalation count + graph-health (DAY-02).
- Isolate-and-degrade + no-false-completed hold; escalate never auto-written (DAY-03, D-03/D-05).
- All Task-1 tests GREEN; no parent graph, no double-logging, no stdout writes.
</success_criteria>

<output>
Create `.planning/phases/13-daily-cycle-composition/13-01-SUMMARY.md` when done.
</output>
