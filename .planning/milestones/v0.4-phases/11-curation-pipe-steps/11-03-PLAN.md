---
phase: 11-curation-pipe-steps
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/construct/capabilities/catalog.py
  - src/construct/cli.py
autonomous: true
requirements: [CUR-01]
must_haves:
  truths:
    - "User can run `construct curation run -w <ws>` from the CLI and receive the curation result"
    - "User can run `construct curation inspect -w <ws> --run-id <id>` read-only from the CLI"
    - "curation.run and curation.inspect are registered with cli_name + mcp_tool_name and auto-discovered by the stdio MCP server"
    - "The shims reject positional args (keyword-only) and wrap CurationRunResult into OperationResult with success = status != failed"
    - "CLI --json output keys == MCP-serialized data keys == CurationRunResult.model_fields (schema parity)"
    - "The legacy workflow.run curation-cycle placeholder lambdas remain untouched (D-11)"
  artifacts:
    - path: "src/construct/capabilities/catalog.py"
      provides: "curation.run + curation.inspect registration; _curation_run_shim/_curation_inspect_shim/_curation_result_to_operation"
      contains: "curation.run"
    - path: "src/construct/cli.py"
      provides: "curation Typer sub-app with run/inspect commands + _render_curation_result"
      contains: "curation_app"
  key_links:
    - from: "src/construct/cli.py:curation_run_cmd"
      to: "get_registry().get('curation.run').handler"
      via: "registry dispatch"
      pattern: "curation\\.run"
    - from: "src/construct/capabilities/catalog.py:_curation_run_shim"
      to: "construct.llm.curation_run.run_curation_run"
      via: "keyword-only shim → OperationResult"
      pattern: "run_curation_run"
    - from: "stdio MCP server"
      to: "construct_curation_run / construct_curation_inspect"
      via: "registry auto-discovery (no server.py edit)"
      pattern: "construct_curation_run"
---

<objective>
Wire the `curation.run` and `curation.inspect` capabilities into the shared registry (catalog.py)
with dual-mode RT-03 shims, and add the `curation` Typer sub-app (run/inspect) to the CLI. MCP
parity is free via registry auto-discovery — `mcp/server.py` is NOT edited. This turns the Plan 01
contract suite GREEN and delivers the CLI + stdio MCP invocation surface for CUR-01.

Purpose: Criterion #1 requires `curation.run` runnable from BOTH the construct CLI and the stdio MCP
server through the shared capability registry. This plan is the invoke surface; the legacy
`workflow.run curation-cycle` placeholder path is left intact (D-11) and flagged for Phase 12.

Output: catalog registration + shims, CLI sub-app + curation renderer. Full suite green (API-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-curation-pipe-steps/11-RESEARCH.md
@.planning/phases/11-curation-pipe-steps/11-PATTERNS.md

<interfaces>
<!-- catalog.py analogs -->
catalog.py:392–422 — research.run/research.inspect registration block (CapabilityRecord shape)
catalog.py:481–512 — _run_result_to_operation (curation drops the ResearchScoreOutageError branch)
catalog.py:515–539 — _research_run_shim / _research_inspect_shim (keyword-only; positional → TypeError)
catalog.py:653–669 — legacy _get_workflow_steps("curation-cycle") placeholder lambdas — LEAVE UNTOUCHED (D-11)
CapabilityRecord fields: id, name, description, input_model, output_model, handler, cli_name, mcp_tool_name

<!-- Module exports from Plan 02 -->
construct.llm.curation_run: CurationRunInput, CurationInspectInput, run_curation_run, inspect_curation_run, CurationRunResult

<!-- cli.py analogs -->
cli.py:402–407 — research_app = typer.Typer(...) ; app.add_typer(research_app)
cli.py:596–614 — research_run_cmd (registry dispatch + _emit_run_result)
cli.py:664–681 — research_inspect_cmd (--run-id)
cli.py:583–593 — _emit_run_result (JSON passthrough or _render_run_result table)
cli.py:562 — _render_run_result(data) renders the RunResult shape (status/run_id/gate_queue) — NOT the curation steps/events shape
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register curation.run + curation.inspect with dual-mode shims (catalog.py)</name>
  <files>src/construct/capabilities/catalog.py</files>
  <read_first>
    - src/construct/capabilities/catalog.py (imports block ~49–57; research.run registration 392–422; _run_result_to_operation 481–512; _research_run_shim/_research_inspect_shim 515–539; legacy curation-cycle placeholders 653–669 — DO NOT EDIT THESE)
    - .planning/phases/11-curation-pipe-steps/11-PATTERNS.md (catalog.py section: import block, registration, shim helper code shapes)
    - src/construct/llm/curation_run.py (the Plan 02 exports being imported)
    - tests/contract/test_curation_run_cli_mcp.py (test_registered, test_shims_reject_positional_args assertions)
  </read_first>
  <action>
    Add a curation-import block near the other phase imports importing CurationInspectInput, CurationRunInput,
    inspect_curation_run, run_curation_run from construct.llm.curation_run. Add `_curation_result_to_operation(cap_id,
    runner)` mirroring `_run_result_to_operation` but SIMPLER — NO ResearchScoreOutageError branch: try `result =
    runner()`, on `except Exception as exc` return `OperationResult(success=False, message=f"{cap_id} failed:
    {type(exc).__name__}", data={"failed": True})`, else return `OperationResult(success=result.status != "failed",
    message=result.message or result.status, data=result.model_dump(mode="json"))`. Add `_curation_run_shim(*args,
    **kwargs)` and `_curation_inspect_shim(*args, **kwargs)` that raise TypeError if `args` (keyword-only), then call
    `_curation_result_to_operation("curation.run", lambda: run_curation_run(CurationRunInput(**kwargs)))` and the
    inspect equivalent. Register two CapabilityRecords: `curation.run` (name "Curation Run", input_model
    CurationRunInput, output_model OperationResult, handler _curation_run_shim, cli_name "curation.run",
    mcp_tool_name "construct_curation_run", description noting deterministic checks + no canonical writes) and
    `curation.inspect` (input_model CurationInspectInput, handler _curation_inspect_shim, cli_name
    "curation.inspect", mcp_tool_name "construct_curation_inspect"). DO NOT touch the legacy
    `_get_workflow_steps("curation-cycle")` lambdas (catalog.py:653–669) or the `workflow.run` registration — they
    coexist (D-11). Do NOT edit src/construct/mcp/server.py (parity is auto-discovered).
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest "tests/contract/test_curation_run_cli_mcp.py::test_registered" "tests/contract/test_curation_run_cli_mcp.py::test_shims_reject_positional_args" "tests/contract/test_curation_run_cli_mcp.py::test_mcp_no_hardcoded_curation" -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test_registered` passes: both capabilities present with cli_name == id and mcp_tool_name == construct_curation_run/construct_curation_inspect.
    - `test_shims_reject_positional_args` passes: `registry.get("curation.run").handler("positional")` raises TypeError.
    - `test_mcp_no_hardcoded_curation` passes: no `construct_curation_run`/`curation.run` literal in mcp/server.py source.
    - `git diff src/construct/capabilities/catalog.py` shows NO change to lines around the legacy curation-cycle placeholders (D-11) — the placeholder lambdas are byte-identical.
    - `_curation_result_to_operation` has NO ResearchScoreOutageError reference.
  </acceptance_criteria>
  <done>Both curation capabilities registered with dual-mode shims; MCP auto-discovers them; legacy placeholders untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add curation Typer sub-app + curation result renderer (cli.py)</name>
  <files>src/construct/cli.py</files>
  <read_first>
    - src/construct/cli.py (research_app 402–407; research_run_cmd 596–614; research_inspect_cmd 664–681; _emit_run_result 583–593; _render_run_result 562; _display_result 166)
    - .planning/phases/11-curation-pipe-steps/11-PATTERNS.md (cli.py section: sub-app, run command, inspect command, render-mismatch note)
    - tests/contract/test_curation_run_cli_mcp.py (test_cli_commands_present, test_cli_mcp_schema_parity assertions)
  </read_first>
  <action>
    Add `curation_app = typer.Typer(no_args_is_help=True, name="curation", help="Run deterministic curation
    maintenance checks (read-only).")` and `app.add_typer(curation_app)` mirroring research_app (cli.py:402–407).
    Add `curation_run_cmd` (`@curation_app.command(name="run")`) mirroring research_run_cmd but DROP the `--provider`
    option: options are `--workspace/-w` (required) and `--json/-j`; resolve `get_registry().get("curation.run")`
    (on KeyError echo an error mentioning Phase 11 and `raise typer.Exit(code=1)`), call
    `cap.handler(workspace_path=str(workspace))`, render the result. Add `curation_inspect_cmd`
    (`@curation_app.command(name="inspect")`) mirroring research_inspect_cmd with `--workspace/-w`, `--run-id`
    (required), `--json/-j`, resolving `curation.inspect`. Because the existing `_render_run_result` renders the
    RunResult shape (status/run_id/gate_queue) and CurationRunResult uses steps/events, add a small
    `_render_curation_result(data)` sibling that prints run status, run_id, and a per-step line (step name, status,
    one-line summary) so the user can VISUALLY distinguish completed vs degraded vs skipped (criterion #2). Use it
    from a curation-specific emit path: when `--json` pass through via `_display_result(result, json_output=True)`;
    otherwise on success call `_render_curation_result(result.data)` then echo the message, on failure call
    `_display_result(result, json_output=False)`. Keep full `--json` fidelity (the contract parity test asserts on
    the JSON payload).
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/contract/test_curation_run_cli_mcp.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test_cli_commands_present` passes: `CliRunner().invoke(app, ["curation","run","--help"])` and `["curation","inspect","--help"]` both exit 0.
    - `test_cli_mcp_schema_parity` passes offline (no ANTHROPIC_API_KEY): CLI `--json` keys == MCP-serialized `data` keys == `CurationRunResult.model_fields.keys()`.
    - Entire `tests/contract/test_curation_run_cli_mcp.py` suite GREEN.
    - `_render_curation_result` prints per-step status (visible completed/degraded/skipped distinction) — grep confirms it iterates `steps`.
    - `curation_run_cmd` has NO `--provider` option.
  </acceptance_criteria>
  <done>curation CLI sub-app live with run/inspect; curation renderer shows per-step status; contract suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression sweep (API-05) + flag legacy placeholder for Phase 12</name>
  <files>src/construct/capabilities/catalog.py, src/construct/cli.py</files>
  <read_first>
    - .planning/STATE.md (Pending Todos / Phase 12 boundary — where to record the legacy-placeholder flag)
    - src/construct/capabilities/catalog.py (confirm legacy curation-cycle lambdas still present and unchanged)
  </read_first>
  <action>
    Run the full test suite to confirm no v0.3/v0.4 regression (API-05): existing CLI, MCP, validation, ingestion,
    graph, ask-domain, research.run behavior must still pass alongside the new curation capabilities. In the plan
    SUMMARY, explicitly record the surviving D-11 debt: the legacy `workflow.run curation-cycle` placeholder lambdas
    (catalog.py ~653–669) still return fake-success and must be removed/redirected in Phase 12 under CUR-05. Do NOT
    modify those lambdas in this phase. No production code change is expected in this task unless the regression
    sweep surfaces a real break — if it does, fix the curation-side cause (never weaken an existing test).
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Full suite `pytest -q` is GREEN (curation + all pre-existing tests).
    - The legacy curation-cycle placeholder lambdas remain byte-identical (D-11) — `git diff` shows no edit to that block.
    - The SUMMARY records the Phase 12 / CUR-05 flag for the surviving legacy fake-success path.
  </acceptance_criteria>
  <done>Full suite green (API-05 holds); legacy placeholder debt flagged for Phase 12; no canonical-write or placeholder regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP arg → shim | workspace_path and run_id arrive as untrusted kwargs through the registry handler. |
| registry handler → curation_run | the shim constructs the validated CurationRunInput (run-id kebab guard fires here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | run_id at the invoke boundary | mitigate | The shim constructs CurationRunInput/CurationInspectInput, whose field_validator enforces KEBAB_CASE_PATTERN before run_id reaches the SQLite thread_id / checkpoint path (CR-01 / ASVS V5). |
| T-11-05 | Spoofing (positional injection) | dual-mode shim | mitigate | Shims raise TypeError on positional args (keyword-only), preventing accidental positional misuse from CLI/MCP; asserted by `test_shims_reject_positional_args`. |
| T-11-07 | Tampering | legacy placeholder coexistence (D-11) | accept | Legacy `workflow.run curation-cycle` fake-success path left intact this phase (separate capability id); flagged for Phase 12/CUR-05 removal. Acceptable: criterion #3 targets `curation.run` real steps, not the legacy path. |
| T-11-SC | Tampering | npm/pip installs | accept | No installs this phase (all deps shipped+vetted Phases 8–10). |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest -q` → full suite GREEN (API-05 regression intact + new curation suites).
- `./.venv/bin/python -m pytest tests/contract/test_curation_run_cli_mcp.py -q` → GREEN (registration + CLI + MCP parity).
- `grep -n "construct_curation" src/construct/mcp/server.py` → no match (no server.py edit; parity auto-discovered).
- `git diff src/construct/capabilities/catalog.py` → legacy curation-cycle lambdas (≈653–669) unchanged (D-11).
</verification>

<success_criteria>
- curation.run + curation.inspect registered with cli_name + mcp_tool_name; auto-discovered by stdio MCP.
- `construct curation run -w <ws>` and `construct curation inspect -w <ws> --run-id <id>` work from the CLI.
- CLI/MCP schema parity holds; dual-mode shims reject positional args.
- Legacy placeholder path untouched (D-11) and flagged for Phase 12.
- Full suite green (API-05).
</success_criteria>

<output>
Create `.planning/phases/11-curation-pipe-steps/11-03-SUMMARY.md` when done
</output>
