---
phase: quick-260726-lqd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/construct/services/init.py
  - tests/contract/test_generated_workspace_doc.py
autonomous: true
requirements: [FIX-02]
quick: true
quick_id: 260726-lqd

must_haves:
  truths:
    - "A workspace created by `construct init` carries a WORKSPACE.md whose Support Paths entry for `.construct/model-routing.yaml` records it as deprecated and inert, not as a live routing source."
    - "That generated entry names `src/construct/llm/config.yaml` as the LLM configuration authority, matching workspace-contract.md:84 in substance."
    - "A guard test fails loudly if a future edit reintroduces an authority claim for model-routing.yaml OR silently deletes the Support Paths section / the model-routing row entirely."
    - "`construct init` still writes `.construct/model-routing.yaml` from the template — the file layout is unchanged, only the prose describing it."
  artifacts:
    - "src/construct/services/init.py — amended `_write_workspace_doc` Support Paths bullet"
    - "tests/contract/test_generated_workspace_doc.py — new non-vacuous doc-truth guard"
  key_links:
    - "`initialize_workspace()` -> generated `WORKSPACE.md` -> guard test reads the *generated bytes*, not the source string, so the guard cannot be satisfied by editing a constant the generator does not use."
    - "Guard extractor -> `## Support Paths` heading -> `model-routing.yaml` bullet: both anchors asserted present before any content assertion runs."
---

<objective>
Close the FIX-02 documentation gap found by the v0.4.1 milestone audit (2026-07-26).

`src/construct/services/init.py:175` writes a Support Paths bullet into every new
workspace's `WORKSPACE.md` claiming `.construct/model-routing.yaml` holds live routing
guidance. Four other docs (`workspace-contract.md:84`, `nfrs.md:76`, `AGENTS.md:91`
and `:135`) record the opposite: the file is deprecated and inert, and the LLM
configuration authority is `src/construct/llm/config.yaml`. `init.py` was the fifth
writer and was missed in Phase 14. Every `construct init` therefore ships a
self-contradicting document into the user's own workspace.

This is a RED -> GREEN pair: Task 1 lands a non-vacuous guard that fails against the
current generated text; Task 2 amends the generator until the guard passes.

Purpose: the generated per-workspace doc is the copy a user is most likely to read;
it must agree with the spec.
Output: one amended Support Paths bullet, one new contract guard, suite green.

**Scope fences (from the task brief — do not cross):**
- Do NOT touch `resolve_llm_config_path()` or anything under `src/construct/llm/`. The
  code half of FIX-02 is delivered and correct.
- Do NOT remove the template copy at `src/construct/services/init.py:58`. The file stays
  a `REQUIRED_PATHS` entry; deleting it is a workspace-format change (Phase 14 D-01,
  "deprecate-but-scaffold"). Only the prose describing it is wrong.
- Do NOT reopen the v0.4.1 milestone, edit `ROADMAP.md`, or edit anything under
  `.planning/milestones/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@src/construct/services/init.py
@tests/contract/test_artifact_catalog.py
</context>

<interface_notes>
Facts verified against the working tree on 2026-07-26 — do not re-derive:

- `_write_workspace_doc(root: Path) -> None` lives at `src/construct/services/init.py:160`.
  It writes `WORKSPACE.md` in a single `write_text()` of adjacent string literals. The
  Support Paths block is the last two literals; the model-routing bullet is line 175.
- `initialize_workspace(root: str | Path, domain: DomainInitInput) -> Path` returns the
  resolved workspace root and raises `WorkspaceInitError` if the target exists and is
  non-empty — so the guard must init into a fresh `tmp_path` subdirectory.
- `DomainInitInput` is a frozen dataclass with fields `domain_id`, `display_name`,
  `scope`, `taxonomy_seeds`, `source_priorities`, `research_seeds`. Construction
  precedent to copy: `tests/unit/test_artifact_write_gates.py:90-98`.
- `construct init` is interactive (prompts for slug, name, scope, taxonomy, priorities,
  seeds). Call `initialize_workspace()` directly; do not drive the CLI.
- NO existing test asserts any WORKSPACE.md *content* — only
  `tests/fixtures/expected-workspace-tree.txt:5` asserts the file exists. This guard is
  net-new and no existing assertion will conflict with it.
- `tests/contract/` is a package (`__init__.py` present). Plain
  `from construct.services.init import ...` works; pytest config sets
  `pythonpath = ["src"]`.
- House style to follow: `tests/contract/test_artifact_catalog.py` — a module docstring
  stating the design contract, and a dedicated
  `test_catalog_introspection_is_not_vacuous` (lines 112-134) whose only job is to make
  a broken extractor fail loudly rather than pass on an empty set.
- Canonical wording to match in substance (`CONSTRUCT-CLAUDE-spec/workspace-contract.md:84`):
  "**Deprecated and inert** — nothing reads it; retained only for workspace-contract
  stability. LLM configuration authority is `src/construct/llm/config.yaml`, resolved by
  `src/construct/llm/config.py`". `WORKSPACE.md` is a terse orientation file, so the
  generated bullet drops the resolver clause and keeps the rest.
- Test baseline before this plan: **524 passed, 1 skipped**. This plan adds exactly
  2 test functions -> expected terminal state **526 passed, 1 skipped**.
</interface_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the non-vacuous generated-WORKSPACE.md guard (RED)</name>
  <files>tests/contract/test_generated_workspace_doc.py</files>
  <precondition>`.venv/bin/python -m pytest --version` succeeds from the repo root (per AGENTS.md, the repo-local venv is the only sanctioned test runtime).</precondition>
  <behavior>
    Two test functions, in `tests/contract/test_generated_workspace_doc.py`.

    Shared setup — a module-level helper (or session/function fixture) that calls
    `initialize_workspace(tmp_path / "ws", DomainInitInput(...))` with the same field
    values used at `tests/unit/test_artifact_write_gates.py:90-98`, then reads the
    resulting `WORKSPACE.md` text. The guard reads the GENERATED BYTES, never the
    source string in `init.py` — that is the whole point of the guard.

    Two extractor helpers, both of which must be tolerant of heading order:
    - `_support_paths_section(text) -> str` — the text between the `## Support Paths`
      heading and the next `## ` heading or end of file.
    - `_model_routing_row(section) -> str` — the single bullet line in that section
      containing `model-routing.yaml`.
    Each extractor returns an empty string when its anchor is absent; the non-vacuity
    test is what converts an empty return into a loud failure.

    Test 1 — `test_generated_workspace_doc_extraction_is_not_vacuous`
    The anti-vacuity floor, modelled on
    `test_catalog_introspection_is_not_vacuous`. Asserts, each with its own message:
    - the generated text is non-empty and contains `# CONSTRUCT Workspace`;
    - all three headings are present: `## Canonical Paths`, `## Derived Paths`,
      `## Support Paths` — a gutted generator fails here, not silently downstream;
    - the Support Paths section holds at least 2 bullet lines;
    - the model-routing row is found and starts with `- `;
    - a SECOND anchor row is found in the same section — the bullet containing
      `search.yaml` — proving the extractor matches by structure rather than by one
      hardcoded string.

    Test 2 — `test_model_routing_row_records_deprecation` (the guard; RED today)
    Re-asserts the row is non-empty (so this test also cannot pass vacuously), then:
    - the row records deprecation: `deprecated` and `inert` both appear,
      case-insensitively;
    - the row names the real authority: the literal `src/construct/llm/config.yaml`
      appears;
    - the row makes NO authority claim about model-routing.yaml itself. Use a
      module-level compiled pattern, e.g.
      `_AUTHORITY_CLAIM = re.compile(r"model-routing\.yaml`?\s+(?:stores|holds|controls|configures|drives|provides)\b", re.IGNORECASE)`
      and assert `_AUTHORITY_CLAIM.search(row) is None`. Scoping the pattern to the
      verb that immediately follows the filename is deliberate: the corrected row does
      name an authority (the llm config path), so an unscoped search for authority
      wording would fail on the fixed text.

    Expected RED signature against the current generator: Test 1 PASSES (the section
    and both rows already exist — the structure is fine, only the prose is wrong) and
    Test 2 FAILS on both the missing deprecation marker and the matched authority
    claim. A run where Test 1 also fails means the extractors are broken, not the
    prose — fix the extractors before touching `init.py`.
  </behavior>
  <action>
    Create `tests/contract/test_generated_workspace_doc.py` implementing the two tests
    described in `<behavior>`, in the house style of
    `tests/contract/test_artifact_catalog.py`: `from __future__ import annotations`
    first, then a module docstring that states what FIX-02 is, why `init.py` was the
    fifth and missed writer, and the design contract — read the generated bytes, never
    the source constant; vacuity is the real threat, so the extractors have an explicit
    floor test. Give every assertion a failure message that names the file and the
    expectation, per the repo's error-message conventions.

    Do NOT modify `src/construct/services/init.py` in this task. Its wrong prose is
    the RED fixture that proves the guard bites.

    Do NOT weaken or delete `tests/fixtures/expected-workspace-tree.txt` — the
    generated file layout is unchanged by this plan.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/contract/test_generated_workspace_doc.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>The new test file exists and, against the unmodified generator, `test_generated_workspace_doc_extraction_is_not_vacuous` PASSES while `test_model_routing_row_records_deprecation` FAILS. The failure output names the missing deprecation marker. Committed as a `test(...)` commit.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the generated Support Paths bullet (GREEN)</name>
  <files>src/construct/services/init.py</files>
  <action>
    In `_write_workspace_doc` at `src/construct/services/init.py:160`, replace the
    single Support Paths bullet for `.construct/model-routing.yaml` (currently line
    175, the one asserting the file is a live source of runtime routing) with a bullet
    that records the Phase 14 deprecation and points at the real authority. Leave the
    `search.yaml` bullet, both other sections, and every other line of the function
    untouched.

    The replacement is ONE markdown bullet — write it as three adjacent Python string
    literals so no source line runs long; they concatenate into a single line in the
    generated file. Contents, in order: `- ` then backticked `.construct/model-routing.yaml`,
    an em dash, bold `deprecated and inert`, then `; nothing reads it.`; then
    `Retained only for workspace-contract stability.`; then
    `LLM configuration authority is ` + backticked `src/construct/llm/config.yaml` + `.`
    and a trailing newline. Mind the inter-literal spaces — the first two literals must
    each end with a space so the concatenated sentence does not run words together.

    This mirrors `CONSTRUCT-CLAUDE-spec/workspace-contract.md:84` in substance, minus
    its resolver clause — `WORKSPACE.md` is a terse orientation file, not a spec.

    Change nothing else in this file. In particular the `shutil.copy` of the
    model-routing template at line 58 stays exactly as it is: the file remains a
    `REQUIRED_PATHS` entry (`src/construct/schemas/workspace.py:25`) and is still
    validated (`src/construct/services/validation.py:127`), so removing the copy would
    break `tests/fixtures/expected-workspace-tree.txt`,
    `tests/unit/test_artifact_write_gates.py:107`, and the workspace format itself.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/contract/test_generated_workspace_doc.py -v && .venv/bin/python -m pytest -q 2>&1 | tail -5</automated>
  </verify>
  <done>Both guard tests pass. Full suite reports 526 passed, 1 skipped (baseline 524 + the 2 new tests), with no pre-existing test regressed — in particular `tests/unit/test_artifact_write_gates.py` and `tests/integration/test_init_cli.py` stay green. Committed as a `fix(...)` commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator -> user workspace | `initialize_workspace` writes files a user reads and acts on. No untrusted input crosses here: `_write_workspace_doc` takes only a path and emits a fixed literal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-lqd-01 | Repudiation | generated `WORKSPACE.md` | medium | mitigate | This IS the plan: a shipped doc that contradicts the spec is an audit trail that lies. Corrected prose plus a guard that fails on regression. |
| T-lqd-02 | Information Disclosure | new bullet naming `src/construct/llm/config.yaml` | low | accept | The path is already public in `workspace-contract.md:84`, `nfrs.md:76`, and `AGENTS.md:135`. It is a repo-relative config path, not a secret or a credential location. |
| T-lqd-03 | Tampering | package installs | n/a | accept | No npm/pip/cargo install task in this plan; no new dependency. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `.venv/bin/python -m pytest -q` -> `526 passed, 1 skipped`.
2. Sanity-read the generated artifact end-to-end: initialize a throwaway workspace in a
   scratch dir via `initialize_workspace` (or `construct init`, answering the prompts)
   and read its `WORKSPACE.md` Support Paths section. The model-routing line must read
   as deprecated and must not read as a live routing source.
3. Confirm the throwaway workspace still contains `.construct/model-routing.yaml` — the
   file is scaffolded as before; only its description changed.
4. Confirm `git diff --stat` touches exactly two paths:
   `src/construct/services/init.py` and `tests/contract/test_generated_workspace_doc.py`.
   Nothing under `src/construct/llm/`, nothing under `.planning/milestones/`, no
   `ROADMAP.md`.
</verification>

<success_criteria>
- FIX-02's doc half is closed: the fifth writer agrees with the other four.
- The guard is non-vacuous — it fails if the deprecation marker is removed AND fails if
  the Support Paths section or the model-routing row is deleted outright.
- The guard was demonstrated RED before the fix, not merely asserted to be.
- Zero behavioural change to workspace layout, `REQUIRED_PATHS`, or LLM config
  resolution.
</success_criteria>

<output>
Create `.planning/quick/260726-lqd-fix-fix-02-gap-workspace-md-model-routin/260726-lqd-SUMMARY.md` when done.
</output>
