---
phase: 01-workspace-canonical-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/construct/schemas/card.py
  - src/construct/schemas/config.py
  - src/construct/schemas/workspace.py
  - templates/workspace/domains.yaml
  - templates/workspace/model-routing.yaml
  - templates/workspace/governance.yaml
  - tests/unit/test_schema_contracts.py
autonomous: true
requirements:
  - WORK-02
  - CARD-01
  - CARD-04
must_haves:
  truths:
    - "Canonical workspace files have explicit machine-validated schemas."
    - "Model routing is configurable from workspace files rather than application code."
    - "Cards and connections follow one documented source-of-truth contract."
  artifacts:
    - path: "src/construct/schemas/card.py"
      provides: "Knowledge card validation models"
      contains: "epistemic_type"
    - path: "src/construct/schemas/config.py"
      provides: "Domain, routing, and governance config models"
      contains: "domain_init_interview"
    - path: "src/construct/schemas/workspace.py"
      provides: "Workspace scaffold and connections authority models"
      contains: "connections.json"
    - path: "templates/workspace/model-routing.yaml"
      provides: "Editable default routing configuration"
      contains: "frontier:"
  key_links:
    - from: "templates/workspace/model-routing.yaml"
      to: "src/construct/schemas/config.py"
      via: "Pydantic parsing of default routing file"
      pattern: "chat_conversation"
    - from: "src/construct/schemas/card.py"
      to: "src/construct/schemas/workspace.py"
      via: "shared card id and connection validation"
      pattern: "id"
---

<objective>
Lock the canonical workspace, domain, routing, governance, card, and connection contracts before any file-writing workflow is implemented.

Purpose: Make Phase 1 write against stable schemas per D-01 through D-10 so later init and validation logic cannot invent incompatible file shapes.
Output: Pydantic schema modules, default workspace templates, and contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-canonical-data-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-canonical-data-foundation/01-RESEARCH.md
@CONSTRUCT-spec/construct-prd.md
@CONSTRUCT-spec/construct-repo-topology.md
@CONSTRUCT-spec/knowledge-card-schema.md
@CONSTRUCT-spec/construct-data-schemas.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical schema models per D-05 through D-10</name>
  <read_first>
    .planning/phases/01-workspace-canonical-data-foundation/01-CONTEXT.md
    .planning/phases/01-workspace-canonical-data-foundation/01-RESEARCH.md
    CONSTRUCT-spec/knowledge-card-schema.md
    CONSTRUCT-spec/construct-data-schemas.md
  </read_first>
  <files>src/construct/schemas/card.py, src/construct/schemas/config.py, src/construct/schemas/workspace.py, tests/unit/test_schema_contracts.py</files>
  <behavior>
    - Test 1: a valid markdown card frontmatter with kebab-case `id`, valid enums, and matching filename validates successfully.
    - Test 2: malformed YAML, invalid enum values, bad IDs, and duplicate connection edges fail with structured errors.
    - Test 3: root `domains.yaml` and per-domain `domains/{domain_id}/domain.yaml` parse as separate but linked models per D-05, D-06, and D-07.
  </behavior>
  <action>Create Pydantic v2 models for the Phase 1 contract. In `src/construct/schemas/card.py`, encode the card rules from `knowledge-card-schema.md`: required `id`, `title`, `epistemic_type`, `created`, `confidence`, `source_tier`, `domains`, plus warning-level fields such as `lifecycle` and `connects_to`. In `src/construct/schemas/config.py`, define models for `domains.yaml`, per-domain `domain.yaml`, `model-routing.yaml`, and `governance.yaml`. Use the default routing keys from the PRD exactly: `research_ingestion`, `card_formatting`, `source_evaluation`, `taxonomy_tagging`, `duplicate_detection`, `connection_typing`, `cross_domain_ideation`, `synthesis_drafting`, `co_authorship`, `gap_analysis`, `domain_init_interview`, `chat_conversation`. In `src/construct/schemas/workspace.py`, define the required workspace scaffold plus authoritative `connections.json` records; reject duplicate `from` + `to` + `type` edges to lock the canonical edge rule from research. Write tests first in `tests/unit/test_schema_contracts.py`, then implement until they pass.</action>
  <acceptance_criteria>
    - `src/construct/schemas/config.py` contains models for root domain registry and per-domain details
    - `src/construct/schemas/workspace.py` contains a required-path list including `cards`, `domains`, `db`, `views`, `publish`, `log/events.jsonl`, and `connections.json`
    - `tests/unit/test_schema_contracts.py` includes failing/then-passing coverage for malformed YAML, invalid enums, and duplicate edges
    - `python -m pytest tests/unit/test_schema_contracts.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_schema_contracts.py -q</automated>
  </verify>
  <done>The canonical workspace and card/config contract is encoded in tests and Pydantic models, including the root-registry + per-domain-folder decision and strict-vs-warning validation split.</done>
</task>

<task type="auto">
  <name>Task 2: Add editable default templates for routing and governance</name>
  <read_first>
    src/construct/schemas/config.py
    CONSTRUCT-spec/construct-prd.md
    CONSTRUCT-spec/construct-data-schemas.md
  </read_first>
  <files>templates/workspace/domains.yaml, templates/workspace/model-routing.yaml, templates/workspace/governance.yaml, tests/unit/test_schema_contracts.py</files>
  <action>Create default template files that later `construct init` writes into new workspaces. `templates/workspace/model-routing.yaml` must include the tier names `frontier`, `workhorse`, and `lightweight`, using the PRD defaults `anthropic / claude-sonnet-4-6`, `ollama / llama3.3:70b`, and `ollama / llama3.2:8b`; keep `api_key_env` as an env-var name, never a secret. `templates/workspace/governance.yaml` must include promotion, decay, quality, and heartbeat defaults from `construct-data-schemas.md`. `templates/workspace/domains.yaml` must model the canonical registry with each domain record pointing to `domains/{domain_id}/domain.yaml` per D-06 and D-07. Extend the schema contract tests so these templates round-trip through the new Pydantic models.</action>
  <acceptance_criteria>
    - `templates/workspace/model-routing.yaml` contains `frontier`, `workhorse`, and `lightweight`
    - `templates/workspace/model-routing.yaml` contains `domain_init_interview` and `chat_conversation`
    - `templates/workspace/governance.yaml` contains `promotion:`, `decay:`, `quality:`, and `heartbeat:`
    - `templates/workspace/domains.yaml` contains a registry shape that references `domains/{domain_id}/domain.yaml`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_schema_contracts.py -q</automated>
  </verify>
  <done>Phase 1 has schema-validated default config templates ready for the init workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace files → schema models | User-edited YAML/markdown enters validation code and must be treated as untrusted input. |
| templates → init writer | Default config emitted into user workspaces must not embed secrets or unstable paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T | `src/construct/schemas/card.py` | mitigate | Enforce enum, filename, and ID validation at model boundaries so malformed canonical files cannot be accepted silently per D-08/D-09. |
| T-01-04 | I | `templates/workspace/model-routing.yaml` | mitigate | Store only env-var names such as `ANTHROPIC_API_KEY`; never write actual credential values into templates. |
| T-01-05 | T | `src/construct/schemas/workspace.py` | mitigate | Reject duplicate canonical connections and missing required scaffold paths so the workspace contract cannot drift. |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_schema_contracts.py -q`
- `python -m pytest tests/unit -q`
</verification>

<success_criteria>
- Schema models encode the Phase 1 workspace/card/config contract.
- Editable routing/governance defaults exist in templates, not hard-coded branches in application logic.
- Contract tests cover hard errors and warning-only cases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-canonical-data-foundation/01-02-SUMMARY.md`
</output>
